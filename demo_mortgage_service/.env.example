# banking_mortgage_service — Phase 266 Path A backend
MORTGAGE_SERVICE_PORT=8082
MORTGAGE_SERVICE_HOST=127.0.0.1
# Shared secret the gateway uses to call this service.
# Must match config.demoApiKeyServiceKey on the gateway side.
MORTGAGE_SERVICE_API_KEY=demo-mortgage-key-0000
