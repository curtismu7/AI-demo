---
phase: 124-mfa-hitl-indication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/services/bankingAgentLangGraphClientService.js
  - banking_api_ui/src/services/agentFlowDiagramService.js
  - banking_api_server/services/transactionConsentChallenge.js
  - banking_api_server/config/runtimeSettings.js
autonomous: true
requirements: [ACTLOG-01, ACTLOG-02, ACTLOG-03, ACTLOG-04, ACTLOG-05, ACTLOG-06, ACTLOG-07]

must_haves:
  truths:
    - "The main MFA/HITL prompt explicitly says manual approval is required"
    - "A persistent HITL badge or equivalent visual indicator remains visible through the approval flow"
    - "Existing step-up and approval mechanics are preserved"
    - "Flow/state naming stays consistent across client and server surfaces"
    - "UI changes build cleanly if frontend components are touched"
  artifacts:
    - path: banking_api_ui/src/services/bankingAgentLangGraphClientService.js
      provides: "Client HITL state handling and wording integration"
      contains: "consent"
    - path: banking_api_ui/src/services/agentFlowDiagramService.js
      provides: "Flow-state labels for MFA/HITL indication"
      contains: "MFA Step-up"
    - path: banking_api_server/services/transactionConsentChallenge.js
      provides: "Underlying approval contract preserved while UI indication improves"
      contains: "consent"
---

<objective>
Implement clearer Human-in-the-Loop indication in MFA/step-up flows so users understand they are in a manual approval path. The work should improve wording and persistent visual state without changing the actual approval mechanics.

Output: explicit manual-approval indication across the relevant MFA/HITL flow, with a persistent badge/icon and no contract regressions.
</objective>

<execution_context>
@.planning/phases/124-mfa-hitl-indication/124-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Identify the active MFA/HITL UI surfaces and inject explicit manual-approval copy</name>
  <files>banking_api_ui/src/services/bankingAgentLangGraphClientService.js, banking_api_ui/src/App.js</files>
  <action>
Trace the current client path for MFA/HITL approval prompts and update the primary in-flow copy so it clearly communicates that manual approval is required. Keep the existing approval mechanics intact.
  </action>
  <done>Primary prompt language clearly signals a HITL/manual-approval state</done>
</task>

<task type="auto">
  <name>Task 2: Add a persistent HITL indicator through the approval flow</name>
  <files>banking_api_ui/src/services/agentFlowDiagramService.js, relevant approval UI component(s) discovered during execution</files>
  <action>
Add a persistent badge/icon or equivalent visual marker that remains visible while the user is in the approval flow. Normalize the indication across the relevant step-up scenarios without redesigning the entire system.
  </action>
  <done>Users retain a visible HITL indicator beyond the first prompt</done>
</task>

<task type="auto">
  <name>Task 3: Preserve server contract and step-up behavior</name>
  <files>banking_api_server/services/transactionConsentChallenge.js, banking_api_server/config/runtimeSettings.js</files>
  <action>
Confirm the UI indication changes do not alter consent challenge semantics, thresholds, or approval sequencing. Only adjust server-side surfaces if needed to support clearer state naming or payload labeling.
  </action>
  <done>Approval mechanics remain unchanged while indication improves</done>
</task>

<task type="auto">
  <name>Task 4: Verify flow behavior and build quality</name>
  <files>banking_api_server/src/__tests__/transaction-consent-challenge.test.js, banking_api_ui/</files>
  <action>
Run targeted verification for the approval flow and `cd banking_api_ui && npm run build` if UI files changed.
  </action>
  <done>Manual-approval indication is clearer and the build/flow still pass</done>
</task>

</tasks>

<verification>
- [ ] Main MFA/HITL prompt explicitly communicates manual approval
- [ ] Persistent HITL indicator remains visible through the approval path
- [ ] Existing approval mechanics are unchanged
- [ ] UI build passes if frontend files changed
</verification>

<output>
After completion, create `.planning/phases/124-mfa-hitl-indication/124-01-SUMMARY.md`
</output>