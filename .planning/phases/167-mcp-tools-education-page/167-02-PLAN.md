---
phase: 167-mcp-tools-education-page
plan: 02
type: execute
wave: 2
depends_on: ["167-01-PLAN"]
files_modified:
  - banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx
  - banking_api_ui/src/App.tsx
autonomous: true
requirements: [EDU-05]
user_setup: []

must_haves:
  truths:
    - "MCPToolsEducation component is accessible from the UI"
    - "Navigation link or button leads to the education page/panel"
    - "Component integrates without breaking existing pages"
    - "Build succeeds without errors"
  artifacts:
    - path: "banking_api_ui/src/App.tsx"
      provides: "Updated routing to include MCPToolsEducation route or integration point"
    - path: "banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx"
      provides: "Updated config page with link/tab to MCPToolsEducation"
  key_links:
    - from: "UnifiedConfigurationPage.tsx"
      to: "MCPToolsEducation.tsx"
      via: "Import and render component or link to route"
      pattern: "Tab or collapsible section within config page"
---

<objective>
Integrate MCPToolsEducation component into the UI, making it accessible as a linked page or tab within existing navigation structure (likely Admin Config page).

Purpose: Users can access the MCP tools education directly from the app without file system exploration.

Output: Working integration, verified build, no regressions
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/167-mcp-tools-education-page/CONTEXT.md
@.planning/phases/167-mcp-tools-education-page/167-01-SUMMARY.md (after wave 1 completes)
</execution_context>

<context>
@banking_api_ui/src/App.tsx
@banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx
@banking_api_ui/src/components/MCPToolsEducation.tsx (created in Wave 1)
</context>

<interfaces>
MCPToolsEducation export:
```typescript
export const MCPToolsEducation: React.FC = () => { ... }
```

Can be imported and rendered directly in existing component or as a new route.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add MCPToolsEducation as a tab in UnifiedConfigurationPage</name>
  <files>banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx</files>
  <action>
  Add MCPToolsEducation as a new tab in the UnifiedConfigurationPage component:
  
  1. Import MCPToolsEducation at top:
     ```typescript
     import { MCPToolsEducation } from '../MCPToolsEducation';
     ```
  
  2. Add new tab to the tab list (wherever tabs are defined):
     - Tab name/label: "MCP Tools" or "Available Tools"
     - Tab content: Render MCPToolsEducation component
     - Position: After "Demo Data" or as last tab
  
  3. Ensure tab is only visible for authenticated users (check existing auth guard pattern in UnifiedConfigurationPage)
  
  4. Style tab consistent with existing tabs in the page (no custom styling needed, should auto-inherit)
  
  5. Tab should be keyboard-accessible and follow page's tabbed interface pattern
  </action>
  <verify>
    <automated>
      grep -n "MCPToolsEducation" banking_api_ui/src/components/Configuration/UnifiedConfigurationPage.tsx && echo "Import found"
    </automated>
  </verify>
  <done>
  MCPToolsEducation tab added to UnifiedConfigurationPage, renders correctly, no build errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and test routing</name>
  <files></files>
  <action>
  Run build and verify:
  
  1. Build React app:
     ```bash
     cd banking_api_ui && npm run build
     ```
     Must exit with code 0
  
  2. Check for TypeScript errors:
     ```bash
     cd banking_api_ui && npm run tsc -- --noEmit
     ```
     Should have 0 errors
  
  3. Verify component loads in browser by:
     - Running local dev server
     - Navigating to /admin or /config page
     - Clicking "MCP Tools" tab
     - Confirming MCPToolsEducation renders without console errors
  
  4. Test navigation:
     - Tab can be focused via keyboard
     - Tab content scrolls and displays all tools
     - No visual layout breaks or overlapping text
  </action>
  <verify>
    <automated>
      cd banking_api_ui && npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
  Build successful (exit 0), no TypeScript errors, component renders in browser without console errors
  </done>
</task>

</tasks>

<threat_model>
No new security threats — MCPToolsEducation is purely educational display of tool schemas already exposed by tools/list endpoint.
</threat_model>

<verification>
1. Build passes:
   ```bash
   cd banking_api_ui && npm run build && echo "✓ Build successful"
   ```

2. Navigation works (manual or via UI test):
   - Admin user can login
   - Access /admin or /config page
   - "MCP Tools" tab is visible
   - Tab content renders MCPToolsEducation

3. No regressions:
   - Other tabs in UnifiedConfigurationPage still work
   - No console errors
   - Page load time unchanged
</verification>

<success_criteria>
- [ ] MCPToolsEducation tab added to UnifiedConfigurationPage
- [ ] Component renders when tab is clicked
- [ ] Build succeeds with exit code 0
- [ ] No TypeScript errors
- [ ] No console errors in browser
- [ ] Navigation to and within tab works
- [ ] No existing functionality broken by this change
- [ ] Component is keyboard-accessible as part of page's tabbed interface

</success_criteria>

<output>
After completion, create:
- `.planning/phases/167-mcp-tools-education-page/167-02-SUMMARY.md` — Integration results, build status, verification checklist
</output>
