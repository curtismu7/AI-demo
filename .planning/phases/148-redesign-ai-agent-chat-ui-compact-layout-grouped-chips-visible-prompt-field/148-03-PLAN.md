---
phase: 148-redesign-ai-agent-chat-ui-compact-layout-grouped-chips-visible-prompt-field
plan: 03
type: execute
wave: 2
depends_on: [148-02]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - All six D-01 through D-06 decisions are visually verified across all placement modes
    - Collapsible groups toggle smoothly without layout shift
    - Emoji-only buttons maintain consistent sizing and alignment
    - Message scrolling doesn't cause prompt field to scroll out of view
    - Hover tooltips and visual feedback work correctly
    - No visual regressions in other agent modes
    - Design matches Chase.com banking UI patterns
  artifacts:
    - path: All implementation files from Plans 01-02
      provides: Complete compact redesigned UI
  key_links:
    - from: User interaction (scroll, toggle, hover)
      to: Visual feedback
      via: CSS transitions and JavaScript state updates
---

<objective>
Verify the compact redesigned BankingAgent UI works correctly across all placement modes and meets all design requirements. Identify any refinements needed for visual polish.

Purpose: Final validation checkpoint before closing Phase 148. Ensure all locked decisions (D-01 through D-06) are visually implemented and working as intended.
Output: Visual verification checkpoint; identification of any refinements needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-redesign-ai-agent-chat-ui-compact-layout-grouped-chips-visible-prompt-field/148-CONTEXT.md
</context>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual Verification: Compact UI across all placement modes</name>
  <what-built>
- ACTION_GROUPS with three categories (Account, Transaction, Admin) from Plan 01
- Collapsible group headers with toggle arrows from Plan 02
- Emoji-only action buttons showing emojis with full text on hover from Plan 02
- Condensed message display (smaller font, tighter spacing) from Plan 02
- Prominent prompt field pinned to bottom from Plan 02
- All styling and state management complete
  </what-built>
  <how-to-verify>
**Section A: Launch the application**

```bash
# Terminal 1: Start BFF
cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && npm start

# Terminal 2: Start UI
cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm start
```

Open http://localhost:3000/dashboard in browser (logged-in user view)

---

**Section B: Verify D-01 — Chip Grouping by Category**

1. Look at the left column of the agent (ba-left-col)
2. Verify you see **three group headers**:
   - "Account" (with ▼ arrow)
   - "Transaction" (with ▶ arrow)
   - "Admin" (with ▶ arrow)

✓ PASS: Three headers visible with correct names and arrows
✗ FAIL: Headers missing, incorrect names, or arrows not showing

---

**Section C: Verify D-02 — Collapsible Emoji Buttons**

1. Account group is expanded ▼, showing emojis:
   - 🏦 (My Accounts)
   - 💰 (Check Balance)
   - 👁 (View Sensitive Account Details)

✓ PASS: All three Account emojis visible
✗ FAIL: Emojis missing or showing text labels instead

2. Transaction group is collapsed ▶, showing NO buttons:
   - Should see nothing (height: 0, opacity: 0)

✓ PASS: Transaction group collapsed (empty space)
✗ FAIL: Transaction buttons visible when should be hidden

3. Hover over an emoji button (e.g., 🏦):
   - Full text appears in tooltip: "My Accounts"
   - Button highlights or changes color

✓ PASS: Tooltip shows full text on hover
✗ FAIL: No tooltip or tooltip empty

4. Click "Transaction" group header:
   - Group expands ▼, showing transaction emojis:
     - 📋 (Recent Transactions)
     - ⬇ (Deposit)
     - ⬆ (Withdraw)
     - ↔ (Transfer)
   - Account group stays expanded

✓ PASS: Transaction group expands, shows correct emojis
✗ FAIL: Group doesn't expand, wrong emojis, or Account collapses

5. Reload the page (Ctrl+R):
   - After reload, Account still expanded ▼
   - Transaction still expanded ▼ (if you expanded it)
   - Verify localStorage persists state

✓ PASS: State persists across reload
✗ FAIL: Groups reset to default state

---

**Section D: Verify D-03 — Inline Split-Column Rendering**

1. On dashboard, click "Right Dock" button in the agent UI toggle buttons
2. Observe layout changes:
   - Token panel (left) | Banking panel (center) | Agent panel (right)
   - Agent renders as rightmost column, NOT as overlay/sidebar

✓ PASS: Right agent renders as inline column on right side
✗ FAIL: Right agent renders as overlay/sidebar at top, or doesn't fit layout properly

---

**Section E: Verify D-04, D-05 — Prompt Field & Condensed Messages**

1. Look at the prompt field at **bottom of the agent**:
   - Blue top border (prominent visual indicator)
   - Larger font size (15px)
   - Placeholder text: "Ask about accounts, transfers, etc."
   - Should be **sticky** (stays in view when scrolling messages)

✓ PASS: Blue border visible, larger font, placeholder shows
✗ FAIL: Border missing, normal font size, or placeholder empty

2. Look at the message area:
   - Messages have reduced line-height (1.3x instead of 1.5x)
   - Smaller font (13px instead of normal 14-16px)
   - Tighter vertical padding between messages
   - **More messages visible on screen** than before

✓ PASS: Messages are visibly condensed, more fit on screen
✗ FAIL: Messages look same size, line-height unchanged

3. Type a test message in the prompt field and send:
   - Prompt field stays visible at bottom (doesn't scroll away)
   - Message appears in the condensed format above

✓ PASS: Sticky positioning works, message displays in condensed format
✗ FAIL: Prompt scrolls out of view or message doesn't appear

---

**Section F: Verify D-06 — Smart Default State & localStorage**

1. Open DevTools Console (F12)
2. Run:
   ```javascript
   JSON.parse(localStorage.getItem('ba_chip_groups_state'))
   ```
   Expected output: `{ account: true, transaction: false, admin: false }`

✓ PASS: Correct default state
✗ FAIL: Wrong state or localStorage key missing

3. Click Admin group header to expand admin actions
4. Close and reopen DevTools Console
5. Run the same command again:
   Expected output: `{ account: true, transaction: true, admin: true }` (if you expanded all)

✓ PASS: Changes persisted to localStorage
✗ FAIL: State didn't change or wasn't persisted

---

**Section G: Test All Placement Modes**

Verify compact UI works in all modes:

1. **Inline mode** (default):
   ✓ Groups visible, collapsible, messages condensed

2. **Middle mode** (click Middle button):
   ✓ Groups visible, collapsible, messages condensed
   
3. **Right-Dock mode** (click Right button):
   ✓ Groups visible in right column, collapsible, messages condensed
   
4. **Bottom-Dock mode** (click Bottom button):
   ✓ Groups visible at bottom, collapsible, messages condensed
   
5. **Float mode** (click Float button):
   ✓ Groups visible in floating window, collapsible, messages condensed

---

**Section H: Visual Consistency Check**

1. Scroll through multiple group toggle interactions
2. Verify:
   - Transitions are smooth (collapse/expand animations)
   - No layout shifts or jumping
   - Colors/fonts consistent across all modes
   - Emoji sizing consistent (24px)
   - Button hover states work consistently

✓ PASS: All interactions smooth, no visual glitches
✗ FAIL: Rough animations, layout shifts, or inconsistent styling

---

**Summary Checklist:**

- [ ] D-01 Three groups visible (Account, Transaction, Admin)
- [ ] D-02 Emoji-only buttons with hover tooltips
- [ ] D-02 Collapsible groups with correct default state
- [ ] D-03 Right-dock renders as inline column
- [ ] D-04 Prompt field is prominent (blue border, larger font)
- [ ] D-04 Prompt field stays visible when scrolling messages
- [ ] D-05 Messages condensed (more visible on screen)
- [ ] D-06 Default state: Account expanded, others collapsed
- [ ] D-06 localStorage persists group state across reload
- [ ] All 5 placement modes work correctly
- [ ] No visual regressions in other parts of UI
- [ ] Smooth animations and transitions

**If ALL checks pass:** Type "approved"
**If SOME checks fail:** Describe which sections failed and what the issue is (e.g., "D-02: Group headers not showing arrows", "D-04: Prompt field has no border")
  </how-to-verify>
  <resume-signal>Type "approved" if verification passes, or list specific failures for refinement.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User interactions | Clicks, scrolls, text input; all handled client-side |
| Visual state persistence | localStorage stores UI preferences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-05 | Availability | Sticky prompt field | mitigate | Test across browsers to ensure sticky position supported (CSS fallback for older browsers) |

</threat_model>

<verification>
1. All six D-01 through D-06 decisions visually verified
2. All placement modes tested and working
3. No visual regressions
4. Smooth animations and transitions
5. localStorage persistence working
</verification>

<success_criteria>
✓ Three collapsible groups visible with correct headers
✓ Emoji-only buttons show full text on hover
✓ Collapsible groups work smoothly with default state correct
✓ Prompt field prominent and sticky
✓ Messages condensed with improved information density
✓ localStorage state persists correctly
✓ All 5 placement modes render correctly
✓ No visual regressions or glitches
✓ Checkpoint verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/148-redesign-ai-agent-chat-ui-compact-layout-grouped-chips-visible-prompt-field/148-03-SUMMARY.md`
</output>
