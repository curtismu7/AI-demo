---
phase: 246-floating-agent-redesign-chips-to-popout-simplified-controls
plan: 03
type: execute
wave: 2
depends_on: [246-01, 246-02]
files_modified: []
autonomous: false
requirements: [AGENT-UI-01, AGENT-UI-02, AGENT-UI-03]

must_haves:
  truths:
    - "npm run build exits 0 with no errors"
    - "Float mode panel shows 3-control header with Actions pill"
    - "Actions popout opens above/over chat body with all sections visible"
    - "TESTING section is collapsed by default, expandable via click"
    - "Inline mode (admin dashboard, marketing page) is unchanged"
  artifacts:
    - path: "banking_api_ui/build/"
      provides: "Production build artifacts"
      contains: "index.html"
  key_links:
    - from: "npm run build"
      to: "banking_api_ui/build/"
      via: "react-scripts build"
      pattern: "Successfully compiled"
---

<objective>
Verify Phase 246 implementation is correct and shippable:
1. Run `npm run build` — must exit 0 with no errors or new warnings.
2. Human visual verification of the redesigned floating agent panel.

Purpose: Confirm the chip-to-popout redesign renders correctly before marking phase complete.
Output: Build artifacts confirm no regressions. Human confirms UX is correct.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-CONTEXT.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-UI-SPEC.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-01-SUMMARY.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification</name>
  <read_first>
    - banking_api_ui/src/components/BankingAgent.js — skim for obvious JSX syntax errors around the popout block before building
    - banking_api_ui/src/components/BankingAgent.css — confirm .ba-actions-trigger and .ba-actions-popout classes exist
  </read_first>
  <files></files>
  <action>
    Run the CRA production build:

    ```bash
    cd banking_api_ui && npm run build 2>&1 | tail -40
    ```

    If build fails:
    1. Read the error output carefully — most likely a JSX syntax error in BankingAgent.js near the popout block.
    2. Fix the minimal set of characters needed to resolve the error.
    3. Re-run build until exit 0.

    Common issues to watch for:
    - Unclosed JSX tags in the popout block (the popout is large — count open/close tags)
    - Missing import for any new variable (all variables used in the popout are already in component scope per interface block)
    - `autoFocus` on the search input may cause a React warning — acceptable in demo context
  </action>
  <verify>
    cd banking_api_ui && npm run build 2>&1 | tail -5
    # Must contain "Successfully compiled" or "Compiled successfully"
    # Exit code: echo $?  → must be 0
  </verify>
  <done>
    `npm run build` exits 0. "Successfully compiled" appears in output.
    No new ESLint errors introduced (warnings about `autoFocus` are acceptable).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 246 floating agent redesign:
    - Header stripped from 7 controls to 3 (+1 trigger): [Actions▾] [⊞/⊟] [▼]
    - Left column removed (chat fills full width in float mode)
    - Actions popout with search, chip sections, SESSION/VIEW/SETTINGS controls, status bar
    - ⧉ New window chip in VIEW section (replaces header ↗ button)
    - TESTING section collapsible (collapsed by default)
    - Inline mode unchanged
  </what-built>
  <how-to-verify>
    Start the dev server if not running: `cd banking_api_ui && npm start`
    (or use run-bank.sh for the full stack)

    Checklist:
    1. **Float panel — default state**: Open floating panel. Header shows: title/subtitle left, [Actions▾][⊞][▼] right. NO appearance select, NO ☀️/🌙, NO Sign out, NO 🔗 in header. Chat area fills full width (no left column).

    2. **Open Actions popout**: Click "Actions ▾". Popout appears overlaying the chat body. Button label changes to "Actions ▴". Popout contains:
       - Search input at top with placeholder "Search actions…"
       - SUGGESTIONS section with suggestion chips
       - ACCOUNT, TRANSACTION sections with action chips
       - SESSION section with 🔄 Refresh token + 🚪 Sign out chips
       - VIEW section with 🔗 Token chain + ⧉ New window chips
       - SETTINGS section with appearance select + ☀️/🌙 toggle
       - Status bar at bottom with BFF dot (and MCP server dot if MCP connected)

    3. **Search**: Type "trans" in the search box. Only TRANSACTIONS section and any matching suggestions remain visible. Clear search → all sections reappear.

    4. **TESTING section**: TESTING section shows a "▶ Testing" toggle collapsed by default. Click it to expand, click again to collapse.

    5. **ADMIN section** (when signed in as admin): ADMIN section is visible with MCP Tools / Admin chips. As regular customer: ADMIN section is hidden.

    6. **Chip click**: Click any action chip in the popout. Popout closes. Action executes (message appears in chat).

    7. **⧉ New window**: Click ⧉ New window in VIEW section. A new browser window opens at /agent. Popout closes.

    8. **Backdrop click**: Open popout, then click anywhere in the chat body area. Popout closes.

    9. **Escape key**: Open popout, type "abc" in search, press Escape → search clears. Press Escape again → popout closes, focus returns to Actions button.

    10. **Token chain**: Click 🔗 Token chain chip in VIEW section → token chain column appears. Click again → disappears.

    11. **Inline mode regression check**: Visit /admin or /config (inline agent). Verify: left column still visible, header controls unchanged from before this phase.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checklist items pass.
    Or describe any visual/functional issues found so they can be fixed.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output → browser | CRA production build; standard React security controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-246-verify-01 | T (Tampering) | Source files during build | accept | Local dev environment; no external dependencies modified |
</threat_model>

<verification>
- `npm run build` exits 0
- Human verifies all 11 checklist items pass
- No inline mode regressions found
</verification>

<success_criteria>
- Build: `cd banking_api_ui && npm run build` → exit 0, "Successfully compiled"
- Float mode: header has [Actions▾][⊞/⊟][▼] only
- Popout: all 7 sections present and functional
- ⧉ New window works in VIEW section
- Inline mode: unchanged (left column visible, full header controls)
- Human verification: "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-03-SUMMARY.md`
</output>
