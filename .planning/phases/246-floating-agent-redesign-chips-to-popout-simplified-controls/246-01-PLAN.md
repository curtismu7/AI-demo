---
phase: 246-floating-agent-redesign-chips-to-popout-simplified-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/BankingAgent.css
autonomous: true
requirements: [AGENT-UI-01, AGENT-UI-02, AGENT-UI-03]

must_haves:
  truths:
    - "Left rail column is invisible in float mode — chat area fills 100% width"
    - "An Actions pill button exists with accent-blue fill and chevron suffix"
    - "The popout overlay has correct positioning, dimensions, z-index, and open animation"
    - "Backdrop exists as a transparent click-dismiss layer below the popout"
    - "Section labels match existing 10px/700/uppercase .ba-left-label pattern"
    - "Chip rows flex-wrap at the right gap"
    - "Status bar at popout bottom shows server dots"
  artifacts:
    - path: "banking_api_ui/src/components/BankingAgent.css"
      provides: "All new phase-246 CSS classes + left-rail guard + ba-header position:relative"
      contains: ".ba-actions-trigger"
  key_links:
    - from: ".banking-agent-panel:not(.ba-mode-inline)"
      to: ".ba-left-col"
      via: "display:none CSS guard"
      pattern: "ba-mode-inline.*ba-left-col"
    - from: ".ba-header"
      to: ".ba-actions-popout"
      via: "position:relative + position:absolute top:100%"
      pattern: "position: relative"
---

<objective>
Add all phase-246 CSS to BankingAgent.css:
1. Left-rail float-mode guard (`display: none` on `.ba-left-col` when not inline).
2. `position: relative` on `.ba-header` (anchor for popout absolute positioning).
3. `.ba-actions-trigger` — accent-filled pill button for the header.
4. `.ba-actions-popout` — absolute-positioned overlay (below header, overlays chat body).
5. `.ba-popout-backdrop` — full-panel transparent click-dismiss layer.
6. Supporting classes: `.ba-popout-search`, `.ba-popout-section`, `.ba-popout-section-label`,
   `.ba-popout-chip-row`, `.ba-popout-body`, `.ba-popout-status-bar`,
   `.ba-popout-empty`, `.ba-popout-empty-heading`, `.ba-popout-section-toggle`.

Purpose: Provide styled CSS for BankingAgent.js changes in Plan 02 (same wave, parallel execution).
Output: All new CSS classes appended to BankingAgent.css; two existing rules modified.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-CONTEXT.md
@.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-UI-SPEC.md

<interfaces>
<!-- Existing CSS tokens (do not introduce new color values) -->
/* From BankingAgent.css */
--ba-bg: #152a52
--ba-surface: #1a3a6e
--ba-border: rgba(65, 105, 225, 0.35)
--ba-text: #e8eefc
--ba-muted: #9db4e8
--ba-float-btn: #4169e1    /* Actions trigger fill */
--ba-chip-bd: #5b7ef0
--ba-chip-txt: #c7d7ff

/* Existing classes that inform the pattern */
.ba-left-label { font-size: 10px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; }
.ba-icon-btn { height: 28px; /* icon button touch target */ }
.ba-action-item { /* existing action chip */ }
.ba-suggestion { /* existing suggestion chip */ }
.ba-server-chip.ba-server-chip--active { /* existing status chip */ }
.ba-chip-dot { /* existing status dot */ }
.ba-discovery-popout { /* existing Phase-231 popout — reference for box-shadow, border-radius */ }

/* Existing rules to MODIFY (not replace): */
.ba-header { /* line 683 — add position: relative */ }
.banking-agent-panel:not(.ba-mode-inline) .ba-left-col { /* ADD THIS RULE — display: none guard (D-03) */ }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add position:relative to .ba-header and left-col float-mode guard</name>
  <read_first>
    - banking_api_ui/src/components/BankingAgent.css lines 683-760 (ba-header rule)
    - banking_api_ui/src/components/BankingAgent.css — search for existing .ba-mode-inline .ba-left-col rules to place the guard near them
  </read_first>
  <files>banking_api_ui/src/components/BankingAgent.css</files>
  <action>
    1. Find `.ba-header {` (around line 683). Inside the existing rule block, ADD `position: relative;`
       as the FIRST property (before `display: flex`). Do NOT change any other property.

    2. Find the area where `.ba-mode-inline` left-col rules appear (search for "ba-left-col").
       Add the following new rule in that vicinity:

       ```css
       /* Phase 246: hide left rail in float mode — chat area expands to 100% width (D-03) */
       .banking-agent-panel:not(.ba-mode-inline) .ba-left-col {
           display: none;
       }
       ```

    Do NOT modify `.ba-mode-inline .ba-left-col` or any surrounding rules.
  </action>
  <verify>
    grep -n "position: relative" banking_api_ui/src/components/BankingAgent.css | grep -A1 "ba-header"
    grep -n "ba-mode-inline.*ba-left-col\|ba-left-col.*display" banking_api_ui/src/components/BankingAgent.css
  </verify>
  <done>
    `.ba-header` rule contains `position: relative;`.
    New rule `.banking-agent-panel:not(.ba-mode-inline) .ba-left-col { display: none; }` exists in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add all phase-246 CSS classes</name>
  <read_first>
    - banking_api_ui/src/components/BankingAgent.css — search for `.ba-all-actions-btn` (around line 3760) to find a good insertion point after the existing discovery-popout block
    - banking_api_ui/src/components/BankingAgent.css lines 3800-3900 (existing .ba-discovery-popout block, to reference pattern)
  </read_first>
  <files>banking_api_ui/src/components/BankingAgent.css</files>
  <action>
    After the existing `.ba-discovery-*` block (Phase-231 popout classes, ends around line 3920),
    append the following CSS block exactly as written. Do NOT alter `.ba-discovery-*` rules.:

    ```css
    /* ─── Phase 246: Actions trigger + popout ─────────────────────────────── */

    /* Pill button in header — opens the actions popout */
    .ba-actions-trigger {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        height: 28px;
        padding: 6px 12px;
        border-radius: 8px;
        border: none;
        background: var(--ba-float-btn);
        color: #fff;
        font-size: 11px;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.15s;
        white-space: nowrap;
    }
    .ba-actions-trigger:hover {
        background: #3158d3;
    }
    .ba-actions-trigger.active {
        background: #3158d3;
    }
    .ba-actions-trigger:disabled {
        opacity: 0.5;
        cursor: not-allowed;
    }
    .ba-actions-trigger:focus-visible {
        outline: 2px solid var(--ba-chip-bd);
        outline-offset: 2px;
    }

    /* Popout overlay — anchored to .ba-header (position:relative), opens below header */
    .ba-actions-popout {
        position: absolute;
        top: calc(100% + 4px);
        right: 14px;
        width: 320px;
        max-height: 380px;
        overflow-y: auto;
        scrollbar-width: none;
        background: var(--ba-surface);
        border: 1px solid var(--ba-border);
        border-radius: 12px;
        box-shadow: 0 8px 32px rgba(10, 20, 50, 0.45);
        z-index: 100061;
        animation: popout-slide-in 0.15s ease;
    }
    .ba-actions-popout::-webkit-scrollbar {
        display: none;
    }
    @keyframes popout-slide-in {
        from { opacity: 0; transform: translateY(-4px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    /* Full-panel transparent backdrop — z-index below popout, catches click-outside */
    .ba-popout-backdrop {
        position: absolute;
        inset: 0;
        z-index: 100060;
        cursor: default;
    }

    /* Search input at top of popout */
    .ba-popout-search {
        display: block;
        box-sizing: border-box;
        width: calc(100% - 24px);
        margin: 12px 12px 8px;
        padding: 8px 12px;
        font-size: 12px;
        border-radius: 8px;
        border: 1px solid rgba(65, 105, 225, 0.45);
        background: var(--ba-bg);
        color: var(--ba-text);
    }
    .ba-popout-search:focus {
        outline: none;
        border-color: var(--ba-chip-bd);
    }

    /* Scrollable content area inside popout */
    .ba-popout-body {
        padding: 0 0 8px;
    }

    /* Section wrapper — adds vertical padding between sections */
    .ba-popout-section {
        padding: 8px 12px 4px;
        border-top: 1px solid var(--ba-border);
    }
    .ba-popout-section:first-child {
        border-top: none;
    }

    /* Section label — matches .ba-left-label (10px/700/uppercase) */
    .ba-popout-section-label {
        font-size: 10px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: var(--ba-muted);
        margin-bottom: 6px;
        display: block;
    }

    /* Toggle button for collapsible sections (TESTING) */
    .ba-popout-section-toggle {
        width: 100%;
        text-align: left;
        border: none;
        background: transparent;
        cursor: pointer;
        color: var(--ba-muted);
        padding: 0;
        margin-bottom: 6px;
    }
    .ba-popout-section-toggle:hover {
        color: var(--ba-text);
    }

    /* Chip rows within popout sections */
    .ba-popout-chip-row {
        display: flex;
        flex-wrap: wrap;
        gap: 6px;
        margin-bottom: 4px;
    }

    /* Empty search state */
    .ba-popout-empty {
        padding: 20px 12px;
        text-align: center;
        color: var(--ba-muted);
        font-size: 11px;
    }
    .ba-popout-empty-heading {
        font-size: 12px;
        font-weight: 600;
        color: var(--ba-text);
        margin-bottom: 6px;
    }

    /* Status bar at popout bottom */
    .ba-popout-status-bar {
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 8px 12px;
        border-top: 1px solid var(--ba-border);
        font-size: 10px;
        color: var(--ba-muted);
    }

    /* Light mode overrides for trigger and popout */
    .banking-agent-panel.ba-mode-light .ba-actions-trigger {
        background: var(--ba-float-btn);
        color: #fff;
    }
    .banking-agent-panel.ba-mode-light .ba-actions-trigger:hover,
    .banking-agent-panel.ba-mode-light .ba-actions-trigger.active {
        background: #2d4fc0;
    }
    .banking-agent-panel.ba-mode-light .ba-actions-popout {
        background: var(--ba-surface);
        border-color: var(--ba-border);
    }
    /* ─────────────────────────────────────────────────────────────────────── */
    ```
  </action>
  <verify>
    grep -c "ba-actions-trigger\|ba-actions-popout\|ba-popout-backdrop\|ba-popout-search\|ba-popout-section\|ba-popout-chip-row\|ba-popout-status-bar\|ba-popout-empty" banking_api_ui/src/components/BankingAgent.css
  </verify>
  <done>
    All 8 new class names exist in BankingAgent.css.
    No existing .ba-discovery-* or .ba-mode-inline rules were modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → browser render | CSS-only changes; no executable logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-246-CSS-01 | T (Tampering) | BankingAgent.css | accept | CSS changes are client-side styling only; no security boundary crossed |
</threat_model>

<verification>
After both tasks:
- grep confirms `.ba-header` has `position: relative`
- grep confirms the `.ba-left-col` float guard exists
- grep confirms all new phase-246 classes exist
- `cd banking_api_ui && npm run build` exits 0 (run after Plan 02 also applied)
</verification>

<success_criteria>
- `.ba-header { position: relative; }` added
- `.banking-agent-panel:not(.ba-mode-inline) .ba-left-col { display: none; }` added
- All new `.ba-actions-trigger`, `.ba-actions-popout`, `.ba-popout-*` CSS classes added
- No existing CSS rules modified beyond the two stated changes
- Build passes (verified in Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/246-floating-agent-redesign-chips-to-popout-simplified-controls/246-01-SUMMARY.md`
</output>
