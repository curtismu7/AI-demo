---
phase: 147
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/context/AgentUiModeContext.js
  - banking_api_ui/src/components/BankingAgent.css
  - banking_api_ui/tests/e2e/banking-agent.spec.js
  - banking_api_ui/tests/e2e/banking-agent.real.spec.js
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "'left-dock' is no longer a valid placement option for the agent"
    - "User cannot place agent in left sidebar; all other placements work"
    - "Agent remains fully functional in inline, float, bottom-dock, right-dock modes"
    - "ba-left-col (left column with buttons/suggestions) is preserved in all remaining modes"
  artifacts:
    - path: banking_api_ui/src/context/AgentUiModeContext.js
      provides: "Placement mode definitions"
      must_not_contain: "'left-dock'"
    - path: banking_api_ui/src/components/BankingAgent.css
      provides: "CSS styling for agent"
      no_orphaned_left_dock_rules: "true"
  key_links:
    - from: "AgentUiModeContext.js (placement definition)"
      to: "BankingAgent.js (useContext reads placement)"
      pattern: "placement prop validates against valid options"
    - from: "placement prop usage in BankingAgent.js"
      to: "Invalid placement silently falls back to default"
      pattern: "e2e tests verify fallback behavior"
---

<objective>
Remove 'left-dock' as a valid placement mode for the BankingAgent component to prevent conflicts with dashboard sidebar buttons.

Purpose: The left-dock mode places the agent in the left sidebar where it overlaps with dashboard navigation buttons. By removing this mode, the agent cannot be placed there, eliminating the conflict while preserving all other functionality.

Output: Modified AgentUiModeContext.js (placement options), updated tests, verified fallback behavior
</objective>

<execution_context>
@CLAUDE.md
@REGRESSION_PLAN.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/147-get-rid-of-left-agent-keep-the-rest/147-CONTEXT.md

## Current State

**AgentUiModeContext.js (line 9):** Documents placement prop as `'middle' | 'bottom' | 'none' | 'left-dock' | 'right-dock'`

**AgentUiModeContext.js (lines 48, 73, 82):** Contains conditional logic and validation that reference 'left-dock' placement

**Existing modes (line 73):** Validates placement against array: `['middle', 'bottom', 'none', 'left-dock', 'right-dock']`

**Related validation (line 82):** Special handling for dock modes: `if ((p === 'left-dock' || p === 'right-dock') && ...)`

## Scope Clarification

- **Remove:** 'left-dock' string from all placement options, validations, and conditionals
- **Keep:** ba-left-col (agent's internal left column with buttons/suggestions) — only the sidebar placement mode is removed
- **Keep:** All other modes (inline, float, bottom-dock, right-dock, side-dock, middle, none)
- **Keep:** CSS styling for ba-left-col in non-left-dock contexts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 'left-dock' from AgentUiModeContext.js placement options</name>
  <files>banking_api_ui/src/context/AgentUiModeContext.js</files>
  <read_first>
    - banking_api_ui/src/context/AgentUiModeContext.js (full file to understand structure)
    - banking_api_ui/src/components/BankingAgent.js (line 1-100 to see import of AgentUiModeContext)
  </read_first>
  <action>
Open AgentUiModeContext.js and make these edits:

1. **Line 9 JSDoc:** Change
```
@property {'middle' | 'bottom' | 'none' | 'left-dock' | 'right-dock'} placement
```
To:
```
@property {'middle' | 'bottom' | 'none' | 'right-dock'} placement
```

2. **Line 48 condition:** Remove or modify the `if (state.placement === 'left-dock')` conditional block. Check what it does and remove or consolidate with other mode handling. Replace with a comment: `// left-dock mode removed`

3. **Line 73 validation:** Change
```javascript
(p === 'middle' || p === 'bottom' || p === 'none' || p === 'left-dock' || p === 'right-dock')
```
To:
```javascript
(p === 'middle' || p === 'bottom' || p === 'none' || p === 'right-dock')
```

4. **Line 82 condition:** Change
```javascript
if ((p === 'left-dock' || p === 'right-dock') && typeof fab !== 'boolean') {
```
To:
```javascript
if (p === 'right-dock' && typeof fab !== 'boolean') {
```

Result: 'left-dock' completely removed from placement options. If code tries to set placement='left-dock', validation will fail and it will either: (a) throw error or (b) reject invalid value, depending on error handling strategy.
  </action>
  <verify>
    <automated>grep -n "left-dock" banking_api_ui/src/context/AgentUiModeContext.js || echo "✓ No left-dock found"</automated>
  </verify>
  <done>
    ✓ No 'left-dock' string remains in AgentUiModeContext.js
    ✓ Valid placements now: 'middle', 'bottom', 'none', 'right-dock' only
    ✓ File is syntactically valid (no broken conditionals)
    ✓ grep confirms no left-dock references in file
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove left-dock-specific CSS and validate stylesheet</name>
  <files>banking_api_ui/src/components/BankingAgent.css</files>
  <read_first>
    - banking_api_ui/src/components/BankingAgent.css (full file to find any left-dock-specific rules)
    - Search for `.ba-mode-left`, `left-dock`, `left_dock` patterns
  </read_first>
  <action>
1. Search BankingAgent.css for any rules containing 'left-dock' or `.ba-mode-left` (not `.ba-mode-light`). Based on initial search, there may be none, but verify thoroughly.

2. If any rules found, delete them. Rules to look for patterns:
   - `.ba-mode-left-dock { ... }`
   - `.ba-mode-left-dock .ba-left-col { ... }`
   - `.ba-mode-left-dock .ba-input-field { ... }`
   - Media queries with left-dock: `@media (max-width: ...) { .ba-mode-left-dock { ... } }`

3. After deletion, verify ba-left-col CSS rules (for other modes like inline, bottom-dock, right-dock) are still intact. These should remain.

4. Run a quick visual inspection: CSS file should have no orphaned selectors or broken declarations.
  </action>
  <verify>
    <automated>npm run build --prefix banking_api_ui 2>&1 | tail -20</automated>
  </verify>
  <done>
    ✓ No left-dock-specific CSS rules in BankingAgent.css
    ✓ ba-left-col styling preserved for remaining modes
    ✓ CSS file is valid (no syntax errors)
    ✓ `npm run build` completes without CSS-related errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update e2e tests and verify no left-dock test cases remain</name>
  <files>
    - banking_api_ui/tests/e2e/banking-agent.spec.js
    - banking_api_ui/tests/e2e/banking-agent.real.spec.js
  </files>
  <read_first>
    - banking_api_ui/tests/e2e/banking-agent.spec.js (search for left-dock, left_dock, or 'left-dock' references)
    - banking_api_ui/tests/e2e/banking-agent.real.spec.js (search for left-dock references)
  </read_first>
  <action>
1. Search both test files for 'left-dock' or 'leftDock' or 'left_dock' references.

2. If found: Remove or update test cases that explicitly test left-dock mode behavior. This includes:
   - Any `page.goto(...placement=left-dock...)` calls
   - Any selectors like `.ba-mode-left-dock`
   - Any assertions checking left-dock specific layout/visibility

3. Keep test cases for other modes (inline, bottom-dock, right-dock, float).

4. If test for "invalid placement fallback" exists, verify it tests other invalid values (not left-dock).

Result: No test cases remaining that assume left-dock is a valid mode.
  </action>
  <verify>
    <automated>grep -r "left-dock\|left_dock\|leftDock" banking_api_ui/tests/e2e/ || echo "✓ No left-dock references in e2e tests"</automated>
  </verify>
  <done>
    ✓ No left-dock references in banking-agent.spec.js
    ✓ No left-dock references in banking-agent.real.spec.js
    ✓ Test cases for other modes (inline, bottom-dock, right-dock) remain
    ✓ e2e tests pass (or fail only for pre-existing reasons, not left-dock removal)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed 'left-dock' placement mode from agent component configuration and tests. Agent can now only be accessed via: inline (`/agent` route), float (FAB), bottom-dock, right-dock, side-dock, middle, none. Left column with buttons/suggestions preserved in all remaining modes.</what-built>
  <how-to-verify>
1. **Launch the app:** `npm start` in banking_api_ui and banking_api_server
2. **Test inline mode:** Visit `http://localhost:3000/agent` — agent appears inline with left column visible (buttons, suggestions)
3. **Test float mode (FAB):** Navigate to `http://localhost:3000/` — click FAB button to toggle agent float panel — left column visible with all controls
4. **Test bottom-dock:** In admin console or config, set agent placement to 'bottom' — agent appears at bottom of screen with left column visible
5. **Test that left-dock no longer works:** Try to manually set placement='left-dock' in browser console or config — agent should either show error or fallback to default mode (likely 'middle' or 'bottom')
6. **Verify dashboard remains unaffected:** Dashboard sidebar buttons on left side should be fully visible and clickable — no overlap or conflict with agent
7. **Verify build succeeds:** `npm run build --prefix banking_api_ui` should complete with exit code 0
  </how-to-verify>
  <resume-signal>Type "approved" if all verification steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Placement Prop | Untrusted placement values may be passed to AgentUiModeContext |
| External Config → Placement | Config files or localStorage might contain invalid 'left-dock' values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-147-01 | Elevation | AgentUiModeContext validation | mitigate | Validation rejects 'left-dock' → invalid value throws or silently falls back to safe default |
| T-147-02 | Information | Stale config with left-dock | mitigate | Old localStorage/config with 'left-dock' value: validation catches, resets to 'bottom' or 'middle' |
| T-147-03 | Availability | App breakage on invalid placement | accept | Fallback behavior ensures app remains functional even if invalid placement attempted |
</threat_model>

<verification>
**Phase Verification Checklist:**

- [ ] AgentUiModeContext.js: no 'left-dock' string remains (grep confirms)
- [ ] BankingAgent.css: build succeeds, no CSS errors
- [ ] e2e tests: no left-dock test cases remain
- [ ] Agent works in inline, float, bottom-dock, right-dock modes
- [ ] Dashboard sidebar unaffected by agent removal
- [ ] Build completes with exit code 0
- [ ] No regression in other agent modes
</verification>

<success_criteria>
- ✓ 'left-dock' completely removed as placement option (user cannot select it)
- ✓ Agent displays correctly in all remaining modes (inline, float, bottom-dock, right-dock, side-dock)
- ✓ Left column (ba-left-col) with buttons/suggestions remains in all modes
- ✓ Dashboard sidebar buttons no longer conflicted by agent in left-dock mode (because mode no longer exists)
- ✓ Build succeeds: `npm run build --prefix banking_api_ui` exits 0
- ✓ No orphaned CSS rules or test cases
- ✓ Invalid placement gracefully handles 'left-dock' attempts (rejects or falls back)
</success_criteria>

<output>
After completion, create `.planning/phases/147-get-rid-of-left-agent-keep-the-rest/147-01-SUMMARY.md` documenting:
- Files modified
- Commits made
- Test results
- Any issues encountered or handled
</output>
