---
phase: 212
plan: 02
type: execute
wave: 2
depends_on: ["212-01"]
files_modified:
  - banking_api_ui/src/components/McpTrafficPage.js
  - banking_api_ui/src/App.js
  - banking_api_ui/src/components/AdminSideNav.jsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A /mcp-traffic route exists in the app and is accessible to authenticated users"
    - "The page auto-refreshes every 3 seconds, polling GET /api/mcp/traffic"
    - "Each entry renders with timestamp, direction badge (BFF→MCP / PingOne→BFF etc), type, tool name, duration, and summary"
    - "Entries are color-coded: green=ok, red=error/false, amber=exchange"
    - "A 'Clear' button clears the displayed list; a Live/Paused toggle stops polling"
    - "MCP Traffic link appears in AdminSideNav under 'Monitoring' section"
    - "npm run build passes (exit 0)"
  artifacts:
    - path: "banking_api_ui/src/components/McpTrafficPage.js"
      provides: "MCP traffic viewer page component"
      contains: "McpTrafficPage"
  key_links:
    - from: "McpTrafficPage.js"
      to: "GET /api/mcp/traffic"
      via: "fetch with credentials:include on 3s interval"
      pattern: "api/mcp/traffic"
    - from: "App.js Route path=/mcp-traffic"
      to: "McpTrafficPage"
      via: "import and Route element"
      pattern: "mcp-traffic"
    - from: "AdminSideNav.jsx Monitoring section"
      to: "path: '/mcp-traffic'"
      via: "children array entry"
      pattern: "/mcp-traffic"
---

<objective>
Create a minimal but functional MCP Traffic page at `/mcp-traffic` that polls
`GET /api/mcp/traffic` every 3 seconds and renders the log entries in a clean,
color-coded table. Add the page to App.js routing and the AdminSideNav Monitoring section.

No new CSS file needed — use inline styles consistent with the existing card/panel aesthetic.

Purpose: Gives the demo a live "protocol lens" showing exactly what flows between
the BFF, MCP server, and PingOne/Authorize — useful for demos and debugging.
Output: McpTrafficPage.js component, App.js route, AdminSideNav link.
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/212-mcp-traffic-log-and-watch-ui/212-01-SUMMARY.md
@/Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/App.js
@/Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/AdminSideNav.jsx

<interfaces>
<!-- From 212-01: GET /api/mcp/traffic response shape -->
Response: { entries: Entry[], logFile: string, count: number }
Entry: {
  ts: string,           // ISO timestamp
  dir: string,          // 'BFF→MCP' | 'MCP→BFF' | 'BFF→PingOne' | 'PingOne→BFF' | 'BFF→Authorize' | 'Authorize→BFF'
  type: string,         // 'rpc_request' | 'rpc_response' | 'exchange_request' | 'exchange_response' | 'authorize_request' | 'authorize_response' | 'error'
  method: string|null,
  tool: string|null,
  statusCode: number|null,
  durationMs: number|null,
  ok: boolean,
  summary: string,
  correlationId: string|null
}

<!-- From AdminSideNav: Monitoring section children array (line ~92) -->
{ label: 'Activity Logs', path: '/activity', icon: '📝' },
{ label: 'Audit Trail', path: '/audit', icon: '🔍' },
{ label: 'API Traffic', path: '/api-traffic', icon: '📡' },
// → ADD: { label: 'MCP Traffic', path: '/mcp-traffic', icon: '🔌' },

<!-- From App.js: existing protected routes (line ~676) -->
<Route path="/logs"        element={user ? <LogViewerPage /> : <Navigate to="/" replace />} />
<Route path="/api-traffic" element={user ? <ApiTrafficPage /> : <Navigate to="/" replace />} />
// → ADD: <Route path="/mcp-traffic" element={user ? <McpTrafficPage /> : <Navigate to="/" replace />} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create McpTrafficPage component</name>
  <files>banking_api_ui/src/components/McpTrafficPage.js</files>
  <action>
Create `banking_api_ui/src/components/McpTrafficPage.js`:

- React functional component using useState + useEffect + useRef
- Polls `${process.env.REACT_APP_API_URL || ''}/api/mcp/traffic?limit=200` every 3000ms with `credentials: 'include'`
- State: `entries[]`, `live` (boolean, default true), `error` (string|null), `lastUpdated` (Date|null)
- A "Live ●" / "Paused ◼" toggle button stops/starts the poll interval
- A "Clear" button sets entries to []
- Entry table columns: Time (HH:MM:SS from ts), Direction (dir), Type (type), Tool (tool || '—'), Duration (durationMs ? `${durationMs}ms` : ''), Summary (summary)
- Color coding per row: ok===false → red-tinted bg; type includes 'exchange' → amber-tinted; rpc_response → green-tinted; default → subtle
- Direction badge: small pill badge with color: BFF→MCP=blue, MCP→BFF=teal, BFF→PingOne=purple, PingOne→BFF=indigo, error-type=red
- Show entry count + logFile path in a subtle header line
- If no entries yet: show "No MCP traffic captured yet. Trigger a tool action to see entries here." message
- If fetch error: show error banner but keep showing existing entries

Use CSS variables for card background (`var(--color-bg-card, #1a1d27)`), border (`var(--color-border, #2e3147)`), text (`var(--color-text, #e2e4ef)`). Table should be full-width with sticky header row. Font size 12px for table data, 13px for header.

Component export: `export default function McpTrafficPage() { ... }`
  </action>
  <verify>node --check banking_api_ui/src/components/McpTrafficPage.js && echo "OK"</verify>
  <done>File exists and passes --check. Contains McpTrafficPage export, fetch call to /api/mcp/traffic, Live toggle, Clear button.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into App.js and AdminSideNav + build</name>
  <files>banking_api_ui/src/App.js, banking_api_ui/src/components/AdminSideNav.jsx</files>
  <action>
**App.js:**

1. Add import near the other page imports (after ApiTrafficPage import):
```js
import McpTrafficPage from './components/McpTrafficPage';
```

2. Add route after the `/api-traffic` route (line ~677):
```jsx
<Route path="/mcp-traffic" element={user ? <McpTrafficPage /> : <Navigate to="/" replace />} />
```

**AdminSideNav.jsx:**

In the `Monitoring` section children array (around line 94, after the API Traffic entry), add:
```js
{ label: 'MCP Traffic', path: '/mcp-traffic', icon: '🔌' },
```

After both changes, run `cd banking_api_ui && npm run build` and fix any errors.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - McpTrafficPage imported in App.js
    - /mcp-traffic route present in App.js
    - MCP Traffic link present in AdminSideNav Monitoring section
    - npm run build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→GET /api/mcp/traffic | Session-authenticated; no user input except limit param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-212-04 | Information Disclosure | McpTrafficPage renders log entries | accept | No raw JWT payloads in log entries (only summaries with tool names/scopes); route is auth-gated |
| T-212-05 | Spoofing | Polling fetch uses credentials:include | accept | Session cookie is HttpOnly; not accessible to JS; CSRF not applicable to GET |
</threat_model>

<verification>
1. Login as admin → navigate to `/mcp-traffic`
2. Trigger a transfer (scope gated) — watch entries appear in Live mode
3. See direction badges, color coding, duration column
4. Click "Paused" → polling stops; click "Live" → resumes
5. Click "Clear" → entries empty (log file intact on server)
6. Check AdminSideNav Monitoring section has "MCP Traffic" link
7. `cd banking_api_ui && npm run build` → exit 0
</verification>

<success_criteria>
- McpTrafficPage renders entries from GET /api/mcp/traffic
- Live/Pause toggle works
- Nav link present
- build passes
</success_criteria>

<output>
After completion, create `.planning/phases/212-mcp-traffic-log-and-watch-ui/212-02-SUMMARY.md`
</output>
