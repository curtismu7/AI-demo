---
phase: 234
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/utils/embeddedAgentFabVisibility.js
  - banking_api_ui/src/context/TokenChainContext.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
must_haves:
  truths:
    - "Token-chain auto polling does not run on routes without token-chain UI"
    - "Token-chain routes still load and refresh token-chain data"
    - "Navigating into a token-chain route triggers a fresh token-chain update"
  artifacts:
    - path: "banking_api_ui/src/utils/embeddedAgentFabVisibility.js"
      provides: "Single route helper for token-chain-capable pages"
      contains: "isTokenChainRoute"
    - path: "banking_api_ui/src/context/TokenChainContext.js"
      provides: "Route-aware polling and refresh guard"
      contains: "isTokenChainRoute"
  key_links:
    - from: "TokenChainContext polling effect"
      to: "isTokenChainRoute(pathname)"
      via: "guard before fetch/setInterval"
      pattern: "if (!isTokenChainRoute"
    - from: "window location changes"
      to: "fetchMCPToolCalls()"
      via: "route-change listener"
      pattern: "popstate"
---

<objective>
Restrict token-chain updates to routes that actually render token-chain UI by adding a centralized route helper and applying it to TokenChainContext polling/refresh logic.

Purpose: Fix the over-frequent token-chain updates reported by the user while preserving normal behavior on token-chain pages.

Output:
- A reusable token-chain route guard helper
- TokenChainContext polling and refresh gated by current route
- No behavior changes on eligible token-chain routes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/234-token-chain-is-updated-way-to-often-it-should-only-udpate-wh/234-CONTEXT.md
@.planning/phases/234-token-chain-is-updated-way-to-often-it-should-only-udpate-wh/234-RESEARCH.md

<interfaces>
From banking_api_ui/src/utils/embeddedAgentFabVisibility.js:
```js
export function isBankingAgentDashboardRoute(pathname) { ... }
export function isEmbeddedAgentDockRoute(pathname) { ... }
```

From banking_api_ui/src/context/TokenChainContext.js:
```js
export function TokenChainProvider({ children }) {
  useEffect(() => {
    const fetchMCPToolCalls = async () => {
      const res = await fetch('/api/token-chain', { credentials: 'include' });
      ...
    };
    const startPolling = () => {
      fetchMCPToolCalls();
      pollInterval = setInterval(fetchMCPToolCalls, 15000);
    };
    ...
  }, []);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add token-chain route helper in embeddedAgentFabVisibility</name>
  <files>banking_api_ui/src/utils/embeddedAgentFabVisibility.js</files>
  <action>
Add a new exported helper `isTokenChainRoute(pathname)` that returns true only for routes where token-chain UI is present.

Implementation guidance:
- Reuse existing route-normalization style in this file (`pathname.replace(/\/$/, '') || '/'`).
- Include currently supported token-chain routes:
  - `/`
  - `/dashboard`
  - `/admin`
  - `/agent-flow-inspector`
- Add a short comment that this helper controls token-chain update eligibility and must be updated when new token-chain surfaces are added.
- Do not change existing helper behavior (isBankingAgentDashboardRoute, isEmbeddedAgentDockRoute, etc.).
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking && node -e "const r=require('./banking_api_ui/src/utils/embeddedAgentFabVisibility.js'); console.assert(r.isTokenChainRoute('/dashboard')===true); console.assert(r.isTokenChainRoute('/config')===false); console.log('PASS')"</automated>
  </verify>
  <done>
- `isTokenChainRoute` exists and is exported
- helper returns true for token-chain pages and false for non-token-chain pages
- no regressions in existing route helper exports
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gate TokenChainContext polling by active token-chain route</name>
  <files>banking_api_ui/src/context/TokenChainContext.js</files>
  <action>
Update the MCP-tool-call polling effect to only fetch/poll when `isTokenChainRoute(window.location.pathname)` is true (per D-01).

Implementation requirements:
- Import `isTokenChainRoute` from `../utils/embeddedAgentFabVisibility`.
- In `fetchMCPToolCalls`, return early when current pathname is not token-chain eligible.
- In poll start logic, only create interval when route is eligible.
- Add route-change handling so entering an eligible route triggers an immediate fetch (for example via `popstate` listener and existing auth event path).
- Preserve existing auth checks and silent-fail behavior.
- Keep minimal diff: do not refactor unrelated identity/session/history logic.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build</automated>
  </verify>
  <done>
- no `/api/token-chain` polling while on non-token-chain routes
- polling/fetch resumes on eligible token-chain routes
- token-chain data still appears on dashboard/inspector flows
- UI build exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser route state -> polling logic | Untrusted navigation state influences whether token-chain fetch runs |
| UI fetch -> BFF /api/token-chain | Authenticated request path for token-chain data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-234-01 | Denial of Service | TokenChainContext polling loop | mitigate | Route gate prevents unnecessary polling outside token-chain pages |
| T-234-02 | Tampering | Route helper maintenance drift | mitigate | Centralize route decisions in single helper with explicit comment and tests |
| T-234-03 | Information Disclosure | token-chain fetch on irrelevant pages | mitigate | Suppress fetch when route has no token-chain surface |
| T-234-04 | Spoofing | Client-controlled pathname | accept | Route gating is optimization only, not security enforcement; server auth still required |
</threat_model>

<verification>
1. `npm run build` in banking_api_ui exits 0
2. On non-token-chain route (e.g., `/config`), network panel shows no recurring `/api/token-chain` polling calls
3. On `/dashboard` and `/agent-flow-inspector`, token-chain data fetches and updates normally
4. Route switch from non-token-chain to token-chain route triggers a fresh fetch without full page reload
</verification>

<success_criteria>
- Token-chain auto updates are route-scoped to token-chain pages only
- Token-chain routes keep functional refresh behavior
- Non-token-chain pages no longer incur background token-chain update churn
- Changes remain minimal and isolated to route-helper + context polling code
</success_criteria>

<output>
After completion, create `.planning/phases/234-token-chain-is-updated-way-to-often-it-should-only-udpate-wh/234-01-SUMMARY.md`
</output>
