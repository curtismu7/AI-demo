---
phase: 274
plan: 02
type: execute
wave: 2
depends_on:
  - 274-01
files_modified:
  - langchain_agent/tests/test_mcp_tool_provider.py
  - langchain_agent/tests/test_mcp_dynamic_tool_schemas.py
  - langchain_agent/tests/test_agent_token_log_lines.py
  - langchain_agent/tests/test_tier1_warning_fixes.py
  - langchain_agent/tests/test_langchain_mcp_agent.py
  - langchain_agent/tests/test_integration_langchain_agent.py
autonomous: true
requirements:
  - ADAPT-02

must_haves:
  truths:
    - "Tests for deleted classes (MCPTool, MCPToolProvider) are removed"
    - "Tests for deleted functions (create_tool_input_schema) are removed"
    - "Surviving tests (WR-05, WR-06, agent integration) pass without errors"
    - "pytest exits 0 for the langchain_agent test suite on the remaining tests"
  artifacts:
    - path: "langchain_agent/tests/test_mcp_tool_provider.py"
      provides: "Deleted — no MCPTool/MCPToolProvider to test"
      contains: "DELETED"
    - path: "langchain_agent/tests/test_mcp_dynamic_tool_schemas.py"
      provides: "Deleted — create_tool_input_schema removed from stub"
      contains: "DELETED"
    - path: "langchain_agent/tests/test_tier1_warning_fixes.py"
      provides: "WR-05 and WR-06 tests updated for stub imports"
    - path: "langchain_agent/tests/test_agent_token_log_lines.py"
      provides: "Updated or deleted — file text check no longer valid"
    - path: "langchain_agent/tests/test_langchain_mcp_agent.py"
      provides: "Agent tests updated to mock MultiServerMCPClient"
    - path: "langchain_agent/tests/test_integration_langchain_agent.py"
      provides: "Integration tests updated to remove MCPToolProvider mocks"
  key_links:
    - from: "test_tier1_warning_fixes.py WR-06 test"
      to: "mcp_tool_provider._current_tracer"
      via: "from agent.mcp_tool_provider import _current_tracer (stub export)"
    - from: "test_langchain_mcp_agent.py"
      to: "langchain_mcp_agent.LangChainMCPAgent"
      via: "mock MultiServerMCPClient.get_tools return value"
---

<objective>
Clean up the test suite after the Plan 01 MCPToolProvider deletion. Remove tests that
tested code which no longer exists. Update surviving tests that mock MCPToolProvider
to instead mock MultiServerMCPClient. Verify the full remaining test suite passes.

Purpose: A clean test suite with no import errors or dead tests gives confidence that
the migration did not break agent behavior.

Output:
- 2 test files deleted (test_mcp_tool_provider.py, test_mcp_dynamic_tool_schemas.py)
- 4 test files updated (tier1, agent_token_log_lines, langchain_mcp_agent, integration)
- `cd langchain_agent && pytest tests/ -x --ignore=tests/test_mcp_connection.py -q` exits 0
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/274-migrate-langchain-mcp-adapters/274-01-SUMMARY.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Imports that exist after Plan 01 — use these in updated tests -->

From langchain_agent/src/agent/mcp_tool_provider.py (stub after Plan 01):
```python
from src.agent.mcp_tool_provider import build_auth_popup_message, _current_tracer
# MCPToolProvider, MCPTool, MCPToolInput, create_tool_input_schema DO NOT EXIST
```

From langchain_mcp_adapters (installed in Plan 01):
```python
from langchain_mcp_adapters.client import MultiServerMCPClient
# Used as async context manager; mock target for agent tests
```

Mocking pattern for MultiServerMCPClient in tests:
```python
from unittest.mock import AsyncMock, MagicMock, patch

mock_tool = MagicMock(spec=BaseTool)
mock_tool.name = "banking_get_my_accounts"
mock_tool.description = "Get accounts"

mock_client = MagicMock()
mock_client.get_tools = AsyncMock(return_value=[mock_tool])
mock_client.__aenter__ = AsyncMock(return_value=mock_client)
mock_client.__aexit__ = AsyncMock(return_value=None)

with patch("src.agent.langchain_mcp_agent.MultiServerMCPClient", return_value=mock_client):
    # agent.initialize_tools() will use the mock client
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete test files and update test_agent_token_log_lines.py</name>
  <files>
    langchain_agent/tests/test_mcp_tool_provider.py
    langchain_agent/tests/test_mcp_dynamic_tool_schemas.py
    langchain_agent/tests/test_agent_token_log_lines.py
  </files>
  <read_first>
    langchain_agent/tests/test_mcp_tool_provider.py
    langchain_agent/tests/test_mcp_dynamic_tool_schemas.py
    langchain_agent/tests/test_agent_token_log_lines.py
  </read_first>
  <action>
Delete test_mcp_tool_provider.py: This file tests MCPTool, MCPToolProvider, MCPToolInput,
and TestTracerContextIsolation. After Plan 01:
- MCPTool and MCPToolProvider are gone from the codebase
- TestTracerContextIsolation tests using `mcp_tool_provider.set_tracer()` are invalid
  (the method no longer exists on any object)
Delete the file entirely: `rm langchain_agent/tests/test_mcp_tool_provider.py`

Delete test_mcp_dynamic_tool_schemas.py: This file tests `create_tool_input_schema` and
`MCPToolInput` from `src.agent.mcp_tool_provider` — both removed in Plan 01 stub.
Delete the file entirely: `rm langchain_agent/tests/test_mcp_dynamic_tool_schemas.py`

Update test_agent_token_log_lines.py: Currently reads `mcp_tool_provider.py` as a text
file and checks for `masked_fingerprint()`. After Plan 01, the stub no longer has any
token handling code at all, making this check meaningless. Two options:
- If the test is checking token masking in the agent layer, point it at `langchain_mcp_agent.py`
  and verify that file doesn't log raw tokens (grep for bare `{agent_token}` or
  `{self._current_agent_token}` without masked_fingerprint).
- If the check is no longer relevant (no token debug logging in stub), delete the file.

Read test_agent_token_log_lines.py to decide: it reads `SRC = Path(...) / "mcp_tool_provider.py"`
and checks for logger.debug with bare token interpolation. Since the stub no longer has
any logger.debug statements with tokens, the check trivially passes but tests nothing useful.
Convert it to check `langchain_mcp_agent.py` instead — update SRC path to point at
`langchain_mcp_agent.py` and verify the same invariant (no bare `{agent_token}` in debug
logs) holds in the new file.
  </action>
  <verify>
    <automated>ls /Users/curtismuir/Development/AI-Demo/langchain_agent/tests/test_mcp_tool_provider.py 2>/dev/null; echo $?</automated>
  </verify>
  <done>
- test_mcp_tool_provider.py does not exist (ls returns non-zero)
- test_mcp_dynamic_tool_schemas.py does not exist
- test_agent_token_log_lines.py either deleted or updated to point at langchain_mcp_agent.py
- `cd langchain_agent && pytest tests/test_agent_token_log_lines.py -q` exits 0 (or file deleted)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_tier1_warning_fixes.py, test_langchain_mcp_agent.py, and test_integration_langchain_agent.py</name>
  <files>
    langchain_agent/tests/test_tier1_warning_fixes.py
    langchain_agent/tests/test_langchain_mcp_agent.py
    langchain_agent/tests/test_integration_langchain_agent.py
  </files>
  <read_first>
    langchain_agent/tests/test_tier1_warning_fixes.py
    langchain_agent/tests/test_langchain_mcp_agent.py
    langchain_agent/tests/test_integration_langchain_agent.py
  </read_first>
  <action>
Update test_tier1_warning_fixes.py:
- Find the WR-05 test that imports `from agent.mcp_tool_provider import build_auth_popup_message`
  — this import still works (build_auth_popup_message is in the stub). No change needed here.
- Find the WR-06 test that imports `from agent.langchain_mcp_agent import build_auth_popup_message as agent_build`
  — after Plan 01, langchain_mcp_agent still imports and re-exports build_auth_popup_message.
  This import should still work. Verify; if the agent no longer re-exports it, update the
  test to import directly from `agent.mcp_tool_provider`.
- Run the file: `cd langchain_agent && pytest tests/test_tier1_warning_fixes.py -q` and
  fix any remaining import errors.

Update test_langchain_mcp_agent.py:
- Find every occurrence of `MCPToolProvider` import and usage in this file.
- Replace `from src.agent.mcp_tool_provider import MCPToolProvider` with:
  `from langchain_mcp_adapters.client import MultiServerMCPClient`
- Replace any fixture that creates a `MCPToolProvider` mock with a fixture that creates
  a `MultiServerMCPClient` mock (use the mock pattern from the interfaces section above).
- In each test that patches `mcp_tool_provider` on the agent, patch
  `src.agent.langchain_mcp_agent.MultiServerMCPClient` instead.
- Tests that check `agent.mcp_tool_provider.set_session_context` or `set_tracer` are
  invalid — either delete those specific test methods or convert them to verify that
  `MultiServerMCPClient` is called with the correct auth header.
- Run: `cd langchain_agent && pytest tests/test_langchain_mcp_agent.py -q` — all remaining
  tests must pass.

Update test_integration_langchain_agent.py:
- Same pattern: replace `MCPToolProvider` import and mock with `MultiServerMCPClient` mock.
- Tests that call `agent.mcp_tool_provider.get_langchain_tools()` should instead mock
  `MultiServerMCPClient.get_tools()` to return a list of mock BaseTool instances.
- Run: `cd langchain_agent && pytest tests/test_integration_langchain_agent.py -q` — all
  remaining tests must pass.

After updating all three files, run the full suite (excluding connection tests which have
pre-existing WebSocket mock issues unrelated to this migration):
`cd langchain_agent && pytest tests/ --ignore=tests/test_mcp_connection.py -q`

Fix any remaining failures that were introduced by the Plan 01 changes (do NOT fix
pre-existing failures in unrelated test files — only fix failures caused by this migration).
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/AI-Demo/langchain_agent && python -m pytest tests/ --ignore=tests/test_mcp_connection.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
- No ImportError for MCPToolProvider, MCPTool, MCPToolInput, or create_tool_input_schema
- pytest exits 0 for all tests except test_mcp_connection.py (pre-existing WebSocket issues)
- test_tier1_warning_fixes.py passes (WR-05 build_auth_popup_message + WR-06 _current_tracer)
- test_langchain_mcp_agent.py passes with MultiServerMCPClient mocks
- test_integration_langchain_agent.py passes with MultiServerMCPClient mocks
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → source modules | Tests import from src.*; import errors indicate broken stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-274-03 | Tampering | Test file deletion | accept | Deleted tests only covered removed classes; no security regression |
| T-274-SC | Tampering | pip/npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Full verification after both tasks:

1. No deleted imports cause errors:
   `cd langchain_agent && python -c "import tests.test_tier1_warning_fixes" 2>&1 | grep -c Error` → 0

2. No MCPToolProvider reference in test files:
   `grep -r "MCPToolProvider\|MCPToolInput\|create_tool_input_schema" langchain_agent/tests/ 2>/dev/null | grep -v "\.pyc"` → empty

3. Full test suite (minus pre-existing WebSocket failures):
   `cd langchain_agent && python -m pytest tests/ --ignore=tests/test_mcp_connection.py -q` → exits 0

4. WR-05/WR-06 tests explicitly pass:
   `cd langchain_agent && python -m pytest tests/test_tier1_warning_fixes.py -v` → all PASSED
</verification>

<success_criteria>
- test_mcp_tool_provider.py and test_mcp_dynamic_tool_schemas.py deleted
- test_tier1_warning_fixes.py all pass (WR-05 + WR-06 invariants)
- test_langchain_mcp_agent.py passes with MultiServerMCPClient mocking
- test_integration_langchain_agent.py passes
- pytest exits 0 for tests/ excluding test_mcp_connection.py
- No ImportError anywhere in the test suite caused by this migration
</success_criteria>

<output>
Create `.planning/phases/274-migrate-langchain-mcp-adapters/274-02-SUMMARY.md` when done
</output>
