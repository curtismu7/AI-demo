---
phase: 185
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/TokenDisplay.css
  - banking_api_ui/src/components/TokenChainDisplay.css
  - banking_api_ui/src/components/PingOneTestPage.jsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "TokenChainDisplay, TokenDisplay, and DecodedTokenPanel all show colored dots (subject=red, actor=blue, mcp=green)"
    - "TokenColorLegend appears in TokenChainDisplay and PingOneTestPage Token Exchange section"
    - "Token type badges in EventRow use RFC 8693 colors (user_token=red, agent_token=blue, exchanged_token=green)"
    - "CSS classes .token-color-dot and .token-color-legend exist for theme override"
    - "Build passes"
  artifacts:
    - path: banking_api_ui/src/components/TokenDisplay.css
      provides: ".token-color-dot and .token-color-legend CSS classes"
      contains: "token-color-dot"
    - path: banking_api_ui/src/components/TokenChainDisplay.css
      provides: "Updated token type badge colors matching RFC 8693 color system"
      contains: "tcd-token-type--user"
    - path: banking_api_ui/src/components/PingOneTestPage.jsx
      provides: "TokenColorLegend imported and rendered in Token Exchange Tests section"
      contains: "TokenColorLegend"
  key_links:
    - from: "banking_api_ui/src/components/TokenColorSystem.js"
      to: "banking_api_ui/src/components/PingOneTestPage.jsx"
      via: "import { TokenColorLegend } added"
      pattern: "TokenColorLegend"
    - from: "banking_api_ui/src/components/TokenDisplay.css"
      to: "banking_api_ui/src/components/TokenColorSystem.js"
      via: "CSS classes match inline style values in TokenColorSystem"
      pattern: "token-color-dot"

---

<objective>
Complete Phase 185 token color integration — most work was done in Phase 188.
Remaining: add CSS classes for theme overrides, fix inconsistent token-type badge colors,
and add TokenColorLegend to PingOneTestPage.

Purpose: Ensure RFC 8693 color system (red=Subject, blue=Actor, green=MCP) is consistent
across ALL token displays and the legend appears wherever tokens are exchanged.
Output: CSS classes, corrected token-type badge colors, legend on PingOneTestPage.
</objective>

<execution_context>
@.planning/phases/185-token-color-legend-and-consistent-token-type-color-coding-across-all-token-displays-including-token-chain-decoded-panels-and-education-components/185-CONTEXT.md
@.planning/phases/185-token-color-legend-and-consistent-token-type-color-coding-across-all-token-displays-including-token-chain-decoded-panels-and-education-components/185-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@banking_api_ui/src/components/TokenColorSystem.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS classes for token-color-dot and token-color-legend</name>
  <files>banking_api_ui/src/components/TokenDisplay.css</files>
  <read_first>
    - banking_api_ui/src/components/TokenColorSystem.js (inline styles to formalize)
    - banking_api_ui/src/components/TokenDisplay.css (append to end)
  </read_first>
  <action>
Append to the END of `banking_api_ui/src/components/TokenDisplay.css`:

```css
/* ─── Token Color System (Phase 185 / RFC 8693) ─────────────────────────── */
/* CSS classes mirror the inline styles in TokenColorSystem.js.              */
/* Allows theme overrides without JS changes.                                */

/* Colored dot representing token type */
.token-color-dot {
  display: inline-block;
  border-radius: 50%;
  flex-shrink: 0;
  margin-right: 6px;
  vertical-align: middle;
}
.token-color-dot--subject { background-color: #dc2626; }
.token-color-dot--actor   { background-color: #2563eb; }
.token-color-dot--mcp     { background-color: #16a34a; }

/* Inline legend bar showing all three token types */
.token-color-legend {
  display: flex;
  gap: 1rem;
  align-items: center;
  padding: 0.4rem 0.75rem;
  font-size: 0.76rem;
  color: #94a3b8;
  border-bottom: 1px solid #1e293b;
}
.token-color-legend__label {
  font-weight: 600;
  margin-right: 4px;
}
.token-color-legend__item {
  display: flex;
  align-items: center;
  gap: 4px;
}
.token-color-legend__dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
}
.token-color-legend__dot--subject { background-color: #dc2626; }
.token-color-legend__dot--actor   { background-color: #2563eb; }
.token-color-legend__dot--mcp     { background-color: #16a34a; }
```
  </action>
  <verify>
    <automated>grep -c "token-color-dot" banking_api_ui/src/components/TokenDisplay.css</automated>
  </verify>
  <acceptance_criteria>
    - TokenDisplay.css contains `.token-color-dot` class
    - TokenDisplay.css contains `.token-color-dot--subject`, `--actor`, `--mcp`
    - TokenDisplay.css contains `.token-color-legend` class
    - Colors match: #dc2626 (subject), #2563eb (actor), #16a34a (mcp)
  </acceptance_criteria>
  <done>CSS classes appended; grep returns count ≥ 1</done>
</task>

<task type="auto">
  <name>Task 2: Fix tcd-token-type badge colors to match RFC 8693 color system</name>
  <files>banking_api_ui/src/components/TokenChainDisplay.css</files>
  <read_first>
    - banking_api_ui/src/components/TokenChainDisplay.css lines 350–390 (token type badge section)
    - banking_api_ui/src/components/TokenColorSystem.js (canonical colors)
  </read_first>
  <action>
Update `.tcd-token-type--user_token` to use red (#dc2626 family) and `.tcd-token-type--agent_token` to use blue (#2563eb family).

Current (wrong):
```css
.tcd-token-type--user_token {
  background: linear-gradient(180deg, #dbeafe 0%, #bfdbfe 100%);  /* blue -- wrong */
  color: var(--chase-navy);
  border-color: var(--chase-navy-light);
}

.tcd-token-type--agent_token {
  background: linear-gradient(180deg, #f3e8ff 0%, #e9d5ff 100%);  /* purple -- wrong */
  color: #6b21a8;
  border-color: #a855f7;
}

.tcd-token-type--exchanged_token {
  background: linear-gradient(180deg, #dcfce7 0%, #bbf7d0 100%);  /* green -- already correct */
  color: #14532d;
  border-color: #22c55e;
}
```

Replace with:
```css
/* Subject token (red — RFC 8693 §2.1) */
.tcd-token-type--user_token {
  background: linear-gradient(180deg, #fee2e2 0%, #fecaca 100%);
  color: #991b1b;
  border-color: #f87171;
}

/* Actor/agent token (blue — RFC 8693 §2.2) */
.tcd-token-type--agent_token {
  background: linear-gradient(180deg, #dbeafe 0%, #bfdbfe 100%);
  color: #1e3a8a;
  border-color: #93c5fd;
}

/* MCP-scoped access token (green — RFC 8693 §3.2) — unchanged */
.tcd-token-type--exchanged_token {
  background: linear-gradient(180deg, #dcfce7 0%, #bbf7d0 100%);
  color: #14532d;
  border-color: #22c55e;
}
```
  </action>
  <verify>
    <automated>grep -A3 "tcd-token-type--user_token" banking_api_ui/src/components/TokenChainDisplay.css | grep "fee2e2"</automated>
  </verify>
  <acceptance_criteria>
    - `.tcd-token-type--user_token` background contains `#fee2e2` (red family)
    - `.tcd-token-type--agent_token` background contains `#dbeafe` (blue family)  
    - `.tcd-token-type--exchanged_token` background contains `#dcfce7` (green family — unchanged)
  </acceptance_criteria>
  <done>Badge colors updated; user_token=red, agent_token=blue, exchanged_token=green (unchanged)</done>
</task>

<task type="auto">
  <name>Task 3: Add TokenColorLegend to PingOneTestPage Token Exchange section</name>
  <files>banking_api_ui/src/components/PingOneTestPage.jsx</files>
  <read_first>
    - banking_api_ui/src/components/PingOneTestPage.jsx lines 1-10 (existing imports)
    - banking_api_ui/src/components/PingOneTestPage.jsx lines 1200-1215 (Token Exchange Tests section header)
    - banking_api_ui/src/components/TokenColorSystem.js (TokenColorLegend export)
  </read_first>
  <action>
1. Add import at top of file (after existing imports, near line 3 where DecodedTokenPanel is imported):
```javascript
import { TokenColorLegend } from './TokenColorSystem';
```

2. Find the Token Exchange Tests section heading (around line 1207):
```jsx
<h2 className="pingone-test-section-title">Token Exchange Tests</h2>
```

Add `<TokenColorLegend />` immediately after it:
```jsx
<h2 className="pingone-test-section-title">Token Exchange Tests</h2>
<TokenColorLegend />
```

This places the legend once at the top of all exchange tests so users know what the colored dots on decoded token panels mean.
  </action>
  <verify>
    <automated>grep -c "TokenColorLegend" banking_api_ui/src/components/PingOneTestPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - PingOneTestPage.jsx imports `TokenColorLegend` from `./TokenColorSystem`
    - PingOneTestPage.jsx renders `<TokenColorLegend />` (at least 1 occurrence)
    - `grep -c "TokenColorLegend" PingOneTestPage.jsx` returns at least 2 (import + render)
  </acceptance_criteria>
  <done>TokenColorLegend imported and rendered in PingOneTestPage exchange section</done>
</task>

<task type="auto">
  <name>Task 4: Verify build and update ROADMAP goal</name>
  <files>banking_api_ui/ (build only), .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 185 section)
  </read_first>
  <action>
1. Run build: `cd banking_api_ui && npm run build` — must exit 0

2. Update ROADMAP.md Phase 185 goal from "[To be planned]" to:
"Consistent token-type color coding (Subject=🔴, Actor=🔵, MCP=🟢) across TokenChainDisplay, TokenDisplay, DecodedTokenPanel, and PingOneTestPage; shared TokenColorLegend component; RFC 8693 badge color alignment."
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | grep "compiled successfully\|Build folder"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - ROADMAP.md Phase 185 goal no longer says "[To be planned]"
  </acceptance_criteria>
  <done>Build passes; ROADMAP goal updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | UI-only CSS + label changes; no auth, data, or API surfaces touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-185-01 | Information | CSS class names | accept | Class names `.token-color-dot--subject/actor/mcp` are purely presentational; they reveal token type labels already visible to the user |
</threat_model>

<verification>
- [ ] `grep -c "token-color-dot" banking_api_ui/src/components/TokenDisplay.css` ≥ 1
- [ ] `grep "fee2e2" banking_api_ui/src/components/TokenChainDisplay.css` matches — user token badge is red
- [ ] `grep -c "TokenColorLegend" banking_api_ui/src/components/PingOneTestPage.jsx` ≥ 2
- [ ] `cd banking_api_ui && npm run build` exits 0
</verification>

<success_criteria>
- CSS classes `.token-color-dot` and `.token-color-legend` added to TokenDisplay.css
- Token type badge colors consistent with RFC 8693 system (red/blue/green)
- PingOneTestPage shows TokenColorLegend in exchange section
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/185-token-color-legend-and-consistent-token-type-color-coding-across-all-token-displays-including-token-chain-decoded-panels-and-education-components/185-01-SUMMARY.md`
</output>
