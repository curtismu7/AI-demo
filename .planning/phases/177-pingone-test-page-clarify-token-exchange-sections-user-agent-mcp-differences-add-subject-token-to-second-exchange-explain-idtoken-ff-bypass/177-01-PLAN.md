---
phase: 177-pingone-test-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/PingOneTestPage.jsx
  - banking_api_server/routes/pingoneTestRoutes.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Exchange 2 and Exchange 3 are clearly differentiated in labels and descriptions"
    - "Exchange 3 shows the subject token (original user token) in decoded panels"
    - "WhatIsHappening explains why scope fixes affected Exchange 3 but not Exchange 2"
    - "ID Token exchange card always visible regardless of ff_id_token_exchange"
  artifacts:
    - path: "banking_api_ui/src/components/PingOneTestPage.jsx"
      provides: "Clarified exchange descriptions, Exchange 3 subject token display, ungated ID Token card"
    - path: "banking_api_server/routes/pingoneTestRoutes.js"
      provides: "Exchange 3 returns subjectTokenDecoded in response"
  key_links:
    - from: "PingOneTestPage.jsx WhatIsHappening"
      to: "Exchange2/Exchange3 TestCards"
      via: "Per-exchange explanation text"
---

<objective>
Clarify Exchange 2 vs Exchange 3 on PingOneTestPage. Both involve User+Agent+MCP tokens but work differently. Add subject_token to Exchange 3 response. Make ID Token card always visible.

Purpose: User sees both Exchange 2 and 3 involving the same three tokens and cannot tell them apart. Exchange 3 had scope fixes applied. Exchange 3 does not return or display the original user token. ID Token card should always show for education.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@banking_api_ui/src/components/PingOneTestPage.jsx
@banking_api_server/routes/pingoneTestRoutes.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify Exchange 2 vs 3 and add subject token to Exchange 3</name>
  <files>banking_api_ui/src/components/PingOneTestPage.jsx, banking_api_server/routes/pingoneTestRoutes.js</files>
  <read_first>
    - banking_api_ui/src/components/PingOneTestPage.jsx
    - banking_api_server/routes/pingoneTestRoutes.js
  </read_first>
  <action>
A. Backend: Add subjectTokenDecoded to Exchange 3 response.

In pingoneTestRoutes.js exchange-user-to-agent-to-mcp route (~line 800), subjectDecoded3 is computed but not returned. Add to responseData: subjectTokenDecoded: subjectDecoded3

B. Frontend state: Add exchange3SubjectDecoded.

Near line 208 add: const [exchange3SubjectDecoded, setExchange3SubjectDecoded] = useState(null);
In testExchange3 (~line 695) after setExchange3McpDecoded: setExchange3SubjectDecoded(data.subjectTokenDecoded || null);

C. Frontend: Show subject token in Exchange 3 card (~line 1340).

Before Agent Token DecodedTokenPanel add:
{exchange3SubjectDecoded and DecodedTokenPanel decoded={exchange3SubjectDecoded} label={Subject: t1Label (input to chain)}

D. Update WhatIsHappening steps (~line 1173):

Replace steps array to distinguish each exchange clearly:
- Exchange 1: Single delegation, no actor, MCP Server audience
- Exchange 2: Single POST two tokens, user=subject agent=actor, MCP Gateway audience, act claim
- Exchange 3: Two-step chain, Step 1 user->agent, Step 2 user+agent->MCP, two POST calls
- Explain how 2 and 3 differ: Exchange 2 sends pre-existing agent CC token. Exchange 3 creates agent token first.
- Explain scope fixes: all routes had openid hardcoded. Exchange 3 has two calls and different app/RS.
- ID Token Exchange: id_token as subject, FF-gated on server

E. Update apiFlow (~line 1184) to show per-exchange specifics.

F. Update TEST_CONFIG spel for exchange2 and exchange3:
- exchange2: Single POST with subject+actor, MCP Gateway
- exchange3: Two-step chain, AI Agent App + Agent Gateway
  </action>
  <acceptance_criteria>
    - pingoneTestRoutes.js exchange-user-to-agent-to-mcp responseData contains subjectTokenDecoded
    - PingOneTestPage.jsx contains exchange3SubjectDecoded state
    - Exchange 3 card shows DecodedTokenPanel for subject
    - WhatIsHappening explains how 2 and 3 differ
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd banking_api_ui and npm run build exits 0</automated>
  </verify>
  <done>Exchange 2 vs 3 clearly distinguished. Exchange 3 shows subject token. Scope fix explanation included.</done>
</task>

<task type="auto">
  <name>Task 2: Show ID Token exchange card always (ungated by FF)</name>
  <files>banking_api_ui/src/components/PingOneTestPage.jsx</files>
  <read_first>
    - banking_api_ui/src/components/PingOneTestPage.jsx (lines 1367-1396)
  </read_first>
  <action>
Replace the ternary {ffIdTokenExchange ? full-card : disabled-card} with a single always-rendered card.
The onTest handler still calls testExchangeIdToken (backend returns 400 if FF off).
Show warning div when !ffIdTokenExchange: "ff_id_token_exchange is OFF — enable in Feature Flags to run this test."
Always show DecodedTokenPanel for results.
  </action>
  <acceptance_criteria>
    - PingOneTestPage.jsx does NOT contain status="disabled" for ID token card
    - PingOneTestPage.jsx contains ff_id_token_exchange is OFF warning
    - ID token card NOT in ternary conditional
    - npm run build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd banking_api_ui and npm run build exits 0</automated>
  </verify>
  <done>ID Token card always visible. Warning when FF off. Backend still enforces.</done>
</task>

</tasks>

<threat_model>
Trust Boundaries: Frontend to BFF test routes (read-only GET, no elevation risk)

STRIDE: T-177-01 Info Disclosure (accept, test page purpose). T-177-02 ID Token FF bypass (accept, backend enforces).
</threat_model>

<verification>
1. npm run build exits 0
2. Exchange 2 and 3 have distinct descriptions
3. Exchange 3 shows subject token
4. ID Token card visible when FF off
</verification>

<success_criteria>
Exchange 2 vs 3 differentiated. Exchange 3 subject token. ID Token always visible. Build passes.
</success_criteria>

<output>
After completion create 177-01-SUMMARY.md in the phase directory.
</output>
