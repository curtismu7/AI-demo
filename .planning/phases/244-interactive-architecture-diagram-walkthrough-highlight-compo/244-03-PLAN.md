---
phase: 244-interactive-architecture-diagram-walkthrough-highlight-compo
plan: 03
type: execute
wave: 3
depends_on:
  - 244-02-PLAN.md
files_modified:
  - banking_api_ui/src/App.js
  - banking_api_ui/src/components/AdminSideNav.jsx
  - banking_api_ui/src/utils/embeddedAgentFabVisibility.js
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "Navigating to /architecture/overview renders ArchitectureOverviewPage with AdminSideNav and TopNav"
    - "Navigating to /architecture/token-flow renders ArchitectureTokenFlowPage with AdminSideNav and TopNav"
    - "AdminSideNav shows Architecture group with two child links visible to all logged-in users"
    - "The FAB agent is visible on /architecture/overview and /architecture/token-flow"
    - "isMonitoringRoute('/architecture/overview') returns true"
    - "isEmbeddedAgentDockRoute('/architecture/overview') returns true"
    - "The sidebar renders on /architecture/* routes (sidebarRoutePatterns includes /architecture)"
    - "npm run build exits 0 with no new warnings"
  artifacts:
    - path: "banking_api_ui/src/App.js"
      provides: "Route for /architecture/* outer block; imports ArchitectureOverviewPage + ArchitectureTokenFlowPage; /architecture in sidebarRoutePatterns"
      contains: "path=\"/architecture/*\""
    - path: "banking_api_ui/src/components/AdminSideNav.jsx"
      provides: "Architecture nav group with Overview Diagram and Token Flow Diagram children; NOT adminOnly"
      contains: "Architecture"
    - path: "banking_api_ui/src/utils/embeddedAgentFabVisibility.js"
      provides: "'/architecture' added to isEmbeddedAgentDockRoute startsWith check and MONITORING_PREFIXES"
      contains: "'/architecture'"
  key_links:
    - from: "banking_api_ui/src/App.js"
      to: "banking_api_ui/src/components/ArchitectureOverviewPage.js"
      via: "static import at top of file + <Route path=\"overview\" element={<ArchitectureOverviewPage user={user} />} />"
      pattern: "ArchitectureOverviewPage"
    - from: "banking_api_ui/src/App.js"
      to: "banking_api_ui/src/utils/embeddedAgentFabVisibility.js"
      via: "isMonitoringRoute, isEmbeddedAgentDockRoute already imported; /architecture now handled"
      pattern: "isMonitoringRoute"
---

<objective>
Wire the two new page components into the application: register /architecture/* outer routes in App.js, add Architecture nav group to AdminSideNav, and register /architecture paths in both FAB visibility utilities. This plan must update all three files atomically — partial updates cause the FAB to disappear or the sidebar to vanish on architecture routes.

Purpose: Makes both architecture pages reachable by URL, visible in the sidebar, and correctly shows the embedded agent FAB so users can trigger agent tool calls and watch the diagram highlight in real-time.

Output:
- App.js: /architecture/* route block + two static imports + /architecture in sidebarRoutePatterns
- AdminSideNav.jsx: Architecture group with two nav items (NOT adminOnly)
- embeddedAgentFabVisibility.js: /architecture in isEmbeddedAgentDockRoute and MONITORING_PREFIXES
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/244-interactive-architecture-diagram-walkthrough-highlight-compo/244-02-SUMMARY.md
@banking_api_ui/src/App.js
@banking_api_ui/src/components/AdminSideNav.jsx
@banking_api_ui/src/utils/embeddedAgentFabVisibility.js

<interfaces>
<!-- Exact insertion points extracted from codebase reads. Executor uses these directly. -->

App.js — static imports block (lines 1–110, alphabetical order):
```javascript
// Insert after existing 'A' imports near line 30–35, alphabetical position:
import ArchitectureOverviewPage from "./components/ArchitectureOverviewPage";
import ArchitectureTokenFlowPage from "./components/ArchitectureTokenFlowPage";
```

App.js — sidebarRoutePatterns array (lines 587–612):
```javascript
// Current last entry: "/monitoring",
// ADD after "/monitoring":
"/architecture",
```

App.js — /monitoring/* route block ends around line 809. Insert the new /architecture/* route block AFTER the closing of the /monitoring/* Route element (after line 809):
```javascript
<Route
  path="/architecture/*"
  element={
    <>
      <AdminSideNav user={user} />
      <TopNav user={user} onLogout={logout} />
      <main className="main-content">
        <Routes>
          <Route path="overview" element={<ArchitectureOverviewPage user={user} />} />
          <Route path="token-flow" element={<ArchitectureTokenFlowPage user={user} />} />
        </Routes>
      </main>
    </>
  }
/>
```

AdminSideNav.jsx — Monitoring section (lines 120–135):
```javascript
// Current structure:
{
  label: "Monitoring",
  icon: "📋",
  children: [
    { label: "Activity Logs", ... },
    // ... existing children
  ],
},
// INSERT a new sibling group object AFTER the Monitoring group closing brace:
{
  label: "Architecture",
  icon: "🗺️",
  children: [
    { label: "Overview Diagram",   path: "/architecture/overview",   icon: "🏗️" },
    { label: "Token Flow Diagram", path: "/architecture/token-flow", icon: "🔗" },
  ],
},
// NOTE: NO adminOnly: true field — these pages are accessible to all logged-in users.
```

embeddedAgentFabVisibility.js — isEmbeddedAgentDockRoute (lines 20–28):
```javascript
// Current check:
if (p.startsWith('/monitoring') || p === '/api-traffic' || p === '/mcp-traffic' || p === '/dev-tools') return true;
// Change to:
if (p.startsWith('/monitoring') || p.startsWith('/architecture') || p === '/api-traffic' || p === '/mcp-traffic' || p === '/dev-tools') return true;
```

embeddedAgentFabVisibility.js — MONITORING_PREFIXES array (lines 92–103):
```javascript
// Current array:
const MONITORING_PREFIXES = [
  '/activity',
  '/audit',
  '/logs',
  '/api-traffic',
  '/mcp-traffic',
  '/dev-tools',
  '/monitoring',
];
// Add to this array:
  '/architecture',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.js — imports, sidebarRoutePatterns, and /architecture/* route</name>
  <files>banking_api_ui/src/App.js</files>
  <read_first>
    - banking_api_ui/src/App.js lines 1–40 (import block — find alphabetical insert position for ArchitectureOverviewPage and ArchitectureTokenFlowPage)
    - banking_api_ui/src/App.js lines 585–615 (sidebarRoutePatterns array — find exact position to add "/architecture")
    - banking_api_ui/src/App.js lines 790–815 (/monitoring/* route block — find exact end position for the new /architecture/* block insertion)
  </read_first>
  <action>
Make three targeted edits to banking_api_ui/src/App.js. Use the Edit tool for each; do NOT rewrite the entire file.

EDIT 1 — Add two static imports in the alphabetical import block.
Find the line `import ApiExplorerPanel from "./components/ApiExplorerPanel";` (or the first line that alphabetically follows "Arc..."). Insert these two lines immediately before the existing line that starts with the first component alphabetically after "ArchitectureTokenFlowPage" (i.e., before whatever comes after "Architecture" in the existing list — likely ApiTrafficPage or similar):

```javascript
import ArchitectureOverviewPage from "./components/ArchitectureOverviewPage";
import ArchitectureTokenFlowPage from "./components/ArchitectureTokenFlowPage";
```

IMPORTANT: Read lines 1–40 of App.js first to confirm the exact insertion point. The imports are alphabetical — "Architecture" sorts between "Ag..." and "Api..." approximately. Find the exact gap.

EDIT 2 — Add "/architecture" to sidebarRoutePatterns.
Find the line `"/monitoring",` in the sidebarRoutePatterns array and add `"/architecture",` on the next line immediately after it:

Before:
```javascript
		"/monitoring",
	];
```

After:
```javascript
		"/monitoring",
		"/architecture",
	];
```

EDIT 3 — Add the /architecture/* outer Route block.
Find the closing `/>` of the /monitoring/* Route element (around line 809) and insert the new Route block immediately after it:

```javascript
							<Route
								path="/architecture/*"
								element={
									<>
										<AdminSideNav user={user} />
										<TopNav user={user} onLogout={logout} />
										<main className="main-content">
											<Routes>
												<Route path="overview" element={<ArchitectureOverviewPage user={user} />} />
												<Route path="token-flow" element={<ArchitectureTokenFlowPage user={user} />} />
											</Routes>
										</main>
									</>
								}
							/>
```

Use the same tab indentation as the surrounding /monitoring/* route block. Verify by reading the lines around line 809 before editing.
  </action>
  <acceptance_criteria>
    - grep -n "import ArchitectureOverviewPage" banking_api_ui/src/App.js — returns match
    - grep -n "import ArchitectureTokenFlowPage" banking_api_ui/src/App.js — returns match
    - grep -n '"/architecture"' banking_api_ui/src/App.js — returns match (in sidebarRoutePatterns)
    - grep -n 'path="/architecture/\*"' banking_api_ui/src/App.js — returns match (route block)
    - grep -n "ArchitectureOverviewPage user={user}" banking_api_ui/src/App.js — returns match
    - grep -n "ArchitectureTokenFlowPage user={user}" banking_api_ui/src/App.js — returns match
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -8</automated>
  </verify>
  <done>App.js imports both page components; /architecture in sidebarRoutePatterns; /architecture/* Route block present with inner routes for overview and token-flow; build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update AdminSideNav and embeddedAgentFabVisibility — nav items + FAB registration</name>
  <files>banking_api_ui/src/components/AdminSideNav.jsx, banking_api_ui/src/utils/embeddedAgentFabVisibility.js</files>
  <read_first>
    - banking_api_ui/src/components/AdminSideNav.jsx lines 100–185 (allNavItems array — find exact position after Monitoring group closing brace to insert Architecture group)
    - banking_api_ui/src/utils/embeddedAgentFabVisibility.js lines 20–30 (isEmbeddedAgentDockRoute — exact line to edit)
    - banking_api_ui/src/utils/embeddedAgentFabVisibility.js lines 89–105 (MONITORING_PREFIXES array — exact position to add '/architecture')
  </read_first>
  <action>
Make three targeted edits using the Edit tool. Do NOT rewrite either file.

--- AdminSideNav.jsx ---

EDIT 1 — Add Architecture nav group.
Find the closing brace of the Monitoring section object (around line 135) in allNavItems. Insert a new sibling object immediately after it. The Monitoring group ends with:
```javascript
		},
```
(the closing of the children array followed by the object closing brace). Insert after that closing brace and before the next nav item:

```javascript
		{
			label: "Architecture",
			icon: "🗺️",
			children: [
				{ label: "Overview Diagram",   path: "/architecture/overview",   icon: "🏗️" },
				{ label: "Token Flow Diagram", path: "/architecture/token-flow", icon: "🔗" },
			],
		},
```

IMPORTANT: Do NOT add adminOnly: true. These pages are accessible to all logged-in users per the locked CONTEXT.md decision (UI Placement: "accessible to customer users (non-admin)").

Also update the expandedSections initialization logic in the useState at the top of the component (around lines 34–47). Add '/architecture' to the monitoringPaths array so the Architecture section auto-expands when on an architecture route:

Find:
```javascript
		const monitoringPaths = ['/monitoring', '/activity', '/audit', '/api-traffic', '/mcp-traffic', '/dev-tools'];
```

Replace with:
```javascript
		const monitoringPaths = ['/monitoring', '/activity', '/audit', '/api-traffic', '/mcp-traffic', '/dev-tools', '/architecture'];
```

--- embeddedAgentFabVisibility.js ---

EDIT 2 — Add /architecture to isEmbeddedAgentDockRoute.
Find line 26 (the startsWith check):
```javascript
  if (p.startsWith('/monitoring') || p === '/api-traffic' || p === '/mcp-traffic' || p === '/dev-tools') return true;
```
Replace with:
```javascript
  if (p.startsWith('/monitoring') || p.startsWith('/architecture') || p === '/api-traffic' || p === '/mcp-traffic' || p === '/dev-tools') return true;
```

EDIT 3 — Add '/architecture' to MONITORING_PREFIXES array in isMonitoringRoute.
Find:
```javascript
    '/monitoring',       // covers /monitoring/token-chain, /monitoring/token-diff,
                         //         /monitoring/flow-inspector, /monitoring/mcp-traffic,
                         //         /monitoring/api-explorer
```
Add '/architecture' on the next line after '/monitoring' entry:
```javascript
    '/monitoring',       // covers /monitoring/token-chain, /monitoring/token-diff,
                         //         /monitoring/flow-inspector, /monitoring/mcp-traffic,
                         //         /monitoring/api-explorer
    '/architecture',     // covers /architecture/overview, /architecture/token-flow
```
  </action>
  <acceptance_criteria>
    - grep -n "Architecture" banking_api_ui/src/components/AdminSideNav.jsx — returns match (nav group label)
    - grep -n '"/architecture/overview"' banking_api_ui/src/components/AdminSideNav.jsx — returns match
    - grep -n '"/architecture/token-flow"' banking_api_ui/src/components/AdminSideNav.jsx — returns match
    - grep -n "adminOnly.*Architecture\|Architecture.*adminOnly" banking_api_ui/src/components/AdminSideNav.jsx — returns NO MATCH (must not be admin-only)
    - grep -n "startsWith.*'/architecture'" banking_api_ui/src/utils/embeddedAgentFabVisibility.js — returns match
    - grep -n "'/architecture'" banking_api_ui/src/utils/embeddedAgentFabVisibility.js — returns >= 2 matches (once in isEmbeddedAgentDockRoute, once in MONITORING_PREFIXES)
    - grep -n '/architecture' banking_api_ui/src/components/AdminSideNav.jsx — returns match (in monitoringPaths)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>AdminSideNav.jsx has Architecture group with two children, not adminOnly; embeddedAgentFabVisibility.js has /architecture in both isEmbeddedAgentDockRoute and MONITORING_PREFIXES; build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route access | /architecture/* routes render for any logged-in user; event polling inside pages is admin-gated separately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-244-06 | Elevation of Privilege | /architecture/* route visibility | accept | Routes are visible to all logged-in users; this is intentional per D-UI-Placement locked decision; event endpoint access remains admin-gated inside the page component |
| T-244-07 | Information Disclosure | FAB on architecture pages | accept | FAB triggers agent tool calls; agent scope enforcement is handled upstream by MCP gateway and PingAuthorize; no new exposure from adding FAB to these routes |
</threat_model>

<verification>
Final phase verification — run all checks:

1. Build: `cd banking_api_ui && npm run build` — exit code must be 0
2. Route imports: `grep -n "import ArchitectureOverviewPage\|import ArchitectureTokenFlowPage" banking_api_ui/src/App.js` — both present
3. Route block: `grep -n 'path="/architecture/\*"' banking_api_ui/src/App.js` — returns match
4. Sidebar patterns: `grep -n '"/architecture"' banking_api_ui/src/App.js` — returns match (in sidebarRoutePatterns)
5. Nav items: `grep -n "architecture/overview\|architecture/token-flow" banking_api_ui/src/components/AdminSideNav.jsx` — both present
6. FAB dock route: `grep -n "startsWith.*'/architecture'" banking_api_ui/src/utils/embeddedAgentFabVisibility.js` — returns match
7. Monitoring route: `grep -n "'/architecture'" banking_api_ui/src/utils/embeddedAgentFabVisibility.js` — returns >= 2 matches
</verification>

<success_criteria>
- App.js imports ArchitectureOverviewPage and ArchitectureTokenFlowPage
- App.js has /architecture/* outer Route block with inner routes for overview and token-flow
- App.js sidebarRoutePatterns includes "/architecture"
- AdminSideNav.jsx allNavItems has Architecture group with two children; group has no adminOnly field
- AdminSideNav.jsx monitoringPaths includes '/architecture' for auto-expand
- embeddedAgentFabVisibility.js isEmbeddedAgentDockRoute includes p.startsWith('/architecture')
- embeddedAgentFabVisibility.js MONITORING_PREFIXES includes '/architecture'
- npm run build exits 0 with no new errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/244-interactive-architecture-diagram-walkthrough-highlight-compo/244-03-SUMMARY.md`
</output>
