---
phase: 241-monitoring-pages-promote-tracking-panels-to-sidebar-pages-un
plan: 02
type: execute
wave: 2
depends_on:
  - "241-01"
files_modified:
  - banking_api_ui/src/App.js
autonomous: false
requirements:
  - MONITORING-PAGES-04

must_haves:
  truths:
    - "npm run build in banking_api_ui exits 0 with no new errors"
    - "All 5 /monitoring/* routes render their panel when visited while authenticated"
    - "Sidebar shows (AdminSideNav visible) when navigating to any /monitoring/* URL"
    - "Unauthenticated access to /monitoring/token-chain redirects to /"
    - "Unauthenticated access to /monitoring/mcp-traffic renders McpTrafficPage (no redirect)"
    - "Existing /mcp-traffic and /agent-flow-inspector routes still work"
  artifacts:
    - path: "banking_api_ui/src/App.js"
      provides: "Clean build with all 5 monitoring routes registered"
      contains: "/monitoring/token-chain"
  key_links:
    - from: "npm run build"
      to: "exit code 0"
      via: "no import resolution errors for TokenChainDisplay, TokenDiffPanel, ApiExplorerPanel"
      pattern: "exit 0"
---

<objective>
Verify the Phase 241 route + sidebar changes build successfully and behave correctly in a running dev server.

Purpose: Catch any import resolution errors, missing component exports, or prop-type mismatches before shipping.
Output: Confirmed green build + human verification that sidebar navigation works.
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/241-monitoring-pages-promote-tracking-panels-to-sidebar-pages-un/241-UI-SPEC.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Production build verification</name>
  <files>banking_api_ui/src/App.js</files>
  <read_first>
    - banking_api_ui/src/App.js (scan import block for TokenChainDisplay, TokenDiffPanel, ApiExplorerPanel to confirm plan 241-01 completed)
  </read_first>
  <action>
    Run the production build and confirm it exits 0:

    ```bash
    cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1
    ```

    If the build fails:
    - If error is "Cannot find module './components/TokenChainDisplay'" — check the file exists at `banking_api_ui/src/components/TokenChainDisplay.js` and fix the import casing
    - If error is "Cannot find module './components/TokenDiffPanel'" — check `banking_api_ui/src/components/TokenDiffPanel.js` exists
    - If error is "Cannot find module './components/ApiExplorerPanel'" — check `banking_api_ui/src/components/ApiExplorerPanel.js` exists
    - If error is a JSX prop-types warning on UnifiedTokenFlowInspector — acceptable (warning, not error); build still passes
    - Any other error: read the full error message and fix the root cause — do not comment out routes

    After a green build, verify the component files actually export a default:
    ```bash
    grep -n "export default" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/TokenChainDisplay.js | head -3
    grep -n "export default" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/TokenDiffPanel.js | head -3
    grep -n "export default" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/ApiExplorerPanel.js | head -3
    ```
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Build output contains "The build folder is ready to be deployed." OR "Successfully compiled"
    - Build process exits with code 0 (no "Failed to compile" message in output)
    - No "Cannot find module" errors in build output
  </acceptance_criteria>
  <done>npm run build exits 0; no module resolution errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 241-01 added 5 /monitoring/* routes to App.js and 5 nav items to the AdminSideNav Monitoring group. Plan 241-02 Task 1 confirmed a green build.
  </what-built>
  <how-to-verify>
    1. Start the app: `cd /Users/cmuir/P1Import-apps/Banking && npm start` (or `./run-bank.sh` / `./run.sh`)
    2. Log in as an admin user
    3. Expand the "Monitoring" section in the left sidebar — confirm it now has 10 items including the 5 new ones: Token Chain, Token Diff, Flow Inspector, MCP Traffic (new), API Explorer
    4. Click "Token Chain" — confirm /monitoring/token-chain loads with TokenChainDisplay including its header (not hidden)
    5. Click "Token Diff" — confirm /monitoring/token-diff loads with TokenDiffPanel
    6. Click "Flow Inspector" — confirm /monitoring/flow-inspector loads with UnifiedTokenFlowInspector (not floating, no toggle)
    7. Click "MCP Traffic" (the new /monitoring/mcp-traffic entry) — confirm McpTrafficPage loads
    8. Click "API Explorer" — confirm /monitoring/api-explorer loads with ApiExplorerPanel
    9. Confirm the sidebar remains visible on all 5 pages
    10. Navigate to the ORIGINAL "MCP Traffic" (/mcp-traffic) item — confirm it still works and was not removed
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 pages load correctly with sidebar visible, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline | CRA webpack resolves imports at build time — missing modules fail the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-241-02-01 | Tampering | Build output | accept | Build is deterministic; CRA exits non-zero on errors — Task 1 catches any issues |
</threat_model>

<verification>
```bash
cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | grep -E "compiled|error|Failed"
```

Expected: "Compiled successfully" or "The build folder is ready to be deployed."
</verification>

<success_criteria>
- npm run build exits 0 with no errors
- Human verifies all 5 /monitoring/* pages render their panel with sidebar visible
- Existing /mcp-traffic still works
- No regressions on /agent-flow-inspector or other pre-existing routes
</success_criteria>

<output>
After completion, create `.planning/phases/241-monitoring-pages-promote-tracking-panels-to-sidebar-pages-un/241-02-SUMMARY.md`
</output>
