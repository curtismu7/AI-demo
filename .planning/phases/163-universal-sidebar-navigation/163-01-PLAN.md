---
phase: 163-universal-sidebar-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/AdminSideNav.jsx
  - banking_api_ui/src/components/AdminSideNav.css
  - banking_api_ui/src/App.js
autonomous: true
requirements: [NAV-163-01, NAV-163-02, NAV-163-03]

must_haves:
  truths:
    - "All logged-in users see the sidebar on every page"
    - "Admin-only menu items (Monitoring, OAuth & Security, System Tools) are hidden for non-admin users"
    - "Non-admin users see Home, Dashboard, Users & Accounts in sidebar"
    - "Sidebar renders on /marketing, /dashboard, /configure, and all other routes when logged in"
  artifacts:
    - path: "banking_api_ui/src/components/AdminSideNav.jsx"
      provides: "Role-aware sidebar component accepting user prop"
    - path: "banking_api_ui/src/App.js"
      provides: "Sidebar rendered for all logged-in users, not just admin"
  key_links:
    - from: "banking_api_ui/src/App.js"
      to: "AdminSideNav.jsx"
      via: "user prop passed; rendered when user exists (not just admin)"
      pattern: "user && <AdminSideNav"
---

<objective>
Make AdminSideNav render for ALL logged-in users (not just admins) and filter menu items by role.

Purpose: Users currently have no sidebar — only admins see it. This gives all logged-in users consistent left-side navigation.
Output: AdminSideNav accepts `user` prop, shows role-appropriate menu items, renders on all routes when logged in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@banking_api_ui/src/components/AdminSideNav.jsx
@banking_api_ui/src/App.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make AdminSideNav role-aware with user prop</name>
  <files>banking_api_ui/src/components/AdminSideNav.jsx</files>
  <action>
  Accept a `user` prop. Filter `adminNavItems` by role:
  - ALL users see: Home (/marketing), Dashboard (/dashboard for non-admin, /admin for admin)
  - ALL users see: Users & Accounts section (Users, Accounts, Transactions) — these are user-facing pages too
  - ADMIN ONLY: Monitoring section (Activity Logs, Audit Trail, API Traffic)
  - ADMIN ONLY: OAuth & Security section (Security Settings, OAuth Debug, Client Reg, Scope Audit, Scope Reference)
  - ADMIN ONLY: System Tools section (Feature Flags, MCP Inspector)
  
  Implementation: Add `adminOnly: true` property to the sections that require admin role. Filter them out when `user?.role !== 'admin'`.
  
  Also add non-admin items:
  - PingOne Test (/pingone-test, icon 🧪) — useful for all users
  - MFA Test (/mfa-test, icon 🔒) — useful for all users
  
  Keep the existing action items (Agent, Dark Mode, Log Out) for all users.
  
  The Dashboard path should be role-aware: `/admin` for admin, `/dashboard` for non-admin.
  </action>
  <verify>npm run build in banking_api_ui passes (exit 0)</verify>
  <done>AdminSideNav accepts user prop, renders role-appropriate menu items. Admin sees all items, non-admin sees filtered subset.</done>
</task>

<task type="auto">
  <name>Task 2: Render sidebar for all logged-in users in App.js</name>
  <files>banking_api_ui/src/App.js</files>
  <action>
  Change all 4 instances of `{user?.role === 'admin' && <AdminSideNav />}` to `{user && <AdminSideNav user={user} />}`.
  
  Also add sidebar to routes that currently lack it:
  - The `/marketing` route when user is logged in: wrap in fragment with `{user && <AdminSideNav user={user} />}`
  
  Update the `adminRoutePatterns` array and `isOnAdminRoute` logic — rename to `sidebarRoutePatterns` since sidebar is now universal. This affects the FAB suppression logic (AdminSideNav provides Agent button, so FAB should be suppressed when sidebar is visible).
  
  Pass `user={user}` to every AdminSideNav render.
  </action>
  <verify>npm run build in banking_api_ui passes (exit 0)</verify>
  <done>Sidebar renders on every route for all logged-in users. Admin sees full menu, non-admin sees filtered menu. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client sidebar | Menu items filtered client-side; server-side AdminRoute guards remain unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-01 | Tampering | AdminSideNav role filter | accept | Client-side filter is cosmetic; AdminRoute server guards + React AdminRoute wrapper still enforce access control on actual routes |
| T-163-02 | Information Disclosure | Menu visibility | accept | Showing nav links to admin pages is low risk — actual page access is still guarded by AdminRoute component |
</threat_model>

<verification>
- `cd banking_api_ui && npm run build` → exit 0
- Log in as admin → see full sidebar with all sections
- Log in as non-admin user → see sidebar with Home, Dashboard, Users & Accounts, PingOne Test, MFA Test only
- Sidebar visible on /marketing, /dashboard, /configure, /admin (admin), all routes
</verification>

<success_criteria>
All logged-in users see role-appropriate sidebar navigation on every page. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/163-universal-sidebar-navigation/163-01-SUMMARY.md`
</output>
