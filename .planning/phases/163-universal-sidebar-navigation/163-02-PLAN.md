---
phase: 163-universal-sidebar-navigation
plan: 02
type: execute
wave: 2
depends_on: [163-01]
files_modified:
  - banking_api_ui/src/components/TopNav.js
  - banking_api_ui/src/components/TopNav.css
  - banking_api_ui/src/components/ChaseTopNav.js
  - banking_api_ui/src/components/ChaseTopNav.css
  - banking_api_ui/src/components/DashboardQuickNav.js
  - banking_api_ui/src/components/UserDashboard.js
  - banking_api_ui/src/components/LandingPage.js
  - banking_api_ui/src/App.js
autonomous: false
requirements: [NAV-163-04, NAV-163-05]

must_haves:
  truths:
    - "TopNav shows only brand bar (logo, user greeting, theme toggle, logout) — no navigation dropdown menus"
    - "ChaseTopNav shows only brand bar — no nav link row"
    - "DashboardQuickNav is removed or fully disabled (sidebar replaces it)"
    - "No duplicate navigation exists — sidebar is the single source of page navigation"
  artifacts:
    - path: "banking_api_ui/src/components/TopNav.js"
      provides: "Simplified brand-only top bar"
    - path: "banking_api_ui/src/components/ChaseTopNav.js"
      provides: "Simplified brand-only top bar"
  key_links:
    - from: "banking_api_ui/src/App.js"
      to: "TopNav.js"
      via: "Still rendered but with no navigation links"
    - from: "banking_api_ui/src/components/UserDashboard.js"
      to: "ChaseTopNav.js"
      via: "Still rendered but simplified to brand bar only"
---

<objective>
Strip redundant navigation links from TopNav and ChaseTopNav (keep brand bar only), remove DashboardQuickNav.

Purpose: With sidebar on all pages, top nav links are redundant. Keep only the brand identity bar (logo, user info, theme toggle, logout).
Output: Clean single-source navigation via sidebar. Top bars are brand-only headers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/163-universal-sidebar-navigation/163-01-SUMMARY.md
@banking_api_ui/src/components/TopNav.js
@banking_api_ui/src/components/ChaseTopNav.js
@banking_api_ui/src/components/DashboardQuickNav.js
@banking_api_ui/src/components/UserDashboard.js
@banking_api_ui/src/components/LandingPage.js
@banking_api_ui/src/App.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip nav links from TopNav and ChaseTopNav, remove DashboardQuickNav usage</name>
  <files>
    banking_api_ui/src/components/TopNav.js,
    banking_api_ui/src/components/ChaseTopNav.js,
    banking_api_ui/src/components/DashboardQuickNav.js,
    banking_api_ui/src/components/UserDashboard.js,
    banking_api_ui/src/components/LandingPage.js,
    banking_api_ui/src/App.js
  </files>
  <action>
  **TopNav.js:** Remove the `navGroups` array and the dropdown menu rendering. Keep only:
  - Logo/brand area (MdAccountBalance icon + "BX Finance")
  - UserMenu component (greeting, logout)
  - Search icon (if used)
  - Mobile hamburger opens sidebar instead of dropdown (or remove mobile menu entirely — sidebar handles it)
  
  **ChaseTopNav.js:** Remove the `navLinks` array and the center navigation link row. Keep only:
  - Left: Logo + brand name
  - Right: User greeting, Learn button (TRiSM), theme toggle, role switch (if admin), logout
  - Remove the `chase-top-nav__center` section entirely
  
  **DashboardQuickNav:** In App.js, remove the `<DashboardQuickNav user={user} />` render. Remove the `showQuickNav` logic and `App--has-quick-nav` CSS class. Remove the `isDashboardQuickNavRoute` import. Do NOT delete the DashboardQuickNav.js file (other code may reference it; let it be dead code for now).
  
  **UserDashboard.js:** Remove `<ChaseTopNav ... />` render at line ~1765 — the top nav is already rendered by App.js wrapper. If UserDashboard has its own top nav, remove it to avoid duplication.
  
  **LandingPage.js:** Remove `<ChaseTopNav ... />` render at line ~34 — same reason, App.js wrapper provides it.
  
  **App.js:** Remove `<DashboardQuickNav user={user} />` line. Remove related imports and variables (`isDashboardQuickNavRoute`, `showQuickNav`). Clean up the `App--has-quick-nav` class usage.
  </action>
  <verify>cd banking_api_ui && npm run build → exit 0</verify>
  <done>No navigation links in top bars. Sidebar is the only navigation. DashboardQuickNav no longer renders. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Universal sidebar navigation on all pages, stripped redundant top nav links, removed DashboardQuickNav</what-built>
  <how-to-verify>
    1. Log in as admin at https://api.pingdemo.com:4000 → sidebar visible with all sections (Users & Accounts, Monitoring, OAuth & Security, System Tools)
    2. Navigate /marketing, /admin, /configure, /dashboard → sidebar present on ALL pages
    3. TopNav shows only brand bar (no dropdown menus)
    4. ChaseTopNav shows only brand + user info (no nav link row)
    5. No DashboardQuickNav buttons visible in upper-left
    6. Log in as non-admin user → sidebar visible with filtered items (Home, Dashboard, Users & Accounts, PingOne Test, MFA Test)
    7. All sidebar links navigate correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | UI-only changes; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-163-03 | Information Disclosure | Removed nav links | accept | Navigation links were never a security boundary — route guards remain unchanged |
</threat_model>

<verification>
- `cd banking_api_ui && npm run build` → exit 0
- Admin login: full sidebar on every page, no nav dropdowns in top bar
- User login: filtered sidebar on every page, no nav links in top bar
- No DashboardQuickNav visible
- All 15+ sidebar links navigate correctly
</verification>

<success_criteria>
Single navigation source (sidebar) for all logged-in users. Top bars are brand headers only. Build passes. Visual verification approved.
</success_criteria>

<output>
After completion, create `.planning/phases/163-universal-sidebar-navigation/163-02-SUMMARY.md`
</output>
