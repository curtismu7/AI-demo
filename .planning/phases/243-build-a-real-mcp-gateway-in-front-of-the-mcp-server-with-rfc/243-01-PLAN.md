---
phase: 243
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_mcp_gateway/package.json
  - banking_mcp_gateway/tsconfig.json
  - banking_mcp_gateway/src/index.ts
  - banking_mcp_gateway/src/server/GatewayServer.ts
  - banking_mcp_gateway/tests/gateway-server.test.ts
autonomous: true
requirements:
  - D-01
  - D-02

must_haves:
  truths:
    - "A separate banking_mcp_gateway service exists and can be started independently of the BFF and MCP server"
    - "The gateway exposes GET /.well-known/oauth-protected-resource and POST /mcp as the client-facing MCP boundary"
    - "Gateway responses preserve MCP/OAuth-facing headers needed for discovery, auth challenge, and session flow"
    - "The gateway points upstream to the existing banking MCP server rather than embedding tool logic itself"
  artifacts:
    - path: "banking_mcp_gateway/package.json"
      provides: "Standalone gateway runtime, scripts, and dependency boundary"
    - path: "banking_mcp_gateway/src/server/GatewayServer.ts"
      provides: "Express or HTTP gateway runtime owning metadata and MCP ingress"
    - path: "banking_mcp_gateway/src/index.ts"
      provides: "Gateway bootstrap and env-driven startup"
    - path: "banking_mcp_gateway/tests/gateway-server.test.ts"
      provides: "Foundational tests for metadata and MCP ingress behavior"
  key_links:
    - from: "banking_mcp_gateway/src/server/GatewayServer.ts"
      to: "banking_mcp_server HTTP transport"
      via: "env-configured upstream base URL used for forwarding"
    - from: "GET /.well-known/oauth-protected-resource"
      to: "gateway resource metadata"
      via: "gateway-owned response, not a pass-through to the MCP server"
---

<objective>
Create the standalone gateway runtime and establish the client-facing RFC 9728 / HTTP MCP surface.

Purpose: Deliver the real gateway boundary required by D-01 and D-02 so the rest of the phase can build on a concrete protected resource instead of continuing to overload the BFF.

Output: New `banking_mcp_gateway/` service with startup scripts, `POST /mcp`, `GET /.well-known/oauth-protected-resource`, upstream configuration, and foundational tests.
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-RESEARCH.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/PROJECT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/ROADMAP.md

<interfaces>
```ts
// Existing upstream MCP server behavior to preserve at the new gateway boundary.
// banking_mcp_server/src/server/BankingMCPServer.ts
// - GET /.well-known/oauth-protected-resource
// - POST /mcp
// - MCP-Protocol-Version header expectations
// - MCP-Session-Id propagation
// - WWW-Authenticate challenges on auth failures
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the standalone gateway service shell per D-01</name>
  <files>
    banking_mcp_gateway/package.json,
    banking_mcp_gateway/tsconfig.json,
    banking_mcp_gateway/src/index.ts,
    banking_mcp_gateway/src/server/GatewayServer.ts
  </files>
  <action>
Create a new `banking_mcp_gateway/` TypeScript service as a real runnable process, not a BFF route shim, per D-01.

Use the same general TypeScript/build conventions already used by `banking_mcp_server`, but keep the gateway focused on transport and security boundaries rather than tool execution.

`GatewayServer.ts` must:
- expose `GET /.well-known/oauth-protected-resource` as a gateway-owned metadata response per D-02
- expose `POST /mcp` as the client-facing ingress path
- preserve MCP/OAuth response headers (`MCP-Protocol-Version`, `MCP-Session-Id`, `WWW-Authenticate`) when forwarding/auth-challenging
- read upstream location and audience config from env (`UPSTREAM_MCP_URL`, `MCP_GATEWAY_AUDIENCE`, `MCP_UPSTREAM_AUDIENCE`, `PINGONE_AUTHORIZATION_ENDPOINT` or equivalent)

Do not implement PingOne Authorize or token exchange inline in this task; only define the server shell, request parsing, header propagation, and upstream contract.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_mcp_gateway && npm run build</automated>
  </verify>
  <done>A separate gateway service builds successfully and exposes the two gateway-owned endpoints required by D-01 and D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundational gateway tests for metadata and MCP ingress</name>
  <files>
    banking_mcp_gateway/tests/gateway-server.test.ts
  </files>
  <action>
Add foundational tests that prove the new gateway is a real boundary before deeper auth work lands.

Cover at minimum:
- `GET /.well-known/oauth-protected-resource` returns gateway metadata, not a direct MCP-server passthrough
- `POST /mcp` enforces basic protocol/header expectations and reaches the forwarding layer
- auth failures can emit `WWW-Authenticate` without crashing the gateway

Keep these tests transport-focused. They should not depend on the PingOne Authorize decision path yet.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_mcp_gateway && npm test -- --runInBand gateway-server</automated>
  </verify>
  <done>Gateway tests prove the standalone service owns the metadata and ingress boundary before policy/exchange logic is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> gateway | Untrusted caller reaches the new protected resource |
| gateway -> upstream MCP server | Gateway forwards only validated, next-hop-compatible requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-01 | S | gateway ingress | mitigate | Require explicit auth boundary at the gateway and preserve auth challenge semantics instead of anonymous pass-through |
| T-243-02 | T | metadata + forwarding routes | mitigate | Build tests around header propagation and endpoint ownership so clients cannot silently bypass the gateway contract |
| T-243-03 | D | gateway bootstrap | accept | Basic availability risk is acceptable in this plan; later plans add integration and startup wiring |
</threat_model>

<verification>
- `cd banking_mcp_gateway && npm run build`
- `cd banking_mcp_gateway && npm test -- --runInBand gateway-server`
</verification>

<success_criteria>
The repo contains a real standalone gateway service that builds, serves protected-resource metadata, accepts MCP HTTP ingress, and provides the stable base for the security work in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-01-SUMMARY.md`
</output>
