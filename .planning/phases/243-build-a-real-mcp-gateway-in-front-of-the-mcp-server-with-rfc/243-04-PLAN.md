---
phase: 243
plan: 04
type: execute
wave: 3
depends_on:
  - 243-02
  - 243-03
files_modified:
  - banking_mcp_server/src/server/HttpMCPTransport.ts
  - banking_mcp_server/src/middleware/validateTokenAtGateway.js
  - banking_mcp_server/tests/gateway-upstream.test.ts
  - run-bank.sh
  - docs/TOKEN_FLOW.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "The upstream MCP server accepts only the gateway-style next-hop contract when gateway mode is enabled"
    - "Local dev startup brings up the gateway as part of the MCP path"
    - "Automated tests cover wrong-audience rejection, upstream acceptance, and gateway-first discovery"
    - "Token flow documentation matches the new gateway-first architecture and explicitly states that tokens do not reach the LLM"
  artifacts:
    - path: "banking_mcp_server/src/server/HttpMCPTransport.ts"
      provides: "Gateway-aware upstream contract or discovery behavior"
    - path: "banking_mcp_server/src/middleware/validateTokenAtGateway.js"
      provides: "Upstream enforcement aligned with gateway-issued next-hop tokens"
    - path: "banking_mcp_server/tests/gateway-upstream.test.ts"
      provides: "Upstream acceptance and wrong-audience rejection tests"
    - path: "run-bank.sh"
      provides: "Local startup wiring for the new gateway service"
    - path: "docs/TOKEN_FLOW.md"
      provides: "Gateway-first token-chain documentation with no-token-to-LLM guarantee"
  key_links:
    - from: "gateway exchanged token"
      to: "banking_mcp_server/src/middleware/validateTokenAtGateway.js"
      via: "expectedAudience validation for the upstream hop"
    - from: "run-bank.sh"
      to: "banking_mcp_gateway service"
      via: "local orchestration adds the gateway to the standard demo startup path"
---

<objective>
Harden the upstream MCP server for the gateway-first contract and finish end-to-end verification/documentation.

Purpose: Close the loop so the new gateway is not optional in practice: the upstream server enforces the right next-hop assumptions, local startup includes the gateway, and the documented token flow matches D-04 and D-05.

Output: Gateway-aware upstream validation, gateway startup wiring, end-to-end tests, and updated token-flow documentation.
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-RESEARCH.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the upstream MCP server for gateway-issued next-hop tokens</name>
  <files>
    banking_mcp_server/src/server/HttpMCPTransport.ts,
    banking_mcp_server/src/middleware/validateTokenAtGateway.js,
    banking_mcp_server/tests/gateway-upstream.test.ts
  </files>
  <behavior>
    - Test 1: direct caller tokens with the wrong audience are rejected by the upstream MCP server
    - Test 2: gateway-issued next-hop tokens with the configured MCP-server audience are accepted
    - Test 3: when gateway mode is enabled, discovery and auth behavior do not mislead clients into bypassing the gateway as the front door
  </behavior>
  <action>
Update the upstream MCP-server validation and HTTP transport behavior so the server clearly operates as the protected resource behind the gateway, not as the public first hop.

This task should reinforce D-05 by validating only the next-hop token contract at the upstream server boundary. If direct-public discovery needs to be suppressed, redirected, or clearly marked in gateway mode, do that here rather than leaving two equally-authoritative front doors.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_mcp_server && npm run build && npm test -- --runInBand gateway-upstream</automated>
  </verify>
  <done>The upstream MCP server only accepts the gateway-style next-hop contract and has tests proving wrong-audience rejection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire local startup and document the gateway-first token flow</name>
  <files>
    run-bank.sh,
    docs/TOKEN_FLOW.md
  </files>
  <action>
Update local orchestration so `run-bank.sh` starts the new gateway alongside the existing BFF, UI, MCP server, and LangChain host.

Then update `docs/TOKEN_FLOW.md` to describe the new gateway-first chain explicitly:
- caller -> gateway token audience
- gateway -> PingOne Authorize decision
- gateway -> RFC 8693 exchange
- exchanged token -> upstream MCP-server audience
- tokens do not reach the LLM per D-04

Keep the documentation aligned to the real implementation names and env vars introduced in Plans 01-03.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && npm test -- --testPathPattern='mcp.*gateway|token-exchange-pingone' --forceExit</automated>
  </verify>
  <done>Local startup includes the gateway, and the documented token flow clearly matches the enforced gateway-first architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> upstream MCP server | Only exchanged next-hop tokens should cross this boundary |
| local startup/orchestration | Misconfigured local runs can accidentally bypass the new gateway |
| implementation -> documentation | Drift can hide token leakage or audience mistakes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-10 | S | upstream MCP server | mitigate | Accept only gateway-issued next-hop audiences and reject direct caller audiences |
| T-243-11 | T | local startup | mitigate | Add gateway startup to run-bank.sh so the default dev path matches the secured architecture |
| T-243-12 | R | token-flow docs | mitigate | Update TOKEN_FLOW.md with explicit gateway-first hop mapping and no-token-to-LLM statement |
</threat_model>

<verification>
- `cd banking_mcp_server && npm run build && npm test -- --runInBand gateway-upstream`
- `cd banking_api_server && npm test -- --testPathPattern='mcp.*gateway|token-exchange-pingone' --forceExit`
</verification>

<success_criteria>
The gateway-first design is enforced end-to-end: upstream MCP validation expects gateway-issued next-hop tokens, local startup launches the gateway, and token-flow docs match the shipped behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-04-SUMMARY.md`
</output>
