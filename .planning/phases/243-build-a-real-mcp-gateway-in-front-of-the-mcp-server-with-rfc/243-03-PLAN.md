---
phase: 243
plan: 03
type: execute
wave: 2
depends_on:
  - 243-01
files_modified:
  - banking_api_server/server.js
  - banking_api_server/services/mcpGatewayClient.js
  - langchain_agent/src/config/settings.py
  - langchain_agent/src/main.py
  - langchain_agent/src/authentication/oauth_manager.py
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "The BFF calls the gateway instead of calling the MCP server directly"
    - "The BFF's outward /api/mcp/tool response contract remains stable for the UI"
    - "The LangChain host registers the gateway endpoint instead of the raw MCP-server endpoint"
    - "Token-bearing data is kept out of model-facing prompt construction and model-visible logs"
  artifacts:
    - path: "banking_api_server/services/mcpGatewayClient.js"
      provides: "BFF helper for gateway-bound MCP calls"
    - path: "banking_api_server/server.js"
      provides: "Gateway cutover in POST /api/mcp/tool while preserving UI contract"
    - path: "langchain_agent/src/config/settings.py"
      provides: "Gateway-aware MCP endpoint configuration"
    - path: "langchain_agent/src/main.py"
      provides: "Gateway endpoint registration for the LangChain MCP client manager"
    - path: "langchain_agent/src/authentication/oauth_manager.py"
      provides: "Gateway-aware resource/audience configuration without exposing tokens to the LLM path"
  key_links:
    - from: "banking_api_server/server.js"
      to: "mcpGatewayClient.js"
      via: "POST /api/mcp/tool downstream call changes from MCP server to gateway"
    - from: "langchain_agent/src/config/settings.py"
      to: "langchain_agent/src/main.py"
      via: "MCP_SERVER_* endpoint registration now points at the gateway"
---

<objective>
Cut the BFF and LangChain host over to the gateway while preserving the current UI contract and keeping tokens out of the LLM path.

Purpose: Implement D-03 and D-04 in the actual caller paths so the gateway is used consistently and no model-facing code receives bearer tokens.

Output: New BFF gateway client, `/api/mcp/tool` cutover, LangChain gateway configuration, and LLM-boundary token-isolation hardening.
</objective>

<execution_context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-RESEARCH.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/ROADMAP.md

<interfaces>
```js
// UI-facing BFF contract to preserve.
// banking_api_ui/src/services/bankingAgentService.js expects:
// { result, tokenEvents, activeModel, activeProvider }
```

```py
# LangChain MCP endpoint discovery.
# langchain_agent/src/config/settings.py
# MCP_SERVER_{NAME}_ENDPOINT -> endpoint string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the BFF MCP path to the gateway per D-03</name>
  <files>
    banking_api_server/server.js,
    banking_api_server/services/mcpGatewayClient.js
  </files>
  <action>
Create a dedicated gateway client helper and refactor `POST /api/mcp/tool` so the BFF calls the gateway instead of calling the banking MCP server directly.

Preserve the UI-facing response contract (`result`, `tokenEvents`, `activeModel`, `activeProvider`) so `banking_api_ui/src/services/bankingAgentService.js` does not need a feature rewrite.

Move MCP-specific downstream concerns out of the BFF path where the gateway now owns them:
- direct upstream MCP transport selection for the normal path
- PingOne Authorize gate decisions specific to MCP invocation
- MCP-server-audience token exchange

The BFF may still manage the user's session and token preview telemetry, but the gateway must become the next security hop for actual MCP invocation.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && npm test -- --testPathPattern='mcp.*gateway|authorize-gate' --forceExit</automated>
  </verify>
  <done>The BFF still serves `/api/mcp/tool`, but the real downstream security/invocation path now goes through the gateway instead of directly to the MCP server.</done>
</task>

<task type="auto">
  <name>Task 2: Point LangChain MCP registration at the gateway and enforce no-token-to-LLM per D-04</name>
  <files>
    langchain_agent/src/config/settings.py,
    langchain_agent/src/main.py,
    langchain_agent/src/authentication/oauth_manager.py
  </files>
  <action>
Update the LangChain host so its MCP endpoint registration points to the gateway URL rather than the raw MCP-server URL.

At the same time, audit and harden the touched LangChain files so bearer tokens or token-bearing headers are never inserted into:
- prompt text
- tool-selection context exposed to the model
- model-visible debug snapshots
- logs specifically derived from prompt/model execution

If a token-related field is needed for transport or auth, keep it in non-LLM control flow only and redact it before any model-facing logging. This task implements D-04, not just documents it.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/langchain_agent && python -m py_compile src/main.py src/config/settings.py src/authentication/oauth_manager.py</automated>
  </verify>
  <done>LangChain discovers and uses the gateway endpoint, and the touched model-facing path does not receive or log bearer tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF -> gateway | Session-backed BFF forwards MCP requests to the new gateway |
| LangChain host -> gateway | Agent host discovers MCP through gateway, not raw upstream |
| app code -> LLM | Model-facing context must exclude tokens and token-bearing headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-243-07 | I | BFF /api/mcp/tool path | mitigate | Preserve UI contract while moving real invocation behind gateway and limiting token-bearing work to non-LLM control flow |
| T-243-08 | I | LangChain logging/prompt path | mitigate | Redact or avoid token material before prompt construction and model-facing logs per D-04 |
| T-243-09 | T | LangChain endpoint config | mitigate | Repoint MCP endpoint discovery to the gateway so raw MCP-server bypass is not the configured default |
</threat_model>

<verification>
- `cd banking_api_server && npm test -- --testPathPattern='mcp.*gateway|authorize-gate' --forceExit`
- `cd langchain_agent && python -m py_compile src/main.py src/config/settings.py src/authentication/oauth_manager.py`
</verification>

<success_criteria>
Both the BFF and LangChain host use the new gateway as the MCP boundary, and no token-bearing data leaks into prompt/model-facing code paths.
</success_criteria>

<output>
After completion, create `.planning/phases/243-build-a-real-mcp-gateway-in-front-of-the-mcp-server-with-rfc/243-03-SUMMARY.md`
</output>
