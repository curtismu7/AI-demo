---
phase: 225-retail-theme-toggle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/RetailModeBanner.js
  - banking_api_ui/src/components/RetailModeBanner.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "RetailModeBanner renders a 44px-height full-width bar below TopNav when visible"
    - "Retail-ON state shows blue background with 'Retail Mode — BX Electronics' label and yellow 'Switch to Banking' button"
    - "Banking state shows gray background with 'Banking Mode — BX Finance' label and navy 'Switch to Retail' button"
    - "Toggle button has aria-pressed attribute reflecting current mode"
    - "onToggle prop is called when the button is clicked"
  artifacts:
    - path: "banking_api_ui/src/components/RetailModeBanner.js"
      provides: "RetailModeBanner React component"
      exports: ["default"]
    - path: "banking_api_ui/src/components/RetailModeBanner.css"
      provides: "Banner layout and color styles"
      contains: "retail-mode-banner"
  key_links:
    - from: "RetailModeBanner.js"
      to: "RetailModeBanner.css"
      via: "import './RetailModeBanner.css'"
      pattern: "RetailModeBanner.css"
    - from: "UserDashboard.js (Plan 03)"
      to: "RetailModeBanner.js"
      via: "import RetailModeBanner from './RetailModeBanner'"
      pattern: "RetailModeBanner"
---

<objective>
Create the RetailModeBanner React component and its CSS file. This is the secondary toggle visible
on the Dashboard in both modes. It receives isRetail (boolean) and onToggle (function) as props
and calls onToggle when the user clicks the button. The banner is always visible on the Dashboard
(both modes) so users can discover the toggle without going to the Feature Flags page.

Purpose: Self-contained component; Plan 03 will import and render it inside UserDashboard.js.
Building it in Wave 1 keeps Plan 03 focused on integration logic only.

Output: RetailModeBanner.js + RetailModeBanner.css ready to import in Plan 03.
</objective>

<execution_context>
@/Users/cmuir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmuir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/225-retail-theme-toggle/225-UI-SPEC.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/225-retail-theme-toggle/225-CONTEXT.md
@/Users/cmuir/P1Import-apps/Banking/.planning/phases/225-retail-theme-toggle/225-RESEARCH.md
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md

<interfaces>
<!-- Contracts for RetailModeBanner (from UI-SPEC.md and RESEARCH.md) -->

Props interface:
```javascript
// RetailModeBanner({ isRetail: boolean, onToggle: () => void })
// isRetail  — true = retail mode active, false = banking mode
// onToggle  — called when user clicks the button; caller handles the PATCH + state update
```

Layout contract (from UI-SPEC.md):
- Height: 44px minimum, full-width, 0 16px padding
- Retail-ON:  background #0046BE, text white, button: background #FFE000, text #000000
- Banking:    background #f5f5f5 (--chase-light-gray), text dark, button: background var(--chase-navy), text white
- Label copy: "Retail Mode — BX Electronics" (retail) / "Banking Mode — BX Finance" (banking)
- Button copy: "Switch to Banking" (retail ON) / "Switch to Retail" (banking)
- Button styles: border-radius 6px, padding 6px 12px, font-size 13px, font-weight 600
- Button hover (retail ON): #e6ca00; hover (banking): #003770 (--btn-primary-bg-hover)
- Button focus: outline 2px solid #0046BE, outline-offset 2px
- aria-pressed on button reflects isRetail boolean
- No emoji in labels per CLAUDE.md

CSS class naming (follows project pattern — BEM-style with component prefix):
  .retail-mode-banner
  .retail-mode-banner--retail      (modifier for retail-ON state)
  .retail-mode-banner--banking     (modifier for banking state)
  .retail-mode-banner__label
  .retail-mode-banner__btn
  .retail-mode-banner__btn--retail (yellow button)
  .retail-mode-banner__btn--banking (navy button)

Reference pattern: DashboardLayoutToggle.css (segmented control layout reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RetailModeBanner.js component</name>
  <files>banking_api_ui/src/components/RetailModeBanner.js</files>
  <read_first>
    - banking_api_ui/src/components/DashboardLayoutToggle.js (read for component structure pattern reference — check prop naming and toggle button pattern)
  </read_first>
  <action>
Create banking_api_ui/src/components/RetailModeBanner.js with the following exact content.
This is a new file — do not modify any existing file.

```javascript
// banking_api_ui/src/components/RetailModeBanner.js
import React from "react";
import "./RetailModeBanner.css";

/**
 * Persistent banner shown at the top of UserDashboard (below TopNav) in both
 * Banking and Retail modes. Provides a one-click toggle between modes.
 *
 * Props:
 *   isRetail  {boolean}  — true = retail mode is active
 *   onToggle  {function} — called when the user clicks the toggle button;
 *                          caller handles PATCH + applyIndustryId side-effects
 */
export default function RetailModeBanner({ isRetail, onToggle }) {
  const label = isRetail
    ? "Retail Mode — BX Electronics"
    : "Banking Mode — BX Finance";
  const btnLabel = isRetail ? "Switch to Banking" : "Switch to Retail";

  return (
    <div
      className={`retail-mode-banner ${
        isRetail
          ? "retail-mode-banner--retail"
          : "retail-mode-banner--banking"
      }`}
      role="region"
      aria-label="Mode toggle banner"
    >
      <span className="retail-mode-banner__label">{label}</span>
      <button
        type="button"
        className={`retail-mode-banner__btn ${
          isRetail
            ? "retail-mode-banner__btn--retail"
            : "retail-mode-banner__btn--banking"
        }`}
        onClick={onToggle}
        aria-pressed={isRetail}
      >
        {btnLabel}
      </button>
    </div>
  );
}
```

Notes:
- Em dash in labels uses Unicode escape — (not a literal em-dash) for source safety.
- No emoji in label or button text per CLAUDE.md project convention.
- aria-pressed reflects isRetail boolean (true/false, not "true"/"false" string — React handles conversion).
  </action>
  <verify>
    <automated>grep -n "retail-mode-banner" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/RetailModeBanner.js | head -5 && grep -n "aria-pressed" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/RetailModeBanner.js && echo "COMPONENT OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `banking_api_ui/src/components/RetailModeBanner.js`
    - `grep "aria-pressed" RetailModeBanner.js` returns the button line
    - `grep "Switch to Banking" RetailModeBanner.js` and `grep "Switch to Retail" RetailModeBanner.js` both return results
    - `grep "emoji\|🛒\|🏦" RetailModeBanner.js` returns nothing (no emoji per CLAUDE.md)
    - `grep "import.*RetailModeBanner.css" RetailModeBanner.js` confirms CSS import
    - `grep "onToggle" RetailModeBanner.js` returns the onClick binding
  </acceptance_criteria>
  <done>RetailModeBanner.js created with correct props interface, aria-pressed, no emoji, CSS import</done>
</task>

<task type="auto">
  <name>Task 2: Create RetailModeBanner.css styles</name>
  <files>banking_api_ui/src/components/RetailModeBanner.css</files>
  <read_first>
    - banking_api_ui/src/components/DashboardLayoutToggle.css (read for CSS pattern reference — border-radius values, button patterns)
    - banking_api_ui/src/index.css (lines 1-80 already read in research — confirms --chase-light-gray, --chase-navy, --btn-primary-bg-hover var names)
  </read_first>
  <action>
Create banking_api_ui/src/components/RetailModeBanner.css with the following exact content.
This is a new file — do not modify any existing CSS file.

```css
/* RetailModeBanner.css
   Persistent mode indicator + toggle banner for UserDashboard.
   Shown in both Banking and Retail modes (always visible on dashboard).
   Colors: Retail ON = #0046BE bg / #FFE000 btn; Banking = #f5f5f5 bg / var(--chase-navy) btn.
*/

.retail-mode-banner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  min-height: 44px;         /* WCAG 2.5.8 minimum touch target */
  padding: 0 16px;
  box-sizing: border-box;
  font-family: var(--font-primary, -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", sans-serif);
}

/* Retail-ON state: Best Buy blue background */
.retail-mode-banner--retail {
  background-color: #0046BE;
  color: #ffffff;
}

/* Banking state: light gray background */
.retail-mode-banner--banking {
  background-color: var(--chase-light-gray, #f5f5f5);
  color: #1a1a1a;
}

/* Label text */
.retail-mode-banner__label {
  font-size: 14px;
  font-weight: 400;
  line-height: 1.6;
}

/* Toggle button base */
.retail-mode-banner__btn {
  border: none;
  cursor: pointer;
  border-radius: 6px;
  padding: 6px 12px;
  font-size: 13px;
  font-weight: 600;
  font-family: inherit;
  line-height: 1.4;
  transition: background-color 0.15s ease, opacity 0.15s ease;
  outline: none;
}

.retail-mode-banner__btn:focus-visible {
  outline: 2px solid #0046BE;
  outline-offset: 2px;
}

/* Retail-ON button: yellow background, black text */
.retail-mode-banner__btn--retail {
  background-color: #FFE000;
  color: #000000;
}

.retail-mode-banner__btn--retail:hover {
  background-color: #e6ca00;
}

.retail-mode-banner__btn--retail:active {
  opacity: 0.85;
}

/* Banking button: navy background, white text */
.retail-mode-banner__btn--banking {
  background-color: var(--chase-navy, #002d62);
  color: #ffffff;
}

.retail-mode-banner__btn--banking:hover {
  background-color: var(--btn-primary-bg-hover, #003770);
}

.retail-mode-banner__btn--banking:active {
  opacity: 0.85;
}
```

Notes:
- All color values match the UI-SPEC exactly.
- Uses CSS custom properties with fallback values for portability.
- No new CSS custom property definitions — only consumes existing project vars.
- min-height 44px on the container (not the button) ensures the touch target requirement.
  </action>
  <verify>
    <automated>grep -n "#0046BE\|#FFE000\|chase-navy\|44px" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/RetailModeBanner.css | head -10 && echo "CSS OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `banking_api_ui/src/components/RetailModeBanner.css`
    - `grep "min-height: 44px" RetailModeBanner.css` returns the container rule
    - `grep "#0046BE" RetailModeBanner.css` returns the retail background color
    - `grep "#FFE000" RetailModeBanner.css` returns the retail button color
    - `grep "chase-navy\|#002d62" RetailModeBanner.css` returns the banking button color
    - `grep "focus-visible" RetailModeBanner.css` returns the focus outline rule
    - `grep "#e6ca00" RetailModeBanner.css` returns the retail button hover state
    - `cd banking_api_ui && npm run build` exits 0 (RetailModeBanner.js is not yet imported by any component, so the build just compiles the existing tree — new files do not affect the build until imported)
  </acceptance_criteria>
  <done>RetailModeBanner.css created with correct colors, min-height 44px, hover/focus/active states, and both mode variants</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (this plan) | RetailModeBanner is a pure presentational component; trust boundary handling is in UserDashboard (Plan 03) and the BFF auth middleware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-03 | Spoofing | RetailModeBanner | accept | Component is purely presentational; no auth decisions made here; onToggle callback is defined by UserDashboard which controls the PATCH call |
</threat_model>

<verification>
After both tasks:
- `ls banking_api_ui/src/components/RetailModeBanner.js` confirms file exists
- `ls banking_api_ui/src/components/RetailModeBanner.css` confirms file exists
- `grep "aria-pressed" RetailModeBanner.js` returns button line
- `grep "min-height: 44px" RetailModeBanner.css` returns container rule
- `cd banking_api_ui && npm run build` exits 0
</verification>

<success_criteria>
- RetailModeBanner.js is a standalone component accepting { isRetail, onToggle } props
- Both mode variants render with correct label copy and button copy (verified by grep)
- No emoji in any rendered text
- CSS file has all required states: default, hover, active, focus-visible for both mode variants
- npm run build exits 0 (no import chain changes yet)
</success_criteria>

<output>
After completion, create `.planning/phases/225-retail-theme-toggle/225-02-SUMMARY.md`
</output>
