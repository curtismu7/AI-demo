---
phase: 181-we-need-to-add-a-training-slide-out-for-cua-for-ai
plan: 02
type: execute
wave: 2
depends_on:
  - 181-01
files_modified:
  - banking_api_server/services/geminiNlIntent.js
  - banking_api_server/services/nlIntentParser.js
  - banking_api_ui/src/components/education/RFCIndexPanel.js
  - banking_api_ui/src/components/AdminSideNav.jsx
  - banking_api_ui/src/components/education/educationCommands.js
autonomous: true
requirements:
  - CUA-02
decision_trace:
  - D-03
  - D-04
  - D-05
must_haves:
  truths:
    - "Natural-language prompts for CUA route to the new education panel through both prompt and heuristic paths"
    - "Users can discover the CUA panel from the RFC Index and the live sidebar learn menu"
    - "The banking agent learn-command surface includes the new CUA topic"
  artifacts:
    - path: "banking_api_server/services/geminiNlIntent.js"
      provides: "LLM prompt routing for CUA-related education requests"
      contains: "computer use agent"
    - path: "banking_api_server/services/nlIntentParser.js"
      provides: "Fallback heuristic routing for CUA-related requests"
      contains: "computer use"
    - path: "banking_api_ui/src/components/education/RFCIndexPanel.js"
      provides: "CUA row or entry in the education index"
      contains: "CUA"
    - path: "banking_api_ui/src/components/AdminSideNav.jsx"
      provides: "Live sidebar learn-menu entry for the CUA panel"
      contains: "CUA"
  key_links:
    - from: "geminiNlIntent.js"
      to: "educationIds.js"
      via: "education.panel response value"
      pattern: "education"
    - from: "AdminSideNav.jsx"
      to: "EducationUIContext"
      via: "learn menu opens EDU id"
      pattern: "EDU"
---

<objective>
Wire discoverability for the new CUA panel across NL routing and the main learn-entry surfaces.

Purpose: make the new panel reachable the same way users reach the rest of the education system, whether they click through navigation or ask about CUA in natural language.

Output: CUA discoverability across Gemini prompt routing, fallback parsing, RFC Index, live sidebar, and agent education commands.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/181-we-need-to-add-a-training-slide-out-for-cua-for-ai/181-CONTEXT.md
@.planning/phases/181-we-need-to-add-a-training-slide-out-for-cua-for-ai/181-RESEARCH.md
@banking_api_server/services/geminiNlIntent.js
@banking_api_server/services/nlIntentParser.js
@banking_api_ui/src/components/education/RFCIndexPanel.js
@banking_api_ui/src/components/AdminSideNav.jsx
@banking_api_ui/src/components/education/educationCommands.js
@banking_api_ui/src/components/education/educationIds.js

<interfaces>
From `geminiNlIntent.js`:
- Education responses use `{ kind: "education", education: { panel, tab } }`

From `nlIntentParser.js`:
- Heuristic routing should mirror the same panel id for fallback consistency

From `AdminSideNav.jsx` and `educationCommands.js`:
- Learn surfaces should use existing education open patterns rather than custom navigation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CUA NL routing in Gemini prompt and fallback parser</name>
  <files>
    banking_api_server/services/geminiNlIntent.js
    banking_api_server/services/nlIntentParser.js
  </files>
  <read_first>
    - banking_api_server/services/geminiNlIntent.js
    - banking_api_server/services/nlIntentParser.js
    - banking_api_ui/src/components/education/educationIds.js
  </read_first>
  <action>
Update both NL routing layers so these phrases reliably open the new CUA education panel:
- `CUA`
- `computer use agent`
- `computer use`

Implementation requirements:
- Route to the canonical panel id `cua` from both routing layers.
- Keep routing specific enough that normal banking prompts like account or transfer requests do not get misclassified.
- Mirror the same panel/tab behavior in both Gemini prompt routing and heuristic fallback routing.
- Do not change unrelated prompt routing semantics.
  </action>
  <verify>
    <automated>
      node - <<'NODE'
      const { parseHeuristic } = require('./banking_api_server/services/nlIntentParser');
      const cases = ['what is cua', 'computer use agent', 'computer use'];
      const results = cases.map((message) => parseHeuristic(message));
      const panels = results.map((result) => result?.education?.panel);
      if (!panels.every((panel) => panel === 'cua')) {
        console.error(JSON.stringify(results, null, 2));
        process.exit(1);
      }
      NODE
      grep -Eq "computer use agent|computer use|CUA" banking_api_server/services/geminiNlIntent.js && \
      grep -Eq "cua" banking_api_server/services/geminiNlIntent.js
    </automated>
  </verify>
  <done>
    Both NL routing layers can identify CUA education requests and route them to the new panel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add discoverability entry points in sidebar, RFC Index, and agent education commands</name>
  <files>
    banking_api_ui/src/components/education/RFCIndexPanel.js
    banking_api_ui/src/components/AdminSideNav.jsx
    banking_api_ui/src/components/education/educationCommands.js
  </files>
  <read_first>
    - banking_api_ui/src/components/education/RFCIndexPanel.js
    - banking_api_ui/src/components/AdminSideNav.jsx
    - banking_api_ui/src/components/education/educationCommands.js
    - banking_api_ui/src/components/education/educationIds.js
  </read_first>
  <action>
Add the new CUA education topic to the repo's main live discovery surfaces:

1. **RFC Index**
   - Add a row or entry for the CUA panel with accurate educational framing

2. **Admin sidebar learn section**
   - Add a visible CUA learn-menu item in the live sidebar experience

3. **Agent education commands**
   - Add a learn command/chip entry so the banking agent can suggest or open the panel

Implementation requirements:
- Reuse the existing open-panel pattern and EDU id; do not create a new route for this content.
- Keep labels consistent across surfaces so users recognize the same topic.
- Preserve ordering and styling conventions already used in those files.
  </action>
  <verify>
    <automated>
      cd banking_api_ui && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    Users can find the new CUA panel from sidebar navigation, RFC Index, and agent education commands, and the UI build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prompt -> NL router | Over-broad heuristics could misroute non-education prompts |
| Learn surfaces -> education host | Inconsistent labels or wrong EDU id could create dead links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-181-03 | S (Spoofing) | NL routing | mitigate | Keep CUA phrase matching narrow and education-specific so normal banking tasks are not hijacked |
| T-181-04 | T (Tampering) | Learn menus | mitigate | Reuse existing EDU ids and open-panel mechanisms without inventing alternate flows |
</threat_model>

<verification>
- `cd banking_api_ui && npm run build` exits 0
- CUA phrases exist in both NL routing layers
- Sidebar, RFC Index, and education commands all contain a CUA entry wired to the same panel id
</verification>

<success_criteria>
- CUA NL prompts route correctly through both routing paths
- Sidebar discoverability is added to the live learn menu
- RFC Index and agent education commands expose the new panel
- Build passes after UI updates
</success_criteria>

<output>
After completion, create `.planning/phases/181-we-need-to-add-a-training-slide-out-for-cua-for-ai/181-02-SUMMARY.md`
</output>