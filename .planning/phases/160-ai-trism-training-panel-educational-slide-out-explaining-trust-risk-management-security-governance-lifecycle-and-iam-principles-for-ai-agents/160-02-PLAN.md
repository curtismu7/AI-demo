---
phase: 160-ai-trism-training-panel
plan: 02
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - banking_api_ui/src/components/ChaseTopNav.js
  - banking_api_ui/src/components/ChaseTopNav.css
  - banking_api_ui/src/components/TRiSMGlossary.jsx
  - banking_api_ui/src/components/TRiSMGlossary.css
autonomous: true
requirements:
  - REQ-160-03
  - REQ-160-05
  - REQ-160-06
must_haves:
  truths:
    - "User can click a 📚 Learn button in the top nav to open the TRiSM training panel"
    - "Clicking the Learn button toggles the panel open/closed"
    - "Panel includes a glossary of AI TRiSM terms"
    - "The Learn button is visible on all authenticated pages"
  artifacts:
    - path: "banking_api_ui/src/components/ChaseTopNav.js"
      provides: "Updated top nav with Learn button that toggles TRiSMTrainingPanel"
      contains: "TRiSMTrainingPanel"
    - path: "banking_api_ui/src/components/TRiSMGlossary.jsx"
      provides: "Glossary component with AI TRiSM terminology definitions"
      min_lines: 60
    - path: "banking_api_ui/src/components/TRiSMGlossary.css"
      provides: "Glossary styling"
      min_lines: 40
  key_links:
    - from: "ChaseTopNav.js"
      to: "TRiSMTrainingPanel.jsx"
      via: "imports and renders with isOpen state"
      pattern: "import TRiSMTrainingPanel"
---

<objective>
Integrate TRiSM Training Panel into the app: add 📚 Learn button to top nav, create glossary component, and wire everything together so the panel is accessible from any authenticated page.

Purpose: Make the training panel discoverable and complete with glossary for AI TRiSM terminology.
Output: Updated ChaseTopNav.js with Learn button, TRiSMGlossary component, CSS for both.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-ai-trism-training-panel-educational-slide-out-explaining-trust-risk-management-security-governance-lifecycle-and-iam-principles-for-ai-agents/160-CONTEXT.md
@.planning/phases/160-ai-trism-training-panel-educational-slide-out-explaining-trust-risk-management-security-governance-lifecycle-and-iam-principles-for-ai-agents/160-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Wave 1 -->

From banking_api_ui/src/components/TRiSMTrainingPanel.jsx (created in Plan 01):
```jsx
export default function TRiSMTrainingPanel({ isOpen, onClose })
// Props:
//   isOpen: boolean — controls visibility
//   onClose: () => void — called to close panel
```

From banking_api_ui/src/components/ChaseTopNav.js (current):
```jsx
export default function ChaseTopNav({ user, onLogout, currentPage, onRoleSwitch })
// Right section buttons use className="chase-nav-button chase-nav-button--{type}"
// Pattern: <button className="chase-nav-button chase-nav-button--theme" onClick={handler} title="..." aria-label="..."> emoji </button>
```

From banking_api_ui/src/components/ChaseTopNav.css:
```css
.chase-nav-button { /* base button styles */ }
.chase-nav-button--theme { /* specific variant */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 📚 Learn button to ChaseTopNav and wire TRiSMTrainingPanel</name>
  <files>banking_api_ui/src/components/ChaseTopNav.js, banking_api_ui/src/components/ChaseTopNav.css</files>
  <read_first>
    - banking_api_ui/src/components/ChaseTopNav.js (current full content — must preserve all existing functionality)
    - banking_api_ui/src/components/ChaseTopNav.css (current styles — add new button variant)
    - banking_api_ui/src/components/TRiSMTrainingPanel.jsx (import target, props: isOpen, onClose)
  </read_first>
  <action>
**ChaseTopNav.js changes:**

1. Add import at top:
```jsx
import TRiSMTrainingPanel from '../components/TRiSMTrainingPanel';
```

2. Add state inside the component function body:
```jsx
const [showTRiSMPanel, setShowTRiSMPanel] = useState(false);
```

3. Add the Learn button in the `chase-top-nav__right` div, BEFORE the theme toggle button:
```jsx
{/* AI TRiSM Training Button */}
<button
  className={`chase-nav-button chase-nav-button--learn ${showTRiSMPanel ? 'active' : ''}`}
  onClick={() => setShowTRiSMPanel(!showTRiSMPanel)}
  title="AI TRiSM Training"
  aria-label="Open AI TRiSM Training Panel"
>
  📚 Learn
</button>
```

4. Add the panel render at bottom of the component return, just before the closing `</nav>`:
```jsx
<TRiSMTrainingPanel
  isOpen={showTRiSMPanel}
  onClose={() => setShowTRiSMPanel(false)}
/>
```

**ChaseTopNav.css changes:**

Add these rules after the existing `.chase-nav-button--theme` block:
```css
.chase-nav-button--learn {
  background: linear-gradient(135deg, #1e40af 0%, #3b82f6 100%);
  color: white;
  border: 1px solid #1e40af;
  font-weight: 600;
  letter-spacing: 0.5px;
  padding: 6px 14px;
  border-radius: 6px;
  transition: all 0.2s ease;
}

.chase-nav-button--learn:hover {
  background: linear-gradient(135deg, #1e3a8a 0%, #2563eb 100%);
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(30, 64, 175, 0.3);
}

.chase-nav-button--learn.active {
  background: #1e3a8a;
  box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.2);
}
```

IMPORTANT: Do NOT change any existing buttons, links, or functionality in ChaseTopNav. Only ADD the learn button and panel import.
  </action>
  <acceptance_criteria>
    - ChaseTopNav.js contains `import TRiSMTrainingPanel from`
    - ChaseTopNav.js contains `const [showTRiSMPanel, setShowTRiSMPanel] = useState(false)`
    - ChaseTopNav.js contains `chase-nav-button--learn`
    - ChaseTopNav.js contains `📚 Learn`
    - ChaseTopNav.js contains `<TRiSMTrainingPanel isOpen={showTRiSMPanel}`
    - ChaseTopNav.css contains `.chase-nav-button--learn`
    - ChaseTopNav.css contains `.chase-nav-button--learn:hover`
    - ChaseTopNav.css contains `.chase-nav-button--learn.active`
    - All existing ChaseTopNav.js buttons (theme, role, logout) still present
  </acceptance_criteria>
  <verify>
    <automated>grep -c "import TRiSMTrainingPanel" banking_api_ui/src/components/ChaseTopNav.js && grep -c "showTRiSMPanel" banking_api_ui/src/components/ChaseTopNav.js && grep -c "chase-nav-button--learn" banking_api_ui/src/components/ChaseTopNav.css && grep -c "Sign Out" banking_api_ui/src/components/ChaseTopNav.js</automated>
  </verify>
  <done>Learn button visible in top nav, toggles TRiSMTrainingPanel open/closed, all existing nav functionality preserved</done>
</task>

<task type="auto">
  <name>Task 2: Create TRiSMGlossary component and integrate into training panel</name>
  <files>banking_api_ui/src/components/TRiSMGlossary.jsx, banking_api_ui/src/components/TRiSMGlossary.css, banking_api_ui/src/components/TRiSMTrainingPanel.jsx</files>
  <read_first>
    - banking_api_ui/src/components/TRiSMTrainingPanel.jsx (created in Plan 01 — add glossary tab/section)
    - .planning/phases/160-ai-trism-training-panel-educational-slide-out-explaining-trust-risk-management-security-governance-lifecycle-and-iam-principles-for-ai-agents/160-CONTEXT.md (terminology reference)
  </read_first>
  <action>
**Create TRiSMGlossary.jsx:**

Functional component, no props needed. Contains a GLOSSARY_TERMS constant array:

```javascript
const GLOSSARY_TERMS = [
  { term: "AI TRiSM", definition: "AI Trust, Risk and Security Management — Gartner's framework for making AI systems reliable, secure, and governed." },
  { term: "Trust & Transparency", definition: "Making AI decisions explainable, auditable, and understandable to stakeholders." },
  { term: "Risk Management", definition: "Identifying, assessing, and mitigating potential AI failures including bias, drift, and cascade errors." },
  { term: "Security by Design", definition: "Building security into AI systems from the start: encryption, scope isolation, data minimization." },
  { term: "Governance", definition: "Policies, roles, and guardrails that define who can build, deploy, and use AI systems." },
  { term: "Lifecycle Management", definition: "Treating AI as a product with requirements, testing, deployment, monitoring, and retirement phases." },
  { term: "Least Privilege", definition: "Granting only the minimum permissions needed. Agents get agent scopes, not user scopes." },
  { term: "Token Exchange", definition: "RFC 8693 mechanism where an agent exchanges its token + user token for a delegated token with act claim." },
  { term: "Delegation Chain", definition: "Cryptographic proof that Agent X is acting on behalf of User Y, visible in the act JWT claim." },
  { term: "Kill Switch", definition: "Emergency mechanism to immediately revoke an agent's OAuth credentials and freeze its state." },
  { term: "Rate Limiting", definition: "Capping the number of requests per time window to prevent cascade failures and resource exhaustion." },
  { term: "Audit Trail", definition: "Immutable log of all sensitive operations — who did what, when, and why — for compliance and forensics." },
  { term: "PKCE", definition: "Proof Key for Code Exchange — prevents authorization code interception in OAuth flows." },
  { term: "MCP", definition: "Model Context Protocol — standard for AI agents to discover and call tools via a secure server." },
  { term: "BFF", definition: "Backend for Frontend — server-side proxy that keeps tokens and secrets away from the browser." },
  { term: "act Claim", definition: "JWT claim identifying the actor (agent) in a delegation scenario. Proves who is performing the action." },
  { term: "may_act Claim", definition: "JWT claim specifying which agents are authorized to act on behalf of a user." },
  { term: "Scope Isolation", definition: "Ensuring user tokens and agent tokens have different, non-overlapping permission sets." }
];
```

Render:
```jsx
<div className="trism-glossary">
  <h4>📖 AI TRiSM Glossary</h4>
  <div className="trism-glossary-list">
    {GLOSSARY_TERMS.map(item => (
      <div key={item.term} className="trism-glossary-entry">
        <dt className="trism-glossary-term">{item.term}</dt>
        <dd className="trism-glossary-def">{item.definition}</dd>
      </div>
    ))}
  </div>
</div>
```

**Create TRiSMGlossary.css:**
- `.trism-glossary`: Padding 0
- `.trism-glossary h4`: Font-size 16px, color #1f2937, margin 0 0 16px
- `.trism-glossary-list`: Display flex, flex-direction column, gap 0
- `.trism-glossary-entry`: Padding 12px 0, border-bottom 1px solid #f3f4f6
- `.trism-glossary-entry:last-child`: Border-bottom none
- `.trism-glossary-term`: Font-weight 700, color #1e40af, font-size 14px, margin 0 0 4px
- `.trism-glossary-def`: Font-size 13px, color #4b5563, line-height 1.5, margin 0, padding-left 0

**Update TRiSMTrainingPanel.jsx:**

1. Add import: `import TRiSMGlossary from '../components/TRiSMGlossary';`
2. Add import: `import './TRiSMGlossary.css';` (in the glossary file already)
3. Add state: `const [showGlossary, setShowGlossary] = useState(false);`
4. Add a glossary toggle button in the panel header, next to the close button:
```jsx
<button
  className={`trism-glossary-toggle ${showGlossary ? 'active' : ''}`}
  onClick={() => setShowGlossary(!showGlossary)}
  aria-label="Toggle glossary"
>
  📖 Glossary
</button>
```
5. In the panel content area, conditionally render glossary OR slide:
```jsx
<div className="trism-panel-content">
  {showGlossary ? <TRiSMGlossary /> : <TRiSMSlide slide={SLIDES[activeSlideIndex]} />}
</div>
```

6. Add CSS for glossary toggle in TRiSMTrainingPanel.css:
```css
.trism-glossary-toggle {
  padding: 6px 14px;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  background: #f9fafb;
  color: #374151;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}
.trism-glossary-toggle:hover { background: #f3f4f6; }
.trism-glossary-toggle.active { background: #eff6ff; border-color: #1e40af; color: #1e40af; }
```

IMPORTANT: When modifying TRiSMTrainingPanel.jsx, preserve ALL existing content (slides, navigation, keyboard handler). Only ADD the glossary import, state, toggle button, and conditional render.
  </action>
  <acceptance_criteria>
    - TRiSMGlossary.jsx contains `export default function TRiSMGlossary`
    - TRiSMGlossary.jsx contains `GLOSSARY_TERMS` with at least 15 entries
    - TRiSMGlossary.jsx contains `trism-glossary-term` and `trism-glossary-def` classNames
    - TRiSMGlossary.css contains `.trism-glossary-entry`
    - TRiSMGlossary.css contains `.trism-glossary-term`
    - TRiSMTrainingPanel.jsx contains `import TRiSMGlossary`
    - TRiSMTrainingPanel.jsx contains `showGlossary` state
    - TRiSMTrainingPanel.jsx contains `trism-glossary-toggle` className
    - TRiSMTrainingPanel.jsx still contains `const SLIDES` (preservation check)
    - TRiSMTrainingPanel.jsx still contains Escape key handler (preservation check)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export default function TRiSMGlossary" banking_api_ui/src/components/TRiSMGlossary.jsx && grep -c "GLOSSARY_TERMS" banking_api_ui/src/components/TRiSMGlossary.jsx && grep -c "import TRiSMGlossary" banking_api_ui/src/components/TRiSMTrainingPanel.jsx && grep -c "const SLIDES" banking_api_ui/src/components/TRiSMTrainingPanel.jsx</automated>
  </verify>
  <done>Glossary component created with 18 terms, integrated into training panel as toggle, all existing panel functionality preserved</done>
</task>

<task type="auto">
  <name>Task 3: Build verification — confirm all components compile</name>
  <files></files>
  <read_first>
    - banking_api_ui/src/components/TRiSMTrainingPanel.jsx (verify imports resolve)
    - banking_api_ui/src/components/ChaseTopNav.js (verify imports resolve)
  </read_first>
  <action>
Run `cd banking_api_ui && npm run build` and verify exit code is 0.

If build fails:
- Read the error messages
- Fix import paths, missing CSS imports, or JSX syntax issues
- Re-run build until it passes

Common issues to check:
- CSS import in TRiSMSlide.jsx: `import './TRiSMSlide.css';`
- CSS import in TRiSMGlossary.jsx: `import './TRiSMGlossary.css';`
- CSS import in TRiSMTrainingPanel.jsx: `import './TRiSMTrainingPanel.css';`
- useState import in ChaseTopNav.js: ensure `useState` is in the React import
  </action>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No "Module not found" errors in build output
    - No "SyntaxError" in build output
  </acceptance_criteria>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All Phase 160 components compile successfully, build passes with exit code 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | All changes are frontend-only; no new server endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-160-03 | T (Tampering) | ChaseTopNav.js | mitigate | Only add new button + panel import; preserve existing auth checks and role-gating. Code review confirms no existing functionality removed. |
| T-160-04 | I (Information Disclosure) | TRiSMGlossary | accept | Static glossary data only; no secrets, tokens, or PII exposed. |
</threat_model>

<verification>
- `npm run build` in banking_api_ui exits 0
- ChaseTopNav.js contains 📚 Learn button
- Clicking Learn opens TRiSMTrainingPanel
- Glossary toggle shows/hides glossary content
- All existing nav buttons (theme, role switch, logout) still work
- Panel closes on Escape key
</verification>

<success_criteria>
- 📚 Learn button visible in top navigation bar
- Clicking opens TRiSM training panel
- Glossary with 18 AI TRiSM terms accessible from panel
- All existing ChaseTopNav functionality preserved
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/160-ai-trism-training-panel-educational-slide-out-explaining-trust-risk-management-security-governance-lifecycle-and-iam-principles-for-ai-agents/160-02-SUMMARY.md`
</output>
