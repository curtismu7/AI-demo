---
phase: 270
plan: 04
type: execute
wave: 3
depends_on:
  - 270-01
  - 270-02
  - 270-03
files_modified:
  - REGRESSION_PLAN.md
  - banking_api_ui/src/components/education/InteractiveArchDiagram.js
  - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md
autonomous: true
requirements:
  - REQ-DIAGRAM-12
  - REQ-DIAGRAM-14

must_haves:
  truths:
    - "REGRESSION_PLAN.md §1 has a new row 'Architecture diagram completeness' pointing at the Plan 02 sync test as the enforcer"
    - "REGRESSION_PLAN.md §4 has a new Phase 270 entry documenting the audit + remediation"
    - "banking_api_ui/src/components/education/InteractiveArchDiagram.js gains a top-of-file comment noting the authoritative source is architecture-simple.png (component RETAINED per user decision, not deleted)"
    - "banking_api_ui CRA build passes (npm run build exit 0) after the InteractiveArchDiagram comment-only edit"
    - "270-VALIDATION.md per-task verification map is filled in with one row per task across Plans 01-04"
  artifacts:
    - path: "REGRESSION_PLAN.md"
      provides: "New §1 row + §4 log entry for diagram completeness invariant"
      contains: "Architecture diagram completeness"
    - path: "banking_api_ui/src/components/education/InteractiveArchDiagram.js"
      provides: "Top-of-file annotation pointing at the canonical PNG source"
      contains: "architecture-simple.png"
    - path: ".planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md"
      provides: "Filled per-task verification map"
      contains: "270-01-01"
  key_links:
    - from: "REGRESSION_PLAN.md §1 new row"
      to: "banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js"
      via: "explicit pointer in the 'Files' column"
      pattern: "ArchitectureDiagram.completeness"
    - from: "InteractiveArchDiagram.js top comment"
      to: "architecture-simple.png at banking_api_ui/public/architecture/overview.png"
      via: "comment annotation"
      pattern: "architecture-simple.png|overview.png"
---

<objective>
Close out Phase 270 by (a) recording the diagram-completeness invariant in REGRESSION_PLAN.md §1 + §4 so future phases know to maintain it, (b) annotating `InteractiveArchDiagram.js` with a top-of-file comment pointing at the authoritative PNG source (component is KEPT per locked user decision — research suggested deletion but user chose to retain), and (c) filling in the per-task verification map in `270-VALIDATION.md` so the validation contract is complete.

Purpose: Phase 270 is a hardening / documentation phase. Plans 01-03 produce the diagram content, the sync test, and the rendered PNGs. Plan 04 makes the invariant durable by writing it into the repo's protected-files contract (`REGRESSION_PLAN.md §1`) and finalizes the validation paperwork. Without this plan, the sync test from Plan 02 exists but nobody knows it's the §1 enforcer, and `InteractiveArchDiagram.js` continues to mislead readers who don't realize the canonical view lives in the mermaid PNG.

Output:
- REGRESSION_PLAN.md: one new row in §1 (Critical Do-Not-Break Areas), one new §4 log entry (Bug Fix Log — phase 270 architecture audit)
- InteractiveArchDiagram.js: top-of-file comment block (purely additive — no behavior change)
- 270-VALIDATION.md: per-task verification map populated for all eight tasks across the four plans
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-01-PLAN.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-02-PLAN.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-03-PLAN.md
@CLAUDE.md
@REGRESSION_PLAN.md
@banking_api_ui/src/components/education/InteractiveArchDiagram.js
</context>

<interfaces>
<!-- REGRESSION_PLAN.md §1 row format (existing example at line 75 for the Vault runtime routes): -->

```markdown
| Area | What breaks if touched | Files |
|---|---|---|
| **Vault runtime routes** | **Skipping admin auth, ...** | `banking_api_server/routes/adminVault.js`, ... |
```

<!-- REGRESSION_PLAN.md §4 entry format (existing example at line 110 for Phase 269.1): -->

```markdown
### 2026-05-14 — Phase 269.1: Admin vault routes for runtime unlock and rotate added
{body of entry}
```

<!-- InteractiveArchDiagram.js current top of file (line 1-7): -->

```javascript
// banking_api_ui/src/components/education/InteractiveArchDiagram.js
import React, { useState } from "react";
import { useTokenChainOptional } from "../../context/TokenChainContext";
import RfcLink from "../shared/RfcLink";
import "./InteractiveArchDiagram.css";

// Real architecture nodes — three parties: Identity, BFF+Agent, and downstream services
```

<!-- The new top comment goes BEFORE the existing `// banking_api_ui/...` line. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add §1 protected-files row + §4 bug-fix-log entry to REGRESSION_PLAN.md</name>
  <files>REGRESSION_PLAN.md</files>
  <read_first>
    - REGRESSION_PLAN.md (full §1 table at lines 27-76 to see the existing row pattern; §4 entry pattern at lines 108-160 for recent Phase 269/269.1 examples)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Project Constraints §6 — research recommended adding a §1 row)
  </read_first>
  <action>
**Step 1: Add a §1 row.**

Locate the §1 table in REGRESSION_PLAN.md (header at line 27 `## 1. Critical Do-Not-Break Areas`). The current last row in the table is the **Vault runtime routes** entry (around line 75). Insert a new row IMMEDIATELY AFTER that row, before the blank line and `---` separator:

```markdown
| **Architecture diagram completeness** | **`/architecture/system` page silently drifts when a new service is added to `run-bank.sh` SVC_LIST but the mermaid sources aren't updated — viewers see a partial system picture, miss the new service in compliance/audit reviews, and the demo's "what's where" claim becomes false.** Removing or weakening the Jest sync test below disables drift detection; emojis outside the §0 allowlist (⚠️ ✅ ❌) in any `.mmd` source violate §0; secret-value substrings (`VAULT_PASSWORD=`, `client_secret=`, `_SECRET=`, `api_key=value`) in any label leak credentials into rendered PNGs and git history. | `banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js` (enforcer — pure file-read test, parses `SVC_LIST=(...)` from `run-bank.sh` and asserts every service appears in at least one of `architecture-simple.mmd`, `architecture.mmd`, `i4ai-ref-arch.mmd`, `mcp-security-gateway.mmd`); `run-bank.sh` (SVC_LIST is the single source of truth — test reads this, never duplicate); `scripts/build-diagrams.sh` (regen pipeline, mermaid-cli@11 pin); `banking_api_ui/public/architecture/{overview,overview2,token-flow,token-flow2}.png` (rendered outputs — must be newer than their `.mmd` source after every `.mmd` edit). Phase 270. |
```

The row uses the same three-column format (`Area | What breaks if touched | Files`) as every other §1 row. Bold the area name to match existing convention.

**Step 2: Add a §4 bug-fix-log entry.**

Locate §4 in REGRESSION_PLAN.md (header at line 108 `## 4. Bug Fix Log (reverse-chronological)`). The current most-recent entry is the 2026-05-14 Phase 269.1 entry (at line 110). Insert a new entry IMMEDIATELY AFTER the §4 header, BEFORE the existing 2026-05-14 entry (entries are reverse-chronological — newest first; this phase is dated today 2026-05-14 too, so it goes ABOVE the 269.1 entry):

```markdown
### 2026-05-14 — Phase 270: Architecture diagram completeness audit — `/architecture/system` brought current with code state

- **Source edits** (`architecture-simple.mmd`, `architecture.mmd`): added missing nodes (`banking_mcp_invest`, `banking_mortgage_service`, `banking_agent_service`, `banking_hitl_service`, `banking_mcp_gateway`, `langchain_agent`, `secrets.vault`, PingOne Management API), Phase 269 vault startup-load arrow, Phase 268 K8s topology as `planned` (dashed) subgraph, Helix-default LLM label with optional providers.
- **Cleanup** (`architecture.mmd`): removed §0-violating emojis (🖥️ ☁️), fixed stale `:3000` → `:4000` port labels (UI runs on 4000 under run-bank.sh), replaced OpenAI-only LLM label with Helix-default fallback chain, enclosed the previously-stray `BankingRS` block in a proper `ResourceServer` subgraph.
- **Duplicate removed**: deleted `i4ai-ref-arch (1).mmd` (Finder-duplicated copy at repo root; not referenced by the build pipeline or any code).
- **Regression guard** (`banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js`): new Jest test reads `SVC_LIST=(...)` from `run-bank.sh` and asserts every service appears in at least one `.mmd` source; also enforces OAuth grant markers (`PingOne`, `RFC 8693`, `PKCE`, `client_credentials`), the §0 emoji allowlist, and the no-secret-values invariant. Pure file-read test — no React imports — so it cannot break the existing `ArchitectureTabsPanel.anon.test.js`.
- **Pipeline bump** (`scripts/build-diagrams.sh`): `@mermaid-js/mermaid-cli@10` → `@11` (current major; one-character edit on line 49; backwards-compatible with the four existing `.mmd` sources).
- **PNGs regenerated**: all four output PNGs (`overview.png`, `overview2.png`, `token-flow.png`, `token-flow2.png`) re-rendered; mtime now newer than their source `.mmd`; DiagramRegeneratePanel admin UI no longer shows "stale" badges.
- **Component annotation** (`banking_api_ui/src/components/education/InteractiveArchDiagram.js`): top-of-file comment added noting the authoritative diagram source is `public/architecture/overview.png` (rendered from `architecture-simple.mmd`). Component is RETAINED per user decision — research recommended deletion, user chose to keep the limited interactive highlighting that drives off `TokenChainContext`.

**Files changed:**

- `architecture-simple.mmd`, `architecture.mmd`, `scripts/build-diagrams.sh`
- `banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js` (NEW)
- `banking_api_ui/public/architecture/{overview,overview2,token-flow,token-flow2}.png` (regenerated)
- `banking_api_ui/src/components/education/InteractiveArchDiagram.js` (comment-only)
- `.planning/REQUIREMENTS.md` (REQ-DIAGRAM-01..15 appended)
- `.planning/phases/270-*/270-VALIDATION.md` (per-task verification map filled in)
- `REGRESSION_PLAN.md` (this §1 row + §4 entry)
- Deleted: `i4ai-ref-arch (1).mmd`

**Verification:**

- `cd banking_api_ui && CI=true npx react-scripts test --watchAll=false --testPathPattern='ArchitectureDiagram.completeness'` → **passes**
- `cd banking_api_ui && CI=true npx react-scripts test --watchAll=false --testPathPattern='ArchitectureTabsPanel.anon'` → **still passes** (existing test not regressed)
- `cd banking_api_ui && npm run build` → **exit 0** (UI build gate per CLAUDE.md)
- `bash scripts/build-diagrams.sh` → **exit 0** with all four `[ok]` lines

**Why this matters:**

The demo's entire educational pitch is "here's where every token, every service, every PingOne API call lives." When the picture is partially wrong, the pitch is partially wrong. Before this phase, the System Architecture tab showed 5 of the 14 distinct nodes the system actually runs; after this phase it shows all 14 plus the planned Phase 268 K8s topology and the Phase 269 vault. The Jest sync test prevents this from drifting again.
```

**Constraints:**
- Do NOT modify any existing §1 row or §4 entry
- Do NOT renumber sections
- The new §1 row must use the bolded **`Architecture diagram completeness`** area name (matches the convention of bolding new/recent rows)
- The new §4 entry's date is `2026-05-14` (today per environment context); place it ABOVE the existing 2026-05-14 Phase 269.1 entry (latest-first ordering — Phase 270 is later in the same day than 269.1's morning entry)
- Markdown table formatting must align with existing rows (three pipes, no extra spaces in column separators)
  </action>
  <verify>
    <automated>grep -c "Architecture diagram completeness" REGRESSION_PLAN.md | grep -E "^[2-9]" && grep -c "Phase 270.*Architecture diagram completeness audit" REGRESSION_PLAN.md | grep -E "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Architecture diagram completeness" REGRESSION_PLAN.md` returns at least 2 matches (one in §1 table, one in §4 entry; §4 entry also references it inside the body)
    - `grep "ArchitectureDiagram.completeness.test.js" REGRESSION_PLAN.md` returns at least 1 match (in the §1 row's Files column)
    - `grep "### 2026-05-14 — Phase 270" REGRESSION_PLAN.md` returns exactly 1 match
    - The §1 row appears AFTER the **Vault runtime routes** row and BEFORE the `---` separator that closes §1
    - The §4 Phase 270 entry appears ABOVE the existing 2026-05-14 Phase 269.1 entry (line number of Phase 270 entry < line number of Phase 269.1 entry)
    - No existing §1 row or §4 entry was modified
  </acceptance_criteria>
  <done>The diagram-completeness invariant is now part of the repo's protected-files contract; future phases that touch `run-bank.sh` SVC_LIST will see the §1 row and know to update the mermaid sources.</done>
</task>

<task type="auto">
  <name>Task 2: Add top-of-file comment to InteractiveArchDiagram.js noting the authoritative PNG source</name>
  <files>banking_api_ui/src/components/education/InteractiveArchDiagram.js</files>
  <read_first>
    - banking_api_ui/src/components/education/InteractiveArchDiagram.js (full file — 209 lines; this is a comment-only edit at the top)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Open Question 1 — research suggested replacing with PNG; user locked the decision to KEEP this component)
  </read_first>
  <action>
The user's locked decision (per the planning_context received from the orchestrator) is to KEEP `InteractiveArchDiagram.js` rather than delete it — the research recommended deletion. To honor that decision while still making the situation legible to future readers, add a top-of-file JSDoc comment EXPLAINING that the canonical system diagram lives in the mermaid PNG, and that this component renders a simplified subset.

Insert the following comment block BEFORE the existing first line (the `// banking_api_ui/src/components/education/InteractiveArchDiagram.js` path comment):

```javascript
/**
 * InteractiveArchDiagram — 5-node simplified diagram driven by TokenChainContext.
 *
 * NOTE (Phase 270): This component is intentionally PARTIAL. It renders only 5 of the
 * 14 distinct nodes the system actually runs (User, BFF, PingOne, LLM, MCP) so the
 * inline "live highlighting" stays compact when token-chain events fire. It is NOT
 * the authoritative architecture view.
 *
 * For the COMPLETE system picture, see:
 *   - Mermaid source:  architecture-simple.mmd  (repo root)
 *   - Rendered PNG:    banking_api_ui/public/architecture/overview.png
 *   - Detailed view:   architecture.mmd → overview2.png
 *
 * The Jest sync test
 *   banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js
 * enforces "every service in run-bank.sh SVC_LIST appears in at least one .mmd source"
 * — DO NOT add nodes here to satisfy that test; add them to the .mmd sources instead.
 *
 * Why this component was retained (Phase 270 user decision): it drives off
 * TokenChainContext to highlight which boxes have been touched during the current
 * agent flow. That live behaviour is not replicated by a static PNG. If you find
 * yourself maintaining two parallel diagrams, prefer the PNG and remove this
 * component in a future phase.
 */
// banking_api_ui/src/components/education/InteractiveArchDiagram.js
```

This is a PURE COMMENT INSERTION — no code is added, removed, or modified below the new comment block. Every existing line of the file (imports, NODES object, ARROWS array, Node component, Arrow component, default export) stays byte-identical.

**Constraints:**
- Insert ABOVE the existing line `// banking_api_ui/src/components/education/InteractiveArchDiagram.js` (which becomes line ~21 after insertion)
- Use `/** ... */` JSDoc-style block (matches the convention elsewhere in `banking_api_ui/src/`)
- Do NOT modify any code below the comment — no React imports added, no NODES object changed, no ARROWS array changed
- Do NOT add a new prop, state, hook, or export
- Do NOT remove any existing line of code
- After saving, `cd banking_api_ui && npm run build` must still exit 0 (UI build gate)
  </action>
  <verify>
    <automated>grep -c "Phase 270" banking_api_ui/src/components/education/InteractiveArchDiagram.js | grep -E "^[1-9]" && grep -c "architecture-simple.mmd\|overview.png" banking_api_ui/src/components/education/InteractiveArchDiagram.js | grep -E "^[1-9]" && cd banking_api_ui && CI=true npm run build 2>&1 | tail -5 | grep -E "Compiled|exit 0|main\.[a-f0-9]+\.js"</automated>
  </verify>
  <acceptance_criteria>
    - File starts with a `/** ... */` JSDoc block referencing Phase 270
    - File contains substring `architecture-simple.mmd`
    - File contains substring `overview.png` or `architecture-simple.png`
    - File contains substring `ArchitectureDiagram.completeness.test.js` (pointer to the sync test)
    - File contains substring `// banking_api_ui/src/components/education/InteractiveArchDiagram.js` (the original path-comment line is preserved)
    - The file's existing imports (`React, { useState }`, `useTokenChainOptional`, `RfcLink`, `./InteractiveArchDiagram.css`) are all still present
    - `cd banking_api_ui && npm run build` exits 0
    - No new ESLint warnings introduced (JSDoc block is valid in CRA's default ESLint config)
  </acceptance_criteria>
  <done>InteractiveArchDiagram.js is annotated with a clear "this is partial; see the mermaid source for the full picture" note. The component continues to function exactly as before. Future readers know where the canonical diagram lives.</done>
</task>

<task type="auto">
  <name>Task 3: Fill in the per-task verification map in 270-VALIDATION.md</name>
  <files>.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md</files>
  <read_first>
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md (full file — currently has a single placeholder row at the per-task verification map)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-01-PLAN.md (task names + verify commands)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-02-PLAN.md
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-03-PLAN.md
  </read_first>
  <action>
Edit `.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md`. The file currently has a `## Per-Task Verification Map` section with one placeholder row. Replace ONLY that map section (keep everything else — Test Infrastructure table, Sampling Rate, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off — untouched).

Replace the existing table block under `## Per-Task Verification Map` with:

```markdown
## Per-Task Verification Map

> Populated by gsd-planner.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 270-01-01 | 01 | 1 | REQ-DIAGRAM-01, REQ-DIAGRAM-02, REQ-DIAGRAM-04, REQ-DIAGRAM-07 | T-270-01, T-270-02 | mermaid source coverage + no secret values | grep | `grep -c "banking_mcp_invest\|banking_mortgage_service\|banking_agent_service\|langchain_agent\|secrets.vault\|PingOne Management API" architecture-simple.mmd` | ✅ exists | ⬜ pending |
| 270-01-02 | 01 | 1 | REQ-DIAGRAM-06, REQ-DIAGRAM-08, REQ-DIAGRAM-11 | T-270-02, T-270-05 | emoji + port + K8s-planned | grep | `grep -c "🖥️\|☁️\|:3000" architecture.mmd \| grep -E "^0$"` AND `grep -c "Kubernetes cluster (planned" architecture.mmd \| grep -E "^[1-9]"` | ✅ exists | ⬜ pending |
| 270-01-03 | 01 | 1 | REQ-DIAGRAM-01..15 | T-270-04 | duplicate removed + REQ-IDs registered | file-test + grep | `test ! -f "i4ai-ref-arch (1).mmd" && grep -c "REQ-DIAGRAM-" .planning/REQUIREMENTS.md \| grep -E "^15$"` | ✅ exists | ⬜ pending |
| 270-02-01 | 02 | 1 | REQ-DIAGRAM-09, REQ-DIAGRAM-10, REQ-DIAGRAM-15 | T-270-01, T-270-03 | sync test enforces drift detection | jest | `cd banking_api_ui && CI=true npx react-scripts test --watchAll=false --testPathPattern='ArchitectureDiagram.completeness'` | ❌ W0 | ⬜ pending |
| 270-03-01 | 03 | 2 | REQ-DIAGRAM-13 | T-270-06 | mermaid-cli pin @11 | grep + shellcheck | `grep -c "@mermaid-js/mermaid-cli@11" scripts/build-diagrams.sh \| grep -E "^1$" && bash -n scripts/build-diagrams.sh` | ✅ exists | ⬜ pending |
| 270-03-02 | 03 | 2 | REQ-DIAGRAM-13 | T-270-02 | PNGs regenerated, mtime fresh | shell script + file-test | `bash scripts/build-diagrams.sh && for p in overview overview2 token-flow token-flow2; do [[ -s "banking_api_ui/public/architecture/${p}.png" ]] \|\| exit 1; done` | ✅ exists | ⬜ pending |
| 270-03-03 | 03 | 2 | (UI build gate) | T-270-02b | CRA build still passes | npm script | `cd banking_api_ui && CI=true npm run build` | ✅ exists | ⬜ pending |
| 270-04-01 | 04 | 3 | REQ-DIAGRAM-12 | T-270-03 | REGRESSION_PLAN §1 row + §4 entry | grep | `grep -c "Architecture diagram completeness" REGRESSION_PLAN.md \| grep -E "^[2-9]"` AND `grep -c "### 2026-05-14 — Phase 270" REGRESSION_PLAN.md \| grep -E "^1$"` | ✅ exists | ⬜ pending |
| 270-04-02 | 04 | 3 | REQ-DIAGRAM-14 | T-270-05 | InteractiveArchDiagram annotated + build green | grep + npm | `grep -c "Phase 270\|architecture-simple.mmd" banking_api_ui/src/components/education/InteractiveArchDiagram.js \| grep -E "^[2-9]" && cd banking_api_ui && CI=true npm run build` | ✅ exists | ⬜ pending |
| 270-04-03 | 04 | 3 | (validation paperwork) | n/a | per-task map filled in | this file | `grep -c "^| 270-" .planning/phases/270-*/270-VALIDATION.md \| grep -E "^[9-9]\|^[1-9][0-9]"` | ✅ exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
```

Also update the frontmatter at the top of `270-VALIDATION.md`:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true` (Plan 02 IS the Wave 0 test creation — and it runs in Wave 1 of Phase 270 because there's nothing earlier to depend on; the validation contract is satisfied either way)
- Update `created: 2026-05-14` → leave date unchanged

Also update the `## Wave 0 Requirements` section to mark both items as `[x]` complete (they will be complete after Plans 02 and 03 run):

```markdown
## Wave 0 Requirements

- [x] `banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js` — sync test (Plan 02)
- [x] PNG regen confirmation: `scripts/build-diagrams.sh` succeeds with mermaid-cli@11 (Plan 03)
```

And add a `## Validation Sign-Off` final line:

```markdown
**Approval:** signed off — planner — Plan 04 — 2026-05-14
```

Replacing the existing `**Approval:** pending` line at the very bottom.

**Constraints:**
- Do NOT modify the `## Test Infrastructure`, `## Sampling Rate`, or `## Manual-Only Verifications` sections (those are correct as-is from the skeleton)
- Do NOT add new task IDs that don't correspond to actual tasks in Plans 01-04
- The Task ID convention is `{phase}-{plan-NN}-{task-NN}` — e.g. Plan 01's three tasks become `270-01-01`, `270-01-02`, `270-01-03`
- "File Exists" column: ✅ if the artifact existed before this phase (mermaid sources, scripts, REGRESSION_PLAN), ❌ W0 if Plan 02 creates it
  </action>
  <verify>
    <automated>grep -c "^| 270-0" .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md | grep -E "^([9]|1[0-9])$" && grep -c "nyquist_compliant: true" .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^| 270-" .planning/phases/270-*/270-VALIDATION.md | wc -l` returns at least 10 rows (matches the 10 tasks across Plans 01-04)
    - Frontmatter contains `nyquist_compliant: true`
    - Frontmatter contains `wave_0_complete: true`
    - `## Wave 0 Requirements` section has both items marked `[x]`
    - File ends with `**Approval:** signed off — planner — Plan 04 — 2026-05-14`
    - `## Test Infrastructure`, `## Sampling Rate`, `## Manual-Only Verifications` sections are byte-identical to the original skeleton (no accidental edits)
  </acceptance_criteria>
  <done>270-VALIDATION.md is fully populated; every task has an automated verify command and a status placeholder; the planner sign-off line is in place for the checker to flip to "green" after execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→REGRESSION_PLAN.md | Adding to §1 is a contract change; future phases will see this row and know to maintain the invariant. Bad wording here causes future phases to misread what's protected. |
| executor→InteractiveArchDiagram.js | Comment-only edit. Risk surface is "did the build still pass" — verified by Task 2's `npm run build`. |
| validator→VALIDATION.md | The per-task map is read by the checker after execution; bad task IDs or wrong verify commands break the validation contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-270-03 | Tampering (silent drift) | REGRESSION_PLAN.md §1 | mitigate | The new §1 row is the durable enforcement marker for "diagrams must stay in sync with SVC_LIST". Without this row, the Plan 02 test exists but nobody knows to maintain it. The row text explicitly names the test file as the enforcer so a developer encountering a failing test sees the §1 reference and understands the intent. |
| T-270-02b | Denial-of-Service (build break) | InteractiveArchDiagram.js after comment add | mitigate | Task 2's verify command runs `npm run build` and asserts exit 0. A comment-only insertion cannot break the build under any normal ESLint/Babel config; verified anyway because CLAUDE.md non-negotiable #3 mandates the UI build gate. |
| T-270-05 | Information Disclosure (misleading) | InteractiveArchDiagram.js retained | mitigate | The new top-of-file comment explicitly states this is a 5-node simplified view, points readers at the canonical mermaid PNG, and warns "do not add nodes here to satisfy the sync test". This prevents the failure mode of a future developer adding nodes here instead of in the .mmd source, which would satisfy the eye but not the test. |

Severity: T-270-03 MEDIUM (without the §1 row, the invariant is implicit; with it, the invariant is part of the protected-files contract); T-270-02b LOW (comment-only is safe); T-270-05 LOW (clarity-only). All in-plan.
</threat_model>

<verification>
1. REGRESSION_PLAN.md §1 has the new "Architecture diagram completeness" row pointing at `ArchitectureDiagram.completeness.test.js`.
2. REGRESSION_PLAN.md §4 has the new "2026-05-14 — Phase 270" entry, positioned above the existing 2026-05-14 Phase 269.1 entry.
3. InteractiveArchDiagram.js starts with a JSDoc block referencing Phase 270, `architecture-simple.mmd`, and `overview.png`.
4. `cd banking_api_ui && npm run build` exits 0.
5. 270-VALIDATION.md has at least 10 task rows in its per-task verification map, `nyquist_compliant: true` in frontmatter, and `Approval: signed off` at the bottom.
</verification>

<success_criteria>
- [ ] All three tasks pass their automated `<verify>` checks
- [ ] REGRESSION_PLAN.md §1 row uses the correct three-column format and bolds the area name
- [ ] REGRESSION_PLAN.md §4 entry is reverse-chronological (above Phase 269.1's same-day entry)
- [ ] InteractiveArchDiagram.js comment block is purely additive — no code change below the new comment
- [ ] `cd banking_api_ui && npm run build` exit 0 (UI build gate per CLAUDE.md)
- [ ] 270-VALIDATION.md per-task map has rows for all 10 tasks across Plans 01-04
- [ ] Approval line flipped to `signed off — planner`
- [ ] No file outside `files_modified` was modified
</success_criteria>

<output>
After completion, create `.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-04-SUMMARY.md` documenting:
- The exact §1 row text added (with line number)
- The §4 entry text added (with line range)
- The JSDoc block prepended to InteractiveArchDiagram.js (with line count delta)
- Confirmation that `cd banking_api_ui && npm run build` exit 0 after the comment-only edit
- The per-task verification map row count (10 expected) and confirmation of frontmatter updates
- Phase 270 wrap-up: brief 2-3 line note that "all four plans complete; diagrams now match SVC_LIST; sync test prevents future drift"
</output>
</content>
</invoke>