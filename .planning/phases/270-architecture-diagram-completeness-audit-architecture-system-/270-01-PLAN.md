---
phase: 270
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - architecture-simple.mmd
  - architecture.mmd
  - .planning/REQUIREMENTS.md
files_deleted:
  - "i4ai-ref-arch (1).mmd"
autonomous: true
requirements:
  - REQ-DIAGRAM-01
  - REQ-DIAGRAM-02
  - REQ-DIAGRAM-03
  - REQ-DIAGRAM-04
  - REQ-DIAGRAM-05
  - REQ-DIAGRAM-06
  - REQ-DIAGRAM-07
  - REQ-DIAGRAM-08
  - REQ-DIAGRAM-11

must_haves:
  truths:
    - "Every banking_* service from run-bank.sh SVC_LIST is named in architecture-simple.mmd"
    - "langchain_agent is named in architecture-simple.mmd or architecture.mmd"
    - "Phase 269 vault is drawn with a startup-load arrow into BFF"
    - "Phase 268 K8s topology appears in architecture.mmd as a 'planned' (dashed) subgraph"
    - "All non-allowlist emojis are removed from architecture.mmd (no 🖥️ or ☁️)"
    - "architecture.mmd port labels read :4000 for the UI (not :3000)"
    - "architecture.mmd LLM provider label says Helix as default with optional providers (not OpenAI-only)"
    - "Duplicate file 'i4ai-ref-arch (1).mmd' is deleted from repo root"
    - "REQUIREMENTS.md contains REQ-DIAGRAM-01..15 entries"
  artifacts:
    - path: "architecture-simple.mmd"
      provides: "Clean-view mermaid source with all 8 services + langchain + vault + Phase 266 paths"
      contains: "banking_mcp_invest"
    - path: "architecture-simple.mmd"
      provides: "Vault startup arrow"
      contains: "secrets.vault"
    - path: "architecture.mmd"
      provides: "Detailed-view mermaid source — emoji-free, port-correct, Helix-default"
      contains: "banking_mcp_gateway"
    - path: "architecture.mmd"
      provides: "K8s planned subgraph"
      contains: "Kubernetes cluster (planned"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 270 requirement IDs"
      contains: "REQ-DIAGRAM-01"
  key_links:
    - from: "architecture-simple.mmd"
      to: "every SVC_LIST entry in run-bank.sh"
      via: "node label substring match"
      pattern: "banking_(api_server|mcp_server|api_ui|mcp_gateway|hitl_service|agent_service|mcp_invest|mortgage_service)"
    - from: "architecture-simple.mmd"
      to: "secrets.vault"
      via: "startup-load arrow"
      pattern: "Vault.*-->.*BFF"
---

<objective>
Bring the two primary mermaid sources (`architecture-simple.mmd` and `architecture.mmd`) into line with the live code state and the locked user decisions: include every Node service from `run-bank.sh` SVC_LIST, the Python `langchain_agent`, the Phase 269 vault (shipped), and the Phase 268 K8s topology (planned, dashed). Strip §0-violating emojis from `architecture.mmd`, fix its stale `:3000` port labels, replace the OpenAI-only LLM label with the actual Helix-default fallback chain, complete the stray `BankingRS` block, and delete the duplicate `i4ai-ref-arch (1).mmd` Finder copy. Add `REQ-DIAGRAM-01..15` to REQUIREMENTS.md so the rest of the phase has IDs to anchor against.

Purpose: The diagrams on `/architecture/system` are visibly behind the code by 6+ phases (no Gateway, no Agent Service, no MCP Invest, no Mortgage, no vault). The sync test in Plan 02 enforces "every SVC_LIST entry appears in at least one .mmd file" — this plan provides the content that test asserts against.

Output:
- `architecture-simple.mmd` extended with missing nodes (banking_mcp_invest, banking_mortgage_service, banking_agent_service, langchain_agent, MCP Server as a distinct node, secrets.vault, PingOne Management API)
- `architecture.mmd` cleaned: port `:3000` → `:4000`, emojis removed, OpenAI label replaced, stray BankingRS block completed, missing services added, Phase 268 K8s planned subgraph added
- `i4ai-ref-arch (1).mmd` removed
- `.planning/REQUIREMENTS.md` gains 15 REQ-DIAGRAM-* entries
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md
@CLAUDE.md
@REGRESSION_PLAN.md
@architecture-simple.mmd
@architecture.mmd
@run-bank.sh
@scripts/build-diagrams.sh
</context>

<interfaces>
<!-- Service inventory — authoritative, from run-bank.sh:616 -->

```bash
SVC_LIST=(banking_api_server banking_mcp_server banking_api_ui banking_mcp_gateway banking_hitl_service banking_agent_service banking_mcp_invest banking_mortgage_service)
```

Plus the Python service launched separately:
- langchain_agent (uvicorn :8888, chat WS :8889, health/inspector :8890)

Plus external systems:
- PingOne AS (auth.pingone.{region}/{envId}/as/{authorize,token,jwks,introspect})
- PingOne Management API (api.pingone.{region}/v1/environments/{envId}/*)
- PingOne Authorize PDP (api.pingone.{region}/v1/environments/{envId}/governance/policyDecisionPoints/{id}/evaluate)
- Helix LLM (default; helixLlmService.js POSTs to ${base}/dpc/jas/helix/v1/conversations)
- OpenAI / Anthropic / Groq / Gemini (configurable fallback providers)

<!-- Existing classDef styles in architecture-simple.mmd (reuse, don't redefine) -->

```mermaid
classDef user      fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e,stroke-width:2px
classDef frontend  fill:#ecfdf5,stroke:#059669,color:#064e3b,stroke-width:2px
classDef bff       fill:#fef9c3,stroke:#ca8a04,color:#713f12,stroke-width:2px
classDef tool      fill:#f3e8ff,stroke:#7c3aed,color:#3b0764,stroke-width:2px
classDef authorize fill:#fed7aa,stroke:#ea580c,color:#7c2d12,stroke-width:3px
classDef backend   fill:#fce7f3,stroke:#be185d,color:#831843,stroke-width:2px
classDef planned   fill:#fce7f3,stroke:#be185d,color:#831843,stroke-width:2px,stroke-dasharray: 6 4
classDef cloud     fill:#fef2f2,stroke:#dc2626,color:#7f1d1d,stroke-width:2px
classDef external  fill:#f1f5f9,stroke:#475569,color:#0f172a,stroke-width:2px
```

<!-- Existing REQUIREMENTS.md ends after Family Delegation Phase 38 block (line ~135). -->
<!-- Append a new "## Architecture Diagram Completeness (Phase 270)" section. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend architecture-simple.mmd with missing services + vault + LLM fallback chain</name>
  <files>architecture-simple.mmd</files>
  <read_first>
    - architecture-simple.mmd (full file — 126 lines)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Service Inventory section + Audit Findings → architecture-simple.mmd)
  </read_first>
  <action>
Edit `architecture-simple.mmd` in place. Make the following additive changes only — do NOT remove existing nodes/edges/classDefs (those already serve Phase 266 R2 Paths A/B/C correctly):

1. **Add five new nodes** before the existing `subgraph Backends` block (insert after the `Authorize[...]` declaration around current line 44):

   ```mermaid
       %% ── Additional Node services (Phase 270 audit) ─────────────────────
       MCPServer["banking_mcp_server<br/>MCP protocol · WebSocket :8080<br/>tools/list · tools/call"]
       MCPInvest["banking_mcp_invest<br/>Investment MCP server :8081<br/>(WebSocket + HTTP)"]
       Mortgage["banking_mortgage_service<br/>Phase 266 Path A backend :8082<br/>X-API-Key gated"]
       LangChain["langchain_agent<br/>Python uvicorn :8888<br/>chat WS :8889 - health :8890"]
       Vault[("secrets.vault<br/>AEAD + Argon2id<br/>startup-loaded - Phase 269")]
       MgmtAPI["PingOne Management API<br/>/v1/environments/{env}/users<br/>worker token (CC)"]
   ```

2. **Add a Helix-default LLM label.** Replace the existing `LLM["LLM provider<br/>(Helix · Anthropic · OpenAI)"]` line (currently around line 68) with a clearer two-line label that names Helix as default:

   ```mermaid
       LLM["LLM provider<br/>Helix (default) - x-api-key<br/>OpenAI - Anthropic - Groq - Gemini (optional)"]
   ```

3. **Add edges into the existing Connections block** (insert after the existing `Agent -->|"tool call + TX token"| Gateway` line):

   ```mermaid
       %% ── Phase 270: edges for newly-named services ─────────────────────
       Gateway -->|"WS MCP - exchanged Bearer<br/>tools/call"| MCPServer
       Gateway -->|"WS MCP - invest tools"| MCPInvest
       Gateway -->|"X-API-Key (Path A)<br/>OAuth Bearer dropped"| Mortgage
       MCPServer -->|"REST - exchanged Bearer<br/>BankingAPIClient"| OAuthRS
       MCPServer -->|"RFC 7662 introspect<br/>RFC 8693 chained exchange"| AS
       MCPInvest -->|"REST - exchanged Bearer"| OAuthRS
       BFF -->|"PingOne Management API<br/>worker token (CC) - RFC 6749 §4.4"| MgmtAPI
       BFF -->|"vaultLoader.js startup-load<br/>configStore.setRaw persist:false"| Vault
       LangChain -->|"WS MCP - Bearer in JSON-RPC"| MCPServer
       LangChain -->|"HTTPS - provider API key"| LLM
       User -.->|"WebSocket chat :8889"| LangChain
   ```

4. **Apply classes to the new nodes** (insert near the existing `class … user`/`class … bff` lines at the bottom):

   ```mermaid
       class MCPServer,MCPInvest tool
       class Mortgage backend
       class MgmtAPI cloud
       class LangChain external
       class Vault planned
   ```

   `Vault` uses `planned` because Phase 269.1 admin-unlock UI shipped but Gateway-side vault consumption (REQ-VAULT-08) is not yet wired — the dashed border signals "current load path is BFF-only".

5. **Preserve existing structure.** Do NOT modify the existing classDefs, the `Backends` subgraph, the `PingOne` subgraph, the User/UI/BFF/Agent/Gateway/HITL/Authorize/OAuthRS/APIKeyRS/BankingRS/SQLite/AS nodes, or the existing edges. The only edit to existing content is the LLM label clarification in step 2.

6. **No emojis** anywhere in the new content (verify with `grep -E "[\xF0-\xF4][\x80-\xBF]{3}" architecture-simple.mmd` returning empty). Existing file is already emoji-clean.

7. **No secret values.** No node or edge label may contain `VAULT_PASSWORD=...`, `HELIX_API_KEY=...`, `client_secret=...`, or any value-bearing secret token. The vault arrow says "startup-load" (mechanism), never the password.

8. **Do not regenerate the PNG yet** — Plan 03 owns the regen pipeline + UI build gate. Just edit the source.
  </action>
  <verify>
    <automated>grep -c "banking_mcp_invest\|banking_mortgage_service\|banking_agent_service\|langchain_agent\|secrets.vault\|PingOne Management API" architecture-simple.mmd | grep -E "^[6-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File contains substring `banking_mcp_invest`
    - File contains substring `banking_mortgage_service`
    - File contains substring `langchain_agent`
    - File contains substring `secrets.vault`
    - File contains substring `PingOne Management API`
    - File contains substring `Helix (default)`
    - `grep -E "[\u{1F300}-\u{1FAFF}]" architecture-simple.mmd` returns empty (no non-allowlist emojis)
    - `grep -E "VAULT_PASSWORD\s*=\s*\S|client_secret\s*=\s*\S" architecture-simple.mmd` returns empty
    - File still starts with `flowchart LR` line (unchanged)
    - Existing `OAuthRS`, `APIKeyRS`, `BankingRS`, `SQLite`, `AS`, `Authorize` node IDs remain intact
  </acceptance_criteria>
  <done>architecture-simple.mmd renders without mermaid syntax errors (verified in Plan 03 PNG regen) and every SVC_LIST service name appears as a substring in the file (verified by Plan 02 sync test).</done>
</task>

<task type="auto">
  <name>Task 2: Clean up architecture.mmd — remove emojis, fix :3000→:4000, complete stray BankingRS block, add missing services, add Phase 268 K8s planned subgraph</name>
  <files>architecture.mmd</files>
  <read_first>
    - architecture.mmd (full file — 204 lines)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Audit Findings → architecture.mmd section)
    - REGRESSION_PLAN.md §0 (emoji allowlist)
  </read_first>
  <action>
Edit `architecture.mmd` in place. This file is the detailed-view source rendered into `overview2.png`. Three categories of edits:

**A. Style/correctness fixes (do these first):**

1. **Remove the 🖥️ emoji from AdminBrowser and CustomerBrowser** (current lines 7-8):
   ```mermaid
       AdminBrowser["Admin Browser<br/>(port 4000)"]
       CustomerBrowser["Customer Browser<br/>(port 4000)"]
   ```
   Note both port-label fix (`3000` → `4000`) and emoji removal.

2. **Remove the 🖥️ emoji + fix port** in the UI subgraph title (current line 10):
   ```mermaid
       subgraph UI["banking_api_ui  (React 18 · CRA · port 4000)"]
   ```

3. **Remove the ☁️ emojis** from `PingOneCloud`, `PingOneAuthorize`, and `OpenAI` (current lines 89, 97, 101). Final form:
   ```mermaid
       subgraph PingOneCloud["PingOne  (auth.pingone.com / {region})"]
   ```
   ```mermaid
       subgraph PingOneAuthorize["PingOne Authorize"]
   ```

4. **Replace the OpenAI-only LLM node** (current line 101) with a Helix-default fallback chain. Replace:
   ```
   OpenAI["☁️ OpenAI API\ngpt-3.5-turbo · gpt-4"]
   ```
   With:
   ```mermaid
       LLM["LLM provider<br/>Helix (default) - x-api-key<br/>OpenAI - Anthropic - Groq - Gemini (optional)"]
   ```
   Also update every reference to `OpenAI` in edges (currently `LangChain -->|"HTTPS  API Key"| OpenAI` near line 147) to point at `LLM` instead. And update the `class PingOneCloud,PingOneAuthorize,OpenAI external` line near 198 → `class PingOneCloud,PingOneAuthorize,LLM external`.

5. **Complete the stray BankingRS block.** Current lines 166-170 declare `BankingRS["banking_resource_server\n/identity ..."]` and `SQLite[("SQLite\n...")]` OUTSIDE the existing subgraphs after the closing brace — likely an unfinished Phase 266 edit that landed in main. Move both nodes INTO a new subgraph just above the `%% ── Connections: API ↔ PingOne ──` comment block, and ensure both have appropriate `class` assignments. The result should be:
   ```mermaid
       subgraph ResourceServer["banking_resource_server  (logical sub-routes on :3001)"]
           direction TB
           BankingRS["banking_resource_server<br/>/identity (Path B dual_token)<br/>/accounts /transactions (Path C oauth_bearer)<br/>RFC 6750 authenticateToken"]
           BankingSQLite[("SQLite<br/>banking-resource-server.db<br/>seeded from data/store.js")]
       end
       BankingRS -->|"reads accounts + transactions"| BankingSQLite
   ```
   (Rename the standalone `SQLite[(...)]` to `BankingSQLite` to avoid collision with the existing class assignments. Update the existing `BankingRS -.->|"reads accounts\n+ transactions"| SQLite` edge to point at `BankingSQLite`.) Apply `class BankingRS,BankingSQLite backend` near the bottom.

**B. Service additions (missing nodes per research):**

Add these new nodes/subgraphs inside the appropriate region of the file. Insert after the existing `Agent` (LangChain) subgraph (around line 86):

```mermaid
    %% ── MCP Gateway (Phase 243 + 266) ──────────────────────────────────
    subgraph Gateway["banking_mcp_gateway  (TypeScript · port 3005)"]
        direction TB
        GwProxy["Policy-aware credential broker<br/>RFC 8693 chained exchange<br/>Paths A api_key - B dual_token - C oauth_bearer"]
        GwAuthorize["pingAuthorizeGuard.ts<br/>per-tool-call PDP evaluation"]
    end

    %% ── Additional Node services ───────────────────────────────────────
    AgentService["banking_agent_service<br/>TypeScript LangGraph agent :3006"]
    MCPInvestSvc["banking_mcp_invest<br/>Investment MCP server :8081"]
    MortgageSvc["banking_mortgage_service<br/>Phase 266 Path A backend :8082<br/>X-API-Key gated"]
    HITLSvc["banking_hitl_service<br/>Out-of-band consent :3009"]

    %% ── Phase 269 vault ────────────────────────────────────────────────
    Vault[("secrets.vault<br/>AEAD + Argon2id<br/>startup-load - Phase 269")]
```

Then add the missing edges into the connections block (insert after the existing `LangChain --> MCPClientPy` line, around line 146):

```mermaid
    %% ── Phase 270: edges for newly-named services ─────────────────────
    BFF -->|"RFC 8693 exchanged Bearer"| GwProxy
    GwProxy --> GwAuthorize
    GwProxy -->|"WS MCP - exchanged Bearer"| MCPProtocol
    GwProxy -->|"WS MCP - invest tools"| MCPInvestSvc
    GwProxy -->|"X-API-Key (Path A)<br/>OAuth Bearer dropped"| MortgageSvc
    GwProxy -->|"Bearer + id_token (Path B)<br/>Bearer exchanged (Path C)"| BankingRS
    BFF -->|"prompt + user token"| AgentService
    AgentService -->|"WS exchanged Bearer"| GwProxy
    BFF -->|"consent challenge"| HITLSvc
    HITLSvc -.->|"approval / deny callback"| BFF
    BFF -->|"vaultLoader.js startup-load<br/>configStore.setRaw persist:false"| Vault
    MCPInvestSvc -->|"REST exchanged Bearer"| TxRoutes
    GwAuthorize -->|"PDP evaluate"| PDP
```

**C. Phase 268 K8s planned subgraph:**

Insert this subgraph near the top of the file (just after `flowchart TB` and the title block, before `AdminBrowser`):

```mermaid
    %% ── Phase 268 future K8s topology — DASHED = PLANNED, NOT SHIPPED ─
    subgraph K8s["Kubernetes cluster (planned — Phase 268, NOT live today)"]
        direction TB
        Ingress["Ingress + cert-manager<br/>Let's Encrypt"]
        BFFPod["banking_api_server pod<br/>Service: LoadBalancer (public)"]
        GWPod["banking_mcp_gateway pod<br/>Service: LoadBalancer (public)"]
        MCPPod["banking_mcp_server pod<br/>Service: ClusterIP"]
        AgentPod["banking_agent_service pod<br/>Service: ClusterIP"]
        HITLPod["banking_hitl_service pod<br/>Service: ClusterIP"]
        InvestPod["banking_mcp_invest pod<br/>Service: ClusterIP"]
        MortgagePod["banking_mortgage_service pod<br/>Service: ClusterIP"]
    end
```

Add a new classDef and class assignment for the K8s nodes (insert near the existing classDef block around line 191):

```mermaid
    classDef k8s fill:#e0f2fe,stroke:#0284c7,color:#0c4a6e,stroke-width:2px,stroke-dasharray: 6 4
    class K8s,Ingress,BFFPod,GWPod,MCPPod,AgentPod,HITLPod,InvestPod,MortgagePod k8s
```

**D. Class assignments for new nodes** (insert near the existing `class …` lines around line 198):

```mermaid
    class GwProxy,GwAuthorize,AgentService,MCPInvestSvc,MortgageSvc,HITLSvc backend
    class LLM external
    class Vault planned
    classDef planned fill:#fce7f3,stroke:#be185d,color:#831843,stroke-width:2px,stroke-dasharray: 6 4
```

**Constraints:**
- No emojis except the §0 allowlist (⚠️ ✅ ❌) anywhere in the file
- No secret-value substrings (`VAULT_PASSWORD=`, `client_secret=`, `_SECRET=`, `api_key=`)
- All port labels `:3000` MUST become `:4000` (UI runs on 4000 under run-bank.sh)
- Mermaid syntax: use `<br/>` for newlines in labels (NOT `\n` — but the existing file uses `\n`; leave existing `\n` alone, just use `<br/>` for new content)
- The stray `BankingRS` block must be properly inside a subgraph or properly declared at the top level with class — NOT dangling after another subgraph's closing brace
- Do not regenerate PNG yet — Plan 03 owns that
  </action>
  <verify>
    <automated>grep -c "🖥️\|☁️" architecture.mmd | grep -E "^0$" && grep -c ":3000" architecture.mmd | grep -E "^0$" && grep -c "banking_mcp_gateway\|banking_mcp_invest\|banking_mortgage_service\|banking_agent_service\|banking_hitl_service\|secrets.vault\|Kubernetes cluster" architecture.mmd | grep -E "^[7-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "🖥️\|☁️" architecture.mmd` returns no matches
    - `grep ":3000" architecture.mmd` returns no matches
    - `grep -E "OpenAI API" architecture.mmd` returns no matches (replaced with LLM Helix-default)
    - File contains substring `banking_mcp_gateway`
    - File contains substring `banking_mcp_invest`
    - File contains substring `banking_mortgage_service`
    - File contains substring `banking_agent_service`
    - File contains substring `banking_hitl_service`
    - File contains substring `secrets.vault`
    - File contains substring `Kubernetes cluster (planned`
    - File contains classDef `k8s` with `stroke-dasharray: 6 4` AND classDef `planned` with `stroke-dasharray: 6 4`
    - `grep -E "VAULT_PASSWORD\s*=\s*\S|client_secret\s*=\s*\S|api_key\s*=\s*[^\sX]" architecture.mmd` returns empty (allow `X-API-Key` header references)
    - No stray `BankingRS[...]` declaration outside any subgraph at end of file
  </acceptance_criteria>
  <done>architecture.mmd is emoji-free per §0, port-correct, contains every missing service, includes the Phase 268 K8s planned subgraph and Phase 269 vault, and the stray BankingRS block is cleanly enclosed. PNG regen + UI build happen in Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Delete duplicate `i4ai-ref-arch (1).mmd` and add REQ-DIAGRAM-01..15 to REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <files_deleted>
    "i4ai-ref-arch (1).mmd"
  </files_deleted>
  <read_first>
    - .planning/REQUIREMENTS.md (especially the end of file — append a new section)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Phase Requirements table → REQ-DIAGRAM-01..15)
  </read_first>
  <action>
**Step 1: Confirm the duplicate is not referenced by anything, then delete it.**

The file `i4ai-ref-arch (1).mmd` (with the space + parenthesized "1") is a Finder-duplicated copy that landed in the repo. Plan 266-05 already noted "It is NOT in the build script's allow-list, so the build pipeline will NOT pick it up. Leave it alone — deleting it is out of scope." Phase 270 is the right scope to delete it (research recommended; user locked the decision).

Pre-flight check (must pass before deletion):
```bash
grep -rn "i4ai-ref-arch (1)" /Users/curtismuir/Development/banking --include="*.sh" --include="*.js" --include="*.ts" --include="*.json" 2>/dev/null
```

Acceptable matches: ONLY `.planning/phases/266-*/266-05-PLAN.md` (historical note) and `.planning/phases/270-*/270-RESEARCH.md` (audit reference). Both are documentation, not consumers. If any `scripts/` or `*.json` config references the file, STOP and surface the issue — do not delete.

Then:
```bash
git rm "i4ai-ref-arch (1).mmd"
```

(Or `rm` followed by `git add -u` if `git rm` complains.)

**Step 2: Append REQ-DIAGRAM-01..15 to REQUIREMENTS.md.**

Open `.planning/REQUIREMENTS.md`. The file currently ends with the "Family Delegation (Phase 38)" block (around line 135 — DELEG-01 through DELEG-07). Append a new section AT THE END of the file:

```markdown

---

## Architecture Diagram Completeness (Phase 270)

- [ ] **REQ-DIAGRAM-01**: Every `run-bank.sh` SVC_LIST entry (banking_api_server, banking_mcp_server, banking_api_ui, banking_mcp_gateway, banking_hitl_service, banking_agent_service, banking_mcp_invest, banking_mortgage_service) appears as a node label substring in at least one `.mmd` source at repo root.
- [ ] **REQ-DIAGRAM-02**: Inter-service edges drawn — BFF→Gateway, Gateway→MCP Server, Gateway→banking_resource_server, MCP→MCP Invest, BFF→Helix, BFF→PingOne AS, BFF→PingOne Management, HITL↔BFF.
- [ ] **REQ-DIAGRAM-03**: Every OAuth grant in active use is labelled — Auth Code+PKCE, Client Credentials, RFC 8693 single-exchange, RFC 8693 dual-token, CIBA, RFC 7662 introspection (in at least one of the four `.mmd` sources).
- [ ] **REQ-DIAGRAM-04**: External cloud nodes present and distinguished — PingOne AS, PingOne Management API, PingOne Authorize PDP, Helix LLM (with optional providers).
- [ ] **REQ-DIAGRAM-05**: Phase 266 paths (A api_key, B dual_token, C oauth_bearer) drawn with distinct edge styles in at least one .mmd source.
- [ ] **REQ-DIAGRAM-06**: Phase 268 K8s topology drawn as `planned` (stroke-dasharray subgraph) in `architecture.mmd`.
- [ ] **REQ-DIAGRAM-07**: Phase 269 `secrets.vault` startup-load arrow into BFF drawn in `architecture-simple.mmd` and `architecture.mmd`.
- [ ] **REQ-DIAGRAM-08**: No emojis outside REGRESSION_PLAN §0 allowlist (⚠️ ✅ ❌) in any `.mmd` source.
- [ ] **REQ-DIAGRAM-09**: Jest sync test `banking_api_ui/src/components/__tests__/ArchitectureDiagram.completeness.test.js` asserts every SVC_LIST entry appears in at least one .mmd source.
- [ ] **REQ-DIAGRAM-10**: Jest sync test asserts OAuth grant markers (PingOne, RFC 8693, PKCE, client_credentials) appear in at least one .mmd source.
- [ ] **REQ-DIAGRAM-11**: Stale port label `:3000` removed from `architecture.mmd` (UI runs on `:4000` under run-bank.sh); OpenAI-only LLM label replaced with Helix-default fallback chain.
- [ ] **REQ-DIAGRAM-12**: New REGRESSION_PLAN §1 row added: "Architecture diagram completeness" pointing at the sync test as the enforcer.
- [ ] **REQ-DIAGRAM-13**: PNGs regenerated via `scripts/build-diagrams.sh`; all four output PNGs (overview, overview2, token-flow, token-flow2) have non-zero size and mtime newer than their .mmd source.
- [ ] **REQ-DIAGRAM-14**: `InteractiveArchDiagram.js` retained per locked user decision (not removed), with a top-of-file comment noting the authoritative source is `architecture-simple.png`.
- [ ] **REQ-DIAGRAM-15**: Sync test asserts no .mmd source contains a secret-value substring pattern (VAULT_PASSWORD=, client_secret=, _SECRET=, api_key= with value); arrow labels reference mechanisms (`startup-load`, `X-API-Key`), never values.
```

**Constraints:**
- Use markdown checkbox syntax `- [ ]` (matches existing requirements file convention)
- Don't renumber any existing requirements
- Insert the new section at the END of the file (after the last `DELEG-07` entry)
- The `Traceability` table earlier in the file does NOT need updating — this is a Phase 270-only addition and the table is for v1 requirements
  </action>
  <verify>
    <automated>test ! -f "i4ai-ref-arch (1).mmd" && grep -c "REQ-DIAGRAM-0[1-9]\|REQ-DIAGRAM-1[0-5]" .planning/REQUIREMENTS.md | grep -E "^15$"</automated>
  </verify>
  <acceptance_criteria>
    - File `i4ai-ref-arch (1).mmd` no longer exists at repo root
    - `grep "REQ-DIAGRAM-" .planning/REQUIREMENTS.md | wc -l` returns 15
    - `grep "REQ-DIAGRAM-15" .planning/REQUIREMENTS.md` finds the secret-value invariant entry
    - `grep "Architecture Diagram Completeness (Phase 270)" .planning/REQUIREMENTS.md` finds the new section header
    - No other section of REQUIREMENTS.md was modified
  </acceptance_criteria>
  <done>Duplicate mermaid file is removed from repo root; REQUIREMENTS.md gains the 15 REQ-DIAGRAM-* entries that the sync test in Plan 02 and the regression-plan row in Plan 04 reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→repo | Mermaid source files are authored by developers; the regen pipeline trusts them as inputs. Diagram labels can leak secrets if authors paste real values. |
| filesystem→git | `i4ai-ref-arch (1).mmd` deletion is destructive; pre-flight grep confirms no consumer references it. |
| REQUIREMENTS.md→planner/checker | Other plans reference REQ-DIAGRAM-* IDs; appending must not collide with existing IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-270-01 | Information Disclosure | architecture-simple.mmd, architecture.mmd | mitigate | Vault arrow label is `startup-load · configStore.setRaw persist:false` (mechanism, not value); no `VAULT_PASSWORD=...` or `HELIX_API_KEY=...` substring appears anywhere; Plan 02 sync test will fail-loud if a secret-value pattern is ever added. |
| T-270-02 | Denial-of-Service (build break) | Mermaid syntax | mitigate | New nodes use the canonical `Name["Label<br/>line2"]` pattern (no apostrophes, no unmatched `<`); Plan 03's PNG regen step will surface any parse error before merge by failing `scripts/build-diagrams.sh`. |
| T-270-04 | Tampering (destructive delete) | `i4ai-ref-arch (1).mmd` | mitigate | Pre-flight grep of `--include="*.sh" --include="*.js" --include="*.ts" --include="*.json"` MUST return no matches before `git rm`; documentation references in `.planning/phases/*/PLAN.md` and `RESEARCH.md` are acceptable (they are not consumers). |
| T-270-05 | Information Disclosure (misleading) | architecture.mmd K8s subgraph | mitigate | The K8s subgraph title is literally `"Kubernetes cluster (planned — Phase 268, NOT live today)"` — phrasing AND the `stroke-dasharray: 6 4` visual cue together prevent the picture being misread as "shipped". |

Severity: T-270-01 HIGH (secret leak); T-270-02 MEDIUM (CI break); T-270-04 LOW (documentation-only refs); T-270-05 LOW (visual clarity). All mitigated in-plan.
</threat_model>

<verification>
1. `architecture-simple.mmd` contains every SVC_LIST service substring + `secrets.vault` + `PingOne Management API` + `Helix (default)` + `langchain_agent`.
2. `architecture.mmd` is emoji-free per §0, `:3000` is fully replaced by `:4000`, the OpenAI-only label is gone, the stray BankingRS block is enclosed in a subgraph, and the Phase 268 K8s + Phase 269 vault content is present.
3. `i4ai-ref-arch (1).mmd` no longer exists at repo root and was confirmed unreferenced by any script/config before deletion.
4. `.planning/REQUIREMENTS.md` ends with a new "Architecture Diagram Completeness (Phase 270)" section containing REQ-DIAGRAM-01 through REQ-DIAGRAM-15.
5. No file outside this plan's `files_modified` list was changed.
</verification>

<success_criteria>
- [ ] All three tasks pass their automated `<verify>` checks
- [ ] No emojis outside §0 allowlist in either `architecture-simple.mmd` or `architecture.mmd`
- [ ] No secret-value substrings in either `.mmd` file (Plan 02 sync test will independently verify)
- [ ] Phase 268 K8s subgraph uses `stroke-dasharray` (visually marked as planned)
- [ ] Phase 269 vault arrow points at BFF with `startup-load` mechanism label
- [ ] `.planning/REQUIREMENTS.md` has exactly 15 new REQ-DIAGRAM-* entries (REQ-DIAGRAM-01..15)
- [ ] `i4ai-ref-arch (1).mmd` deleted; no `.sh`/`.js`/`.ts`/`.json` file references it
- [ ] PNGs NOT regenerated in this plan (Plan 03 owns that and the UI build gate)
- [ ] No edit to `InteractiveArchDiagram.js`, `ArchitectureTabsPanel.jsx`, or any other React component (Plan 04 owns the one-line top comment for InteractiveArchDiagram.js)
</success_criteria>

<output>
After completion, create `.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-01-SUMMARY.md` documenting:
- Exact lines added/modified in each `.mmd` file
- The grep-confirmed list of files that referenced `i4ai-ref-arch (1).mmd` before deletion (must be only `.planning/**` docs)
- REQ-DIAGRAM-01..15 entries appended to REQUIREMENTS.md (with line ranges)
- Confirmation that no PNG was regenerated in this plan (Plan 03 owns regen)
</output>
</content>
</invoke>