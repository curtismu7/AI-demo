---
phase: 270
plan: 03
type: execute
wave: 2
depends_on:
  - 270-01
files_modified:
  - scripts/build-diagrams.sh
  - banking_api_ui/public/architecture/overview.png
  - banking_api_ui/public/architecture/overview2.png
  - banking_api_ui/public/architecture/token-flow.png
  - banking_api_ui/public/architecture/token-flow2.png
autonomous: true
requirements:
  - REQ-DIAGRAM-13

must_haves:
  truths:
    - "scripts/build-diagrams.sh pins mermaid-cli to @11 (or current major) and renders all four PNGs end-to-end without errors"
    - "All four output PNGs exist with non-zero file size"
    - "Each output PNG's mtime is newer than its source .mmd's mtime (no stale renders)"
    - "banking_api_ui CRA build passes (npm run build exit 0) after the new PNGs land — the React app imports them via /architecture/*.png paths"
    - "The DiagramRegeneratePanel admin UI no longer shows 'stale' badges for any of the four diagrams"
  artifacts:
    - path: "scripts/build-diagrams.sh"
      provides: "Mermaid CLI regen pipeline; @10 → @11 bump"
      contains: "@mermaid-js/mermaid-cli@"
    - path: "banking_api_ui/public/architecture/overview.png"
      provides: "Rendered architecture-simple.mmd"
    - path: "banking_api_ui/public/architecture/overview2.png"
      provides: "Rendered architecture.mmd"
    - path: "banking_api_ui/public/architecture/token-flow.png"
      provides: "Rendered i4ai-ref-arch.mmd"
    - path: "banking_api_ui/public/architecture/token-flow2.png"
      provides: "Rendered mcp-security-gateway.mmd"
  key_links:
    - from: "scripts/build-diagrams.sh"
      to: "architecture-simple.mmd, architecture.mmd, i4ai-ref-arch.mmd, mcp-security-gateway.mmd"
      via: "mermaid-cli npx invocation per ENTRIES row"
      pattern: "ENTRIES=\\("
    - from: "banking_api_ui CRA build"
      to: "banking_api_ui/public/architecture/*.png"
      via: "static asset path served from /architecture/*"
      pattern: "public/architecture"
---

<objective>
Regenerate all four architecture PNGs from the updated `.mmd` sources Plan 01 produced, and bump the mermaid-cli pin in `scripts/build-diagrams.sh` from `@10` to `@11` (current major). Confirm the banking_api_ui CRA build still passes (UI build gate per CLAUDE.md). This is the visual-output half of the diagram audit — without this, viewers of `/architecture/system` continue to see stale PNGs even though the `.mmd` sources are correct.

Purpose: The DiagramRegeneratePanel admin UI tracks `srcStat.mtime > pngStat.mtime` and surfaces "stale" badges; after Plan 01's `.mmd` edits, all four diagrams show stale. This plan clears those badges and lands fresh PNG assets that ship with the React app. Bumping the mermaid-cli pin aligns the pipeline with the current major (no breaking changes per research; one-character edit at line 49 of build-diagrams.sh).

Output:
- `scripts/build-diagrams.sh` line 49: `@mermaid-js/mermaid-cli@10` → `@mermaid-js/mermaid-cli@11`
- Four fresh PNGs in `banking_api_ui/public/architecture/`
- Confirmed `npm run build` exit 0 from `banking_api_ui/`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md
@.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-VALIDATION.md
@CLAUDE.md
@scripts/build-diagrams.sh
@architecture-simple.mmd
@architecture.mmd
</context>

<interfaces>
<!-- scripts/build-diagrams.sh current ENTRIES table (line 30-35): -->

```bash
ENTRIES=(
  "overview          architecture-simple.mmd  ${OUT_DIR}/overview.png      2400"
  "overview-full     architecture.mmd         ${OUT_DIR}/overview2.png     2800"
  "token-flow        i4ai-ref-arch.mmd        ${OUT_DIR}/token-flow.png    2800"
  "mcp-gateway       mcp-security-gateway.mmd ${OUT_DIR}/token-flow2.png   2400"
)
```

<!-- The one-character bump (line 49): -->

```bash
# Current:
if ! npx -y @mermaid-js/mermaid-cli@10 \
# After:
if ! npx -y @mermaid-js/mermaid-cli@11 \
```

<!-- OUT_DIR (line 20): banking_api_ui/public/architecture/ — these PNGs ship as static assets via CRA. -->

<!-- npm script (banking_api_server/package.json or root package.json): -->
<!-- The shell script can be run via `bash scripts/build-diagrams.sh` directly. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bump mermaid-cli pin @10 → @11 and verify the script syntax</name>
  <files>scripts/build-diagrams.sh</files>
  <read_first>
    - scripts/build-diagrams.sh (full file — 83 lines; the bump is a one-character edit on line 49)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (State of the Art section — mermaid-cli @11 is backwards-compatible per assumption A1)
  </read_first>
  <action>
Edit `scripts/build-diagrams.sh` line 49 ONLY. Change:

```bash
  if ! npx -y @mermaid-js/mermaid-cli@10 \
```

to:

```bash
  if ! npx -y @mermaid-js/mermaid-cli@11 \
```

That is the ONLY edit to this file in this task. Do NOT:
- Change the `ENTRIES` array (entries already correct per research)
- Change OUT_DIR or BASEDIR resolution
- Add new diagram names (Plan 04 may add a §1 row, but the diagram name list stays at four — research confirmed extending in place, not adding a fifth source)
- Remove or modify the existing fallback message pointing at mermaid.live

After the edit, verify the script still parses:
```bash
bash -n scripts/build-diagrams.sh
echo "exit: $?"
```
Must print `exit: 0`.

Do NOT run the script yet — Task 2 runs it as the regen step.
  </action>
  <verify>
    <automated>grep -c "@mermaid-js/mermaid-cli@11" scripts/build-diagrams.sh | grep -E "^1$" && bash -n scripts/build-diagrams.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@mermaid-js/mermaid-cli@10" scripts/build-diagrams.sh` returns no matches
    - `grep "@mermaid-js/mermaid-cli@11" scripts/build-diagrams.sh` returns exactly 1 match (on line 49)
    - `bash -n scripts/build-diagrams.sh` exits 0 (script parses cleanly)
    - No other line of the script was modified (diff shows exactly one-character change `0` → `1`)
  </acceptance_criteria>
  <done>The mermaid-cli pin matches the current major; the script remains shellcheck-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate all four PNGs from the updated .mmd sources</name>
  <files>
    banking_api_ui/public/architecture/overview.png
    banking_api_ui/public/architecture/overview2.png
    banking_api_ui/public/architecture/token-flow.png
    banking_api_ui/public/architecture/token-flow2.png
  </files>
  <read_first>
    - scripts/build-diagrams.sh (now with @11 pin from Task 1)
    - .planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-RESEARCH.md (Pitfall 2 — mermaid syntax breaks render as parse errors INSIDE the PNG; Pitfall 3 — first run downloads Chromium ~150MB, takes 1-2 min)
  </read_first>
  <action>
Run the regen pipeline from the repo root:

```bash
cd /Users/curtismuir/Development/banking
bash scripts/build-diagrams.sh
```

Expected behavior:
- On a machine without Puppeteer's Chromium cached, the first invocation downloads ~150 MB (1-2 minutes). Allow up to 10 minutes via the Bash `timeout` parameter.
- On subsequent runs, regen completes in 5-10 seconds per diagram (~40s total).
- The script prints `[render] {name}: ...` for each entry and `[ok]` on success.
- On exit, the script prints `Done. Refresh /architecture/overview to see the updated image.`

After the script finishes, verify each output PNG is non-zero and visually plausible:

```bash
ls -la banking_api_ui/public/architecture/{overview,overview2,token-flow,token-flow2}.png
file banking_api_ui/public/architecture/{overview,overview2,token-flow,token-flow2}.png
```

Each `file` output line MUST say "PNG image data" with a width ≥ 1000px (the script renders at 2400-2800px).

**If the script fails on any diagram with `[fail]`:**

The most likely cause is mermaid syntax in a label — Pitfall 2 in 270-RESEARCH.md. Open the failing `.mmd` source, look for:
- Unescaped `<` (use `&lt;` instead)
- Single quotes inside labels (use double quotes only)
- Unmatched `<br/>`
- `[(...)]` cylinder syntax with mismatched parens
- classDef applied to a node that doesn't exist (typo)

Fix the .mmd file in place (this WILL touch a file owned by Plan 01 — note in the summary that a Plan-01-owned file was corrected during PNG regen, and that the syntax fix is purely cosmetic to make mermaid parse it). Re-run `bash scripts/build-diagrams.sh` until it exits 0.

**If the first-run Chromium download hangs:**

Wait up to 10 minutes. If it still hangs, the manual fallback is `https://mermaid.live` per the script's error message — but for this phase, we want the pipeline to work, so debug rather than fall back. Common causes: corporate proxy blocking puppeteer's chromium download; resolve via `PUPPETEER_SKIP_DOWNLOAD=false npx ...` or by pre-installing Chromium.

After all four PNGs are non-zero and `file` confirms valid PNG:

```bash
# Verify each PNG mtime is newer than its source mtime
for pair in "architecture-simple.mmd:overview.png" "architecture.mmd:overview2.png" "i4ai-ref-arch.mmd:token-flow.png" "mcp-security-gateway.mmd:token-flow2.png"; do
  src="${pair%:*}"
  png="banking_api_ui/public/architecture/${pair#*:}"
  if [[ "$src" -nt "$png" ]]; then
    echo "STALE: $src is newer than $png — regen failed for this pair"
    exit 1
  fi
  echo "OK: $png is newer than $src"
done
```

All four pairs MUST print `OK:`.
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/banking && bash scripts/build-diagrams.sh && for pair in "architecture-simple.mmd:overview.png" "architecture.mmd:overview2.png" "i4ai-ref-arch.mmd:token-flow.png" "mcp-security-gateway.mmd:token-flow2.png"; do src="${pair%:*}"; png="banking_api_ui/public/architecture/${pair#*:}"; [[ -s "$png" ]] || { echo "MISSING: $png"; exit 1; }; [[ "$src" -nt "$png" ]] && { echo "STALE: $src newer than $png"; exit 1; }; done; echo "all PNGs regenerated"</automated>
  </verify>
  <acceptance_criteria>
    - `banking_api_ui/public/architecture/overview.png` exists with size > 50 KB and `file` reports PNG image
    - `banking_api_ui/public/architecture/overview2.png` exists with size > 50 KB and `file` reports PNG image
    - `banking_api_ui/public/architecture/token-flow.png` exists with size > 50 KB and `file` reports PNG image
    - `banking_api_ui/public/architecture/token-flow2.png` exists with size > 50 KB and `file` reports PNG image
    - Each PNG's mtime is newer than the source `.mmd`'s mtime (no stale renders)
    - Visual smoke check: open `overview.png` and confirm it shows banking_mcp_invest, banking_mortgage_service, langchain_agent, and secrets.vault as labeled nodes (manual verification recommended but not gating)
  </acceptance_criteria>
  <done>All four PNGs are freshly regenerated, non-zero, and newer than their .mmd sources. The DiagramRegeneratePanel admin UI will no longer show any "stale" badges after this lands.</done>
</task>

<task type="auto">
  <name>Task 3: Verify banking_api_ui CRA build still passes (UI build gate per CLAUDE.md)</name>
  <files>banking_api_ui (build-only; no source changes)</files>
  <read_first>
    - CLAUDE.md (UI build gate non-negotiable #3 — `cd banking_api_ui && npm run build` exit 0)
  </read_first>
  <action>
The UI build gate from CLAUDE.md applies any time anything in `banking_api_ui/` changes — including its `public/architecture/*.png` assets. Run:

```bash
cd /Users/curtismuir/Development/banking/banking_api_ui
npm run build
```

Expected: exit code 0, no new warnings, no new errors. The build copies `public/` into `build/` so the new PNGs ship with any deployment.

**If the build fails:**

The most likely cause is NOT the PNG change itself (PNGs are static assets and don't enter the CRA module graph). More likely causes:
1. An unrelated regression in `banking_api_ui/src/` that pre-existed this phase — surface this clearly in the summary and DO NOT attempt to fix scope-creep (CLAUDE.md non-negotiable #2: minimal diff)
2. ESLint catching something in `ArchitectureDiagram.completeness.test.js` from Plan 02 — verify by running:
   ```bash
   cd banking_api_ui && npx eslint src/components/__tests__/ArchitectureDiagram.completeness.test.js
   ```
   If lint errors exist there, surface them but do NOT modify that test (Plan 02 owns it; redirect feedback there)
3. A genuine new failure introduced by this phase's PNG changes — extremely unlikely (PNGs are not parsed at build time); if it happens, investigate and fix in this plan

Record the build output size and exit code in the summary.
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/banking/banking_api_ui && npm run build 2>&1 | tail -20 && echo "build exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` from `banking_api_ui/` exits with code 0
    - No new ESLint errors or warnings introduced by this phase
    - Build output mentions the four PNG files in `build/architecture/` (they were copied from `public/`)
    - Build artifact size for `main.*.js` is comparable to prior builds (no unexpected bloat — PNGs ship as static assets, not in the JS bundle)
  </acceptance_criteria>
  <done>UI build gate per CLAUDE.md is satisfied. The fresh PNGs ship via the static-asset path and `/architecture/system` will display them correctly on next render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→mermaid CLI | The CLI shells out to Puppeteer + Chromium; first-run downloads ~150 MB from `storage.googleapis.com`. Trust the npm registry and Google CDN. |
| build pipeline→public/ | Generated PNGs ship with the CRA build to all clients viewing `/architecture/system`. Source authenticity (Plan 01's edits) is the input integrity boundary. |
| script→shell | `bash scripts/build-diagrams.sh` runs `set -euo pipefail`; one failed render fails the whole script (correct behavior). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-270-02 | Denial-of-Service (build break) | mermaid syntax in .mmd → PNG render failure | mitigate | `bash -n scripts/build-diagrams.sh` validates shell syntax before run; the script's `set -euo pipefail` fails-loud on any render error so we never ship a partial regen. Plan 01 already used canonical mermaid label syntax (no apostrophes, no unescaped `<`). If a syntax break IS found, Task 2's action documents the fix path. |
| T-270-02b | Denial-of-Service (UI build break) | banking_api_ui CRA build | mitigate | Task 3 runs `npm run build` and asserts exit 0. PNGs are static assets — they cannot break the JS bundle build, but the test runs anyway because CLAUDE.md non-negotiable #3 mandates the UI build gate after any banking_api_ui change. |
| T-270-06 | Supply-chain (mermaid-cli upgrade) | @mermaid-js/mermaid-cli @10 → @11 | accept | The major bump is one-character per research assumption A1; mermaid 11.x is GA since Oct 2024 and backwards-compatible with our `.mmd` syntax (flowchart LR/TB, sequenceDiagram, classDef, subgraph). If A1 is wrong, Task 2's render step fails-loud and surfaces the regression for a revert decision. Risk: low. |

Severity: T-270-02 MEDIUM (caught by the script's pipefail behavior); T-270-02b LOW (PNGs can't break CRA bundle build); T-270-06 LOW (accepted risk; revertible). All in-plan.
</threat_model>

<verification>
1. `grep "@mermaid-js/mermaid-cli@11" scripts/build-diagrams.sh | wc -l` returns 1.
2. `grep "@mermaid-js/mermaid-cli@10" scripts/build-diagrams.sh | wc -l` returns 0.
3. All four PNGs in `banking_api_ui/public/architecture/` are non-zero and newer than their corresponding `.mmd` sources.
4. `cd banking_api_ui && npm run build` exits 0.
5. Visual spot-check (manual): open `banking_api_ui/public/architecture/overview.png`; confirm `banking_mcp_invest`, `banking_mortgage_service`, `langchain_agent`, and `secrets.vault` are visible as nodes.
</verification>

<success_criteria>
- [ ] mermaid-cli pin bumped to @11; script still parses
- [ ] All four PNGs regenerated, non-zero, newer than .mmd sources
- [ ] No "stale" badges in DiagramRegeneratePanel after this lands
- [ ] `cd banking_api_ui && npm run build` exit 0 (UI build gate)
- [ ] No file modified outside `scripts/build-diagrams.sh` and `banking_api_ui/public/architecture/*.png` (except if a mermaid syntax fix in a Plan-01-owned `.mmd` was unavoidable — documented in summary)
- [ ] Visual smoke-check confirms the four key Phase 270 additions (MCP Invest, Mortgage, LangChain, Vault) are rendered legibly in overview.png
</success_criteria>

<output>
After completion, create `.planning/phases/270-architecture-diagram-completeness-audit-architecture-system-/270-03-SUMMARY.md` documenting:
- Exact diff applied to scripts/build-diagrams.sh (one-character bump)
- mermaid-cli version actually downloaded by npx (verify with `npx -y @mermaid-js/mermaid-cli@11 --version`)
- File sizes of all four regenerated PNGs (`ls -la banking_api_ui/public/architecture/*.png`)
- `npm run build` exit code and final bundle size for `main.*.js`
- Any mermaid syntax fixes applied to Plan-01-owned .mmd files during regen (should be none if Plan 01 used the canonical syntax pattern; if any, list them)
</output>
</content>
</invoke>