---
phase: 145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls
plan: 02
type: execute
wave: 2
depends_on:
  - 145-01
files_modified:
  - langchain_agent/src/agent/mcp_tool_provider.py
  - langchain_agent/src/mcp/tool_registry.py
  - langchain_agent/tests/test_mcp_dynamic_tool_schemas.py
autonomous: true
requirements:
  - ACTLOG-05
  - ACTLOG-06
  - ACTLOG-07
must_haves:
  truths:
    - "Agent-side input schemas are primarily derived from MCP metadata rather than hardcoded banking-tool maps"
    - "Hardcoded schema fallback no longer overrides valid server-provided tool schemas"
    - "Tests verify metadata-driven schema behavior end-to-end within the Python agent layer"
  artifacts:
    - path: "langchain_agent/src/agent/mcp_tool_provider.py"
      provides: "Metadata-first schema construction path for MCP tools"
      contains: "_get_input_schema_for_tool"
    - path: "langchain_agent/tests/test_mcp_dynamic_tool_schemas.py"
      provides: "Regression tests for dynamic schema behavior"
      contains: "metadata"
    - path: "langchain_agent/src/mcp/tool_registry.py"
      provides: "Tool schema propagation and registry-side metadata handling"
      contains: "register_server_tools"
  key_links:
    - from: "langchain_agent/src/mcp/tool_registry.py"
      to: "langchain_agent/src/agent/mcp_tool_provider.py"
      via: "tool schema metadata ingestion and consumption"
      pattern: "tool_schemas"
    - from: "langchain_agent/src/agent/mcp_tool_provider.py"
      to: "langchain_agent/tests/test_mcp_dynamic_tool_schemas.py"
      via: "metadata-first schema assertions"
      pattern: "_get_input_schema_for_tool"
---

<objective>
Harden agent-side MCP tool schema handling so dynamic decisions come from server-advertised schemas, not hardcoded banking assumptions.

Purpose: reduce schema drift and ensure tool execution paths track runtime metadata contracts.

Output: metadata-first schema logic and targeted regression tests in `langchain_agent`.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls/145-RESEARCH.md
@.planning/phases/145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls/145-01-SUMMARY.md
@langchain_agent/src/agent/mcp_tool_provider.py
@langchain_agent/src/mcp/tool_registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch to metadata-first schema resolution in Python MCP tool provider</name>
  <files>langchain_agent/src/agent/mcp_tool_provider.py</files>
  <behavior>
    - Test 1: when server provides `inputSchema.properties`, provider uses that schema path.
    - Test 2: provider does not route known banking tools to hardcoded schemas when valid metadata exists.
    - Test 3: missing metadata falls back to generic schema, not tool-name-specific hidden behavior.
  </behavior>
  <action>
Refactor `_get_input_schema_for_tool()` to prioritize server-provided metadata and eliminate/neutralize hardcoded banking-tool schema branches that cause drift.
Retain a safe generic fallback path for truly absent metadata.
Add logging at debug level to make schema source (metadata vs fallback) observable during troubleshooting.
  </action>
  <verify>
    <automated>cd langchain_agent && bash scripts/run-pytest.sh tests/test_mcp_dynamic_tool_schemas.py</automated>
  </verify>
  <done>
    Agent schema resolution is metadata-first, and hardcoded banking schema dependence is removed from the normal path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dynamic-schema regression tests and registry plumbing checks</name>
  <files>langchain_agent/tests/test_mcp_dynamic_tool_schemas.py, langchain_agent/src/mcp/tool_registry.py</files>
  <behavior>
    - Test 1: registry captures and stores tool schema metadata from server tool definitions.
    - Test 2: provider-generated input model reflects server schema fields for at least one read tool and one write tool.
    - Test 3: unknown tool with no schema uses generic fallback model.
  </behavior>
  <action>
Create `test_mcp_dynamic_tool_schemas.py` to validate metadata ingestion and provider behavior.
Adjust `tool_registry.py` only as needed to ensure schema metadata is consistently available to provider logic under test.
Keep changes narrow and avoid unrelated agent behavior changes.
  </action>
  <verify>
    <automated>cd langchain_agent && bash scripts/run-pytest.sh tests/test_mcp_dynamic_tool_schemas.py</automated>
  </verify>
  <done>
    Dynamic-schema tests pass and guard against future hardcoded tool-schema regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server metadata -> agent tool model | Incorrect or stale schema mapping can cause malformed tool calls |
| Agent schema fallback path | Hardcoded fallback can bypass server intent and drift from actual contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-04 | T (Tampering) | schema resolution path | mitigate | Enforce metadata-first logic with explicit regression tests |
| T-145-05 | R (Repudiation) | schema source traceability | mitigate | Add debug-level source logging (metadata vs fallback) for auditability |
| T-145-06 | D (Denial of Service) | malformed tool calls | mitigate | Generic fallback for absent schema and strict tests for required-field mapping |
</threat_model>

<verification>
- `cd langchain_agent && bash scripts/run-pytest.sh tests/test_mcp_dynamic_tool_schemas.py`
- Optionally run focused MCP/agent tests covering tool provider and registry modules.
</verification>

<success_criteria>
- Agent tool schema generation is driven by MCP metadata in normal operation.
- Hardcoded banking-tool schema path no longer determines behavior when metadata exists.
- Regression tests enforce metadata-first behavior and fallback safety.
</success_criteria>

<output>
After completion, create `.planning/phases/145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls/145-02-SUMMARY.md`
</output>