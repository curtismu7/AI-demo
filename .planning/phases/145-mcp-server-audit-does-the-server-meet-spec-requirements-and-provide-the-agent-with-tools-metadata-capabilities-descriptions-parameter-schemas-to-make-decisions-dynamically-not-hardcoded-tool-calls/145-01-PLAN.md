---
phase: 145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_mcp_server/src/server/MCPMessageHandler.ts
  - banking_mcp_server/tests/server/MCPMessageHandler.test.ts
  - banking_mcp_server/tests/tools/BankingToolRegistry.test.ts
  - docs/mcp/MCP_TOOL_METADATA_AUDIT.md
autonomous: true
requirements:
  - ACTLOG-01
  - ACTLOG-02
  - ACTLOG-03
  - ACTLOG-04
must_haves:
  truths:
    - "MCP tools/list response exposes a complete and consistent metadata contract for clients"
    - "Registry and server tests reflect current tool inventory and metadata expectations"
    - "Audit artifacts clearly document metadata fields, gaps, and acceptance checks"
  artifacts:
    - path: "docs/mcp/MCP_TOOL_METADATA_AUDIT.md"
      provides: "Audit report of MCP tool metadata contract and compliance checks"
      contains: "tools/list contract"
    - path: "banking_mcp_server/src/server/MCPMessageHandler.ts"
      provides: "tools/list response mapping aligned with registry metadata"
      contains: "handleListTools"
    - path: "banking_mcp_server/tests/server/MCPMessageHandler.test.ts"
      provides: "Regression tests for tools/list payload contract"
      contains: "tools/list"
    - path: "banking_mcp_server/tests/tools/BankingToolRegistry.test.ts"
      provides: "Registry tests aligned to current tool set"
      contains: "getAllTools"
  key_links:
    - from: "banking_mcp_server/src/tools/BankingToolRegistry.ts"
      to: "banking_mcp_server/src/server/MCPMessageHandler.ts"
      via: "tools/list metadata mapping"
      pattern: "handleListTools"
    - from: "banking_mcp_server/src/server/MCPMessageHandler.ts"
      to: "banking_mcp_server/tests/server/MCPMessageHandler.test.ts"
      via: "response contract assertions"
      pattern: "tools/list"
---

<objective>
Audit and harden MCP server tool-metadata exposure so clients can make decisions from complete, stable metadata.

Purpose: remove ambiguity between registry-defined tool capabilities and the metadata actually returned by `tools/list`.

Output: updated tools/list mapping, refreshed tests, and an explicit audit artifact.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls/145-RESEARCH.md
@banking_mcp_server/src/tools/BankingToolRegistry.ts
@banking_mcp_server/src/server/MCPMessageHandler.ts
@banking_mcp_server/tests/server/MCPMessageHandler.test.ts
@banking_mcp_server/tests/tools/BankingToolRegistry.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and enforce MCP tools/list metadata contract</name>
  <files>banking_mcp_server/src/server/MCPMessageHandler.ts, banking_mcp_server/tests/server/MCPMessageHandler.test.ts</files>
  <behavior>
    - Test 1: tools/list returns all expected registry-derived fields for each tool (including capability metadata used by clients).
    - Test 2: tools/list does not drop required schema/scopes/auth indicators.
    - Test 3: contract remains stable when tool count changes.
  </behavior>
  <action>
Update `handleListTools()` to propagate the registry metadata needed for dynamic tool selection and educational visibility.
Then update/add tests in `MCPMessageHandler.test.ts` to assert the contract fields explicitly and fail on partial mapping regressions.
Keep compatibility for existing clients: do not remove currently returned keys.
  </action>
  <verify>
    <automated>cd banking_mcp_server && npm run test -- tests/server/MCPMessageHandler.test.ts --runInBand</automated>
  </verify>
  <done>
    tools/list contract is explicitly tested and server output is aligned with registry-defined metadata.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh registry baseline tests and produce audit artifact</name>
  <files>banking_mcp_server/tests/tools/BankingToolRegistry.test.ts, docs/mcp/MCP_TOOL_METADATA_AUDIT.md</files>
  <behavior>
    - Test 1: registry test assertions match current tool inventory and key metadata expectations.
    - Test 2: tests fail if required metadata fields are removed from registry entries.
  </behavior>
  <action>
Update `BankingToolRegistry.test.ts` to reflect current tool inventory and metadata shape.
Create `docs/mcp/MCP_TOOL_METADATA_AUDIT.md` documenting: current contract, observed gaps fixed in this plan, and verification checklist for future regressions.
Keep the document concise and implementation-focused.
  </action>
  <verify>
    <automated>cd banking_mcp_server && npm run test -- tests/tools/BankingToolRegistry.test.ts --runInBand</automated>
  </verify>
  <done>
    Registry tests are current and the audit document captures the server metadata contract and checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server -> MCP client | Incomplete metadata can cause unsafe or incorrect client-side tool decisions |
| Registry -> tools/list route | Contract drift can silently break downstream dynamic tool selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-145-01 | T (Tampering) | tools/list payload | mitigate | Lock contract with tests that assert required metadata fields |
| T-145-02 | I (Information Disclosure) | tool metadata | accept | Metadata is intentionally non-secret and already part of tool discovery surface |
| T-145-03 | D (Denial of Service) | client decision loop | mitigate | Maintain backward-compatible keys while extending metadata fidelity |
</threat_model>

<verification>
- `cd banking_mcp_server && npm run test -- tests/server/MCPMessageHandler.test.ts --runInBand`
- `cd banking_mcp_server && npm run test -- tests/tools/BankingToolRegistry.test.ts --runInBand`
- Confirm `docs/mcp/MCP_TOOL_METADATA_AUDIT.md` exists and references the final tools/list contract.
</verification>

<success_criteria>
- MCP tools/list metadata contract is explicit and regression-tested.
- Registry test baselines match the current tool inventory.
- Audit report exists with concrete verification checks for future changes.
</success_criteria>

<output>
After completion, create `.planning/phases/145-mcp-server-audit-does-the-server-meet-spec-requirements-and-provide-the-agent-with-tools-metadata-capabilities-descriptions-parameter-schemas-to-make-decisions-dynamically-not-hardcoded-tool-calls/145-01-SUMMARY.md`
</output>