---
phase: 999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe
plan: 03
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - banking_api_server/services/http2McpBridge.js
  - banking_api_server/server.js
  - run-bank.sh
  - REGRESSION_PLAN.md
  - docs/adr/0003-mcp-server-http2-transport.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "When MCP_SERVER_HTTP2_URL is set, the BFF h2 bridge connects to it and records the negotiated transport (h2 / h2c / http/1.1) on the pool entry"
    - "When MCP_SERVER_HTTP2_URL is unset, useHttp2 is false and every demo flow stays on WebSocket (D-04) — zero behavior change"
    - "Every BFF→MCP tool call pushes a bff-mcp-transport tokenEvent for BOTH the h2 path and the ws path"
    - "h1-fallback is detected from runtime observation (alpnProtocol / :status), not from 'env var is set'"
    - "run-bank.sh port sweeps + REGRESSION_PLAN §3 include 8083; §4 documents the new optional env vars"
    - "The stale 'negotiates DOWN to HTTP/1.1' docstring in http2McpBridge.js is corrected"
  artifacts:
    - path: "banking_api_server/services/http2McpBridge.js"
      provides: "negotiatedTransport detection + extended pool entry + mcp/transport events + corrected docstring"
      contains: "negotiatedTransport"
    - path: "banking_api_server/server.js"
      provides: "MCP_SERVER_HTTP2_URL-preferred mcpUrl resolution + bff-mcp-transport tokenEvent on both paths"
      contains: "MCP_SERVER_HTTP2_URL"
    - path: "run-bank.sh"
      provides: "8083 in both port-sweep arrays + sweep comment + optional MCP_SERVER_HTTP2_URL discovery line"
      contains: "8083"
    - path: "REGRESSION_PLAN.md"
      provides: "section 3 port row for 8083 + section 4 bug-fix-log entry for the new optional env vars"
      contains: "8083"
    - path: "docs/adr/0003-mcp-server-http2-transport.md"
      provides: "ADR documenting D-01..D-05"
      contains: "h2c"
  key_links:
    - from: "banking_api_server/server.js useHttp2 branch"
      to: "http2McpBridge._pool entry negotiatedTransport"
      via: "buildTokenEvent('bff-mcp-transport', ...)"
      pattern: "bff-mcp-transport"
    - from: "http2McpBridge.createHttp2Session"
      to: "appEventService 'mcp/transport'"
      via: "session.once('connect') alpn snapshot"
      pattern: "mcp/transport"
---

<objective>
Make the BFF actually exercise and observe the HTTP/2 path: (1) extend `http2McpBridge.js` to detect the negotiated transport and record it + emit `mcp/transport` app-events; (2) extend `server.js` mcpUrl resolution to prefer the new opt-in `MCP_SERVER_HTTP2_URL` (else fall back to existing WS `MCP_SERVER_URL` — D-04), and push a `bff-mcp-transport` tokenEvent on BOTH the h2 and ws paths so the Token Chain pill always has data; (3) wire ops: `run-bank.sh` port sweeps + REGRESSION_PLAN section 3/4 + correct the stale docstring + add ADR 0003.

Purpose: This is the transport-flip wave. It is independently shippable and observable via logs (`[http2McpBridge] alpnProtocol=...`) BEFORE any UI lands — exactly the sequencing RESEARCH Summary mandates.
Output: bridge detection + server.js resolution + ops/docs, all opt-in, WS-default behavior provably unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-RESEARCH.md
@banking_api_server/services/http2McpBridge.js

<interfaces>
banking_api_server/services/http2McpBridge.js (existing):
- poolKey(mcpServerUrl, bearerToken) -> `${url}::${first-16-chars-of-token}`
- createHttp2Session(mcpServerUrl, bearerToken) -> does http2.connect(connectUrl, options) (options.allowHTTP1=true for http:), registers session.on('error'|'close') deleting the pool key, then pool.set(key, { session, lastUsed, pendingStreams: 0 }) and returns session.
- module.exports already includes _pool: pool (line ~356). buildTokenEvent is NOT here - it's in server.js.

banking_api_server/server.js (existing, exact lines):
- L1138 const http2McpBridge = require("./services/http2McpBridge");
- L1727 const useGateway = !!process.env.MCP_GATEWAY_HTTP_URL;
- L1728 const mcpUrl = getMcpServerUrl();
- L1730 const useHttp2 = !useGateway && (mcpUrl.startsWith("http://") || mcpUrl.startsWith("https://"));
- L1741-1745: } else if (useHttp2) { const h2Session = http2McpBridge.createHttp2Session(mcpUrl, mcpAccessToken); result = await http2McpBridge.forwardToolCall(...); } else { result = await mcpCallTool(...); }
- buildTokenEvent(id, label, status, ttl, message, claimsObj) is in scope here (used at L1636, L1664). tokenEvents array is in scope in this handler.
- appEventService is in scope (L1736 uses appEventService.logEvent('mcp', ...)).
- getMcpServerUrl() resolves the WS default.

RESEARCH Examples 2 (bridge detection), 3 (server.js tokenEvent on both paths), and Pattern 3 (mcpUrl resolution) are the verbatim target shapes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge negotiated-transport detection + extended pool entry + mcp/transport events + docstring fix</name>
  <read_first>
    - banking_api_server/services/http2McpBridge.js (FULL — current createHttp2Session body lines ~60-78, poolKey ~28-32, the lead docstring lines ~1-11, module.exports ~349-356)
    - 999.1-RESEARCH.md Example 2 (VERBATIM target: negotiatedTransport state machine, session.once('connect') alpn snapshot, extended pool.set with createdAt/connectUrl/negotiatedTransport getter)
    - 999.1-RESEARCH.md Pattern 2 + the "Important caveat — h2c and alpnProtocol" note (h2c has empty alpnProtocol; infer from http: + not destroyed)
    - 999.1-RESEARCH.md Pitfall 4 (label MUST come from runtime observation, not "env var is set") and Pitfall 6 (docstring is stale, drop the "negotiates DOWN" disclosure — replacement language in 999.1-CONTEXT.md specifics section)
    - banking_api_server/tests/services/http2McpBridge.regression.test.js + .integration.test.js (the RED contracts from plan 01 — they define the exact pool-entry shape required)
    - banking_api_server/services/appEventService.js (EVENT_CATEGORIES — confirm 'mcp' category + free-form tag; reuse tag 'mcp/transport')
    - .claude/skills/banking-api-server/SKILL.md (CommonJS module, service patterns, regression vs integration test pattern)
  </read_first>
  <files>banking_api_server/services/http2McpBridge.js</files>
  <action>
Run RED tests first: `cd banking_api_server && npx jest http2McpBridge.regression http2McpBridge.integration` — confirm fail. Implement to GREEN per RESEARCH Example 2 VERBATIM:

1. In createHttp2Session, after `const session = http2.connect(connectUrl, options);`, add the negotiated-transport state machine:
```javascript
let negotiatedTransport = 'pending';
session.once('connect', () => {
  if (session.alpnProtocol === 'h2') {
    negotiatedTransport = 'h2';
  } else if (parsed.protocol === 'http:' && !session.destroyed) {
    negotiatedTransport = 'h2c'; // h2c has no ALPN; success inferred (RESEARCH Pattern 2 caveat)
  } else {
    negotiatedTransport = 'http/1.1';
  }
  appEventService.logEvent('mcp', negotiatedTransport === 'http/1.1' ? 'warning' : 'info',
    `[http2McpBridge] alpnProtocol=${session.alpnProtocol || 'h2c'} negotiated=${negotiatedTransport}`,
    { tag: 'mcp/transport', metadata: { url: mcpServerUrl, transport: negotiatedTransport } });
});
session.once('error', () => { negotiatedTransport = 'error'; });
```
   - If appEventService is not already required in this file, add `const appEventService = require('./appEventService');` at top with the other requires.
   - Per RESEARCH Open Question 3: on http/1.1 detection, LOG the warning (above) but do NOT tear down — h1 still serves correctness, just not multiplexing.

2. Replace the existing `pool.set(key, { session, lastUsed: Date.now(), pendingStreams: 0 });` with the extended entry (RESEARCH Example 2):
```javascript
pool.set(key, {
  session,
  lastUsed: Date.now(),
  pendingStreams: 0,
  createdAt: Date.now(),
  connectUrl,
  get negotiatedTransport() { return negotiatedTransport; },
});
```
   - Keep the existing session.on('error') / session.on('close') pool.delete handlers exactly as they are (do not remove the existing error logging).

3. Fix the stale lead docstring (lines ~1-11, Pitfall 6): remove the "Status (2026-05-14) ... negotiates DOWN to HTTP/1.1 ... theoretical" paragraph and replace with accurate post-phase language: the bridge negotiates real HTTP/2 (h2c) when MCP_SERVER_HTTP2_URL points at an MCP server started with MCP_HTTP2_PORT (port 8083 by default); WebSocket on MCP_SERVER_URL remains the default transport (D-04). Keep it factual and concise — minimal-diff discipline.

4. Confirm module.exports still exposes _pool: pool (it does at ~line 356 — do not remove; plan 04's admin inspector + server.js read it).

Two-tier: regression test mocks http2+appEventService; integration uses a real loopback h2c server. Both must go GREEN.
  </action>
  <verify>
    <automated>cd banking_api_server && npx jest http2McpBridge.regression http2McpBridge.integration --bail</automated>
  </verify>
  <acceptance_criteria>
    - banking_api_server/services/http2McpBridge.js contains negotiatedTransport, session.once('connect', tag: 'mcp/transport', get negotiatedTransport(), createdAt, connectUrl
    - The lead docstring no longer contains the substring "negotiates DOWN" (grep returns 0)
    - module.exports still contains _pool
    - `cd banking_api_server && npx jest http2McpBridge.regression` exits 0 (AC-02 regression GREEN)
    - `cd banking_api_server && npx jest http2McpBridge.integration` exits 0 (AC-02 integration GREEN, real h2c, asserts non-http/1.1)
    - On h1 fallback the code calls appEventService.logEvent('mcp', 'warning', ...) (grep: 'warning' near mcp/transport)
    - Maps to AC-02
  </acceptance_criteria>
  <done>Bridge records negotiated transport from runtime observation, emits mcp/transport events, extended pool entry exposed, docstring corrected, AC-02 green.</done>
</task>

<task type="auto">
  <name>Task 2: server.js — prefer MCP_SERVER_HTTP2_URL + bff-mcp-transport tokenEvent on both paths</name>
  <read_first>
    - banking_api_server/server.js lines 1720-1760 (the useGateway/mcpUrl/useHttp2 block L1727-1745 — the exact code being extended) and L1138 (http2McpBridge require)
    - banking_api_server/server.js where buildTokenEvent is called nearby (e.g. L1636, L1664) to copy the exact arg signature
    - 999.1-RESEARCH.md Pattern 3 (verbatim mcpUrl resolution) and Example 3 (verbatim tokenEvent push on BOTH h2 and ws branches)
    - 999.1-RESEARCH.md AC-03 (default-unset must keep useHttp2 false -> WS) and Pitfall 4
    - banking_api_server/tests/routes/oauthStatus.regression.test.js / hitlRoute.regression.test.js (the critical chain that must stay green — AC-03/AC-08)
    - .claude/skills/regression-guard/SKILL.md (section 1 pre-edit discipline — server.js is section-1-adjacent; state what you will NOT break)
  </read_first>
  <files>banking_api_server/server.js</files>
  <action>
Pre-edit statement (regression-guard): server.js is section-1-adjacent. This change is purely additive to the MCP-tool dispatch block. It will NOT change: the gateway path (useGateway), the WS default when MCP_SERVER_HTTP2_URL is unset, OAuth/session/HITL routes, or any existing tokenEvents shape. MCP_SERVER_URL semantics (WS default) are untouched per D-03.

Apply RESEARCH Pattern 3 + Example 3 VERBATIM:

1. Replace L1728 `const mcpUrl = getMcpServerUrl();` with the opt-in-preferred resolution (Pattern 3):
```javascript
// D-03/D-04: prefer MCP_SERVER_HTTP2_URL when set; else existing MCP_SERVER_URL (WS default, untouched).
const mcpHttp2Url = process.env.MCP_SERVER_HTTP2_URL;
const mcpUrl = mcpHttp2Url || getMcpServerUrl();
```
   - L1730 useHttp2 line is left as-is — it already correctly evaluates to false when mcpUrl is the ws:// default (AC-03). When MCP_SERVER_HTTP2_URL=http://localhost:8083 is set, mcpUrl.startsWith('http') makes useHttp2 true.

2. In the `else if (useHttp2)` branch (currently L1741-1743), AFTER `result = await http2McpBridge.forwardToolCall(...)`, push the transport tokenEvent (Example 3):
```javascript
const poolEntry = [...http2McpBridge._pool.values()].find(e => e.session === h2Session);
const transportLabel = (poolEntry?.negotiatedTransport === 'h2' || poolEntry?.negotiatedTransport === 'h2c')
  ? 'HTTP/2' : 'HTTP/1.1';
tokenEvents.push(buildTokenEvent(
  'bff-mcp-transport', 'BFF → MCP transport', 'info', null,
  `Transport negotiated: ${transportLabel}`,
  { transport: transportLabel, alpn: poolEntry?.negotiatedTransport }
));
```

3. In the final `else` branch (WS path, currently L1745 `result = await mcpCallTool(...)`), AFTER the result line, push the WS transport event so the same event slot is ALWAYS populated (Example 3 — this is what makes the pill's default state work):
```javascript
tokenEvents.push(buildTokenEvent(
  'bff-mcp-transport', 'BFF → MCP transport', 'info', null,
  'Transport: WebSocket', { transport: 'WS' }
));
```
   - Verify tokenEvents and buildTokenEvent are in scope at these lines (they are — used at L1636/L1664). Match buildTokenEvent's exact arg order from a nearby existing call.
   - Do NOT add a tokenEvent to the useGateway branch (out of scope per CONTEXT.md domain — gateway transport unchanged).

No UI files. No bridge files. Minimal diff inside the existing dispatch block only.
  </action>
  <verify>
    <automated>cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration --bail</automated>
  </verify>
  <acceptance_criteria>
    - banking_api_server/server.js contains MCP_SERVER_HTTP2_URL and `mcpHttp2Url || getMcpServerUrl()`
    - server.js contains 'bff-mcp-transport' at least twice (h2 branch + ws branch); `grep -c "'bff-mcp-transport'" banking_api_server/server.js` >= 2
    - The ws-branch event uses { transport: 'WS' }; the h2-branch derives transportLabel from poolEntry?.negotiatedTransport (not from the env var) — grep both present
    - MCP_SERVER_URL resolution via getMcpServerUrl() still present (WS default preserved, D-03)
    - No bff-mcp-transport push inside the useGateway branch
    - `cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration` exits 0 (AC-03 + AC-08 critical chain green, >=48 tests)
    - Maps to AC-03, AC-08
  </acceptance_criteria>
  <done>BFF prefers opt-in h2 URL, falls back to WS by default, emits bff-mcp-transport on both paths from runtime observation; critical chain green.</done>
</task>

<task type="auto">
  <name>Task 3: Ops + docs — run-bank.sh 8083, REGRESSION_PLAN section 3/4, ADR 0003</name>
  <read_first>
    - run-bank.sh lines 320 and 332 (the two port-sweep arrays `for port in 3001 4000 8080 8888 8889 8890 3005 3006 3009 8081 8082; do`), line 412 (the "Sweeping ports..." echo), and the MCP-Invest launch block ~line 753 (where to add the optional MCP_SERVER_HTTP2_URL discovery comment)
    - REGRESSION_PLAN.md section 3 "Port Layout (Local Dev)" table (lines ~91-101) and the section 4 "Bug Fix Log" header (~line 110) + an existing section 4 entry for the entry format/template
    - docs/adr/0001-banking-resource-server-colocated-in-bff.md and docs/adr/0002-mcp-invest-skips-token-introspection.md (ADR format/tone — mirror exactly)
    - 999.1-CONTEXT.md decisions section D-01..D-05 (the ADR content source) and 999.1-RESEARCH.md Pitfall 3 (run-bank.sh sweep requirement, both arrays + comment + the 8083-side-by-side note)
    - .claude/skills/regression-guard/SKILL.md (section 3/4 discipline, no-emoji rule for the section 4 entry)
  </read_first>
  <files>run-bank.sh, REGRESSION_PLAN.md, docs/adr/0003-mcp-server-http2-transport.md</files>
  <action>
1. run-bank.sh (RESEARCH Pitfall 3):
   - In BOTH port-sweep arrays at lines ~320 and ~332, append 8083 to the list: `for port in 3001 4000 8080 8888 8889 8890 3005 3006 3009 8081 8082 8083; do`
   - Update the "Sweeping ports ..." echo at ~line 412 to include `, h2 :8083 (opt-in)` in the human-readable port list.
   - Near the MCP-Invest launch block (~line 753) OR near the MCP server launch, add a commented-out discovery block documenting the opt-in env vars:
     `# Opt-in HTTP/2 (h2c) transport (Phase 999.1, D-04 default OFF):`
     `#   export MCP_HTTP2_PORT=8083            # MCP server: enable h2c listener on :8083 (side-by-side with WS :8080)`
     `#   export MCP_SERVER_HTTP2_URL=http://localhost:8083  # BFF: route BFF->MCP over h2c instead of WS`
   - Do NOT set these by default anywhere (D-04 — WS stays default, zero risk to existing demos).

2. REGRESSION_PLAN.md:
   - Section 3 Port Layout table: add a row `| MCP Server HTTP/2 (h2c, opt-in) | 8083 | auto-swept by run-bank.sh; enabled only when MCP_HTTP2_PORT is set (loopback only) |`.
   - Section 4 Bug Fix Log: add a new reverse-chronological entry dated 2026-05-15 documenting Phase 999.1: new OPTIONAL env vars MCP_HTTP2_PORT (MCP server side, enables h2c listener on 8083) and MCP_SERVER_HTTP2_URL (BFF side, opt-in BFF->MCP h2c route). State explicitly: default behavior unchanged — when both are unset the BFF uses WebSocket on MCP_SERVER_URL exactly as before; MCP_SERVER_URL semantics untouched. Follow the existing section 4 entry template (What was broken / What was fixed / Files / Verification — adapt: this is a feature-add note, frame "What was broken" as "Bridge docstring claimed HTTP/2 multiplexing that never happened; MCP server only spoke HTTP/1.1"). No emoji except the three allowed.

3. docs/adr/0003-mcp-server-http2-transport.md (NEW):
   - Mirror the format/tone of ADR 0001/0002 exactly (Title, Status: Accepted, Context, Decision, Consequences).
   - Content: document D-01 (h2c, no TLS, defer secure variant), D-02 (split ports, WS stays on 8080 unchanged), D-03 (port 8083 + env vars MCP_HTTP2_PORT / MCP_SERVER_HTTP2_URL), D-04 (WS remains default, h2 opt-in), D-05 (empirical Node-24 crash verification + try/catch fallback, no engines.node ceiling). Keep it ADR-concise (~one page), minimal-diff discipline — this is the only doc-only addition.
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/banking && test "$(grep -c 8083 run-bank.sh)" -ge 3 && grep -q 8083 REGRESSION_PLAN.md && grep -q MCP_SERVER_HTTP2_URL REGRESSION_PLAN.md && test -f docs/adr/0003-mcp-server-http2-transport.md && grep -q h2c docs/adr/0003-mcp-server-http2-transport.md && echo OPS-OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 8083 run-bank.sh returns >= 3 (both sweep arrays + sweep echo; the discovery comment adds more)
    - run-bank.sh contains MCP_HTTP2_PORT and MCP_SERVER_HTTP2_URL in the commented-out discovery block; neither is exported/set by default
    - REGRESSION_PLAN.md section 3 table has a row containing 8083 and "opt-in"
    - REGRESSION_PLAN.md section 4 has a 2026-05-15 entry naming both MCP_HTTP2_PORT and MCP_SERVER_HTTP2_URL and stating WS default preserved
    - docs/adr/0003-mcp-server-http2-transport.md exists, contains h2c, and follows the ADR 0001/0002 Status/Context/Decision/Consequences structure
    - No emoji other than the three allowed anywhere in the edited files
    - Maps to AC-07
  </acceptance_criteria>
  <done>8083 swept by run-bank.sh in both arrays; REGRESSION_PLAN section 3/4 updated; ADR 0003 added; WS default preserved and documented; AC-07 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF h2 client -> MCP h2c listener (loopback :8083) | Bearer token crosses cleartext loopback; same surface as existing WS on 8080 |
| env var (MCP_SERVER_HTTP2_URL) -> transport selection | Operator-controlled config selects transport; misconfig must fail safe to WS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-07 | Spoofing / Tampering | Pill/inspector label trusting intent not reality | mitigate | transportLabel derives from poolEntry.negotiatedTransport (runtime alpn/:status observation), never from "env var is set" (RESEARCH Pitfall 4). h1 fallback emits an explicit appEventService warning |
| T-999.1-08 | Information Disclosure | mcp/transport app-event metadata | mitigate | Event metadata carries only { url, transport } — no bearer token, no Authorization header. Pool key elsewhere uses 16-char prefix only (RESEARCH A7) |
| T-999.1-09 | Denial of Service | Misconfigured MCP_SERVER_HTTP2_URL pointing at a dead/h1 host | accept | Bridge already deletes pool entry on session error/close; h1 still serves correctness; demo degrades to a visible HTTP/1.1 pill rather than breaking (RESEARCH Open Question 3) |
| T-999.1-10 | Information Disclosure (local) | Cleartext bearer over loopback h2c | accept | Loopback-only, opt-in, identical to existing WS on 8080; TLS variant consciously deferred (D-01) |
</threat_model>

<verification>
- MCP_SERVER_HTTP2_URL unset -> useHttp2 false -> WS path; critical chain green (AC-03).
- MCP_SERVER_HTTP2_URL=http://localhost:8083 + a real loopback h2c server -> bridge records h2/h2c; integration test green (AC-02).
- bff-mcp-transport tokenEvent present on both h2 and ws branches; gateway branch untouched.
- grep -c 8083 run-bank.sh >= 3; REGRESSION_PLAN section 3/4 updated; ADR 0003 present (AC-07).
- Stale "negotiates DOWN" docstring removed.
- Full critical chain: cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration http2McpBridge.regression http2McpBridge.integration -> all green (AC-08).
</verification>

<success_criteria>
Transport flip is complete and observable via logs before any UI: bridge detects + records + emits negotiated transport; server.js prefers opt-in h2 URL with WS fallback default; both paths emit bff-mcp-transport; ops/docs (run-bank.sh, REGRESSION_PLAN 3/4, ADR 0003) done; AC-02, AC-03, AC-07, AC-08 satisfied; zero behavior change when env vars unset.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-03-SUMMARY.md`
</output>
