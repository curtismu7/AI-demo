---
phase: 999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe
plan: 04
type: execute
wave: 3
depends_on: ["999.1-01", "999.1-03"]
files_modified:
  - banking_api_server/routes/adminH2Inspector.js
  - banking_api_server/server.js
  - banking_api_ui/src/components/TokenChainDisplay.js
  - banking_api_ui/src/components/AdminH2InspectorPage.jsx
  - banking_api_ui/src/components/AdminH2InspectorPage.css
  - banking_api_ui/src/App.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Token Chain UI renders a TransportPill on the bff-mcp-transport step for every tool call (HTTP/2 / WebSocket / HTTP/1.1 / Transport pending)"
    - "GET /api/admin/h2/sessions returns 401 unauthenticated and 200 + JSON when admin-authed"
    - "The admin inspector page renders the h2 pool snapshot (or the instructional empty state when WS-default)"
    - "No bearer token is ever exposed in the inspector response or page (16-char pool-key prefix only)"
    - "UI build exits 0; the pill is plain text/CSS, no emoji glyph"
  artifacts:
    - path: "banking_api_server/routes/adminH2Inspector.js"
      provides: "GET /sessions read-only h2 pool snapshot"
      contains: "_pool"
    - path: "banking_api_ui/src/components/TokenChainDisplay.js"
      provides: "TransportPill component + render on bff-mcp-transport event"
      contains: "TransportPill"
    - path: "banking_api_ui/src/components/AdminH2InspectorPage.jsx"
      provides: "Read-only admin inspector page mirroring AdminVaultPage"
      contains: "HTTP/2 Bridge Inspector"
  key_links:
    - from: "banking_api_ui/src/components/TokenChainDisplay.js"
      to: "event.claims.transport (bff-mcp-transport tokenEvent from plan 03)"
      via: "TransportPill render in the BFF->MCP step row"
      pattern: "bff-mcp-transport"
    - from: "banking_api_ui/src/components/AdminH2InspectorPage.jsx"
      to: "GET /api/admin/h2/sessions -> http2McpBridge._pool"
      via: "apiClient fetch on mount + Refresh button"
      pattern: "/api/admin/h2/sessions"
---

<objective>
Deliver the two D-06 educational UI surfaces, sequenced AFTER the transport flip (plan 03) so they observe real data: (1) a Token Chain transport pill reusing the existing tcd-badge family on the bff-mcp-transport step; (2) a read-only admin inspector — new route /api/admin/h2/sessions + new React page AdminH2InspectorPage — mirroring the Phase 269.1 vault precedent exactly.

Purpose: D-06 educational surfacing is the strongest justification for this phase (PROJECT.md / CONTEXT.md canonical_refs). Without it the phase is a quiet optimization. Both surfaces REUSE shipped patterns — no new design system.
Output: 1 new route + 1 mount line + TransportPill in TokenChainDisplay + 1 new admin page (jsx+css) + 1 App.js route, UI build 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-RESEARCH.md
@.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-UI-SPEC.md
@banking_api_ui/src/components/AdminVaultPage.jsx

<interfaces>
From plan 03 (must be merged first — depends_on 999.1-03):
- banking_api_server/services/http2McpBridge.js exports _pool (Map). Each entry: { session, lastUsed, pendingStreams, createdAt, connectUrl, get negotiatedTransport() }.
- server.js pushes a tokenEvent { id: 'bff-mcp-transport', label: 'BFF -> MCP transport', claims: { transport: 'HTTP/2'|'WS'|'HTTP/1.1', alpn } } on BOTH h2 and ws tool-call paths.

Existing patterns to mirror EXACTLY (read them):
- banking_api_ui/src/components/TokenChainDisplay.js StatusBadge function (~lines 79-100) + .tcd-badge--* CSS (TokenChainDisplay.css ~407-452). RESEARCH Example 4.
- banking_api_ui/src/components/AdminVaultPage.jsx + AdminVaultPage.css (Phase 269.1: apiClient only, mount-time useEffect fetch + Refresh button, ❌ ${message} error banner). UI-SPEC "Reference implementations".
- banking_api_server/routes/adminVault.js + server.js:899 `app.use('/api/admin/vault', authenticateToken, require('./routes/adminVault'));` (mount precedent). RESEARCH Example 5.
- App.js: import at line 26, AdminVaultPage route at ~line 1115 `path="/admin/vault"` inside `<AdminRoute user={user}>`. Mirror for /admin/h2.

LOCKED UI contract (999.1-UI-SPEC.md — do not deviate):
- Pill states: HTTP/2 -> tcd-badge--active "HTTP/2"; h2c -> tcd-badge--active "HTTP/2 (h2c)"; WS -> tcd-badge--exchanged "WebSocket"; HTTP/1.1 -> tcd-badge--skipped "HTTP/1.1"; unknown -> tcd-badge--waiting "Transport pending".
- Pill labels are full names (HTTP/2, WebSocket, HTTP/1.1) NOT h2/mux. No new CSS classes.
- Inspector: title "HTTP/2 Bridge Inspector"; the empty state is the PRIMARY case (WS default) and must read instructional, not error; exact copy in UI-SPEC "Copywriting Contract"; manual Refresh button only, NO auto-polling; read-only; column contract in UI-SPEC "Inspector pool table".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin inspector route + mount (AC-05)</name>
  <read_first>
    - banking_api_server/routes/adminVault.js (the route-file precedent: express.Router, no inline auth, module.exports = router)
    - banking_api_server/server.js line 899 (the exact `app.use('/api/admin/vault', authenticateToken, require('./routes/adminVault'));` mount pattern) and surrounding admin mounts (733-734, 899-909)
    - banking_api_server/services/http2McpBridge.js module.exports (_pool shape from plan 03 — negotiatedTransport getter, connectUrl, createdAt, pendingStreams, lastUsed)
    - 999.1-RESEARCH.md Example 5 (VERBATIM route shape) and Security Domain (token-custody: 16-char prefix only, never full token)
    - banking_api_server/tests/routes/adminH2Inspector.regression.test.js (the RED contract from plan 01 — defines exact response shape + 401/200 expectations)
    - .claude/skills/regression-guard/SKILL.md (new admin route trips section-1 admin-mount rules; state what you will NOT break)
  </read_first>
  <files>banking_api_server/routes/adminH2Inspector.js, banking_api_server/server.js</files>
  <action>
Run RED test first: cd banking_api_server && npx jest adminH2Inspector.regression — confirm fail. Implement to GREEN per RESEARCH Example 5 VERBATIM:

1. Create banking_api_server/routes/adminH2Inspector.js (CommonJS, mirror routes/adminVault.js structure):
   - const express = require('express'); const router = express.Router(); const http2McpBridge = require('../services/http2McpBridge');
   - router.get('/sessions', (req, res) => { map [...http2McpBridge._pool.entries()] to objects exposing: poolKey (the Map key — already only url + 16-char token prefix, SAFE per RESEARCH A7), connectUrl, negotiatedTransport, pendingStreams, lastUsed (ISO string), createdAt (ISO string or null), destroyed: entry.session.destroyed, closed: entry.session.closed. res.json({ count, sessions }); })
   - Do NOT include the session object, raw token, or Authorization header in the response (token-custody — RESEARCH Security Domain "Token exfiltration via admin inspector").
   - module.exports = router;

2. In server.js, add ONE mount line next to the other admin mounts (~line 899, immediately after the adminVault line):
   `app.use('/api/admin/h2', authenticateToken, require('./routes/adminH2Inspector'));`
   - authenticateToken at mount time provides the 401-unauthenticated / admin-only gate (V4 Access Control, RESEARCH Security Domain). Match the vault precedent exactly.

Minimal diff: one new file, one mount line. No other server.js changes (the bff-mcp-transport tokenEvent already landed in plan 03).
  </action>
  <verify>
    <automated>cd banking_api_server && npx jest adminH2Inspector.regression --bail</automated>
  </verify>
  <acceptance_criteria>
    - banking_api_server/routes/adminH2Inspector.js exists, contains _pool and '/sessions', module.exports = router
    - server.js contains exactly one line `app.use('/api/admin/h2', authenticateToken, require('./routes/adminH2Inspector'))`
    - The route response never serializes a full token: the regression test's no-full-token assertion passes
    - cd banking_api_server && npx jest adminH2Inspector.regression exits 0 (AC-05 GREEN: 401 unauth, 200+JSON admin)
    - Critical chain still green: cd banking_api_server && npx jest oauthStatus.regression hitlRoute.regression --bail
    - Maps to AC-05
  </acceptance_criteria>
  <done>Read-only /api/admin/h2/sessions route mounted under authenticateToken, returns count+sessions with no token leak, AC-05 green.</done>
</task>

<task type="auto">
  <name>Task 2: TransportPill in TokenChainDisplay (AC-04)</name>
  <read_first>
    - banking_api_ui/src/components/TokenChainDisplay.js (StatusBadge function ~lines 79-100 — copy its shape; and the event-row render block where per-event badges render, to place the pill on the bff-mcp-transport step)
    - banking_api_ui/src/components/TokenChainDisplay.css (.tcd-badge and .tcd-badge--active/--exchanged/--skipped/--waiting ~lines 407-452 — confirm these classes exist; reuse, add NO new classes)
    - 999.1-RESEARCH.md Example 4 (VERBATIM TransportPill function + class map)
    - 999.1-UI-SPEC.md "Transport pill — state -> existing class mapping (LOCKED)" + "Copywriting Contract" + "Hard Constraints" (no emoji, reuse not invent)
    - banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js (the RED contract from plan 01 — defines exact labels/classes and whether TransportPill must be exported)
  </read_first>
  <files>banking_api_ui/src/components/TokenChainDisplay.js</files>
  <action>
Run RED test first: cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill — confirm fail. Implement to GREEN per RESEARCH Example 4 + UI-SPEC LOCKED map:

1. Add a TransportPill function as a sibling of StatusBadge:
```javascript
function TransportPill({ transport }) {
  if (!transport) return <span className="tcd-badge tcd-badge--waiting">Transport pending</span>;
  const map = {
    'HTTP/2':  { cls: 'tcd-badge--active',    label: 'HTTP/2' },
    'h2':      { cls: 'tcd-badge--active',    label: 'HTTP/2' },
    'h2c':     { cls: 'tcd-badge--active',    label: 'HTTP/2 (h2c)' },
    'HTTP/1.1':{ cls: 'tcd-badge--skipped',   label: 'HTTP/1.1' },
    'WS':      { cls: 'tcd-badge--exchanged', label: 'WebSocket' },
  };
  const s = map[transport] || { cls: 'tcd-badge--waiting', label: 'Transport pending' };
  return <span className={`tcd-badge ${s.cls}`}>{s.label}</span>;
}
```
   - If plan 01's test imports TransportPill directly, add it to the file's exports (named export) so it is unit-testable. Otherwise keep it module-local and ensure the render path is reachable via the component fixture.
   - NO emoji, NO icon font — plain text only (UI-SPEC Hard Constraint 1, REGRESSION_PLAN section 0). Do not add new CSS classes (Hard Constraint 2).

2. In the event-row render block, where per-event badges render, add (alongside StatusBadge):
   `{event.id === 'bff-mcp-transport' && <TransportPill transport={event.claims?.transport} />}`
   - Match the exact event/claims access shape used by adjacent badges in that render block (read it — the field may be event.claims or event.metadata depending on the row mapper; align with how the plan-03 tokenEvent's claims object surfaces in this component).

3. UI build gate: cd banking_api_ui && npm run build must exit 0 (CLAUDE.md non-negotiable #3, AC-08).

Minimal diff — one function + one conditional render. No restyle of existing rows.
  </action>
  <verify>
    <automated>cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - banking_api_ui/src/components/TokenChainDisplay.js contains the string TransportPill and the conditional `event.id === 'bff-mcp-transport'`
    - The 4 LOCKED states resolve to the correct tcd-badge--* class + label (active/HTTP/2, exchanged/WebSocket, skipped/HTTP/1.1, waiting/Transport pending) — asserted by the plan-01 test going GREEN
    - No new CSS class added to TokenChainDisplay.css (grep tcd-badge-- class count unchanged for new names); no emoji glyph in pill text
    - cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill exits 0 (AC-04 GREEN)
    - cd banking_api_ui && npm run build exits 0 (AC-08 UI build gate)
    - Maps to AC-04, AC-08
  </acceptance_criteria>
  <done>TransportPill renders the LOCKED state map on the bff-mcp-transport step, reuses tcd-badge classes, no emoji, AC-04 green + UI build 0.</done>
</task>

<task type="auto">
  <name>Task 3: AdminH2InspectorPage + App.js route (D-06 inspector UI)</name>
  <read_first>
    - banking_api_ui/src/components/AdminVaultPage.jsx (FULL — clone its structure: apiClient import, useEffect mount fetch, Refresh button calling same fetch, loading state "Loading…", error banner "❌ ${message}", section/intro markup)
    - banking_api_ui/src/components/AdminVaultPage.css (FULL — clone class names/spacing; the inspector reuses admin-vault-page* geometry per UI-SPEC Spacing/Typography/Color tables)
    - banking_api_ui/src/App.js line 26 (AdminVaultPage import) and ~line 1115 (the `path="/admin/vault"` route inside `<AdminRoute user={user}>`) — mirror exactly for /admin/h2
    - 999.1-UI-SPEC.md FULL "Copywriting Contract", "Inspector pool table — column contract (LOCKED)", "Inspector placement decision", "Refresh behavior decision", "Hard Constraints"
    - banking_api_server/routes/adminH2Inspector.js (the GET /sessions response shape from Task 1 — { count, sessions:[{poolKey, connectUrl, negotiatedTransport, pendingStreams, lastUsed, createdAt, destroyed, closed}] })
    - .claude/skills/banking-api-ui/SKILL.md (apiClient/bffAxios only — never raw axios/fetch; token custody; UI build gate)
  </read_first>
  <files>banking_api_ui/src/components/AdminH2InspectorPage.jsx, banking_api_ui/src/components/AdminH2InspectorPage.css, banking_api_ui/src/App.js</files>
  <action>
Clone AdminVaultPage exactly (UI-SPEC mandates "clone of AdminVaultPage structure/CSS; apiClient only — no raw axios/fetch"):

1. Create banking_api_ui/src/components/AdminH2InspectorPage.jsx:
   - Use the SAME data-fetch idiom as AdminVaultPage (apiClient, useEffect mount fetch of GET /api/admin/h2/sessions, a manual "Refresh" button calling the same fetch — NO setInterval/auto-poll per UI-SPEC "Refresh behavior decision").
   - h1 page title: `HTTP/2 Bridge Inspector`. Intro paragraph (muted): the exact verbatim string from UI-SPEC Copywriting Contract "Inspector intro".
   - Empty state is the PRIMARY render (D-04 WS default): when sessions.length === 0, show heading `No active HTTP/2 sessions` + the verbatim instructional body string from UI-SPEC (the MCP_SERVER_HTTP2_URL=http://localhost:8083 + restart + trigger sequence). Style as instructional/muted, NEVER error red.
   - When sessions.length >= 1: render a table per the LOCKED column contract (Pool key as monospace code, Negotiated protocol, Active streams right-aligned, Reuse count right-aligned, Last used, Status). Fallback (HTTP/1.1) rows use warning-color treatment (no glyph), healthy h2 rows default white. Map from the Task-1 response fields.
   - Loading state: `Loading…`. Fetch error banner: `❌ Could not load HTTP/2 pool state. The BFF may be unreachable or the inspector route is not mounted. Confirm the BFF is running and retry.` (verbatim UI-SPEC). If any session has negotiatedTransport http/1.1, show the verbatim ⚠️ fallback banner from UI-SPEC.
   - READ-ONLY: no POST, no mutating actions, no confirmations. Only ⚠️ ✅ ❌ emoji allowed (UI-SPEC Hard Constraint 1).

2. Create banking_api_ui/src/components/AdminH2InspectorPage.css:
   - Clone the relevant admin-vault-page* rules (page wrapper 24px 20px, section 16px 20px, max-width 720px centered, 14px rows, #f1f5f9 code-span bg, #0369a1 accent ONLY on Refresh button, warning #fffbeb/#78350f for fallback rows). Pin to UI-SPEC Spacing/Typography/Color tables. No new design tokens.

3. banking_api_ui/src/App.js:
   - Add `import AdminH2InspectorPage from "./components/AdminH2InspectorPage";` next to the line-26 AdminVaultPage import.
   - Add a route mirroring the /admin/vault route (~line 1115) exactly: `path="/admin/h2"` element wrapped in `<AdminRoute user={user}><AdminH2InspectorPage /></AdminRoute>`.
   - For nav discoverability, mirror however the vault page is reached (UI-SPEC says a text-only sidebar entry `HTTP/2 Bridge` matching the text-only "Vault" precedent). Read how /admin/vault is linked and add the analogous text-only link — NO emoji in the nav label. If vault has no explicit nav link, match that (route-only) rather than inventing one (minimal-diff).

4. UI build gate: cd banking_api_ui && npm run build must exit 0.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - banking_api_ui/src/components/AdminH2InspectorPage.jsx exists, contains "HTTP/2 Bridge Inspector" and "No active HTTP/2 sessions" and fetches /api/admin/h2/sessions via apiClient (no raw axios/fetch)
    - AdminH2InspectorPage.css exists and uses cloned admin-vault-page-style geometry (no new design tokens; accent #0369a1 only on Refresh)
    - App.js contains `import AdminH2InspectorPage` and a `path="/admin/h2"` route inside `<AdminRoute`
    - No auto-poll: AdminH2InspectorPage.jsx contains no setInterval/setTimeout polling loop
    - Only ⚠️ ✅ ❌ emoji present; empty-state copy is instructional (not error-styled)
    - cd banking_api_ui && npm run build exits 0 (AC-08 UI build gate)
    - Maps to AC-05 (UI consumer), AC-08
  </acceptance_criteria>
  <done>Read-only AdminH2InspectorPage cloned from AdminVaultPage, wired at /admin/h2 under AdminRoute, instructional empty state primary, no token leak, no auto-poll, UI build 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (admin) -> /api/admin/h2/sessions | Admin-authed read; must 401 unauth and never leak tokens |
| http2McpBridge._pool -> inspector response/page | Internal pool state crosses to a browser-rendered surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-11 | Elevation of Privilege | /api/admin/h2 route un-authenticated | mitigate | Mounted `app.use('/api/admin/h2', authenticateToken, ...)` — exact section-1 admin-mount precedent (server.js:899). adminH2Inspector.regression asserts 401 unauthenticated (AC-05) |
| T-999.1-12 | Information Disclosure | Bearer token exfiltration via inspector | mitigate | Response serializes only poolKey (url + 16-char prefix, RESEARCH A7), connectUrl, negotiatedTransport, counts, timestamps, destroyed/closed booleans — never the session object, raw token, or Authorization header. Regression test asserts no full token in body |
| T-999.1-13 | Spoofing | Pill showing HTTP/2 while wire is h1 | mitigate | Pill reads event.claims.transport derived in plan 03 from runtime alpn/:status observation, not env-var intent (RESEARCH Pitfall 4); HTTP/1.1 state is its own visually de-emphasized pill so a presenter sees the truth |
| T-999.1-14 | Tampering | Inspector mutating bridge state | accept | Both surfaces strictly READ-ONLY (UI-SPEC Hard Constraint 3); GET-only route, no POST/confirm — no mutation surface |
</threat_model>

<verification>
- cd banking_api_server && npx jest adminH2Inspector.regression --bail -> 0 (AC-05).
- cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill -> 0 (AC-04).
- cd banking_api_ui && npm run build -> exit 0 (AC-08 UI gate).
- cd banking_mcp_server && npm run build -> exit 0 (AC-08 MCP gate — unchanged by this plan but part of phase gate).
- Full critical chain green: cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration http2McpBridge.regression http2McpBridge.integration adminH2Inspector.regression.
- Manual (AC-D5, phase gate): with MCP_HTTP2_PORT=8083 + MCP_SERVER_HTTP2_URL=http://localhost:8083 set, run the RESEARCH curl --http2-prior-knowledge probe (expect HTTP/2 200), the ws-on-8080 node check, and fire >=10 sequential tools/call via the h2 bridge asserting no _http_incoming:211 in /tmp/bank-mcp.log; confirm the Token Chain pill shows HTTP/2 and the admin inspector lists the live h2 session.
</verification>

<success_criteria>
Both D-06 surfaces ship reusing existing patterns: TransportPill on every bff-mcp-transport step with the LOCKED state map; read-only /api/admin/h2 route + AdminH2InspectorPage with instructional empty state as the primary case; no token leak; no emoji beyond the three allowed; AC-04, AC-05, AC-08 green; UI + MCP build 0.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-04-SUMMARY.md`
</output>
