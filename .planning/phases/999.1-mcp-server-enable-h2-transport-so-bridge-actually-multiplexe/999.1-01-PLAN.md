---
phase: 999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_mcp_server/src/__tests__/h2Listener.test.ts
  - banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts
  - banking_api_server/tests/services/http2McpBridge.regression.test.js
  - banking_api_server/tests/services/http2McpBridge.integration.test.js
  - banking_api_server/tests/routes/adminH2Inspector.regression.test.js
  - banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 6 Wave 0 test files exist and execute (RED — they fail because the production code does not exist yet)"
    - "Each test asserts the AC behavior it maps to, not a placeholder"
    - "No test uses watch mode; all run headless and exit with a code"
  artifacts:
    - path: "banking_mcp_server/src/__tests__/h2Listener.test.ts"
      provides: "AC-01 contract: h2 listener binds MCP_HTTP2_PORT"
      contains: "MCP_HTTP2_PORT"
    - path: "banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts"
      provides: "AC-06 contract: try/catch fallback on http2.createServer throw"
      contains: "createServer"
    - path: "banking_api_server/tests/services/http2McpBridge.regression.test.js"
      provides: "AC-02 contract: negotiatedTransport label (mocked http2)"
      contains: "negotiatedTransport"
    - path: "banking_api_server/tests/services/http2McpBridge.integration.test.js"
      provides: "AC-02 contract: real loopback h2c server"
      contains: "createServer"
    - path: "banking_api_server/tests/routes/adminH2Inspector.regression.test.js"
      provides: "AC-05 contract: 401 unauth / 200 admin on /api/admin/h2/sessions"
      contains: "/sessions"
    - path: "banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js"
      provides: "AC-04 contract: TransportPill renders for bff-mcp-transport event"
      contains: "bff-mcp-transport"
  key_links:
    - from: "test files"
      to: "AC-01..AC-06 in 999.1-VALIDATION.md"
      via: "1:1 AC→test mapping"
      pattern: "AC-0[1-6]"
---

<objective>
Wave 0: create all 6 failing (RED) test scaffolds named in 999.1-VALIDATION.md so the implementation plans (02, 03, 04) can execute test-first. No production code in this plan — only tests that fail because the code they target does not exist yet.

Purpose: VALIDATION.md `nyquist_compliant` is currently false because every AC's automated command points at a MISSING test file. This plan closes all Wave 0 gaps so feedback latency stays <10s during execution.
Output: 6 test files (2 TS in banking_mcp_server, 3 JS in banking_api_server, 1 JS in banking_api_ui) that run headless and fail RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-RESEARCH.md
@.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-VALIDATION.md

<interfaces>
Test targets these (created by plans 02–04, not this plan):
- `banking_mcp_server`: `BankingMCPServer` class gains `private http2Server` field + a `MCP_HTTP2_PORT`-gated listener in `startServer()`. Existing accessor `getActualPort()` exists; plan 02 adds equivalent h2-port observability.
- `banking_api_server/services/http2McpBridge.js`: `createHttp2Session(mcpServerUrl, token)` returns a session; module exports `_pool` (a Map keyed `{url}::{16-char token prefix}`). Pool entries gain `{ negotiatedTransport, connectUrl, createdAt, pendingStreams, lastUsed }`. RESEARCH Example 2.
- `banking_api_server/routes/adminH2Inspector.js` (NEW, plan 04): `GET /sessions` → `{ count, sessions: [...] }`, mounted `app.use('/api/admin/h2', authenticateToken, ...)`. RESEARCH Example 5.
- `banking_api_ui/src/components/TokenChainDisplay.js`: a `TransportPill({transport})` function rendering `<span className="tcd-badge ...">`. RESEARCH Example 4. Renders when `event.id === 'bff-mcp-transport'` reading `event.claims?.transport`.

Existing test patterns to mirror (read them):
- regression vs integration pattern: `banking_api_server/tests/` `*.regression.test.js` (mock everything incl. configStore) vs `*.integration.test.js` (real config). See CLAUDE.md "Test patterns" section.
- MCP server jest: `banking_mcp_server/jest.config.js`, existing `banking_mcp_server/tests/` files for ts-jest setup.
- UI component test: CRA + RTL, existing `banking_api_ui/src/components/__tests__/*.test.js`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP-server h2 test scaffolds (AC-01, AC-06)</name>
  <read_first>
    - banking_mcp_server/jest.config.js (test runner config — module resolution, ts-jest)
    - banking_mcp_server/src/server/BankingMCPServer.ts (current startServer + listen + getActualPort; the file plan 02 extends)
    - An existing banking_mcp_server test file (e.g. tests/server/HttpMCPTransport.test.ts) for the ts-jest import/mock idiom
    - 999.1-RESEARCH.md Example 1 (the http2 listener + try/catch shape these tests assert against)
    - 999.1-VALIDATION.md rows AC-01, AC-06
  </read_first>
  <files>banking_mcp_server/src/__tests__/h2Listener.test.ts, banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts</files>
  <action>
Create `banking_mcp_server/src/__tests__/h2Listener.test.ts` (AC-01):
- Instantiate `BankingMCPServer` with logging disabled and a free WS port. Set `process.env.MCP_HTTP2_PORT` to a free port (e.g. compute via an ephemeral `net.createServer` or hardcode a high port like `48083` and restore the env in afterEach).
- `await server.startServer()`. Assert the h2 listener is bound on `MCP_HTTP2_PORT`: connect with `http2.connect('http://127.0.0.1:<port>', { allowHTTP1: true })` and assert the session reaches `'connect'` without `'error'`; OR assert a new observability accessor (plan 02 will add `getH2Port()` or expose `http2Server`) returns the configured port. Prefer the wire check (`http2.connect`) so the test is implementation-agnostic.
- Also assert WS on 8080-equivalent still binds (existing behavior unchanged) — open `new (require('ws').WebSocket)('ws://127.0.0.1:<wsPort>')` and expect `'open'`.
- `afterEach`: `await server.stopServer()` and delete `process.env.MCP_HTTP2_PORT`.

Create `banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts` (AC-06, D-05):
- `jest.mock` the `http2` module (or use `jest.spyOn` on a dynamically imported http2) so `http2.createServer` throws `new Error('simulated _http_incoming:211 compat crash')`.
- Set `MCP_HTTP2_PORT` to a free port, spy on `console.warn`.
- `await server.startServer()` and assert it RESOLVES (does NOT throw / does NOT crash the process), assert `console.warn` was called with a message containing `h2 listener failed` (per RESEARCH Example 1 fallback text) and the WS server is still bound (open a ws connection, expect `'open'`).
- This proves D-05: h2 startup failure never breaks the demo.

Both are RED now (BankingMCPServer has no MCP_HTTP2_PORT branch). Do NOT add production code.
Do not use watch mode. Tests must exit with a code.
  </action>
  <verify>
    <automated>cd banking_mcp_server && npx jest h2Listener h2FallbackOnError --bail 2>&1 | grep -qE "FAIL|Cannot find|fail" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File `banking_mcp_server/src/__tests__/h2Listener.test.ts` exists and contains the string `MCP_HTTP2_PORT`
    - File `banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts` exists and contains `createServer` and `console.warn`
    - `cd banking_mcp_server && npx jest h2Listener h2FallbackOnError` exits non-zero (RED) — proves the tests assert real unbuilt behavior, not trivially passing
    - Neither file contains `.only`, `--watch`, or `watchAll`
    - Maps to AC-01 (h2 listener binds MCP_HTTP2_PORT) and AC-06 (try/catch fallback)
  </acceptance_criteria>
  <done>Both MCP-server test files exist, fail RED, headless, mapped to AC-01/AC-06.</done>
</task>

<task type="auto">
  <name>Task 2: BFF bridge + admin inspector test scaffolds (AC-02, AC-05)</name>
  <read_first>
    - banking_api_server/services/http2McpBridge.js (current createHttp2Session, pool Map, exports — the file plan 03 extends)
    - banking_api_server/jest.config.js
    - banking_api_server/tests/routes/hitlRoute.regression.test.js AND a *.integration.test.js (the canonical two-tier mock idiom — regression mocks configStore, integration does not)
    - 999.1-RESEARCH.md Example 2 (negotiatedTransport detection) and Example 5 (adminH2Inspector route)
    - 999.1-VALIDATION.md rows AC-02, AC-05
  </read_first>
  <files>banking_api_server/tests/services/http2McpBridge.regression.test.js, banking_api_server/tests/services/http2McpBridge.integration.test.js, banking_api_server/tests/routes/adminH2Inspector.regression.test.js</files>
  <action>
Create `banking_api_server/tests/services/http2McpBridge.regression.test.js` (AC-02, mock h2):
- `jest.mock('http2')` — `http2.connect` returns a fake EventEmitter session with `alpnProtocol = ''`, `destroyed = false`, `request()` returning a fake stream. Mock `appEventService` (regression = mock everything).
- Call `http2McpBridge.createHttp2Session('http://localhost:8083', 'eyJhbGciOiJSUzI1aaaaaaaa')`, emit `'connect'` on the fake session.
- Assert the pool entry (read via `http2McpBridge._pool`) for the key has `negotiatedTransport` equal to `'h2c'` (h2c path: `alpnProtocol === ''` + `parsed.protocol === 'http:'`), and that the entry exposes `connectUrl` and `createdAt`. This is RESEARCH Example 2's contract.

Create `banking_api_server/tests/services/http2McpBridge.integration.test.js` (AC-02, real loopback h2c):
- Do NOT mock http2 or configStore (integration tier). Boot a minimal real `require('http2').createServer({ allowHTTP1: true }, (req,res)=>{ res.writeHead(200,{'content-type':'application/json'}); res.end('{"jsonrpc":"2.0","id":1,"result":{}}'); })` on an ephemeral port.
- `createHttp2Session('http://127.0.0.1:<port>', '<dummy 32-char token>')`, drive one `forwardToolCall` (or minimal request) through it, assert it succeeds AND the pool entry's `negotiatedTransport` resolves to `'h2'` or `'h2c'` (NOT `'http/1.1'`).
- Tear down the http2 server and bridge pool in afterAll.

Create `banking_api_server/tests/routes/adminH2Inspector.regression.test.js` (AC-05):
- Build a minimal express app: `app.use('/api/admin/h2', mockAuthenticateToken, require('../../routes/adminH2Inspector'))`. Provide two variants of the auth mock: one that 401s (no/invalid token), one that sets `req.user` (admin).
- Assert `GET /api/admin/h2/sessions` → 401 when unauthenticated; → 200 with JSON body shaped `{ count: <number>, sessions: <array> }` when admin. Seed `http2McpBridge._pool` with one fake entry and assert the response item exposes `poolKey`, `connectUrl`, `negotiatedTransport`, `pendingStreams` and does NOT contain a full bearer token (assert the serialized body has no substring longer than 16 chars matching a JWT-ish pattern beyond the pool-key prefix).

All three RED now. No production code. No watch mode.
  </action>
  <verify>
    <automated>cd banking_api_server && npx jest http2McpBridge.regression http2McpBridge.integration adminH2Inspector.regression 2>&1 | grep -qE "FAIL|Cannot find module" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `banking_api_server/tests/services/http2McpBridge.regression.test.js` exists, contains `negotiatedTransport` and `_pool`
    - `banking_api_server/tests/services/http2McpBridge.integration.test.js` exists, contains `createServer` (real loopback h2c) and asserts non-`http/1.1`
    - `banking_api_server/tests/routes/adminH2Inspector.regression.test.js` exists, contains `/sessions`, asserts both `401` and `200`
    - `cd banking_api_server && npx jest http2McpBridge.regression http2McpBridge.integration adminH2Inspector.regression` exits non-zero (RED)
    - No `.only`, `--watch`, `watchAll` in any of the three files
    - regression file mocks `http2` + `appEventService`; integration file does NOT mock `configStore` (two-tier pattern honored)
    - Maps to AC-02 and AC-05
  </acceptance_criteria>
  <done>Three BFF test files exist, fail RED, follow the two-tier pattern, mapped to AC-02/AC-05.</done>
</task>

<task type="auto">
  <name>Task 3: Token Chain TransportPill component test scaffold (AC-04)</name>
  <read_first>
    - banking_api_ui/src/components/TokenChainDisplay.js (the StatusBadge function lines ~79–100 and where event rows render — the pattern plan 04 mirrors)
    - An existing banking_api_ui/src/components/__tests__/*.test.js (RTL + CRA render idiom, how the component is imported/wrapped with providers if needed)
    - 999.1-RESEARCH.md Example 4 (TransportPill shape + tcd-badge class map)
    - 999.1-UI-SPEC.md "Transport pill — state → existing class mapping (LOCKED)" table and "Copywriting Contract"
    - 999.1-VALIDATION.md row AC-04
  </read_first>
  <files>banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js</files>
  <action>
Create `banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js` (AC-04):
- This file targets the not-yet-existing `TransportPill` export/render path. Two acceptable strategies — pick whichever the existing TokenChainDisplay test setup supports:
  (a) If `TransportPill` is exported (plan 04 should export it for testability), import it directly and `render(<TransportPill transport="HTTP/2" />)`.
  (b) Otherwise render `TokenChainDisplay` with a context/props fixture containing an event `{ id: 'bff-mcp-transport', claims: { transport: 'HTTP/2' } }` and assert the pill text appears.
- Assert the LOCKED label mapping from 999.1-UI-SPEC.md:
  - `transport="HTTP/2"` (and `"h2"`, `"h2c"`) → rendered text `HTTP/2` (or `HTTP/2 (h2c)` for `h2c`) with class containing `tcd-badge--active`
  - `transport="WS"` → text `WebSocket`, class `tcd-badge--exchanged`
  - `transport="HTTP/1.1"` → text `HTTP/1.1`, class `tcd-badge--skipped`
  - `transport={undefined}` / unknown → text `Transport pending`, class `tcd-badge--waiting`
  - Assert NO emoji glyph in any rendered pill text (regex: rendered text matches only `[\w/.() ]+`).
- RED now (TransportPill does not exist). No production code. CI mode only (no watch).
  </action>
  <verify>
    <automated>cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill 2>&1 | grep -qE "FAIL|Cannot find|fail" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - File `banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js` exists, contains `bff-mcp-transport` and `tcd-badge--active`
    - Asserts all 4 states from the UI-SPEC LOCKED table (HTTP/2, WebSocket, HTTP/1.1, Transport pending)
    - Asserts no emoji in pill text
    - `cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill` exits non-zero (RED)
    - File contains `--watchAll=false` invocation pattern in its run path; no `.only`
    - Maps to AC-04
  </acceptance_criteria>
  <done>UI TransportPill test exists, fails RED, asserts the LOCKED label/class map, mapped to AC-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → bridge `_pool` | Tests read internal pool state; must assert tokens are not exposed (mirrors the real inspector's information-disclosure control) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | Information Disclosure | adminH2Inspector.regression.test.js | mitigate | Test explicitly asserts the serialized `/sessions` response contains NO full bearer token — bakes the token-custody control into the RED contract so plan 04 cannot regress it |
| T-999.1-02 | Information Disclosure | test fixtures | accept | Test files use dummy/synthetic tokens only (`eyJ...aaaa`); no real PingOne JWTs committed — low risk, no live secret |
</threat_model>

<verification>
- All 6 files exist at their VALIDATION.md paths.
- `cd banking_mcp_server && npx jest h2Listener h2FallbackOnError` → non-zero (RED).
- `cd banking_api_server && npx jest http2McpBridge.regression http2McpBridge.integration adminH2Inspector.regression` → non-zero (RED).
- `cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill` → non-zero (RED).
- The existing critical chain still green (no test file should accidentally break shared setup): `cd banking_api_server && npx jest oauthStatus.regression hitlRoute.regression --bail`.
</verification>

<success_criteria>
All 6 Wave 0 test files exist, run headless, fail RED for the right reason (target code absent), each mapped 1:1 to AC-01..AC-06. `nyquist_compliant` can flip true once these exist. No production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-mcp-server-enable-h2-transport-so-bridge-actually-multiplexe/999.1-01-SUMMARY.md`
</output>
