---
phase: 999.1
slug: mcp-server-enable-h2-transport-so-bridge-actually-multiplexe
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-15
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 999.1-RESEARCH.md "## Verification Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x (banking_api_server, banking_mcp_server, banking_api_ui — all existing) |
| **Config file** | `banking_api_server/jest.config.js`, `banking_mcp_server/jest.config.js`, CRA defaults in `banking_api_ui` |
| **Quick run command** | `cd banking_api_server && npx jest http2McpBridge.regression --bail` |
| **Full suite command** | `cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration http2McpBridge.regression http2McpBridge.integration adminH2Inspector.regression` |
| **Estimated runtime** | ~10 seconds (full critical chain); ~2 seconds (quick) |

---

## Sampling Rate

- **After every task commit:** Run `cd banking_api_server && npx jest http2McpBridge.regression --bail`
- **After every plan wave:** Run the full critical chain above
- **Before `/gsd-verify-work`:** Full suite + UI build + MCP build + manual h2/WS smoke + D-05 empirical reproduction check must all be green
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

| AC ID | Behavior | Wave | Test Type | Automated Command | File Exists | Status |
|-------|----------|------|-----------|-------------------|-------------|--------|
| AC-01 | h2 listener binds 8083 when `MCP_HTTP2_PORT=8083` | 0/1 | integration | `cd banking_mcp_server && npx jest h2Listener` | ❌ W0 — `banking_mcp_server/src/__tests__/h2Listener.test.ts` | ⬜ pending |
| AC-02 | BFF h2 bridge connects + labels session `h2`/`h2c` | 0/1 | regression | `cd banking_api_server && npx jest http2McpBridge.regression --bail` | ❌ W0 — `banking_api_server/tests/services/http2McpBridge.regression.test.js` | ⬜ pending |
| AC-02 | Same path against real loopback h2c server | 0/1 | integration | `cd banking_api_server && npx jest http2McpBridge.integration --bail` | ❌ W0 — `banking_api_server/tests/services/http2McpBridge.integration.test.js` | ⬜ pending |
| AC-03 | Default (no `MCP_SERVER_HTTP2_URL`) routes via WS — `useHttp2` false | 1 | regression | `cd banking_api_server && npx jest oauthStatus.regression hitlRoute.regression --bail` | ✅ existing | ⬜ pending |
| AC-04 | Token Chain UI renders TransportPill for `bff-mcp-transport` | 0/2 | component | `cd banking_api_ui && CI=true npm test -- --watchAll=false --testPathPattern=TokenChainDisplay.transportPill` | ❌ W0 — `banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js` | ⬜ pending |
| AC-05 | `GET /api/admin/h2/sessions` 401 unauth, 200+JSON when admin | 0/2 | regression | `cd banking_api_server && npx jest adminH2Inspector.regression --bail` | ❌ W0 — `banking_api_server/tests/routes/adminH2Inspector.regression.test.js` | ⬜ pending |
| AC-06 | h2 startup try/catch swallows error + warns when createServer throws | 0/1 | regression | `cd banking_mcp_server && npx jest h2FallbackOnError --bail` | ❌ W0 — `banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts` | ⬜ pending |
| AC-07 | run-bank.sh sweeps include 8083; REGRESSION_PLAN §3 has 8083 row | 1 | smoke | `grep -c 8083 run-bank.sh REGRESSION_PLAN.md` → expect ≥3 | manual review | ⬜ pending |
| AC-08 | UI build 0; MCP tsc 0; critical regression chain green | gate | gate | `(cd banking_api_ui && npm run build) && (cd banking_mcp_server && npm run build) && (cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration)` | ✅ gates exist | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `banking_mcp_server/src/__tests__/h2Listener.test.ts` — covers AC-01
- [ ] `banking_mcp_server/src/__tests__/h2FallbackOnError.test.ts` — covers AC-06 (D-05)
- [ ] `banking_api_server/tests/services/http2McpBridge.regression.test.js` — covers AC-02 (mock h2)
- [ ] `banking_api_server/tests/services/http2McpBridge.integration.test.js` — covers AC-02 (real h2c loopback)
- [ ] `banking_api_server/tests/routes/adminH2Inspector.regression.test.js` — covers AC-05
- [ ] `banking_api_ui/src/components/__tests__/TokenChainDisplay.transportPill.test.js` — covers AC-04
- [ ] No new framework install required — Jest is wired in all three packages.

---

## Manual-Only Verifications

| Behavior | AC | Why Manual | Test Instructions |
|----------|----|------------|-------------------|
| h2 endpoint actually speaks h2 on the wire (D-05 empirical) | AC-D5 | Requires running servers + real curl h2 negotiation | `curl -v --http2-prior-knowledge -H 'Accept: application/json, text/event-stream' -H 'Content-Type: application/json' -H "Authorization: Bearer $TOKEN" http://localhost:8083/mcp -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-11-25","capabilities":{},"clientInfo":{"name":"verify","version":"1.0.0"}}}'` — expect `HTTP/2 200`, NOT `HTTP/1.1 200` |
| WS still works on 8080 after h2 listener boots | AC-D5 | Requires running MCP server | `node -e "const W=require('ws');const s=new W.WebSocket('ws://localhost:8080');s.on('open',()=>{console.log('ws ok');s.close();});s.on('error',e=>{console.error('FAIL',e.message);process.exit(1);});"` (run from `banking_api_server/`) |
| Node 24.3 `_http_incoming:211` crash does NOT reproduce against MCP h2 listener | AC-06/D-05 | Empirical predicate — fire ≥10 sequential `tools/call` via h2 bridge, assert no `_http_incoming:211` line in MCP log | See RESEARCH.md Pitfall 1 step 4 |

---

## Validation Sign-Off

- [ ] All ACs have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
