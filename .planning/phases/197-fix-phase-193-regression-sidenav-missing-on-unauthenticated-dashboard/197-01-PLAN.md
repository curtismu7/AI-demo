---
phase: 197-fix-phase-193-regression-sidenav-missing-on-unauthenticated-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/App.js
  - banking_api_ui/src/components/AdminSideNav.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Unauthenticated guest sees sidebar on /dashboard"
    - "Guest sees 'Sign In' action instead of 'Log Out'/'Switch Role'"
    - "Authenticated users see normal sidebar with all actions"
    - "Sign In button triggers OAuth login"
  artifacts:
    - path: "banking_api_ui/src/App.js"
      provides: "AdminSideNav rendered on both authenticated and unauthenticated /dashboard"
      min_lines: 620
    - path: "banking_api_ui/src/components/AdminSideNav.jsx"
      provides: "Guest-aware action items (Sign In vs Log Out)"
      min_lines: 220
  key_links:
    - from: "/dashboard route (unauthenticated)"
      to: "AdminSideNav with user={null}"
      via: "JSX render"
      pattern: "<AdminSideNav user={null} />"
    - from: "Sign In action"
      to: "OAuth login endpoint"
      via: "window.location.href"
      pattern: "/api/auth/oauth/user/login"
---

<objective>
Fix Phase 193 regression: sidebar was missing from unauthenticated `/dashboard` route.

**Root cause:** Phase 193 moved `/dashboard` to an explicit route outside the catch-all. The unauthenticated branch rendered `TopNav + UserDashboard` but omitted `AdminSideNav`, while the authenticated branch had it.

**Solution:** Add `<AdminSideNav user={null} />` to unauthenticated branch. Update AdminSideNav action items to hide auth-dependent actions (Switch Role, Log Out) and show "Sign In" for guests instead.

Purpose: Users clicking "Explore Demo" or "Try as Customer" now see the sidebar and can navigate the entire app.
Output: Sidebar visible on guest dashboard; guest action menu shows Sign In instead of logout/switch; build passes.
</objective>

<context>
@.planning/REGRESSION_PLAN.md
@.planning/phases/193-allow-unauthenticated-dashboard-access-lazy-login-on-agent-chat-and-action-buttons-dual-token-exchange-on-demand/193-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AdminSideNav to unauthenticated /dashboard route in App.js</name>
  <files>banking_api_ui/src/App.js</files>
  <action>
    Locate the explicit `/dashboard` route added in Phase 193 (~line 601).
    In the `!user ? (...)` branch (unauthenticated case), add `<AdminSideNav user={null} />` right after the opening `<>` and before `<TopNav>`.
    This ensures guests see the sidebar on the demo dashboard.
  </action>
  <verify>
    grep -n "AdminSideNav user={null}" banking_api_ui/src/App.js | grep -c "607"
  </verify>
  <done>Line 607 renders `<AdminSideNav user={null} />` before TopNav in the unauthenticated /dashboard branch</done>
</task>

<task type="auto">
  <name>Task 2: Update AdminSideNav to conditionally show guest vs authenticated actions</name>
  <files>banking_api_ui/src/components/AdminSideNav.jsx</files>
  <action>
    1. Update `actionItems` array to conditionally include "Switch Role" and "Log Out" only when `user` exists.
    2. Show "Sign In" (🔑 icon) instead when no user.
    3. Add a new `case 'sign-in'` in `handleAction` that redirects to `/api/auth/oauth/user/login?return_to=/dashboard`.
    4. Also add 'Postman Collections' to System Tools section (uncommitted from earlier work).
    Usage: `...(user ? [...items...] : [...guest_items...])`
  </action>
  <verify>
    npm run build 2>&1 | grep "ready to be deployed" && grep -c "case 'sign-in'" banking_api_ui/src/components/AdminSideNav.jsx
  </verify>
  <done>
    - actionItems includes spread syntax for conditional auth items
    - "Sign In" appears in guest action menu
    - sign-in case handler redirects to OAuth login
    - Build passes (exit 0)
  </done>
</task>

</tasks>

<verification>
- [ ] `npm run build` exits 0 in banking_api_ui/
- [ ] App.js has `<AdminSideNav user={null} />` on line 607 in `/dashboard` unauthenticated branch
- [ ] AdminSideNav.jsx has conditional `actionItems` with spread syntax
- [ ] Sign In handler redirects to `/api/auth/oauth/user/login?return_to=/dashboard`
- [ ] Postman Collections added to System Tools
- [ ] Git diff shows only intended changes (no extra formatting)
</verification>

<success_criteria>
- Sidebar visible on guest dashboard
- Guest action menu shows only "Dark Mode" + "Sign In"  
- Authenticated users see normal "Switch Role" + "Log Out"
- Build passes without errors
- Changes fit within one compact commit
</success_criteria>

<output>
Create `.planning/phases/197-fix-phase-193-regression-sidenav-missing-on-unauthenticated-dashboard/197-01-SUMMARY.md`
</output>
