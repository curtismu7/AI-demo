---
phase: 204
plan: 01
type: execute
wave: 1
depends_on: [203]
files_modified:
  - banking_api_ui/src/pages/ConfigurePage.tsx
  - banking_api_ui/src/components/UnifiedConfigurationPage.jsx
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03

must_haves:
  truths:
    - "Every configuration field and tab has inline explanations or tooltips"
    - "Feature Flags tab exists with complete list of all toggleable flags"
    - "Each flag has description, current status, and toggle button"
    - "Explanations appear as help text, tooltips, or collapsible sections"
    - "Feature Flags tab visually matches other tabs (consistent color scheme)"
    - "npm run build exits 0 with no new console warnings"
  artifacts:
    - path: "banking_api_ui/src/components/UnifiedConfigurationPage.jsx"
      provides: "Feature Flags tab + expanded explanations"
      contains: "featureFlags"
    - path: "banking_api_ui/src/pages/ConfigurePage.tsx"
      provides: "Updated ConfigurePage routing"
  key_links:
    - from: "banking_api_ui/src/pages/ConfigurePage.tsx"
      to: "UnifiedConfigurationPage.jsx"
      via: "render on mount"
      pattern: "UnifiedConfigurationPage"
    - from: "UnifiedConfigurationPage.jsx"
      to: "feature flags state"
      via: "toggle handler"
      pattern: "handleToggleFlag"

---

<objective>
Extend the UnifiedConfigurationPage with a new Feature Flags tab and add inline explanations to all configuration fields across all existing tabs (IDP Setup, OAuth Resources, MCP Config, Demo Data).

Purpose: Users accessing /configure need to understand what each field does and how feature flags affect behavior. Feature Flags tab should make experimental toggles visible and easy to toggle.

Output: Updated UnifiedConfigurationPage with Feature Flags tab + explanatory text/tooltips on all configuration sections.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/203-pingone-test-config-cards-yellow-green-red-status-background/203-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Field Explanations to Existing Tabs</name>
  <files>banking_api_ui/src/components/UnifiedConfigurationPage.jsx</files>
  <action>
Add inline explanations to all configuration sections in UnifiedConfigurationPage:
- IDP Setup tab: Add tooltips to each IDP configuration field (e.g., "Client ID where IDP will send OAuth callbacks")
- OAuth Resources tab: Add help text explaining resource URIs and audience claims
- MCP Config tab: Add descriptions for MCP endpoint, security settings
- Demo Data tab: Add explanations for what each button does

Use Tailwind classes for consistent tooltip styling (rounded, shadow, dark-bg). Tooltips appear on hover or click.
Use `title` attribute for basic popovers or a Tooltip component if one exists.
  </action>
  <verify>
    Browser check: Hover over fields in /configure, verify tooltips appear with helpful text for each section
  </verify>
  <done>
All configuration field explanations present and visible on hover/click
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Feature Flags Tab</name>
  <files>banking_api_ui/src/components/UnifiedConfigurationPage.jsx</files>
  <action>
Add a new tab "Feature Flags" to UnifiedConfigurationPage tab list:
1. Fetch all feature flags from BFF endpoint (GET /api/config/feature-flags) — returns { flags: { [key]: { name, description, enabled } } }
2. Render each flag as a toggle card:
   - Flag name and description
   - Toggle switch (on/off state)
   - "Save" button to POST /api/config/feature-flags/{flagKey} with { enabled: boolean }
3. Style to match existing tabs (same background, border, padding, tab colors)
4. On toggle success, show toast: "Flag {flagName} updated"
5. On toggle failure, show error toast with reason

Flags to display (from config):
- AGENT_DUAL_TOKEN_EXCHANGE (Enable RFC 8693 dual-token exchange): Show when enabled
- DIRECT_MCP_CALLS (Direct MCP calls bypass state machine): Show when enabled
- LAZY_LOGIN_ON_DASHBOARD (Allow unauthenticated dashboard browse): Show when enabled
- FEATURE_FLAG_SAMPLE or similar if others exist in config

Use Tailwind for styling (consistent card design: bg-white, border, shadow, rounded-lg).
  </action>
  <verify>
    npm test (no new test failures)
  </verify>
  <done>
Feature Flags tab renders with all toggleable flags, toggle works, state persists
  </done>
</task>

<task type="auto">
  <name>Task 3: Build & Verify</name>
  <files></files>
  <action>
1. Run npm run build in banking_api_ui/
2. Verify exit code 0
3. No new console errors in browser DevTools
4. Feature Flags tab visually consistent with other tabs
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -5 && echo "Build status: $?"</automated>
  </verify>
  <done>
npm run build exits 0; Feature Flags tab integrated and styled
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client ↔ BFF | Feature flag toggle requests must be authenticated; only admins can toggle flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-204-01 | Authorization | Feature Flags API | Mitigate | Verify request auth token; reject unauthenticated toggle requests with 401 |
| T-204-02 | Information Disclosure | Flag descriptions | Accept | Descriptions are non-sensitive (feature names); available to authenticated users only |

</threat_model>

<verification>
1. All configuration fields have visible explanations
2. Feature Flags tab displays and toggles work
3. npm run build exits 0
4. No new console errors
</verification>

<success_criteria>
✓ Every config field has inline explanation (tooltip or help text)
✓ Feature Flags tab exists and renders all toggleable flags
✓ Toggle works: clicking toggle → POST request → state updates → toast appears
✓ Build passes: npm run build exit 0
✓ Styling consistent with existing tabs
</success_criteria>

<output>
After completion, create `.planning/phases/204-configure-page-add-explanations-for-each-field-option-add-feature-flags-tab/204-01-SUMMARY.md`
</output>
