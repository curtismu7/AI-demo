---
phase: 214
plan: "01"
title: Investigate and Fix FIDO2 Registration and Authentication
wave: 1
depends_on: []
type: execute
autonomous: true
files_modified:
  - demo_api_server/services/mfaService.js
  - demo_api_server/routes/mfaTest.js
  - demo_api_server/src/__tests__/mfaService.test.js
  - REGRESSION_PLAN.md
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
---

<objective>
Verify the FIDO2 registration and authentication flows end-to-end. The Content-Type fix (`application/vnd.pingidentity.device.activate+json`) was applied in a prior pass, but the registration may still fail due to origin mismatch, body shape issues, or other causes. This plan: (1) audits the current mfaService.js implementation vs PingOne docs and curl commands, (2) investigates any remaining failure by reading the full PingOne error response, (3) applies targeted fixes to registration and authentication, and (4) ensures tests cover the corrected behavior.
</objective>

<must_haves>
  <truths>
    - `completeFido2Registration` in demo_api_server/services/mfaService.js uses POST (not PUT) and Content-Type `application/vnd.pingidentity.device.activate+json` — do NOT revert this
    - `submitFido2Assertion` uses Content-Type `application/vnd.pingidentity.assertion.check+json` and hits the auth server (`_authBaseUrl()`), not the management API — preserve this
    - REGRESSION_PLAN.md §4 entry 2026-04-22: RP-ID patch and signed-byte decode must NOT be reverted
    - Worker token must never be returned to the browser
    - `safeBase64ToBytes` helper in MFATestPage.tsx must not be removed or regressed
    - After any server change: `cd demo_api_server && npm test` must exit 0
    - No emoji other than ⚠️ ✅ ❌ in UI or server logs
  </truths>
  <goal_backward>
    FIDO2 registration completes without UNEXPECTED_ERROR or origin mismatch; FIDO2 authentication challenge flow works end-to-end; both paths show request and response details in the test page UI.
  </goal_backward>
</must_haves>

<tasks>

  <task id="214-01-01">
    <title>Audit completeFido2Registration against PingOne docs and curl</title>
    <type>execute</type>
    <read_first>
      - demo_api_server/services/mfaService.js (lines 651-825 — initFido2Registration and completeFido2Registration)
      - demo_api_server/routes/mfaTest.js (lines 892-1019 — enroll-fido2-init and enroll-fido2-complete routes)
      - demo_api_ui/src/components/MFATestPage.tsx (lines 988-1210 — testFidoEnrollInit and testFidoEnrollComplete callbacks)
      - REGRESSION_PLAN.md §4 (2026-04-22 entry — RP-ID patch context)
    </read_first>
    <action>
      Compare the current `completeFido2Registration` implementation against the PingOne Management API spec for "Activate a FIDO2 device":
      - Endpoint must be: `PUT /v1/environments/{envId}/users/{userId}/devices/{deviceId}` (note: management API uses PUT for device activation)
      - Current code uses `POST` — verify whether POST or PUT is correct per PingOne docs for device activation
      - Body must include: `{ attestation: "<JSON string>", origin: "<browser origin>" }` — verify shape matches
      - Check origin: verify `requestOrigin` from client matches `cdj.origin` in the diagnostic log
      - Check the `enroll-fido2-complete` route: confirm it passes `origin || req.headers.origin` to the service
      - Document the exact API call (method, URL, headers, body shape) as a curl command in the investigation log

      Key question to resolve: Does PingOne's "Activate a FIDO2 device" endpoint use POST or PUT? The research noted PUT; the current code uses POST. Investigate by:
      1. Reading `initFido2Registration` to see what endpoint/method PingOne returns in `publicKeyCredentialCreationOptions`
      2. Checking PingOne docs URL: https://apidocs.pingidentity.com/pingone/platform/v1/api/#put-activate-a-fido2-device

      If the method needs to change (POST → PUT or vice versa), make the targeted fix in `completeFido2Registration`.

      Also verify the body shape: PingOne may require `{ type: "FIDO2", attestation: "...", origin: "..." }` with the `type` field included. Check the init response (`publicKeyCredentialCreationOptions` body) for hints about what the activate endpoint expects.
    </action>
    <acceptance_criteria>
      - Investigation documents: correct HTTP method (POST or PUT), correct endpoint URL pattern, required body fields, correct Content-Type
      - If method is wrong: `completeFido2Registration` corrected to use the right method
      - If body is wrong: body shape corrected (e.g., `type: "FIDO2"` added if missing)
      - Diagnostic log entries `[FIDO2-DIAG]` remain in place (do not remove)
      - `cd demo_api_server && npm test` exits 0
    </acceptance_criteria>
  </task>

  <task id="214-01-02">
    <title>Improve error logging to expose full PingOne error body</title>
    <type>execute</type>
    <read_first>
      - demo_api_server/services/mfaService.js (lines 796-807 — completeFido2Registration catch block)
      - demo_api_server/services/mfaService.js (lines 415-418 — submitFido2Assertion catch block)
    </read_first>
    <action>
      In `completeFido2Registration` catch block (lines 796-807), update the error log to use `JSON.stringify(pingErr)` instead of `pingErr?.details || pingErr?.message` so the full PingOne error body (including `innerError`, detail arrays, and `supportedContentType` hints) is captured:

      Change:
      ```
      "[MFA] completeFido2Registration failed: status=%s code=%s details=%j",
      err.response?.status,
      pingErr?.code,
      pingErr?.details || pingErr?.message,
      ```
      To:
      ```
      "[MFA] completeFido2Registration failed: status=%s fullPingError=%j requestBody=%j",
      err.response?.status,
      pingErr,
      body,
      ```

      Also verify that `err._debug` is attached before throwing (already present at line 805 — confirm it's not lost).

      Do NOT change `submitFido2Assertion` catch block — it already attaches `_debug` correctly.
    </action>
    <acceptance_criteria>
      - `completeFido2Registration` catch block logs `fullPingError=%j` with the full `pingErr` object, not just `.details`
      - The `body` (request body) is also logged alongside the error for correlation
      - `err._debug` remains attached before `throw err`
      - `cd demo_api_server && npm test` exits 0
    </acceptance_criteria>
  </task>

  <task id="214-01-03">
    <title>Fix origin handling in FIDO2 registration</title>
    <type>execute</type>
    <read_first>
      - demo_api_server/services/mfaService.js (lines 736-825 — completeFido2Registration full function)
      - demo_api_server/routes/mfaTest.js (lines 971-1019 — enroll-fido2-complete route)
      - demo_api_ui/src/components/MFATestPage.tsx (lines 1150-1210 — testFidoEnrollComplete callback, how origin is sent)
    </read_first>
    <action>
      Verify the origin chain:
      1. `testFidoEnrollComplete` in MFATestPage.tsx — confirm it sends `origin: window.location.origin` in the POST body to `/api/mfa/test/integration/enroll-fido2-complete`
      2. The route at `enroll-fido2-complete` — confirm it reads `origin` from `req.body` and passes it to `completeFido2Registration`
      3. `completeFido2Registration` — confirm the `origin` parameter is used directly in the body (line 775: `origin`) and logged vs `cdj.origin` (line 761)

      The [FIDO2-DIAG] diagnostic already logs origin mismatch warnings. If the origin in the PUT/POST body doesn't match the `cdj.origin`, the fix is:
      - Option A: Remove the `origin` override in the service and let PingOne derive it from the RP ID configuration
      - Option B: Extract `cdj.origin` from the decoded `clientDataJSON` server-side and use that as the authoritative origin (instead of `window.location.origin` from the client)

      Choose Option B if mismatch exists: decode `clientDataJSON` in the route or service and use `cdj.origin` as the origin value sent to PingOne. This ensures the origin PingOne validates matches what the browser actually signed.

      If no mismatch exists (origins already match), document this and mark the task as investigation-only with no code change.
    </action>
    <acceptance_criteria>
      - `[FIDO2-DIAG] ORIGIN MISMATCH` warning does NOT appear in server logs during a successful enrollment flow
      - If Option B was applied: the service extracts `cdj.origin` from `clientDataJSON` and uses it as the `origin` field in the PingOne body
      - If no mismatch found: code unchanged, task documented as verified-no-issue
      - `cd demo_api_server && npm test` exits 0
    </acceptance_criteria>
  </task>

  <task id="214-01-04">
    <title>Verify FIDO2 authentication flow (submitFido2Assertion) end-to-end</title>
    <type>execute</type>
    <read_first>
      - demo_api_server/services/mfaService.js (lines 377-426 — submitFido2Assertion)
      - demo_api_server/routes/mfaTest.js (lines 617-665 — integration/verify-fido2 route)
      - demo_api_ui/src/components/MFATestPage.tsx (lines 752-836 — testFidoVerify callback)
      - demo_api_ui/src/components/MFATestPage.tsx (lines 2241-2330 — FIDO2 Testing section JSX)
    </read_first>
    <action>
      Audit `submitFido2Assertion` and the authentication flow:
      1. Confirm the assertion endpoint: `POST {_authBaseUrl()}/deviceAuthentications/{daId}` — this is the AUTH server (not management API), which is correct
      2. Confirm Content-Type: `application/vnd.pingidentity.assertion.check+json` — this is correct and should not change
      3. Confirm body: `{ origin, assertion: "<JSON string>", compatibility: "FULL" }` — verify `FULL` is the correct compatibility value or if PingOne accepts other values
      4. Check the verify-fido2 route: confirm it passes `origin || req.headers.origin` to `submitFido2Assertion`
      5. Check `testFidoVerify` in MFATestPage.tsx: confirm the assertion payload shape matches what PingOne expects

      If the authentication path works after the registration fix (since the device was properly enrolled), document success. If issues are found:
      - Check if `origin` needs to match the assertion origin (same as registration issue)
      - Check if `compatibility: "FULL"` is correct (alternative: `"BACKWARD"`)
      - Apply targeted fix matching the specific failure

      Also verify that `pingoneRequest` and `pingoneResponse` are properly set in `fidoVerifyPingoneReq` and `fidoVerifyPingoneRes` state after the verify call — these should show in the MFATestCard.
    </action>
    <acceptance_criteria>
      - `submitFido2Assertion` sends the correct body and Content-Type — no change needed or targeted fix applied
      - The MFATestCard for "Verify FIDO2 with Passkey" shows `pingoneRequest` and `pingoneResponse` after verification
      - `[FIDO2-DIAG]` logs confirm no origin mismatch in authentication path
      - `cd demo_api_server && npm test` exits 0
    </acceptance_criteria>
  </task>

  <task id="214-01-05">
    <title>Update mfaService.test.js to assert corrected Content-Type and logging</title>
    <type>execute</type>
    <read_first>
      - demo_api_server/src/__tests__/mfaService.test.js (lines 384-435 — completeFido2Registration tests)
      - demo_api_server/services/mfaService.js (completeFido2Registration implementation — read after task 214-01-01 fixes are applied)
    </read_first>
    <action>
      Update `mfaService.test.js` for the `completeFido2Registration` describe block:
      1. Verify test at line 404 asserts `Content-Type: 'application/vnd.pingidentity.device.activate+json'` — this already exists; confirm it passes after any method/body changes from task 214-01-01
      2. Add a test asserting the error catch block logs `fullPingError` (from task 214-01-02): mock `axios.post` to reject, capture `console.error` calls, assert the log message includes the full pingErr object (not just `pingErr.details`)
      3. If the HTTP method changed from POST to PUT in task 214-01-01, update the mock to intercept `axios.put` instead of `axios.post`
      4. Do not delete any existing passing tests — only add or modify to match the corrected implementation

      Run: `cd demo_api_server && npx jest mfaService --no-coverage` — all tests must pass.
    </action>
    <acceptance_criteria>
      - `completeFido2Registration` describe block contains at least one test asserting `Content-Type: 'application/vnd.pingidentity.device.activate+json'` — test passes
      - New test asserts error logging uses full pingErr object (not just `.details`)
      - `cd demo_api_server && npx jest mfaService --no-coverage` exits 0
      - `cd demo_api_server && npm test` exits 0
    </acceptance_criteria>
  </task>

  <task id="214-01-06">
    <title>Add REGRESSION_PLAN.md §4 entry for Phase 214 FIDO2 fix</title>
    <type>execute</type>
    <read_first>
      - REGRESSION_PLAN.md §4 (Bug Fix Log — read the template and the 2026-04-22 entry for format reference)
    </read_first>
    <action>
      Add a new entry to REGRESSION_PLAN.md §4 (Bug Fix Log) documenting the Phase 214 FIDO2 investigation and fixes:

      - Date: 2026-05-25
      - Phase: 214
      - What was investigated and what was found (Content-Type was already fixed; origin handling; method verification)
      - What was fixed (specific changes made in tasks 214-01-01 through 214-01-04)
      - What must NOT be broken: `application/vnd.pingidentity.device.activate+json` in completeFido2Registration; safeBase64ToBytes; RP-ID from PingOne options

      Follow the exact template format shown in the existing 2026-04-22 entry.
    </action>
    <acceptance_criteria>
      - REGRESSION_PLAN.md §4 contains a new entry dated 2026-05-25 with Phase 214
      - Entry documents which functions were changed and what behavior must be preserved
      - Entry follows the template format (same structure as 2026-04-22 entry)
    </acceptance_criteria>
  </task>

</tasks>

<verification>
1. `cd demo_api_server && npx jest mfaService --no-coverage` — all tests pass
2. `cd demo_api_server && npm test` — full test suite exits 0
3. `cd demo_api_ui && npm run build` — exits 0 (no UI changes in this plan)
4. REGRESSION_PLAN.md §4 contains a 2026-05-25 entry
5. Server log `[MFA] completeFido2Registration failed` includes `fullPingError=%j` format (verify by reading the updated catch block)
6. `[FIDO2-DIAG] ORIGIN MISMATCH` warning does not appear when clientDataJSON origin matches the request body origin
</verification>

<success_criteria>
- completeFido2Registration sends the correct HTTP method, Content-Type `application/vnd.pingidentity.device.activate+json`, and body shape as verified against PingOne docs
- Error logging exposes full PingOne error body (including `innerError`) — not just `details`
- submitFido2Assertion is confirmed working or fixed — uses `application/vnd.pingidentity.assertion.check+json` on auth server
- mfaService.test.js passes with tests covering the corrected Content-Type and error logging
- REGRESSION_PLAN.md §4 documents the investigation findings
- No regressions to SMS OTP, Email OTP, RP-ID handling, or session flows
</success_criteria>
