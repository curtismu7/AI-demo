---
phase: 275
plan: 03
type: execute
wave: 3
depends_on:
  - 275-02
files_modified:
  - langchain_agent/tests/test_langchain_mcp_agent.py
  - langchain_agent/tests/test_conversation_memory.py
autonomous: true
requirements:
  - LANGGRAPH-03

must_haves:
  truths:
    - "test_langchain_mcp_agent.py passes with pytest — no failures from AgentExecutor or ConversationBufferMemory mocks"
    - "test_conversation_memory.py passes with pytest — no failures from _langchain_memories assertions"
    - "Tests that asserted _langchain_memories, _get_agent_executor_for_session, or ConversationBufferMemory are updated or removed"
    - "New tests cover: graph is compiled on initialize_tools(), ainvoke is called with thread_id=session_id, response is extracted from messages[-1].content"
    - "New test covers: get_conversation_history() returns [] (deprecated)"
    - "pytest runs cleanly for both test files (exit 0)"
  artifacts:
    - path: "langchain_agent/tests/test_langchain_mcp_agent.py"
      provides: "Updated tests for LangGraph-based agent"
      exports: ["TestLangChainMCPAgent"]
    - path: "langchain_agent/tests/test_conversation_memory.py"
      provides: "Updated tests without _langchain_memories assertions"
      exports: ["TestConversationMemory"]
  key_links:
    - from: "test_langchain_mcp_agent.py"
      to: "LangChainMCPAgent._graph"
      via: "mock create_react_agent return value"
    - from: "test_conversation_memory.py"
      to: "ConversationMemory.get_conversation_history"
      via: "assert returns [] and emits deprecation warning"
---

<objective>
Update the test suite to match the LangGraph migration completed in Plans 01 and 02.

Purpose: The existing test_langchain_mcp_agent.py mocks ChatOpenAI (now replaced by
Ollama in the LLM factory), references AgentExecutor and ConversationBufferMemory,
and asserts on result["output"] instead of result["messages"][-1].content.
test_conversation_memory.py asserts on _langchain_memories internal state that no longer
exists. These tests will fail until updated.

Output:
- test_langchain_mcp_agent.py: remove AgentExecutor/ConversationBufferMemory mocks; add mocks for create_react_agent + MemorySaver; assert on graph.ainvoke called with thread_id config; assert response extracted from messages[-1].content
- test_conversation_memory.py: remove assertions on _langchain_memories; add test for get_conversation_history returning []; keep all tests that cover is_user_identified, set_user_identified, add_message, clear_session, session context
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/275-migrate-agentexecutor-to-langgraph/275-02-SUMMARY.md
@langchain_agent/tests/test_langchain_mcp_agent.py
@langchain_agent/tests/test_conversation_memory.py
@langchain_agent/src/agent/langchain_mcp_agent.py
@langchain_agent/src/agent/conversation_memory.py

<interfaces>
<!-- Patterns to mock in test_langchain_mcp_agent.py -->

# Mock target for LLM (was ChatOpenAI, now Ollama via get_llm factory)
patch target: 'langchain_agent.src.agent.langchain_mcp_agent.get_llm'

# Mock target for graph construction
patch target: 'langchain_agent.src.agent.langchain_mcp_agent.create_react_agent'
# create_react_agent returns a CompiledStateGraph; mock it as an AsyncMock with:
#   mock_graph.ainvoke = AsyncMock(return_value={
#       "messages": [AIMessage(content="test response")]
#   })
#   mock_graph.get_state = Mock(return_value=Mock(values={"messages": []}))

# Mock target for MemorySaver
patch target: 'langchain_agent.src.agent.langchain_mcp_agent.MemorySaver'

# Key assertion for process_message_with_tracing:
#   mock_graph.ainvoke.assert_called_once()
#   call_args = mock_graph.ainvoke.call_args
#   assert call_args[1]["config"]["configurable"]["thread_id"] == session_id
#   assert call_args[0][0]["messages"][-1].content == user_message  # HumanMessage

<!-- Existing test structure in test_conversation_memory.py that references _langchain_memories -->

# Remove assertions like:
#   assert session_id in memory._langchain_memories
#   assert len(memory._langchain_memories) == 1
#   memory._langchain_memories[session_id].chat_memory.messages

# Keep assertions like:
#   assert session_id in memory._sessions
#   assert len(memory._messages[session_id]) == N
#   assert context["user_identified"] == True
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update test_langchain_mcp_agent.py for LangGraph</name>
  <files>langchain_agent/tests/test_langchain_mcp_agent.py</files>
  <behavior>
    - All tests that patch ChatOpenAI are updated to patch get_llm instead
    - All tests that reference AgentExecutor or create_tool_calling_agent are removed or updated
    - All tests that reference ConversationBufferMemory are removed or updated
    - A new test test_initialize_tools_builds_graph verifies that after initialize_tools(), self._graph is not None and create_react_agent was called with the llm and tools list
    - A new test test_process_message_with_tracing_uses_thread_id verifies that ainvoke is called with config["configurable"]["thread_id"] == session_id
    - A new test test_process_message_with_tracing_extracts_last_message verifies that response = result["messages"][-1].content
    - Existing tests for non-agent behavior (initialize_session_with_token, _looks_like_email, _handle_user_identification, etc.) are preserved if they don't reference deleted internals
    - The fixture that creates the agent does NOT need to patch ChatOpenAI — patch get_llm at 'src.agent.langchain_mcp_agent.get_llm' returning a Mock LLM
    - Tests use pytest-asyncio (@pytest.mark.asyncio) as existing tests do
  </behavior>
  <action>
    Rewrite the test file keeping the fixture structure but updating mocks and assertions.

    Required fixtures:
    1. mock_config — keep as-is (LangChainConfig shape unchanged)
    2. mock_mcp_client_manager — keep as-is
    3. mock_auth_manager — keep as-is
    4. mock_llm — patch 'src.agent.langchain_mcp_agent.get_llm' (not ChatOpenAI)
    5. mock_tools — keep as-is (list of Mock tools with name/description)
    6. mock_graph — AsyncMock for the CompiledStateGraph returned by create_react_agent:
         mock_graph = AsyncMock()
         mock_graph.ainvoke = AsyncMock(return_value={
             "messages": [AIMessage(content="mock response")]
         })
         mock_graph.get_state = Mock(return_value=Mock(values={"messages": []}))

    Required test cases (add these):

    @pytest.mark.asyncio
    async def test_initialize_tools_builds_graph(agent, mock_tools):
        with patch('src.agent.langchain_mcp_agent.create_react_agent') as mock_cra, \
             patch('src.agent.langchain_mcp_agent.MemorySaver') as mock_ms:
            mock_cra.return_value = mock_graph
            agent._tools = mock_tools
            await agent.initialize_tools()
            assert agent._graph is not None
            mock_cra.assert_called_once()

    @pytest.mark.asyncio
    async def test_process_message_with_tracing_uses_thread_id(agent, mock_tools):
        session_id = "test-session-123"
        user_message = "What are my accounts?"
        agent._graph = mock_graph
        agent._tools = mock_tools
        # Mark user as identified to skip identification prompts
        await agent.conversation_memory.set_user_identified(session_id, "user@test.com", "user-1")
        result = await agent.process_message_with_tracing(user_message, session_id)
        call_args = mock_graph.ainvoke.call_args
        config = call_args[1].get("config") or call_args[0][1]
        assert config["configurable"]["thread_id"] == session_id

    @pytest.mark.asyncio
    async def test_process_message_extracts_last_message_content(agent, mock_tools):
        session_id = "test-session-456"
        user_message = "Show my balance"
        agent._graph = mock_graph
        agent._tools = mock_tools
        await agent.conversation_memory.set_user_identified(session_id, "user@test.com", "user-1")
        response = await agent.process_message_with_tracing(user_message, session_id)
        assert response == "mock response"

    Remove or update any tests that assert:
    - self._agent_executor is not None (replace with self._graph is not None)
    - result.get("output", ...) pattern
    - agent_executor.ainvoke calls
    - ConversationBufferMemory creation
    - _get_agent_executor_for_session is called

    Keep tests that verify:
    - User identification flow (is email detection, handle_user_identification)
    - Registration flow
    - Auth popup injection
    - initialize_session_with_token
    - _is_authorization_complete_message
    - _detect_authorization_code
    - _looks_like_email
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/AI-Demo && python3 -m pytest langchain_agent/tests/test_langchain_mcp_agent.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pytest langchain_agent/tests/test_langchain_mcp_agent.py exits 0
    - At least 3 new tests cover graph construction, thread_id config, and messages[-1].content extraction
    - No test references AgentExecutor, create_tool_calling_agent, or ConversationBufferMemory
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update test_conversation_memory.py for removed _langchain_memories</name>
  <files>langchain_agent/tests/test_conversation_memory.py</files>
  <behavior>
    - All assertions on memory._langchain_memories are removed
    - All assertions that count ConversationBufferMemory objects are removed
    - A new test test_get_conversation_history_is_deprecated verifies that get_conversation_history() returns [] and logs a warning
    - All existing tests that cover _sessions, _messages, session context, user identification, session cleanup, and memory stats still pass
    - test_initialization no longer asserts "memory._langchain_memories == {}"
    - test_add_message no longer asserts that _langchain_memories was updated
    - test_get_conversation_history is rewritten: asserts returns [] (not a list of HumanMessage/AIMessage)
    - All other tests (test_clear_session, test_get_session_stats, test_cleanup, etc.) remain as-is unless they reference _langchain_memories
  </behavior>
  <action>
    Audit test_conversation_memory.py for every reference to _langchain_memories and
    ConversationBufferMemory. For each:

    1. In test_initialization: remove "assert memory._langchain_memories == {}" line only.
       Keep all other initialization assertions.

    2. In test_get_or_create_session (if it asserts _langchain_memories was populated):
       Remove those assertions. Keep assertions on _sessions.

    3. In test_add_message (if it checks _langchain_memories):
       Remove _langchain_memories assertions. Keep _messages assertions.

    4. In test_get_conversation_history (existing test asserting HumanMessage/AIMessage):
       Replace entire test body with:
         messages = await conversation_memory.get_conversation_history("session-123")
         assert messages == []

    5. Add new test:
       @pytest.mark.asyncio
       async def test_get_conversation_history_returns_empty_deprecated(conversation_memory, caplog):
           import logging
           with caplog.at_level(logging.WARNING):
               result = await conversation_memory.get_conversation_history("any-session")
           assert result == []
           assert "deprecated" in caplog.text.lower()

    6. In test_trim_messages (if it asserts _langchain_memories rebuilding):
       Remove _langchain_memories assertions. Keep _messages trimming assertions.

    7. In test_cleanup / test_clear_session (if they assert _langchain_memories is empty):
       Remove _langchain_memories assertions.

    Leave ALL other test logic untouched. The goal is surgical removal of
    _langchain_memories references, not a test rewrite.
  </action>
  <verify>
    <automated>cd /Users/curtismuir/Development/AI-Demo && python3 -m pytest langchain_agent/tests/test_conversation_memory.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pytest langchain_agent/tests/test_conversation_memory.py exits 0
    - No test references _langchain_memories
    - test_get_conversation_history_returns_empty_deprecated passes (returns [] + warning log)
    - All session-context, user-identification, and cleanup tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production module | Tests must mock create_react_agent to avoid requiring langgraph installed in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-275-05 | Spoofing | Mocked graph in tests | accept | Tests mock the graph; real behavior verified by integration or manual run once langgraph is pip-installed |
| T-275-06 | Denial of Service | pytest import failure if langgraph not installed | mitigate | Mock create_react_agent and MemorySaver at the module level so tests pass even without langgraph installed in CI |
</threat_model>

<verification>
After both tasks complete, run the full relevant test suite:

cd /Users/curtismuir/Development/AI-Demo
python3 -m pytest langchain_agent/tests/test_langchain_mcp_agent.py langchain_agent/tests/test_conversation_memory.py -v 2>&1 | tail -40

Expected: all collected tests pass, 0 failures, 0 errors.

Also run a broader smoke check to confirm no regressions in related tests:
python3 -m pytest langchain_agent/tests/test_message_processor.py langchain_agent/tests/test_session_manager.py -x -q 2>&1 | tail -10
</verification>

<success_criteria>
- pytest langchain_agent/tests/test_langchain_mcp_agent.py exits 0
- pytest langchain_agent/tests/test_conversation_memory.py exits 0
- At least 3 new tests in test_langchain_mcp_agent.py assert on LangGraph-specific behavior (graph built, thread_id passed, messages[-1].content extracted)
- test_get_conversation_history_returns_empty_deprecated exists and passes
- No test file references ConversationBufferMemory, AgentExecutor, or _langchain_memories
</success_criteria>

<output>
Create `.planning/phases/275-migrate-agentexecutor-to-langgraph/275-03-SUMMARY.md` when done
</output>
