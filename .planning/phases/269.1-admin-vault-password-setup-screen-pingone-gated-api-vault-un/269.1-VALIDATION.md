---
phase: 269.1
slug: admin-vault-password-setup-screen-pingone-gated-api-vault-un
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-14
---

# Phase 269.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Filled by gsd-planner.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest 29.7.0 (banking_api_server) + Create-React-App testing-library (banking_api_ui) |
| **Config file** | `banking_api_server/jest.config.js` + CRA defaults for the UI test |
| **Quick run command** | `cd banking_api_server && npx jest tests/vault/ tests/routes/adminVault --bail` |
| **Full suite command** | `cd banking_api_server && npm test` + `cd banking_api_ui && npm test -- --watchAll=false` + `cd banking_api_ui && npm run build` |
| **Estimated runtime** | ~15s (route+vault tests); ~120s full suite + UI build |

---

## Sampling Rate

- **After every task commit:** Run `npx jest tests/vault/ tests/routes/adminVault --bail`
- **After every plan wave:** Run `cd banking_api_server && npx jest oauthStatus.regression oauthStatus.integration hitlRoute.regression hitlRoute.integration tests/vault/ tests/routes/adminVault --bail`
- **Before `/gsd-verify-work`:** Full `npm test` + `cd banking_api_ui && npm run build` (must exit 0 per CLAUDE.md UI build gate)
- **Max feedback latency:** 15 seconds (route+vault scoped); 120 seconds (full suite + UI build)

---

## Per-Task Verification Map

> Populated by gsd-planner after PLAN.md files exist.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 269.1-01-01 | 01 | 1 | REQ-VAULT-ADMIN-01,02 | T-269.1-03,07 | unlockVaultAtRuntime DI tests | unit | `npx jest tests/vault/vaultLoader-runtime.test.js --bail` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `banking_api_server/tests/vault/vaultLoader-runtime.test.js` — Plan 01
- [ ] `banking_api_server/tests/routes/adminVault.regression.test.js` — Plan 02
- [ ] `banking_api_server/tests/routes/adminVault.integration.test.js` — Plan 02
- [ ] `banking_api_ui/src/pages/admin/__tests__/AdminVaultPage.test.jsx` — Plan 03
- [ ] Wave 0 dependencies are already installed by Phase 269 (`argon2`, `@inquirer/password`, `commander`, `express-rate-limit`) — verified by research

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin unlocks vault via web UI after restart | REQ-VAULT-ADMIN-04 | End-to-end browser flow | Operator: start BFF without VAULT_PASSWORD env, sign in as admin, navigate to /admin/vault, enter password, confirm "vault unlocked" banner + status badge flips to "unlocked" |
| Admin rotates password from web UI | REQ-VAULT-ADMIN-05 | End-to-end browser flow + side-effect verification | Operator: unlock vault, click rotate, enter current + new password (twice), verify success banner, then restart BFF with old VAULT_PASSWORD env — startup should fail (proves rotate took); restart with new password — should succeed |
| Opaque error UX on wrong password | REQ-VAULT-ADMIN-06 | Visual confirmation that no enumeration oracle leaks | Submit wrong password on unlock form, confirm UI shows generic "could not unlock vault" message; check network tab — response body does NOT distinguish bad-password from tampered-file |
| Rate-limit lockout | REQ-VAULT-ADMIN-08 | Time-based UX | Submit 5 consecutive wrong passwords quickly; 6th attempt should fail immediately with "too many attempts" UI banner; wait the documented backoff; subsequent attempt should be allowed |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s (route-scoped); < 120s (full suite + UI build)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
