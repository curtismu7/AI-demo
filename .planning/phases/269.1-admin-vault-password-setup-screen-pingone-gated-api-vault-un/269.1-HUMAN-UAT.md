---
status: partial
phase: 269.1-admin-vault-password-setup-screen
source: [269.1-VERIFICATION.md, 269.1-CODE-AUDIT-2026-05-14]
started: 2026-05-14
updated: 2026-05-14
code_audit:
  date: 2026-05-14
  by: Claude (gsd-plan-milestone-gaps bookkeeping pass)
  phase269.1_tests: "36/36 green (tests/routes/adminVault.regression + .integration + tests/vault/vaultLoader-runtime)"
  critical_regression: "38/38 green (oauthStatus.regression + .integration + hitlRoute.regression + .integration)"
  code_paths_verified:
    - UAT-1: "services/vaultLoader.js unlockVaultAtRuntime + routes/adminVault.js POST /unlock + AdminVaultPage.jsx unlock form — all wired"
    - UAT-2: "lib/vault/index.js rotate (re-derives KEK with new salt, re-wraps DEKs) + save (atomic .tmp + rename, mode 0o600) — file-level side effect provable"
    - UAT-3: "adminVault.js maps VaultAuthError + VaultIntegrityError to byte-identical 401 string 'vault: open failed (bad password or tampered file)'; regression test 8 enforces"
    - UAT-4: "express-rate-limit 5/5min keyed by req.user.sub (then req.ip); regression test 17 enforces 429 at attempt 6"
    - UAT-5: "docs/vault.md:677–751 has MCP Gateway desync caveat + 4-place env update checklist + post-rotate verify command"
---

## Current Test

[awaiting human testing — all 5 UAT items below have passing automated coverage; remaining work is operator-side observation only]

## Code-side audit (2026-05-14)

Before running these 5 UAT items, re-verified that everything the operator needs is still in place:

| Item | Code-side evidence | Status |
|---|---|---|
| 1 | `unlockVaultAtRuntime` in vaultLoader.js:115–185 (DI'd vaultLib + configStore); `POST /unlock` handler in adminVault.js:114–146; AdminVaultPage.jsx unlock form posts via apiClient | ✓ wired |
| 2 | `rotate()` in lib/vault/index.js:261–286 (re-derives KEK from new salt, re-wraps DEKs, overwrites closure kek in place); `save()` in lib/vault/index.js:288–314 (atomic `.tmp` write + `rename`, mode 0o600); adminVault.js:155–198 re-verifies currentPassword before rotate | ✓ wired |
| 3 | adminVault.js:135–138 returns identical 401 body `{error:'unauthorized', message:'vault: open failed (bad password or tampered file)'}` for both `VaultAuthError` and `VaultIntegrityError`; regression test 8 asserts byte-equality | ✓ wired |
| 4 | adminVault.js:61–67 `unlockLimiter`: `windowMs: 5*60*1000`, `max: 5`, keyGenerator returns `req.user?.sub \|\| req.ip`; regression test 17 asserts 429 at attempt 6 before reaching `unlockVaultAtRuntime` | ✓ wired |
| 5 | docs/vault.md:677–699 "MCP Gateway desync caveat (T-269.1-07)"; docs/vault.md:703–745 "After rotating" with 4-place env update + `npm run vault:list` verify command + gateway restart command | ✓ documented |

**Test suites re-run at audit time:** 36/36 Phase 269.1 tests green + 38/38 critical regression suite green (oauthStatus + hitlRoute). No drift since 269.1-VERIFICATION.md was written.

The 5 items below remain `pending` strictly because they cross the BFF/browser/OS boundary: real PingOne admin sign-in + BFF restart cycles + wall-clock waits + cross-process MCP Gateway restart cannot be exercised by an automated agent without fabricating evidence.

## Tests

### 1. Admin unlocks vault via web UI after BFF restart
expected: Operator starts BFF without `VAULT_PASSWORD` env, signs in as admin, navigates to `/admin/vault`, enters vault password, sees green ✅ "vault unlocked" banner + status badge flips to ✅ unlocked + entriesLoaded > 0. After unlock, `configStore.getEffective('helix_api_key')` (etc.) returns the vault-supplied value.
requirement: REQ-VAULT-ADMIN-04
code_audit: "unlockVaultAtRuntime DI-tested; AdminVaultPage form posts to /api/admin/vault/unlock; on 200 setUnlockPassword('') + banner kind='ok'"
how-to:
  - `cd /Users/curtismuir/Development/banking && ./run-bank.sh stop`
  - `unset VAULT_PASSWORD` (verify with `env | grep VAULT_PASSWORD` → no output)
  - `./run-bank.sh`
  - Open `https://api.ping.demo:4000` in browser, sign in as admin via PingOne
  - Navigate to `/admin/vault`
  - Confirm status badge shows "❌ locked" with entriesLoaded = 0
  - Enter the vault password in the Unlock form, click Unlock
  - Confirm green ✅ banner: "Vault unlocked — N entries loaded"
  - Confirm status badge flips to "✅ unlocked" with entriesLoaded > 0
  - Confirm password input field is cleared (AdminVaultPage.jsx:82)
result: [pending]

### 2. Admin rotates password from web UI; restart with new password
expected: With vault unlocked, operator clicks Rotate, enters current + new password (twice for confirm), sees success banner with ⚠️ "Update VAULT_PASSWORD before next BFF restart" warning. After rotate, restarting the BFF with the OLD `VAULT_PASSWORD` in env fails fast with `[vault] startup load failed; refusing to start.`; restarting with the NEW password succeeds. Proves rotate persisted to disk via lib/vault save() atomic write.
requirement: REQ-VAULT-ADMIN-05
code_audit: "rotate() re-derives KEK with newSalt (lib/vault/index.js:266–267), re-wraps every DEK with newKek (line 270–272), overwrites closure kek in place (lines 277–279); save() writes envelope to filePath + '.tmp' with mode 0o600 then rename (lines 305–307); adminVault.js re-verifies currentPassword via openVault before calling handle.rotate (line 174)"
how-to:
  - With vault unlocked from test 1, scroll to Rotate section
  - Enter current password, new password (≥ 12 chars), confirm new password (must match)
  - Click Rotate
  - Confirm green ✅ banner: "Vault password rotated. Update VAULT_PASSWORD before next BFF restart."
  - Confirm all three password inputs cleared (AdminVaultPage.jsx:142-143)
  - In a terminal: `cd banking_api_server && VAULT_PASSWORD="<OLD-password>" npm run vault:list`
  - Confirm command fails with `vault: open failed (bad password or tampered file)` — proves rotate persisted
  - `cd .. && ./run-bank.sh stop`
  - `export VAULT_PASSWORD="<OLD-password>" && ./run-bank.sh`
  - Confirm BFF startup fails fast with `[vault] startup load failed; refusing to start.` in the BFF log
  - `./run-bank.sh stop && export VAULT_PASSWORD="<NEW-password>" && ./run-bank.sh`
  - Confirm BFF startup succeeds with `[vault] loaded N entries from ...`
result: [pending]

### 3. Opaque error UX on wrong password (no enumeration oracle)
expected: Submitting a wrong password on the Unlock form shows a generic red ❌ banner with text `"vault: open failed (bad password or tampered file)"`. The response body shape, HTTP status code (401), banner colour, button state, and status card state do NOT distinguish wrong-password from tampered-vault.
requirement: REQ-VAULT-ADMIN-07
code_audit: "adminVault.js:135-138 maps both VaultAuthError and VaultIntegrityError to the SAME response object {error:'unauthorized', message:'vault: open failed (bad password or tampered file)'} with the same 401 status; regression test 8 asserts byte-equality programmatically"
how-to:
  - With vault locked (after a fresh BFF restart without VAULT_PASSWORD env)
  - Open browser DevTools → Network tab → filter `vault`
  - Enter a deliberately wrong password (e.g. "wrongpassword123"), click Unlock
  - Confirm UI shows red ❌ banner with exactly: "❌ vault: open failed (bad password or tampered file)"
  - In DevTools Network tab, click the POST /api/admin/vault/unlock request
  - Confirm response status = 401
  - Confirm response body = `{"error":"unauthorized","message":"vault: open failed (bad password or tampered file)"}` exactly
  - Optional tampering test: `cd banking_api_server && printf 'X' | dd of=../secrets.vault bs=1 count=1 seek=200 conv=notrunc` to flip a byte
  - Retry unlock with the CORRECT password — confirm IDENTICAL response (same status, body, banner)
  - Restore the vault from git: `cd .. && git checkout -- secrets.vault` (or regenerate from `npm run vault:create`)
result: [pending]

### 4. Rate-limit lockout lived-time UX
expected: 5 consecutive wrong passwords trigger lockout; 6th attempt returns 429 immediately (NO Argon2 cost paid). UI banner shows generic "too many attempts" message. After waiting ~5 min, subsequent attempt is allowed again.
requirement: REQ-VAULT-ADMIN-08
code_audit: "express-rate-limit configured with windowMs: 5*60*1000, max: 5, keyGenerator returns req.user?.sub || req.ip (adminVault.js:61-67); applied BEFORE unlockVaultAtRuntime in the middleware chain (line 114) so 429 is returned without paying KDF cost; regression test 17 asserts 429 at attempt 6 + unlockVaultAtRuntime never called"
how-to:
  - With vault locked, sign in as admin
  - Submit wrong password 5 times in quick succession (within 10s)
  - Each attempt should return 401 with the opaque error from test 3
  - 6th attempt — should return 429 IMMEDIATELY (visible latency < 100ms, vs ~500ms for Argon2-paying attempts)
  - UI banner shows `{"error":"too_many_requests","message":"Too many unlock attempts. Wait 5 minutes."}` via extractMessage
  - Wait less than 5 min, retry — should still be 429
  - Wait > 5 min from the FIRST attempt, retry with correct password — should succeed
result: [pending]

### 5. MCP Gateway desync awareness after web-rotate (T-269.1-07)
expected: Operator reads docs/vault.md:677–751 and understands that web-rotate updates the vault file but does NOT propagate to the MCP Gateway (which loads from its own VAULT_PASSWORD env at startup). After rotate, operator updates env in all 4 places and restarts the gateway. Subsequent agent MCP tool calls succeed without configuration drift.
requirement: REQ-VAULT-ADMIN-13
code_audit: "docs/vault.md:677 'MCP Gateway desync caveat (T-269.1-07)' section explains the issue; docs/vault.md:703 'After rotating: update VAULT_PASSWORD before next restart' lists 4 places (banking_api_server/.env, pm2 ecosystem, secret manager, CI/CD env); docs/vault.md:737-740 provides the verify command `VAULT_PASSWORD=\"<new>\" npm run vault:list`; docs/vault.md:692 documents the gateway restart command `./run-bank.sh stop && ./run-bank.sh`"
how-to:
  - After completing test 2 (rotate succeeded; BFF restarted with new password)
  - Open `docs/vault.md` and read the section "MCP Gateway desync caveat (T-269.1-07)" (~line 677)
  - Confirm the section explicitly mentions BOTH BFF and Gateway need env updates after rotate
  - Read the section "After rotating: update VAULT_PASSWORD before next restart" (~line 703)
  - Confirm it lists 4 places (banking_api_server/.env, pm2 ecosystem, secret manager, CI/CD env)
  - In a real ops workflow: update VAULT_PASSWORD in all 4 places, then `./run-bank.sh stop && ./run-bank.sh`
  - Confirm gateway opens vault on next startup (BFF log shows gateway connected without `[gw-vault] startup load failed`)
  - Sign in to UI; trigger an agent MCP tool call (e.g., "show my accounts" via the agent dock)
  - Confirm tool call succeeds without config drift errors
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5 (all code-side audited 2026-05-14; awaiting operator browser/restart UAT only)
skipped: 0
blocked: 0

## Gaps

None. All 5 items have complete code-side support, all relevant test suites green at audit time (74 tests total). Operator UAT remaining is observation-only — no implementation work required.

## After completing UAT

1. Update each `result:` field above with `pass`, `fail`, or `issue: <description>`
2. Update `passed` / `issues` counts in Summary
3. Re-run `/gsd-verify-work 269.1` to flip VERIFICATION.md status from `human_needed` to `passed`
4. Flip REQUIREMENTS.md `[ ]` → `[x]` for REQ-VAULT-ADMIN-01..15 (15 boxes; brings coverage from 35/74 to 50/74)
