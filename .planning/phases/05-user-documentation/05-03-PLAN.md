---
phase: 05-user-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - docs/ARCHITECTURE_WALKTHROUGH.md
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "OAuth clients table lists all 5 PingOne apps with canonical env var names"
    - "Token state tables use banking:general:read (not banking:read)"
    - "Diagram references use Super-Banking- filenames (not BX-Finance-)"
    - "Flow 3 documents both 1-exchange and 2-exchange with MCP Token Exchanger client"
    - "No reference to PINGONE_AI_CORE_CLIENT_ID remains in the file"
  artifacts:
    - path: "docs/ARCHITECTURE_WALKTHROUGH.md"
      provides: "Architecture walkthrough with accurate client names, scope names, and diagram references"
  key_links:
    - from: "docs/ARCHITECTURE_WALKTHROUGH.md"
      to: "docs/Super-Banking-AuthCode-PKCE-Flow.drawio"
      via: "diagram reference link"
      pattern: "Super-Banking-AuthCode-PKCE-Flow.drawio"
    - from: "docs/ARCHITECTURE_WALKTHROUGH.md"
      to: "docs/SETUP.md"
      via: "consistent env var naming"
      pattern: "PINGONE_ADMIN_CLIENT_ID"
---

<objective>
Audit and fix docs/ARCHITECTURE_WALKTHROUGH.md to match the corrected SETUP.md and draw.io diagrams.

Purpose: The existing walkthrough (275 lines) has stale scope names (`banking:read` instead of `banking:general:read`), uses legacy env var names (`PINGONE_AI_CORE_CLIENT_ID`), and references `BX-Finance-` diagram filenames instead of the actual `Super-Banking-` names. This plan depends on Plans 01 and 02 completing first so that naming is consistent across all docs.

Output: Corrected docs/ARCHITECTURE_WALKTHROUGH.md
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-user-documentation/05-CONTEXT.md
@.planning/phases/05-user-documentation/05-RESEARCH.md
@.planning/phases/05-user-documentation/05-01-SUMMARY.md
@.planning/phases/05-user-documentation/05-02-SUMMARY.md

@docs/ARCHITECTURE_WALKTHROUGH.md
@docs/SETUP.md
@banking_api_server/env.example
@banking_api_server/services/agentMcpTokenService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OAuth clients table, scope names, and diagram references in ARCHITECTURE_WALKTHROUGH.md</name>
  <files>docs/ARCHITECTURE_WALKTHROUGH.md</files>
  <read_first>
    - docs/ARCHITECTURE_WALKTHROUGH.md (full file — 275 lines, the file being modified)
    - docs/SETUP.md (after Plan 01 fixes — for consistent naming reference)
    - banking_api_server/env.example (canonical env var names)
    - banking_api_server/services/agentMcpTokenService.js (lines 1-80 — actual client ID usage for token exchange)
  </read_first>
  <action>
Apply all of the following specific fixes to docs/ARCHITECTURE_WALKTHROUGH.md:

**Fix 1 — OAuth clients table (§1, around line 28-33):**
The current table has 3 rows:

```
| Admin OIDC app | Staff login... | `PINGONE_AI_CORE_CLIENT_ID` |
| End-user OIDC app | Customer login... | `PINGONE_AI_CORE_USER_CLIENT_ID` |
| Agent actor app (optional) | Client credentials... | `AGENT_OAUTH_CLIENT_ID` |
```

Replace with 5-row table using canonical names:

```markdown
| Client | Used for | Config key |
|--------|----------|-----------|
| Admin OIDC app | Staff login to `/admin`; initiates token exchange | `PINGONE_ADMIN_CLIENT_ID` |
| End-user OIDC app | Customer login to `/dashboard` | `PINGONE_USER_CLIENT_ID` |
| Worker app | PingOne Management API (user lookups, MFA) | `PINGONE_WORKER_TOKEN_CLIENT_ID` |
| MCP Token Exchanger | RFC 8693 token exchange to MCP-audience tokens | `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` |
| AI Agent actor app (optional) | Client credentials actor token for 2-exchange delegation | `PINGONE_AI_AGENT_CLIENT_ID` |
```

Update the preceding text from "OAuth clients (three PingOne apps):" → "OAuth clients (up to five PingOne apps):"

**Fix 2 — Scope names in token state tables:**
Line 101 has: `openid profile email offline_access banking:read banking:write...`
→ Change to: `openid profile email offline_access banking:general:read banking:general:write...`

Search the entire file for any `banking:read` or `banking:write` (without `general:` prefix) and replace:
- `banking:read` → `banking:general:read`
- `banking:write` → `banking:general:write`
- `banking:accounts:read` → `banking:general:read`

Also in Flow 3 §5.2 token state tables, verify scope references use `banking:general:read banking:general:write banking:ai:agent`.

**Fix 3 — Diagram reference filenames:**
The file currently references `BX-Finance-AuthCode-PKCE-Flow.drawio`, `BX-Finance-CIBA-Flow.drawio`, `BX-Finance-TokenExchange-Flow.drawio`.

Replace all three with the actual filenames:
- `BX-Finance-AuthCode-PKCE-Flow.drawio` → `Super-Banking-AuthCode-PKCE-Flow.drawio`
- `BX-Finance-CIBA-Flow.drawio` → `Super-Banking-CIBA-Flow.drawio`
- `BX-Finance-TokenExchange-Flow.drawio` → `Super-Banking-TokenExchange-Flow.drawio`

These appear as markdown links like `[BX-Finance-AuthCode-PKCE-Flow.drawio](./BX-Finance-AuthCode-PKCE-Flow.drawio)` — update both the display text and href.

**Fix 4 — Flow 3 token exchange client identity:**
In §5.2 (1-exchange) and §5.3 (2-exchange), verify which client performs the exchange. Read agentMcpTokenService.js to determine if the token exchange call uses:
- The Admin OIDC app credentials, or
- The MCP Token Exchanger credentials (`PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID`)

Update the narrative text to name the correct client. If the code uses MCP Token Exchanger, add a note: "The MCP Token Exchanger app (not the Admin OIDC app) authenticates the exchange request to PingOne."

**Fix 5 — AGENT_OAUTH_CLIENT_ID reference:**
If `AGENT_OAUTH_CLIENT_ID` appears anywhere, check whether the actual env var name in .env.example is `PINGONE_AI_AGENT_CLIENT_ID`. Update to match canonical name.
  </action>
  <verify>
    <automated>grep -c "AI_CORE" docs/ARCHITECTURE_WALKTHROUGH.md | grep "^0$" &amp;&amp; grep -c "BX-Finance-" docs/ARCHITECTURE_WALKTHROUGH.md | grep "^0$" &amp;&amp; grep -c "PINGONE_ADMIN_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md | grep -v "^0$" &amp;&amp; grep -c "Super-Banking-" docs/ARCHITECTURE_WALKTHROUGH.md | grep -v "^0$" &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PINGONE_AI_CORE_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md` returns 0 matches
    - `grep "PINGONE_AI_CORE_USER_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md` returns 0 matches
    - `grep "BX-Finance-" docs/ARCHITECTURE_WALKTHROUGH.md` returns 0 matches
    - `grep "PINGONE_ADMIN_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep "PINGONE_USER_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep "MCP Token Exchanger" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep "Super-Banking-AuthCode-PKCE-Flow.drawio" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep "Super-Banking-CIBA-Flow.drawio" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep "Super-Banking-TokenExchange-Flow.drawio" docs/ARCHITECTURE_WALKTHROUGH.md` returns 1+ match
    - `grep -P "banking:(read|write)[^:]" docs/ARCHITECTURE_WALKTHROUGH.md` returns 0 matches (no stale scopes, only banking:general:read etc.)
    - OAuth clients table has 5 rows (Worker app and MCP Token Exchanger added)
  </acceptance_criteria>
  <done>ARCHITECTURE_WALKTHROUGH.md uses canonical naming, references correct draw.io filenames, documents all 5 PingOne apps, and uses consolidated scope names — fully consistent with updated SETUP.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → User | Architecture docs must not expose real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information Disclosure) | docs/ARCHITECTURE_WALKTHROUGH.md | mitigate | Verify no real client IDs or secrets in code examples; all values use placeholder patterns |
</threat_model>

<verification>
1. `grep -c "AI_CORE" docs/ARCHITECTURE_WALKTHROUGH.md` → 0
2. `grep -c "BX-Finance-" docs/ARCHITECTURE_WALKTHROUGH.md` → 0
3. `grep "Super-Banking-.*Flow.drawio" docs/ARCHITECTURE_WALKTHROUGH.md` → 3 matches
4. `grep "PINGONE_ADMIN_CLIENT_ID" docs/ARCHITECTURE_WALKTHROUGH.md` → present
5. `grep "MCP Token Exchanger" docs/ARCHITECTURE_WALKTHROUGH.md` → present
6. All diagram links resolve to existing files in docs/
</verification>

<success_criteria>
ARCHITECTURE_WALKTHROUGH.md is fully consistent with the updated SETUP.md (Plan 01) and draw.io diagrams (Plan 02). No stale naming remains. All 5 PingOne apps documented. All diagram references resolve to existing files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-documentation/05-03-SUMMARY.md`
</output>
