---
phase: 05-user-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SETUP.md
  - README.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "SETUP.md references all 5 PingOne apps including MCP Token Exchanger"
    - "Env var names in SETUP.md match .env.example canonical names (PINGONE_ADMIN_CLIENT_ID not PINGONE_AI_CORE_CLIENT_ID)"
    - "Port documentation clearly distinguishes run-bank.sh (4000/3002) from start.sh/manual (3000/3001)"
    - "Scope names in SETUP.md consistently use banking:general:read (not banking:read)"
    - "README Quick Start pointer to docs/SETUP.md is present and accurate"
  artifacts:
    - path: "docs/SETUP.md"
      provides: "Comprehensive setup guide with accurate env var names, all 5 PingOne apps, clear port docs"
    - path: "README.md"
      provides: "Quick Start pointer to docs/SETUP.md"
  key_links:
    - from: "docs/SETUP.md"
      to: "banking_api_server/env.example"
      via: "env var names must match"
      pattern: "PINGONE_ADMIN_CLIENT_ID"
---

<objective>
Audit and fix docs/SETUP.md and README.md against the current codebase state.

Purpose: The existing SETUP.md (320 lines) is substantive but has 5 known gaps identified during research. This plan fixes those gaps without rewriting the entire document.

Output: Corrected docs/SETUP.md + verified README.md pointer
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-user-documentation/05-CONTEXT.md
@.planning/phases/05-user-documentation/05-RESEARCH.md

@docs/SETUP.md
@README.md
@banking_api_server/env.example
@docs/PINGONE_RESOURCES_AND_SCOPES_MATRIX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix env var naming, add MCP Token Exchanger app, fix scope names, clarify ports in SETUP.md</name>
  <files>docs/SETUP.md</files>
  <read_first>
    - docs/SETUP.md (current state — 320 lines, the file being modified)
    - banking_api_server/env.example (canonical env var names)
    - banking_api_server/server.js (lines 45-50 — shows fallback chain: PINGONE_ADMIN_CLIENT_ID preferred)
    - docs/PINGONE_RESOURCES_AND_SCOPES_MATRIX.md (authoritative scope definitions)
  </read_first>
  <action>
Apply all of the following specific fixes to docs/SETUP.md:

**Fix 1 — Env var naming (§2.2, §2.3, §3):**
- §2.2 last line says "Copy the Client ID... as `PINGONE_AI_CORE_CLIENT_ID` / `PINGONE_AI_CORE_CLIENT_SECRET`" → change to `PINGONE_ADMIN_CLIENT_ID` / `PINGONE_ADMIN_CLIENT_SECRET` (matching .env.example canonical names and server.js primary lookup)
- §2.3 last line says "`PINGONE_AI_CORE_USER_CLIENT_ID` / `PINGONE_AI_CORE_USER_CLIENT_SECRET`" → change to `PINGONE_USER_CLIENT_ID` / `PINGONE_USER_CLIENT_SECRET`
- §3 env var table: rename `PINGONE_AI_CORE_CLIENT_ID` → `PINGONE_ADMIN_CLIENT_ID`, `PINGONE_AI_CORE_CLIENT_SECRET` → `PINGONE_ADMIN_CLIENT_SECRET`, `PINGONE_AI_CORE_USER_CLIENT_ID` → `PINGONE_USER_CLIENT_ID`, `PINGONE_AI_CORE_USER_CLIENT_SECRET` → `PINGONE_USER_CLIENT_SECRET`
- Add a note after the env var table: "> **Legacy names:** The BFF also accepts `PINGONE_AI_CORE_CLIENT_ID` as a fallback alias for `PINGONE_ADMIN_CLIENT_ID`. Use the canonical names above for new setups."
- §7 troubleshooting "invalid_client" section: update to reference `PINGONE_ADMIN_CLIENT_ID` / `_SECRET`

**Fix 2 — Add MCP Token Exchanger as §2.5 (renumber existing §2.5 to §2.6):**
Insert new section after §2.4 (Worker app):

```markdown
### 2.5 MCP Token Exchanger Application (RFC 8693)

Required for AI agent MCP tool calls with token exchange delegation.

| Setting | Value |
|---------|-------|
| Type | AI_AGENT |
| Grant types | Client Credentials, Token Exchange (`urn:ietf:params:oauth:grant-type:token-exchange`) |
| Token auth method | `client_secret_basic` |
| Required scopes | `banking:general:read banking:general:write banking:ai:agent` |

**Copy the Client ID and Client Secret** — these become `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID` / `PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_SECRET`.

**Also set:** `PINGONE_MCP_TOKEN_EXCHANGER_SCOPES=banking:general:read banking:general:write banking:ai:agent` and `PINGONE_MCP_TOKEN_EXCHANGER_AUTH_METHOD=client_secret_basic` in your `.env`.
```

Add these 4 env vars to the §3 table as "Optional (enables MCP token exchange)".

Renumber old §2.5 "Create test users" → §2.6.

Also update the intro paragraph of §2 from "You need **three PingOne OAuth applications**" to "You need **four PingOne applications** (two for browser login, one worker for Management API calls, one for MCP token exchange). An optional fifth (AI Agent actor) enables 2-exchange delegation."

**Fix 3 — Port clarity in §4:**
Replace the current §4 Option A content with clearer documentation:

```markdown
### Option A — All services in one command (recommended)

\`\`\`bash
./run-bank.sh
\`\`\`

**Ports used by `run-bank.sh`:**

| Service | Port | URL |
|---------|------|-----|
| React UI | 4000 | `https://api.pingdemo.com:4000` |
| BFF API | 3002 | `https://api.pingdemo.com:3002` |
| MCP server | 8080 | `ws://localhost:8080` |

> **Important:** `run-bank.sh` uses HTTPS via mkcert and requires a `/etc/hosts` entry: `127.0.0.1 api.pingdemo.com`. Ensure `banking_api_ui/.env` has `REACT_APP_API_PORT=3002`.

Commands: `./run-bank.sh start`, `./run-bank.sh stop`, `./run-bank.sh status`, `./run-bank.sh tail`
```

Keep Option B as-is (it already shows 3000/3001 correctly). Add a comparison callout after both options:

```markdown
> **Port summary:** `run-bank.sh` = ports 4000/3002 (HTTPS); `start.sh` / manual = ports 3000/3001 (HTTP). The `.env` files in the repo default to 3000/3001. When using `run-bank.sh`, it overrides ports automatically.
```

**Fix 4 — Scope consistency:**
Search for any occurrence of `banking:read` or `banking:write` (without the `general:` prefix) in SETUP.md. If found, replace with `banking:general:read` / `banking:general:write` to match the consolidated 6-scope model defined in §2.1. The only valid scope names are: `banking:general:read`, `banking:general:write`, `banking:admin`, `banking:sensitive`, `banking:ai:agent`.
  </action>
  <verify>
    <automated>grep -c "PINGONE_AI_CORE_CLIENT_ID" docs/SETUP.md | grep "^0$" &amp;&amp; grep -c "PINGONE_ADMIN_CLIENT_ID" docs/SETUP.md | grep -v "^0$" &amp;&amp; grep -c "MCP Token Exchanger" docs/SETUP.md | grep -v "^0$" &amp;&amp; grep -c "4000/3002" docs/SETUP.md | grep -v "^0$" &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PINGONE_AI_CORE_CLIENT_ID" docs/SETUP.md` returns 0 matches (all renamed)
    - `grep "PINGONE_ADMIN_CLIENT_ID" docs/SETUP.md` returns 4+ matches (in §2.2, §3, §7)
    - `grep "PINGONE_USER_CLIENT_ID" docs/SETUP.md` returns 2+ matches (in §2.3, §3)
    - `grep "MCP Token Exchanger" docs/SETUP.md` returns 1+ match (new §2.5)
    - `grep "PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID" docs/SETUP.md` returns 1+ match
    - `grep "4000/3002" docs/SETUP.md` returns 1+ match (port clarity)
    - `grep -P "banking:(read|write)[^:]" docs/SETUP.md` returns 0 matches (no stale scope names)
    - `grep "Legacy names" docs/SETUP.md` returns 1 match (fallback alias note)
    - Section numbering is sequential (2.1, 2.2, 2.3, 2.4, 2.5, 2.6)
  </acceptance_criteria>
  <done>SETUP.md uses canonical env var names, documents all 5 PingOne apps, has clear port documentation, uses consistent scope names</done>
</task>

<task type="auto">
  <name>Task 2: Verify README.md pointer to SETUP.md is accurate</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — 286 lines, verify Quick Start section)
    - docs/SETUP.md (verify the pointer target exists and section names match)
  </read_first>
  <action>
Read README.md and verify:
1. The Quick Start section contains a link to `docs/SETUP.md`
2. The Configuration section references `docs/SETUP.md` with correct section anchors (§2 and §3)
3. No stale env var names remain in README.md itself (no `PINGONE_AI_CORE_CLIENT_ID`)

If all three are already correct (research indicates they are), make NO changes — just report verified.

If any reference is stale or missing, fix it. Specifically check that no Quick Start inline instructions remain that duplicate SETUP.md content — README should be a pointer only.
  </action>
  <verify>
    <automated>grep "docs/SETUP.md" README.md &amp;&amp; grep -c "PINGONE_AI_CORE_CLIENT_ID" README.md | grep "^0$" &amp;&amp; echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "docs/SETUP.md" README.md` returns 1+ match (pointer exists)
    - `grep "PINGONE_AI_CORE_CLIENT_ID" README.md` returns 0 matches
    - README.md does not contain inline PingOne setup instructions (only the pointer)
  </acceptance_criteria>
  <done>README.md accurately points to docs/SETUP.md with no stale references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → User | Docs must not expose real secrets or credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information Disclosure) | docs/SETUP.md | mitigate | Ensure all example env var values are placeholders (no real UUIDs, secrets); verify example commands use `<your-*>` patterns |
</threat_model>

<verification>
1. `grep -c "PINGONE_AI_CORE_CLIENT_ID" docs/SETUP.md` → 0
2. `grep -c "PINGONE_ADMIN_CLIENT_ID" docs/SETUP.md` → 4+
3. `grep "MCP Token Exchanger" docs/SETUP.md` → present
4. `grep "docs/SETUP.md" README.md` → present
5. No real credentials in any .md file
</verification>

<success_criteria>
SETUP.md passes all acceptance criteria grep checks. README pointer verified. No stale env var names or scope names remain.
</success_criteria>

<output>
