---
phase: 05-user-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/Super-Banking-AuthCode-PKCE-Flow.drawio
  - docs/Super-Banking-CIBA-Flow.drawio
  - docs/Super-Banking-TokenExchange-Flow.drawio
autonomous: true
requirements:
  - DOC-02
must_haves:
  truths:
    - "All 3 draw.io files exist at docs/Super-Banking-*.drawio paths"
    - "AuthCode-PKCE diagram shows BFF as token custodian (browser never receives access_token)"
    - "CIBA diagram includes bc-authorize, polling, and device approval steps"
    - "TokenExchange diagram shows both 1-exchange and 2-exchange paths with act claim"
    - "Diagrams reference correct env var names (PINGONE_ADMIN_CLIENT_ID not AI_CORE)"
    - "Scope names in diagrams use banking:general:read (not banking:read)"
  artifacts:
    - path: "docs/Super-Banking-AuthCode-PKCE-Flow.drawio"
      provides: "Authorization Code + PKCE sequence diagram"
    - path: "docs/Super-Banking-CIBA-Flow.drawio"
      provides: "CIBA backchannel auth sequence diagram"
    - path: "docs/Super-Banking-TokenExchange-Flow.drawio"
      provides: "RFC 8693 token exchange sequence diagram (1-exchange and 2-exchange)"
  key_links:
    - from: "docs/Super-Banking-AuthCode-PKCE-Flow.drawio"
      to: "docs/ARCHITECTURE_WALKTHROUGH.md"
      via: "referenced in Flow 1 section"
      pattern: "AuthCode-PKCE-Flow"
---

<objective>
Audit the 3 existing draw.io sequence diagrams for accuracy against the current codebase.

Purpose: All 3 draw.io files exist and are substantive (10-14KB each). This plan audits them for stale naming (env vars, scopes, client IDs) and accuracy against the current token flow implementations.

Output: Corrected draw.io files with accurate labels
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-user-documentation/05-CONTEXT.md
@.planning/phases/05-user-documentation/05-RESEARCH.md

@docs/Super-Banking-AuthCode-PKCE-Flow.drawio
@docs/Super-Banking-CIBA-Flow.drawio
@docs/Super-Banking-TokenExchange-Flow.drawio
@banking_api_server/env.example
@banking_api_server/routes/oauth.js
@banking_api_server/services/agentMcpTokenService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix AuthCode-PKCE and CIBA draw.io diagrams</name>
  <files>docs/Super-Banking-AuthCode-PKCE-Flow.drawio, docs/Super-Banking-CIBA-Flow.drawio</files>
  <read_first>
    - docs/Super-Banking-AuthCode-PKCE-Flow.drawio (full XML — search for stale names)
    - docs/Super-Banking-CIBA-Flow.drawio (full XML — search for stale names)
    - banking_api_server/routes/oauth.js (lines 1-50 — actual OAuth route paths and client ID usage)
    - banking_api_server/env.example (lines 1-60 — canonical env var names)
  </read_first>
  <action>
Read both draw.io XML files and search for these specific stale patterns that need fixing:

**Pattern 1 — Client ID env var names:**
- Replace any occurrence of `PINGONE_AI_CORE_CLIENT_ID` → `PINGONE_ADMIN_CLIENT_ID`
- Replace any occurrence of `PINGONE_AI_CORE_USER_CLIENT_ID` → `PINGONE_USER_CLIENT_ID`
- Replace any occurrence of `PINGONE_AI_CORE_CLIENT_SECRET` → `PINGONE_ADMIN_CLIENT_SECRET`
- Replace any occurrence of `PINGONE_AI_CORE_USER_CLIENT_SECRET` → `PINGONE_USER_CLIENT_SECRET`

**Pattern 2 — Scope names:**
- Replace any occurrence of `banking:read` (without `general:`) → `banking:general:read`
- Replace any occurrence of `banking:write` (without `general:`) → `banking:general:write`
- Replace any occurrence of `banking:accounts:read` → `banking:general:read`
- Replace any occurrence of `banking:accounts:write` → `banking:general:write`
- Replace any occurrence of `banking:transfers:write` → `banking:general:write`

**Pattern 3 — App labels:**
- If any label says "3 PingOne apps" → update to "4-5 PingOne apps"
- Verify the Admin OIDC app label matches "Admin OIDC App" (not "AI Core")

For each file: read the XML, apply text replacements within `value="..."` attributes (draw.io stores label text in value attributes of mxCell elements), write the corrected XML back using the same formatting.

**CRITICAL:** draw.io files are XML. Make changes to text content inside `value` attributes only. Do NOT alter the XML structure, geometry, or styling. If no stale patterns are found in a file, leave it unchanged.
  </action>
  <verify>
    <automated>grep -c "AI_CORE" docs/Super-Banking-AuthCode-PKCE-Flow.drawio docs/Super-Banking-CIBA-Flow.drawio 2>/dev/null; grep -c "banking:accounts:" docs/Super-Banking-AuthCode-PKCE-Flow.drawio docs/Super-Banking-CIBA-Flow.drawio 2>/dev/null; echo "CHECK_COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AI_CORE" docs/Super-Banking-AuthCode-PKCE-Flow.drawio` returns 0 matches
    - `grep "AI_CORE" docs/Super-Banking-CIBA-Flow.drawio` returns 0 matches
    - `grep "banking:accounts:" docs/Super-Banking-AuthCode-PKCE-Flow.drawio` returns 0 matches
    - Both files remain valid XML (no broken tags)
    - File sizes within 20% of originals (no accidental content loss)
  </acceptance_criteria>
  <done>AuthCode-PKCE and CIBA draw.io diagrams use canonical env var names and consolidated scope names</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix TokenExchange draw.io diagram</name>
  <files>docs/Super-Banking-TokenExchange-Flow.drawio</files>
  <read_first>
    - docs/Super-Banking-TokenExchange-Flow.drawio (full XML — the largest at 14.5KB)
    - banking_api_server/services/agentMcpTokenService.js (lines 1-80 — actual token exchange implementation, client IDs used)
    - banking_api_server/env.example (PINGONE_MCP_TOKEN_EXCHANGER_* vars)
  </read_first>
  <action>
Read the TokenExchange draw.io XML and apply the same text replacements as Task 1:

**Pattern 1 — Client ID env var names:**
- `PINGONE_AI_CORE_CLIENT_ID` → `PINGONE_ADMIN_CLIENT_ID`
- `AGENT_OAUTH_CLIENT_ID` → verify this matches actual usage in agentMcpTokenService.js; if the service reads `PINGONE_AI_AGENT_CLIENT_ID`, update diagram labels accordingly

**Pattern 2 — Scope names:**
- `banking:read` → `banking:general:read`
- `banking:write` → `banking:general:write`
- `banking:accounts:read` → `banking:general:read`
- `banking:accounts:write` → `banking:general:write`

**Pattern 3 — Token exchanger references:**
- If the diagram references the "Admin OIDC app" as the token exchange client, verify whether it should be the MCP Token Exchanger app (`PINGONE_MCP_TOKEN_EXCHANGER_CLIENT_ID`) based on agentMcpTokenService.js
- If the actual code uses the MCP Token Exchanger for exchange (not the admin app), update the diagram actor label

**Pattern 4 — Audience values:**
- Verify the MCP audience shown matches `banking_mcp_01` or the value from `PINGONE_RESOURCE_MCP_SERVER_URI` in .env.example

Same rules as Task 1: only modify text in `value` attributes, preserve all XML structure. If no stale patterns found, leave unchanged.
  </action>
  <verify>
    <automated>grep -c "AI_CORE" docs/Super-Banking-TokenExchange-Flow.drawio 2>/dev/null; grep -c "banking:accounts:" docs/Super-Banking-TokenExchange-Flow.drawio 2>/dev/null; echo "CHECK_COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AI_CORE" docs/Super-Banking-TokenExchange-Flow.drawio` returns 0 matches
    - `grep "banking:accounts:" docs/Super-Banking-TokenExchange-Flow.drawio` returns 0 matches
    - File remains valid XML (no broken tags)
    - File size within 20% of original (~14.5KB)
    - Both 1-exchange and 2-exchange paths visible in diagram (verify by grepping for "1-exchange" or "act" in value attributes)
  </acceptance_criteria>
  <done>TokenExchange draw.io diagram uses canonical env var names, correct scope names, and accurate client references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diagrams → User | Draw.io files must not contain real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | I (Information Disclosure) | draw.io files | mitigate | Verify no real client IDs, secrets, or environment IDs appear in diagram labels |
</threat_model>

<verification>
1. All 3 draw.io files exist at `docs/Super-Banking-*.drawio` paths
2. `grep -r "AI_CORE" docs/Super-Banking-*.drawio` → 0 matches
3. `grep -r "banking:accounts:" docs/Super-Banking-*.drawio` → 0 matches
4. All files open correctly in draw.io (valid XML)
</verification>

<success_criteria>
All 3 draw.io diagrams use canonical naming conventions consistent with the updated SETUP.md and .env.example. No stale scope names or env var names remain.
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-documentation/05-02-SUMMARY.md`
</output>
