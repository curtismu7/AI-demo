---
phase: 262-css-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/agent/AgentTokens.css
  - banking_api_ui/src/components/agent/AgentLayout.css
  - banking_api_ui/src/components/agent/ChatInterface.css
  - banking_api_ui/src/components/agent/MessageBubble.js
  - banking_api_ui/src/components/agent/MessageInput.js
  - banking_api_ui/src/components/agent/ResponsiveAgentDock.js
autonomous: true
requirements: [CSS-01]

must_haves:
  truths:
    - "agent/ directory is deleted — no file remains under banking_api_ui/src/components/agent/"
    - "UI build exits 0 after deletion"
  artifacts:
    - path: "banking_api_ui/src/components/agent/"
      provides: "confirmed absent"
  key_links:
    - from: "banking_api_ui/src/components/BankingAgent.css"
      to: "--agent-panel-bottom"
      via: "var(--agent-panel-bottom, 24px)"
      pattern: "agent-panel-bottom"
---

<objective>
Delete the entire dead `agent/` component folder.

Purpose: The 6 files in `banking_api_ui/src/components/agent/` (AgentTokens.css, AgentLayout.css, ChatInterface.css, MessageBubble.js, MessageInput.js, ResponsiveAgentDock.js) are **never imported by any file outside their own directory**. CRA does not bundle them. They contain 137 `--agent-*` design system token declarations (the "146 dead vars" from the CSS audit) that are self-referential and never reach the browser.

**Do NOT touch** `--agent-panel-bottom` in App.css — that is a live variable with a different naming convention, not part of this design system.

Output: Deleted directory, build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead agent/ directory</name>
  <files>banking_api_ui/src/components/agent/ (delete entire directory)</files>
  <action>
    Delete the entire `banking_api_ui/src/components/agent/` directory.

    Verification step first — confirm nothing outside agent/ imports from it:
    ```
    grep -rn "from.*['\"].*agent/" banking_api_ui/src --include="*.js" --include="*.jsx" | grep -v "components/agent/"
    ```
    Expected: no output. If any output appears, STOP and report — do not delete.

    Then delete:
    ```
    rm -rf banking_api_ui/src/components/agent/
    ```

    **Do NOT touch:**
    - `banking_api_ui/src/App.css` line 13 (`--agent-panel-bottom`) — live variable
    - `banking_api_ui/src/components/BankingAgent.css` line 137 (`var(--agent-panel-bottom, 24px)`) — live usage
  </action>
  <verify>
    <automated>ls banking_api_ui/src/components/agent/ 2>&1 | grep "No such file" && cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Directory absent, `npm run build` exits 0 with no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File deletion | Only deletes files confirmed as unimported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-262-01 | Tampering | agent/ deletion | mitigate | Pre-flight grep confirms zero external imports before deletion |
</threat_model>

<verification>
- `ls banking_api_ui/src/components/agent/` → "No such file or directory"
- `cd banking_api_ui && npm run build` → exits 0
- `grep -rn "AgentTokens\|AgentLayout\|ChatInterface" banking_api_ui/src` → no results
</verification>

<success_criteria>
137 dead `--agent-*` CSS variable declarations removed from the bundle. Build passes. No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/262-css-hygiene/262-01-SUMMARY.md`
</output>
