---
phase: "233"
plan: "07"
type: execute
wave: 3
depends_on: ["233-03", "233-04", "233-05"]
files_modified:
  - banking_api_server/routes/admin.js
  - banking_api_ui/src/services/appEventClient.js
  - banking_api_ui/src/components/BankingAgent.js
  - banking_api_ui/src/context/TokenChainContext.js
autonomous: true
requirements:
  - D-05

user_setup: []

must_haves:
  truths:
    - "POST /api/admin/app-events route exists in admin.js, protected by authenticateToken only"
    - "Valid session + correct body returns 201 with the created event object"
    - "Missing category/severity/message returns 400 with error: missing_fields"
    - "No session or invalid token returns 401"
    - "banking_api_ui/src/services/appEventClient.js exports postAppEvent() fire-and-forget function"
    - "postAppEvent() never throws or awaits in caller context"
    - "BankingAgent.js fires agent/processing-start and agent/processing-end events around LLM invoke"
    - "TokenChainContext.js fires token_exchange/frontend-exchange-start and token_exchange/frontend-exchange-end events around RFC 8693 fetch calls"
  artifacts:
    - path: "banking_api_server/routes/admin.js"
      provides: "POST /api/admin/app-events endpoint"
      contains: "router.post.*app-events"
    - path: "banking_api_ui/src/services/appEventClient.js"
      provides: "Frontend fire-and-forget event POST helper"
      exports: ["postAppEvent"]
    - path: "banking_api_ui/src/components/BankingAgent.js"
      provides: "Agent loading-state event hooks"
      contains: "postAppEvent"
    - path: "banking_api_ui/src/context/TokenChainContext.js"
      provides: "Token exchange loading-state event hooks"
      contains: "postAppEvent"
  key_links:
    - from: "BankingAgent.js postAppEvent call"
      to: "POST /api/admin/app-events"
      via: "fetch('/api/admin/app-events') in appEventClient.js"
      pattern: "postAppEvent.*agent"
    - from: "TokenChainContext.js postAppEvent call"
      to: "POST /api/admin/app-events"
      via: "fetch('/api/admin/app-events') in appEventClient.js"
      pattern: "postAppEvent.*token_exchange"
---

<objective>
Create the POST /api/admin/app-events BFF endpoint (D-05), the frontend fire-and-forget postAppEvent() helper, and wire loading-state event hooks into BankingAgent.js and TokenChainContext.js.

Purpose: Frontend loading spinners now leave a trace in Activity Logs — developers can see when agent processing started/ended and when token exchanges were in-flight, correlated with server-side events.

Output: New admin.js POST route (session auth only); new appEventClient.js service; 4 event hook pairs in BankingAgent.js and TokenChainContext.js; npm run build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-CONTEXT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-RESEARCH.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-03-SUMMARY.md

<interfaces>
Existing GET /api/admin/app-events pattern in admin.js (line 954):

  router.get('/app-events', requireAdmin, requireScopes(['banking:admin']), (req, res) => {
    const { category, severity, limit = 100, since } = req.query;
    const events = appEventService.getEvents({ category, severity, limit: ..., since });
    res.json({ events, total: events.length, categories: ... });
  });

New POST /api/admin/app-events (D-05) — session auth only, no requireAdmin:

  const { authenticateToken } = require('../middleware/auth');
  // Note: authenticateToken is already imported in admin.js if it is; verify before adding

  router.post('/app-events', authenticateToken, (req, res) => {
    const { category, severity, message, tag, metadata } = req.body;
    if (!category || !severity || !message) {
      return res.status(400).json({ error: 'missing_fields', message: '...' });
    }
    const event = appEventService.logEvent(category, severity, message, { tag, metadata });
    res.status(201).json({ event });
  });

Frontend helper contract (D-05):

  // banking_api_ui/src/services/appEventClient.js
  async function postAppEvent(category, severity, message, options = {}) {
    try {
      await fetch('/api/admin/app-events', {
        method: 'POST',
        credentials: 'include',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ category, severity, message,
          tag: options.tag || null, metadata: options.metadata || null }),
      });
    } catch (e) {
      console.debug('[appEventClient] POST /app-events failed:', e.message);
    }
  }
  export { postAppEvent };

Loading state events (D-05):

  // BankingAgent.js — around the processAgentMessage or handleDoAction call
  postAppEvent('agent', 'info', 'Agent processing started', { tag: 'agent/processing-start', metadata: { userId } });
  // ... after response arrives:
  postAppEvent('agent', 'info', 'Agent processing complete', { tag: 'agent/processing-end', metadata: { userId } });

  // TokenChainContext.js — around the RFC 8693 fetch call
  postAppEvent('token_exchange', 'info', 'Token exchange in flight', { tag: 'token_exchange/frontend-exchange-start' });
  // ... after response:
  postAppEvent('token_exchange', 'info', 'Token exchange complete', { tag: 'token_exchange/frontend-exchange-end' });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /api/admin/app-events to admin.js</name>

  <read_first>
    - banking_api_server/routes/admin.js (read lines 1-30 for imports, then lines 945-980 for the GET /app-events handler; confirm whether authenticateToken is already imported; confirm appEventService import)
    - banking_api_server/middleware/auth.js (grep for authenticateToken export line — confirm the export name)
    - REGRESSION_PLAN.md §1 (no admin.js section; but confirm GET /app-events uses requireAdmin + requireScopes while POST uses authenticateToken only — these are different middleware, do not conflate)
  </read_first>

  <files>banking_api_server/routes/admin.js</files>

  <action>
1. Check the imports at the top of admin.js. The file already imports { requireAdmin, requireScopes } from '../middleware/auth'. If authenticateToken is not yet imported, add it to that destructure:

   Current (approximate):
   const { requireAdmin, requireScopes } = require('../middleware/auth');

   Update to:
   const { requireAdmin, requireScopes, authenticateToken } = require('../middleware/auth');

2. Find the GET /app-events handler (around line 954). Add the POST handler IMMEDIATELY AFTER the GET handler closes (after the closing `});` of the GET route):

   /**
    * POST /api/admin/app-events — Accept a frontend-emitted app event
    * Auth: authenticateToken only (valid session, any role) — per D-05
    * Body: { category, severity, message, tag?, metadata? }
    */
   router.post('/app-events', authenticateToken, (req, res) => {
     try {
       const { category, severity, message, tag, metadata } = req.body;

       if (!category || !severity || !message) {
         return res.status(400).json({
           error: 'missing_fields',
           message: 'category, severity, and message are required',
         });
       }

       const event = appEventService.logEvent(category, severity, message, { tag, metadata });
       res.status(201).json({ event });
     } catch (error) {
       console.error('[admin] POST /app-events error:', error);
       res.status(500).json({ error: 'internal_server_error' });
     }
   });

3. Do NOT change any other routes in admin.js. The GET /app-events handler uses requireAdmin + requireScopes (admin-only visibility). The POST handler uses only authenticateToken (any authenticated user can post events for their own session).
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./routes/admin')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "router.post.*app-events" banking_api_server/routes/admin.js returns 1
    - grep -c "authenticateToken" banking_api_server/routes/admin.js returns at least 1
    - grep "requireAdmin" banking_api_server/routes/admin.js near "app-events" — POST route must NOT use requireAdmin
    - grep -c "missing_fields" banking_api_server/routes/admin.js returns 1
    - node -e "require('./routes/admin')" exits 0
  </acceptance_criteria>

  <done>POST /api/admin/app-events handler in admin.js uses authenticateToken only, validates required fields, returns 201 + event on success, 400 on missing fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create appEventClient.js and wire loading-state hooks into BankingAgent.js and TokenChainContext.js</name>

  <read_first>
    - banking_api_ui/src/services/ directory listing (confirm appEventClient.js does not exist; identify existing service files for import pattern reference)
    - banking_api_ui/src/components/BankingAgent.js (read lines 1-50 for imports; search for the handleDoAction or processAgentMessage call — find the exact async operation that shows the loading spinner to identify where start/end hooks should go; also check for userId variable availability)
    - banking_api_ui/src/context/TokenChainContext.js (read fully or search for RFC 8693 fetch calls — find the token exchange async operation that creates the in-flight state; identify where start/end hooks should go)
    - REGRESSION_PLAN.md §1 (BankingAgent.js — agent FAB visibility, HITL consent gate, consentBlocked state — do NOT modify any state that controls agent visibility or consent; only add postAppEvent calls around the existing async op)
  </read_first>

  <files>banking_api_ui/src/services/appEventClient.js, banking_api_ui/src/components/BankingAgent.js, banking_api_ui/src/context/TokenChainContext.js</files>

  <action>
**Step 1 — Create banking_api_ui/src/services/appEventClient.js:**

  /**
   * appEventClient.js — Fire-and-forget frontend event POST to BFF
   *
   * Posts app events to POST /api/admin/app-events without blocking UI.
   * Silently fails if the endpoint is unavailable or user is not authenticated.
   * Never throws. Never awaits in caller context (fire-and-forget).
   */

  /**
   * Post an app event to the BFF activity log.
   * Non-blocking — do not await this function. Errors are silently swallowed.
   *
   * @param {string} category - Event category (must match appEventService EVENT_CATEGORIES)
   * @param {string} severity - 'info' | 'warning' | 'error'
   * @param {string} message - Human-readable event message
   * @param {object} [options] - Optional tag and metadata
   * @param {string} [options.tag] - Dot-path tag (e.g. 'agent/processing-start')
   * @param {object} [options.metadata] - Structured metadata (no secrets)
   */
  async function postAppEvent(category, severity, message, options = {}) {
    try {
      await fetch('/api/admin/app-events', {
        method: 'POST',
        credentials: 'include',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          category,
          severity,
          message,
          tag: options.tag || null,
          metadata: options.metadata || null,
        }),
      });
    } catch (e) {
      // Silently fail — logging infrastructure must never block UX
      console.debug('[appEventClient] POST /app-events failed:', e.message);
    }
  }

  export { postAppEvent };

**Step 2 — Wire into BankingAgent.js:**

Read the file to find the exact async operation around agent message submission. Find:
- The function that calls the BFF agent endpoint (likely a fetch to /api/agent/message or similar)
- Where the loading/spinner state is set to true (setIsLoading or similar)
- Where the loading state is set back to false (in finally or after await)

Add the import at the top of BankingAgent.js (with other imports):
  import { postAppEvent } from '../services/appEventClient';

Add event calls around the agent fetch — find the userId or user.id variable in scope:

  // Before the agent API call (where loading spinner starts):
  postAppEvent('agent', 'info', 'Agent processing started', {
    tag: 'agent/processing-start',
    metadata: { userId: user?.id || user?.sub },
  });

  // After the agent API call returns (where loading spinner stops — in try or finally):
  postAppEvent('agent', 'info', 'Agent processing complete', {
    tag: 'agent/processing-end',
    metadata: { userId: user?.id || user?.sub },
  });

Use the actual variable names found by reading BankingAgent.js. Do NOT add postAppEvent inside error handling that might cause double-firing. Place start before the await, end in the success path only (not finally, to avoid duplicate end events if both success and error paths fire).

**Step 3 — Wire into TokenChainContext.js:**

Read the file to find the RFC 8693 token exchange fetch call. Find where the exchange is triggered and where its result is processed.

Add the import at the top:
  import { postAppEvent } from '../services/appEventClient';

Add event calls around the exchange fetch:

  // Before the exchange fetch:
  postAppEvent('token_exchange', 'info', 'Token exchange in flight', {
    tag: 'token_exchange/frontend-exchange-start',
  });

  // After the exchange fetch succeeds:
  postAppEvent('token_exchange', 'info', 'Token exchange complete', {
    tag: 'token_exchange/frontend-exchange-end',
  });

Verify that postAppEvent is called (not awaited) at these sites — the calls should be fire-and-forget. The correct pattern is:
  postAppEvent(...)  // no await
  const result = await actualExchangeFetch(...)
  postAppEvent(...)  // no await
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File banking_api_ui/src/services/appEventClient.js exists
    - grep -c "export.*postAppEvent" banking_api_ui/src/services/appEventClient.js returns 1
    - grep -c "postAppEvent" banking_api_ui/src/components/BankingAgent.js returns at least 2 (start + end)
    - grep -c "postAppEvent" banking_api_ui/src/context/TokenChainContext.js returns at least 2 (start + end)
    - grep "await postAppEvent" banking_api_ui/src/components/BankingAgent.js returns 0 (fire-and-forget — no await)
    - grep "await postAppEvent" banking_api_ui/src/context/TokenChainContext.js returns 0
    - npm run build in banking_api_ui exits 0
  </acceptance_criteria>

  <done>appEventClient.js created with fire-and-forget postAppEvent(). BankingAgent.js and TokenChainContext.js each have start/end event pairs around their async operations. Build passes. No await on postAppEvent calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend SPA → POST /api/admin/app-events | Untrusted user input (category, severity, message, metadata) crosses to BFF |
| BFF → appEventService | Validated but unescaped metadata written to NDJSON ring buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-07-A | Spoofing | Frontend sends arbitrary category/severity/message | accept | Demo app; POST is session-gated (authenticateToken); caller must have a valid session cookie; events are logged but not used for access decisions |
| T-233-07-B | Tampering | Frontend injects malicious metadata into NDJSON | accept | Metadata is rendered via JSON.stringify in ActivityLogs.js — no eval, no dangerouslySetInnerHTML; logged data is display-only |
| T-233-07-C | Denial of Service | Rapid frontend POST flooding ring buffer | mitigate | MAX_EVENTS = 200 (Plan 01); oldest events evicted; NDJSON unbounded growth is out-of-scope per deferred decisions; rate limiting not in scope for this demo |
| T-233-07-D | Elevation of Privilege | POST uses authenticateToken not requireAdmin | accept | Intentional per D-05 — users log their own loading-state events; they cannot read GET /app-events (that requires requireAdmin); write-only for non-admins |
| T-233-07-E | Information Disclosure | appEventClient.js console.debug on failure | accept | console.debug is only visible in browser devtools; not exposed to end user; silently fails |
</threat_model>

<verification>
1. grep "router.post.*app-events" banking_api_server/routes/admin.js — 1 match
2. grep "authenticateToken" banking_api_server/routes/admin.js — present in POST route, not requireAdmin
3. grep "export.*postAppEvent" banking_api_ui/src/services/appEventClient.js — 1 match
4. grep "await postAppEvent" banking_api_ui/src/components/BankingAgent.js — 0 matches (fire-and-forget)
5. npm run build in banking_api_ui exits 0
</verification>

<success_criteria>
- POST /api/admin/app-events in admin.js: 201 on success, 400 on missing fields, 401 on unauthenticated
- appEventClient.js exports postAppEvent() that never throws and never blocks caller
- BankingAgent.js has agent/processing-start and agent/processing-end event pairs
- TokenChainContext.js has token_exchange/frontend-exchange-start and end event pairs
- No await on postAppEvent() at any call site
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-07-SUMMARY.md`
</output>
