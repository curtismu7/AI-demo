---
phase: "233"
plan: "03"
type: execute
wave: 2
depends_on: ["233-02"]
files_modified:
  - banking_api_server/routes/oauth.js
  - banking_api_server/services/cibaService.js
  - banking_api_server/services/agentMcpTokenService.js
  - banking_api_server/services/agentTokenService.js
autonomous: true
requirements:
  - D-02

must_haves:
  truths:
    - "oauth/callback-success events include jwtFullDecode of the ID token in metadata"
    - "ciba/initiated events include jwtFullDecode of the access token from CIBA token poll"
    - "agentMcpTokenService rfc8693-success events include jwtFullDecode of the exchanged MCP token"
    - "agentTokenService agent-token-valid events include jwtFullDecode of the agent actor token"
    - "No raw token strings appear in any logEvent metadata"
    - "All decodes use decodeJwt from utils/tokenUtils — no duplicated decode logic"
  artifacts:
    - path: "banking_api_server/routes/oauth.js"
      provides: "OAuth callback success enriched with idToken decode"
      contains: "jwtFullDecode"
    - path: "banking_api_server/services/cibaService.js"
      provides: "CIBA initiated enriched with token decode"
      contains: "jwtFullDecode"
    - path: "banking_api_server/services/agentMcpTokenService.js"
      provides: "RFC 8693 success enriched with exchanged token decode"
      contains: "jwtFullDecode"
    - path: "banking_api_server/services/agentTokenService.js"
      provides: "Agent actor token valid event enriched with decode"
      contains: "jwtFullDecode"
  key_links:
    - from: "all four files"
      to: "banking_api_server/utils/tokenUtils.js"
      via: "require('../utils/tokenUtils') or require('./utils/tokenUtils')"
      pattern: "decodeJwt"
---

<objective>
Add jwtFullDecode: { header, claims } to the logEvent metadata at all four Phase 232 token call sites: OAuth callback (ID token), CIBA token poll (access token), RFC 8693 exchange (MCP token), and agent actor token validation.

Purpose: This is the highest-visibility enrichment — every token flow now surfaces full JWT payload data in Activity Logs without raw token strings. Matches the existing jwtFullDecode shape consumed by TokenChainDisplay.js.

Output: Four files enriched. Each logEvent that has a token available passes jwtFullDecode: decodeJwt(token) || undefined in its metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-CONTEXT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-RESEARCH.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-02-SUMMARY.md

<interfaces>
From banking_api_server/utils/tokenUtils.js (created in Plan 02):

  const { decodeJwt } = require('../utils/tokenUtils');
  // or for routes/: require('../utils/tokenUtils')  (same relative path from routes/)
  // decodeJwt(token) -> { header, claims } | null

Shape to add in metadata (D-02):

  metadata: {
    jwtFullDecode: decodeJwt(someToken) || undefined,
    // ... existing metadata fields preserved
  }

Existing logEvent calls per Phase 232 SUMMARY (call sites to enrich):

  // oauth.js line 338 — has oauthTokens.idToken available
  logAppEvent('oauth', 'info', 'OAuth admin login success: ...',
    { tag: 'oauth/callback-success', username: ...,
      metadata: { role, clientType, hasIdToken: !!(oauthTokens?.idToken) } });

  // cibaService.js line 112 — has auth_req_id; tokens available after pollForTokens() resolves
  logAppEvent('auth_lifecycle', 'info', 'CIBA: auth request accepted — auth_req_id received',
    { tag: 'ciba/initiated', metadata: { expiresIn, interval, deliveryMode } });

  // agentMcpTokenService.js — rfc8693-success; exchanged token is available as local var
  logAppEvent('token_exchange', 'info', 'RFC 8693 exchange successful', ...);

  // agentTokenService.js line 60 — actorToken is available in scope
  logAppEvent('token_exchange', 'info', 'Agent actor token validated successfully',
    { tag: 'token_exchange/agent-token-valid', metadata: { actorId, subject, scopeCount } });
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich oauth.js and agentTokenService.js with jwtFullDecode</name>

  <read_first>
    - banking_api_server/routes/oauth.js (read lines 1-30 for imports, then lines 320-360 for the callback-success logAppEvent call; confirm oauthTokens.idToken is in scope at that point)
    - banking_api_server/services/agentTokenService.js (read fully — locate the validateAgentActorToken function, find the actor token variable name, and find logAppEvent lines 60-70)
    - REGRESSION_PLAN.md §1 (OAuth admin login and session persistence sections — changes must not affect session.save() call or redirect logic)
  </read_first>

  <files>banking_api_server/routes/oauth.js, banking_api_server/services/agentTokenService.js</files>

  <action>
**oauth.js:**

1. Add import at the top of the file with other requires (after the existing logAppEvent import on line 21):
   const { decodeJwt } = require('../utils/tokenUtils');

2. Find the callback-success logAppEvent call (currently around line 338):
   logAppEvent('oauth', 'info', `OAuth admin login success: ${authedUser?.username || authedUser?.email || 'unknown'}`,
     { tag: 'oauth/callback-success', username: authedUser?.username,
       metadata: { role: authedUser?.role, clientType, hasIdToken: !!(oauthTokens?.idToken) } });

   Replace with:
   const _idTokenDecoded = decodeJwt(oauthTokens?.idToken);
   logAppEvent('oauth', 'info', `OAuth admin login success: ${authedUser?.username || authedUser?.email || 'unknown'}`,
     { tag: 'oauth/callback-success', username: authedUser?.username,
       metadata: {
         role: authedUser?.role,
         clientType,
         hasIdToken: !!(oauthTokens?.idToken),
         jwtFullDecode: _idTokenDecoded || undefined,
       } });

   Place the decodeJwt call on the line BEFORE the logAppEvent call. Do not restructure the surrounding session.save() callback or redirect logic.

**agentTokenService.js:**

1. Add import — check if '../utils/tokenUtils' is already imported (it won't be after Plan 02). Add:
   const { decodeJwt } = require('../utils/tokenUtils');

2. Find the agent-token-valid logAppEvent (around line 60):
   logAppEvent('token_exchange', 'info', 'Agent actor token validated successfully',
     { tag: 'token_exchange/agent-token-valid', metadata: { actorId, subject, scopeCount } });

   Identify the variable name holding the actor token at that call site (read the function to find it — it is likely `token` or `actorToken` passed to validateAgentActorToken). Add decode and enrich:
   const _actorDecoded = decodeJwt(token);   // use the correct local variable name after reading
   logAppEvent('token_exchange', 'info', 'Agent actor token validated successfully',
     { tag: 'token_exchange/agent-token-valid',
       metadata: { actorId, subject, scopeCount, jwtFullDecode: _actorDecoded || undefined } });
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./routes/oauth')" && node -e "require('./services/agentTokenService')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "jwtFullDecode" banking_api_server/routes/oauth.js returns 1
    - grep -c "jwtFullDecode" banking_api_server/services/agentTokenService.js returns 1
    - grep -c "require.*utils/tokenUtils" banking_api_server/routes/oauth.js returns 1
    - grep -c "require.*utils/tokenUtils" banking_api_server/services/agentTokenService.js returns 1
    - node -e "require('./routes/oauth')" exits 0
    - node -e "require('./services/agentTokenService')" exits 0
    - No raw token strings passed to jwtFullDecode result stored in metadata (decodeJwt result only)
  </acceptance_criteria>

  <done>oauth.js callback-success and agentTokenService.js agent-token-valid events carry jwtFullDecode in metadata. Both modules load cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich cibaService.js and agentMcpTokenService.js with jwtFullDecode</name>

  <read_first>
    - banking_api_server/services/cibaService.js (read fully — locate all logAppEvent calls; find where waitForApproval() returns tokens and where those tokens could be decoded; the ciba/initiated event fires before tokens arrive, so jwtFullDecode goes on a different event or requires restructure — read carefully before deciding)
    - banking_api_server/services/agentMcpTokenService.js (read from line 300 onward — find the rfc8693-success logAppEvent call and the local variable holding the exchanged token at that point)
    - REGRESSION_PLAN.md §1 (ff_inject_may_act section — do not modify injection logic in agentMcpTokenService)
  </read_first>

  <files>banking_api_server/services/cibaService.js, banking_api_server/services/agentMcpTokenService.js</files>

  <action>
**cibaService.js:**

1. Add import at top:
   const { decodeJwt } = require('../utils/tokenUtils');

2. The CIBA flow: initiateCiba() fires ciba/initiate and ciba/initiated (no tokens yet).
   Tokens arrive when waitForApproval() resolves in cibaEnhanced.js or cibaService.js caller.

   Find where logAppEvent fires with the CIBA token result. If cibaService.js itself calls logAppEvent after polling returns tokens (check waitForApproval and pollForTokens), add jwtFullDecode there.

   If the token-bearing logAppEvent is in cibaEnhanced.js (not cibaService.js), add the enrich there instead — read cibaEnhanced.js to confirm.

   For the ciba/initiated event (no tokens), add the D-07 fields (authReqId_length, expiresIn, interval) to the existing metadata:
   logAppEvent('auth_lifecycle', 'info', 'CIBA: auth request accepted — auth_req_id received',
     { tag: 'ciba/initiated',
       metadata: {
         expiresIn: expires_in,
         interval,
         deliveryMode,
         authReqId_length: auth_req_id ? auth_req_id.length : 0,
       } });

   Note: authReqId_length satisfies D-07 (CIBA auth_req_id length, not the raw value).

3. If tokens (access_token, id_token) become available after waitForApproval() in a logAppEvent call:
   const _cibaTokenDecoded = decodeJwt(tokens?.access_token);
   logAppEvent('auth_lifecycle', 'info', 'CIBA: tokens received',
     { tag: 'ciba/tokens-received',
       metadata: { jwtFullDecode: _cibaTokenDecoded || undefined } });

   Only add this event if a logAppEvent at token-receipt exists or makes sense — do NOT add a new logEvent category that has no icon in ActivityLogs.js.

**agentMcpTokenService.js:**

1. The decodeJwt import was added in Plan 02.

2. Find the rfc8693-success logAppEvent. Read the function that performs RFC 8693 exchange to locate the exchanged token variable. The call should look like:
   logAppEvent('token_exchange', 'info', 'RFC 8693 exchange successful', ...);
   or similar (search for 'rfc8693-success' tag in the file).

3. Decode the exchanged token and add to metadata:
   const _exchangedDecoded = decodeJwt(exchangedToken);   // use the actual variable name
   // In the existing logAppEvent call, add to metadata:
   metadata: {
     // ... existing fields (aud, scope, etc.)
     jwtFullDecode: _exchangedDecoded || undefined,
   }

   Do not add jwtFullDecode to the rfc8693-error event — no valid token available there.
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./services/cibaService')" && node -e "require('./services/agentMcpTokenService')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "jwtFullDecode" banking_api_server/services/cibaService.js returns at least 1
    - grep -c "jwtFullDecode" banking_api_server/services/agentMcpTokenService.js returns at least 1
    - grep -c "authReqId_length" banking_api_server/services/cibaService.js returns 1
    - grep -c "require.*utils/tokenUtils" banking_api_server/services/cibaService.js returns 1
    - node -e "require('./services/cibaService')" exits 0
    - node -e "require('./services/agentMcpTokenService')" exits 0
    - grep "access_token\|id_token\|refresh_token" emitted into metadata — must be 0 (no raw token strings)
  </acceptance_criteria>

  <done>cibaService.js ciba/initiated has authReqId_length; token-bearing events have jwtFullDecode. agentMcpTokenService.js rfc8693-success has jwtFullDecode. Both modules load cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF service → appEventService | Decoded JWT claims written to NDJSON; must not include raw token strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-03-A | Information Disclosure | jwtFullDecode in NDJSON log | mitigate | decodeJwt() returns only decoded header+claims, never the raw token string; call sites use decodeJwt(token) result only, not the token variable itself |
| T-233-03-B | Information Disclosure | idTokenDecoded.claims may contain PII (email, name) | accept | Demo app; logs are server-side NDJSON only; Activity Logs UI is admin-gated (requireAdmin); PII is the educational value of this feature |
| T-233-03-C | Tampering | OAuth session.save() callback alignment | mitigate | decodeJwt call placed BEFORE logAppEvent, BEFORE session.save() — no restructuring of callback chain; regression guard verified |
</threat_model>

<verification>
1. grep "jwtFullDecode" banking_api_server/routes/oauth.js — 1 match
2. grep "jwtFullDecode" banking_api_server/services/agentTokenService.js — 1 match
3. grep "jwtFullDecode" banking_api_server/services/cibaService.js — at least 1 match
4. grep "jwtFullDecode" banking_api_server/services/agentMcpTokenService.js — at least 1 match
5. node -e "require('./routes/oauth'); require('./services/cibaService'); require('./services/agentTokenService'); require('./services/agentMcpTokenService')" exits 0
</verification>

<success_criteria>
- All four token call sites carry jwtFullDecode: { header, claims } in logEvent metadata
- No raw token strings appear in any metadata object
- All four modules load without errors
- authReqId_length added to ciba/initiated metadata (D-07 partial)
</success_criteria>

<output>
After completion, create `.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-03-SUMMARY.md`
</output>
