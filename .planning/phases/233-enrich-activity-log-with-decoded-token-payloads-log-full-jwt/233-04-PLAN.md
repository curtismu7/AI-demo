---
phase: "233"
plan: "04"
type: execute
wave: 2
depends_on: ["233-02"]
files_modified:
  - banking_api_server/services/agentMcpTokenService.js
  - banking_api_server/services/cibaService.js
  - banking_api_server/routes/oauth.js
autonomous: true
requirements:
  - D-06

must_haves:
  truths:
    - "logEvent metadata for RFC 8693 exchange includes request.grantType, request.audience, response.aud, response.scope"
    - "logEvent metadata for CIBA initiate includes request.scope, request.deliveryMode"
    - "logEvent metadata for OAuth token exchange includes response.hasAccessToken, response.hasIdToken"
    - "No metadata field contains access_token, id_token, refresh_token, or client_secret values"
    - "sanitizePingOneResponse helper strips all raw token fields before any logEvent call"
  artifacts:
    - path: "banking_api_server/utils/tokenUtils.js"
      provides: "sanitizePingOneResponse helper added alongside decodeJwt"
      contains: "sanitizePingOneResponse"
    - path: "banking_api_server/services/agentMcpTokenService.js"
      provides: "RFC 8693 request and response body metadata"
      contains: "metadata.request"
    - path: "banking_api_server/services/cibaService.js"
      provides: "CIBA initiate request metadata"
      contains: "metadata.request"
  key_links:
    - from: "agentMcpTokenService.js logEvent"
      to: "sanitizePingOneResponse in tokenUtils.js"
      via: "require('../utils/tokenUtils')"
      pattern: "sanitizePingOneResponse"
---

<objective>
Add PingOne API request/response body capture to logEvent metadata at BFF call sites that invoke PingOne APIs, following the existing _p1ReqDebug pattern from pingoneTestRoutes.js.

Purpose: ActivityLogs.js shows what the BFF sent to PingOne and what PingOne returned — without leaking raw token strings. Educational value: developers can see the exact RFC 8693 parameters and PingOne's response claims.

Output: sanitizePingOneResponse() added to tokenUtils.js; agentMcpTokenService.js and cibaService.js logEvent calls enriched with request/response metadata; oauth.js token exchange enriched with response metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-CONTEXT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-RESEARCH.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-02-SUMMARY.md

<interfaces>
Existing _p1ReqDebug pattern in pingoneTestRoutes.js (reference — replicate this shape):

  function _p1ReqDebug(method, url, contentType, body) {
    return { method, url, contentType, body };
  }
  // Usage: metadata: { request: _p1ReqDebug('POST', endpoint, 'application/x-www-form-urlencoded', safeParams) }

D-06 sanitization rule — strip from any body before logging:
  - access_token, id_token, refresh_token, client_secret

Shape to add in metadata:
  metadata: {
    request: { grantType, audience, scope, ... },    // params sent to PingOne (no secrets)
    response: { aud, scope, tokenType, hasActClaim }, // sanitized response (no raw tokens)
    // ... existing fields (jwtFullDecode from Plan 03, etc.)
  }

sanitizePingOneResponse (to add to tokenUtils.js):
  function sanitizePingOneResponse(body) {
    if (!body || typeof body !== 'object') { return {}; }
    const { access_token, id_token, refresh_token, client_secret, ...safe } = body;
    return safe;
  }
  module.exports = { decodeJwt, sanitizePingOneResponse };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sanitizePingOneResponse to tokenUtils.js</name>

  <read_first>
    - banking_api_server/utils/tokenUtils.js (read fully — add sanitizePingOneResponse alongside decodeJwt; confirm module.exports line)
    - banking_api_server/routes/pingoneTestRoutes.js (scan for _p1ReqDebug pattern to confirm the shape being mirrored — search for the function name)
  </read_first>

  <files>banking_api_server/utils/tokenUtils.js</files>

  <action>
Add sanitizePingOneResponse function to the existing tokenUtils.js file, after the decodeJwt function and before module.exports:

  /**
   * Sanitize a PingOne API response body for safe logging.
   * Strips raw token strings and client secrets. Keeps status, claims summary, error codes.
   * Use before passing any PingOne response body to logEvent metadata.
   *
   * @param {object} body - PingOne response body
   * @returns {object} Safe object with token fields removed
   */
  function sanitizePingOneResponse(body) {
    if (!body || typeof body !== 'object') { return {}; }
    // Destructure to strip sensitive fields; keep everything else
    const {
      access_token,   // eslint-disable-line no-unused-vars
      id_token,       // eslint-disable-line no-unused-vars
      refresh_token,  // eslint-disable-line no-unused-vars
      client_secret,  // eslint-disable-line no-unused-vars
      ...safe
    } = body;
    return safe;
  }

Update module.exports to export both functions:
  module.exports = { decodeJwt, sanitizePingOneResponse };
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "const t=require('./utils/tokenUtils'); const r=t.sanitizePingOneResponse({access_token:'secret',aud:'api',scope:'read'}); console.log('has aud:', !!r.aud); console.log('no token:', !r.access_token); if(!r.aud || r.access_token) process.exit(1); console.log('PASS');"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "sanitizePingOneResponse" banking_api_server/utils/tokenUtils.js returns at least 2 (definition + export)
    - node -e "require('./utils/tokenUtils').sanitizePingOneResponse({access_token:'x',aud:'a'})" output contains no access_token key
    - node -e "require('./utils/tokenUtils')" exits 0
    - grep "module.exports" banking_api_server/utils/tokenUtils.js shows both decodeJwt and sanitizePingOneResponse exported
  </acceptance_criteria>

  <done>tokenUtils.js exports sanitizePingOneResponse that strips access_token, id_token, refresh_token, client_secret from any response body object.</done>
</task>

<task type="auto">
  <name>Task 2: Add request/response metadata to agentMcpTokenService.js RFC 8693 and cibaService.js logEvent calls</name>

  <read_first>
    - banking_api_server/services/agentMcpTokenService.js (read lines 200-400 — find the axios POST for RFC 8693 exchange; identify local variables holding: grant_type, subject_token_type, actor_token_type, resource/audience, scope, and the raw response data)
    - banking_api_server/services/cibaService.js (read fully — find the axios.post to the bc_authorize endpoint; identify params.toString() or the URLSearchParams object; find the response.data object)
    - banking_api_server/routes/oauth.js (read lines 210-250 — find exchangeCodeForToken call and what tokenData contains)
  </read_first>

  <files>banking_api_server/services/agentMcpTokenService.js, banking_api_server/services/cibaService.js, banking_api_server/routes/oauth.js</files>

  <action>
**agentMcpTokenService.js — RFC 8693 logEvent enrichment:**

1. Update the import line (already has decodeJwt from Plan 02-03) to also import sanitizePingOneResponse:
   const { decodeJwt, sanitizePingOneResponse } = require('../utils/tokenUtils');

2. Find the rfc8693-success logAppEvent (tag: 'token_exchange/rfc8693-success' or similar). Before that call, capture the request params and sanitize the response:

   // Capture request params for logging (D-06)
   const _rfc8693Request = {
     grantType: 'urn:ietf:params:oauth:grant-type:token-exchange',
     subjectTokenType: 'urn:ietf:params:oauth:token-type:access_token',
     requestedTokenType: 'urn:ietf:params:oauth:token-type:access_token',
     audience: audienceValue,   // the actual local variable holding the MCP audience
     scope: scopeValue,         // actual local variable, or undefined if not set
   };
   const _rfc8693Response = sanitizePingOneResponse(exchangeResponseData);  // raw axios response.data

   Then in the logAppEvent metadata add:
   metadata: {
     request: _rfc8693Request,
     response: {
       aud: _rfc8693Response.aud,
       scope: _rfc8693Response.scope,
       token_type: _rfc8693Response.token_type,
       expires_in: _rfc8693Response.expires_in,
       hasActClaim: !!(_exchangedDecoded?.claims?.act),
     },
     jwtFullDecode: _exchangedDecoded || undefined,   // from Plan 03
     // ... any other existing fields
   }

   Use actual local variable names found by reading the file — do not guess names.

**cibaService.js — ciba/initiate enrichment:**

Update the ciba/initiate logAppEvent (line 100-101) to include request details:
   logAppEvent('auth_lifecycle', 'info', `CIBA: initiating backchannel auth for ${loginHint}`,
     { tag: 'ciba/initiate',
       metadata: {
         loginHint,
         scope,
         deliveryMode,
         hasAcrValues: !!(acrValues),
         request: {
           scope,
           deliveryMode,
           loginHintType: typeof loginHint,
         },
       } });

**oauth.js — callback-success response metadata:**

Update the oauth/callback-success logAppEvent metadata (from Plan 03) to add a response summary:
   metadata: {
     role: authedUser?.role,
     clientType,
     hasIdToken: !!(oauthTokens?.idToken),
     jwtFullDecode: _idTokenDecoded || undefined,
     response: {
       hasAccessToken: !!(oauthTokens?.accessToken),
       hasIdToken: !!(oauthTokens?.idToken),
       tokenType: oauthTokens?.tokenType || 'Bearer',
     },
   }

No sanitizePingOneResponse call needed here — the individual boolean flags replace raw body logging.
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./services/agentMcpTokenService')" && node -e "require('./services/cibaService')" && node -e "require('./routes/oauth')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "sanitizePingOneResponse" banking_api_server/services/agentMcpTokenService.js returns 1
    - grep -c "metadata.request\|request:" banking_api_server/services/cibaService.js returns at least 1
    - grep -c "response:" banking_api_server/routes/oauth.js returns at least 1 (in the callback-success metadata)
    - node -e "require('./services/agentMcpTokenService')" exits 0
    - node -e "require('./services/cibaService')" exits 0
    - node -e "require('./routes/oauth')" exits 0
    - grep "\"access_token\"\|'access_token'" banking_api_server/services/agentMcpTokenService.js — no occurrences in logAppEvent calls (only in exchange params which are sent to PingOne, not logged)
  </acceptance_criteria>

  <done>agentMcpTokenService.js RFC 8693 events carry sanitized request+response metadata. cibaService.js ciba/initiate carries request metadata. oauth.js callback-success carries response summary. No raw token strings in any metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PingOne API response → NDJSON log | Response body contains raw tokens; must be sanitized before logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-04-A | Information Disclosure | PingOne response body logged to NDJSON | mitigate | sanitizePingOneResponse() strips access_token, id_token, refresh_token, client_secret before any logEvent call; verified by grep on committed code |
| T-233-04-B | Information Disclosure | CIBA loginHint in log metadata | accept | Demo app; loginHint is a user identifier (email/phone) already known to admin; logs are admin-gated |
| T-233-04-C | Tampering | tokenUtils.js growth (two exports) | accept | tokenUtils.js has no imports; adding sanitizePingOneResponse is purely additive; circular dep risk remains zero |
</threat_model>

<verification>
1. grep "sanitizePingOneResponse" banking_api_server/utils/tokenUtils.js — at least 2 matches
2. grep "sanitizePingOneResponse" banking_api_server/services/agentMcpTokenService.js — 1 match
3. node -e "const t=require('./utils/tokenUtils'); const r=t.sanitizePingOneResponse({access_token:'x',aud:'a'}); if(r.access_token) process.exit(1)" exits 0
4. All three modules load cleanly
</verification>

<success_criteria>
- sanitizePingOneResponse in tokenUtils.js strips raw token fields
- RFC 8693 exchange events include sanitized request + response metadata
- CIBA initiate events include request metadata
- OAuth callback-success includes response summary
- No raw token strings in any logEvent metadata
</success_criteria>

<output>
After completion, create `.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-04-SUMMARY.md`
</output>
