---
phase: "233"
plan: "06"
type: execute
wave: 3
depends_on: ["233-03"]
files_modified:
  - banking_api_server/routes/oauth.js
  - banking_api_server/services/cibaService.js
autonomous: true
requirements:
  - D-07

must_haves:
  truths:
    - "oauth/callback-success metadata includes pkce.code_challenge_method and pkce.code_challenge_length"
    - "oauth/callback-success metadata includes idTokenClaims.sub, idTokenClaims.email, idTokenClaims.acr"
    - "ciba/initiate metadata includes bindingMessage, scope, deliveryMode (already partially there from Plan 03)"
    - "ciba/initiated metadata includes authReqId_length, expiresIn, interval (authReqId_length from Plan 03)"
    - "auth_lifecycle/session-snapshot events fire at login with sessionId_hash (8-char SHA-256 prefix), role, token presence flags"
    - "auth_lifecycle/session-snapshot events fire at logout with the same sessionId_hash and cleared token flags"
    - "No raw session ID values appear in any logEvent metadata"
  artifacts:
    - path: "banking_api_server/routes/oauth.js"
      provides: "PKCE metadata, idTokenClaims, and session-snapshot events at login and logout"
      contains: "session-snapshot"
    - path: "banking_api_server/services/cibaService.js"
      provides: "CIBA binding message metadata in ciba/initiate event"
      contains: "bindingMessage"
  key_links:
    - from: "oauth.js session-snapshot logEvent"
      to: "crypto.createHash"
      via: "SHA-256 hash of req.session.id sliced to 8 chars"
      pattern: "createHash.*sha256.*session"
---

<objective>
Embed PKCE details and session snapshot events in OAuth lifecycle logEvent calls; add CIBA binding message to ciba/initiate metadata.

Purpose: Completes D-07. Developers can see PKCE parameters, ID token claims, and session state transitions in Activity Logs without any raw session IDs or token strings.

Output: oauth.js callback-success enriched with PKCE and idTokenClaims; session-snapshot events at login and logout; cibaService.js ciba/initiate enriched with bindingMessage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-CONTEXT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-RESEARCH.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-03-SUMMARY.md

<interfaces>
crypto already imported in oauth.js (line 2: const crypto = require('crypto')).

Session ID hashing pattern (D-07):

  const sessionHash = crypto.createHash('sha256').update(req.session.id || '').digest('hex').slice(0, 8);

Session-snapshot event shape (D-07):

  logAppEvent('auth_lifecycle', 'info', 'Session snapshot: login', {
    tag: 'auth_lifecycle/session-snapshot',
    metadata: {
      sessionId_hash: sessionHash,
      event: 'login',
      role: authedUser?.role,
      hasAccessToken: !!(req.session.oauthTokens?.accessToken),
      hasIdToken: !!(req.session.oauthTokens?.idToken),
      hasRefreshToken: !!(req.session.oauthTokens?.refreshToken),
    },
  });

PKCE metadata shape (D-07):

  pkce: {
    code_challenge_method: 'S256',          // always S256 in this app
    code_challenge_length: codeVerifier ? 43 : 0,   // PKCE code_verifier is 43 chars
  }

idTokenClaims shape (D-07):

  idTokenClaims: {
    sub: _idTokenDecoded?.claims?.sub || undefined,
    email: _idTokenDecoded?.claims?.email || undefined,
    acr: _idTokenDecoded?.claims?.acr || undefined,
  }

(These are already available via _idTokenDecoded from Plan 03.)

From cibaService.js — initiateCiba function signature:
  async function initiateCiba(loginHint, scope, deliveryMode, acrValues, bindingMessage)

bindingMessage is already a parameter — just needs to be added to the ciba/initiate metadata.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PKCE, idTokenClaims, and session-snapshot to oauth.js</name>

  <read_first>
    - banking_api_server/routes/oauth.js (read lines 195-370 — the full callback handler from state validation through session.save() and redirect; confirm codeVerifier variable name, _idTokenDecoded variable name from Plan 03, and where req.session.id is accessible after session regeneration)
    - REGRESSION_PLAN.md §1 (OAuth admin login and session persistence — changes must not affect req.session.save() call order or the redirect logic inside its callback)
  </read_first>

  <files>banking_api_server/routes/oauth.js</files>

  <action>
Make three targeted additions to the oauth callback handler. Read the file carefully to find exact line positions before editing.

**Addition 1 — Enrich callback-success metadata with PKCE and idTokenClaims:**

Find the existing callback-success logAppEvent (from Plan 03, metadata now includes jwtFullDecode and response). Add pkce and idTokenClaims to the metadata:

  const _idTokenDecoded = decodeJwt(oauthTokens?.idToken);    // already added in Plan 03
  logAppEvent('oauth', 'info', `OAuth admin login success: ...`,
    { tag: 'oauth/callback-success', username: authedUser?.username,
      metadata: {
        role: authedUser?.role,
        clientType,
        hasIdToken: !!(oauthTokens?.idToken),
        jwtFullDecode: _idTokenDecoded || undefined,
        response: { hasAccessToken: !!(oauthTokens?.accessToken), hasIdToken: !!(oauthTokens?.idToken), tokenType: oauthTokens?.tokenType || 'Bearer' },
        pkce: {
          code_challenge_method: 'S256',
          code_challenge_length: codeVerifier ? String(codeVerifier).length : 0,
        },
        idTokenClaims: {
          sub: _idTokenDecoded?.claims?.sub || undefined,
          email: _idTokenDecoded?.claims?.email || undefined,
          acr: _idTokenDecoded?.claims?.acr || undefined,
        },
      } });

Note: codeVerifier is already deleted from session at line 205 (`delete req.session.oauthCodeVerifier`) but it was read into a local variable at line 198 (`const codeVerifier = req.session.oauthCodeVerifier || pkceCookie?.codeVerifier`). Confirm codeVerifier is still in scope at the logAppEvent call site.

**Addition 2 — Session-snapshot event at login (inside session.save() callback, AFTER the logAppEvent above, BEFORE setAuthCookie and res.redirect):**

Inside the `req.session.save((saveErr) => { ... })` callback, after the existing logAppEvent('oauth/callback-success') call, add:

  const _sessionHash = crypto.createHash('sha256').update(req.session.id || '').digest('hex').slice(0, 8);
  logAppEvent('auth_lifecycle', 'info', 'Session snapshot: admin login', {
    tag: 'auth_lifecycle/session-snapshot',
    metadata: {
      sessionId_hash: _sessionHash,
      event: 'login',
      role: authedUser?.role,
      hasAccessToken: !!(req.session.oauthTokens?.accessToken || oauthTokens?.accessToken),
      hasIdToken: !!(req.session.oauthTokens?.idToken || oauthTokens?.idToken),
      hasRefreshToken: !!(req.session.oauthTokens?.refreshToken),
    },
  });

**Addition 3 — Session-snapshot event at logout:**

Find the logout route handler (around line 368 — `router.get('/logout', ...)`). Inside the logout handler, immediately after the session.destroy() call succeeds (find the success callback), add:

  logAppEvent('auth_lifecycle', 'info', 'Session snapshot: admin logout', {
    tag: 'auth_lifecycle/session-snapshot',
    metadata: {
      event: 'logout',
      role: null,
      hasAccessToken: false,
      hasIdToken: false,
      hasRefreshToken: false,
    },
  });

Note: After session.destroy(), req.session.id is no longer valid — do NOT hash it. The logout snapshot uses event: 'logout' with all flags false and no sessionId_hash.
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./routes/oauth')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "pkce:" banking_api_server/routes/oauth.js returns at least 1
    - grep -c "idTokenClaims:" banking_api_server/routes/oauth.js returns at least 1
    - grep -c "session-snapshot" banking_api_server/routes/oauth.js returns at least 2 (login and logout)
    - grep -c "createHash.*sha256" banking_api_server/routes/oauth.js returns at least 1
    - grep "req.session.id" banking_api_server/routes/oauth.js — when in metadata context, always via createHash (never raw)
    - node -e "require('./routes/oauth')" exits 0
  </acceptance_criteria>

  <done>oauth.js callback-success has pkce and idTokenClaims in metadata. Session-snapshot events fire at login and logout. Raw session ID never logged — hashed to 8 chars. Module loads cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add bindingMessage to cibaService.js ciba/initiate metadata</name>

  <read_first>
    - banking_api_server/services/cibaService.js (read lines 50-120 — find initiateCiba function signature to confirm bindingMessage parameter name; find the ciba/initiate logAppEvent; confirm existing metadata fields after Plan 03 additions)
  </read_first>

  <files>banking_api_server/services/cibaService.js</files>

  <action>
The initiateCiba function signature has bindingMessage as a parameter (5th argument). It is already in scope at the ciba/initiate logAppEvent call.

Find the ciba/initiate logAppEvent (around line 100-101, after Plan 03 already added request metadata). Add bindingMessage to the metadata:

Current state after Plan 03 (approximate):
  logAppEvent('auth_lifecycle', 'info', `CIBA: initiating backchannel auth for ${loginHint}`,
    { tag: 'ciba/initiate',
      metadata: {
        loginHint,
        scope,
        deliveryMode,
        hasAcrValues: !!(acrValues),
        request: { scope, deliveryMode, loginHintType: typeof loginHint },
      } });

Add bindingMessage to both the top-level metadata and the request object:
  logAppEvent('auth_lifecycle', 'info', `CIBA: initiating backchannel auth for ${loginHint}`,
    { tag: 'ciba/initiate',
      metadata: {
        loginHint,
        scope,
        deliveryMode,
        hasAcrValues: !!(acrValues),
        bindingMessage: bindingMessage || undefined,
        request: {
          scope,
          deliveryMode,
          loginHintType: typeof loginHint,
          bindingMessage: bindingMessage || undefined,
        },
      } });

No other changes to cibaService.js in this plan.
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./services/cibaService')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "bindingMessage" banking_api_server/services/cibaService.js returns at least 2 (top-level and request object)
    - node -e "require('./services/cibaService')" exits 0
  </acceptance_criteria>

  <done>cibaService.js ciba/initiate metadata includes bindingMessage at both top level and inside request object. Module loads cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oauth.js session.save() callback → logEvent | Session ID must be hashed; raw session ID must never reach logEvent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-06-A | Information Disclosure | Session ID in session-snapshot event | mitigate | SHA-256 hash first 8 chars via crypto.createHash — raw req.session.id never passed to logEvent; grep acceptance criterion enforces this |
| T-233-06-B | Information Disclosure | idTokenClaims.email in log | accept | Demo app; Activity Logs UI is admin-gated; email is educational value of the feature |
| T-233-06-C | Tampering | Inserting logEvent inside session.save() callback | mitigate | session-snapshot fires AFTER existing logAppEvent('oauth/callback-success') and BEFORE setAuthCookie+res.redirect; no change to session.save() callback chain; regression guard verified |
| T-233-06-D | Information Disclosure | CIBA bindingMessage in log | accept | bindingMessage is a user-visible notification text (e.g., "Approve your banking session"); not sensitive |
</threat_model>

<verification>
1. grep "session-snapshot" banking_api_server/routes/oauth.js — at least 2 matches
2. grep "createHash.*sha256" banking_api_server/routes/oauth.js — at least 1 match
3. grep "pkce:" banking_api_server/routes/oauth.js — at least 1 match
4. grep "bindingMessage" banking_api_server/services/cibaService.js — at least 2 matches
5. node -e "require('./routes/oauth'); require('./services/cibaService')" exits 0
</verification>

<success_criteria>
- oauth/callback-success metadata includes pkce.code_challenge_method, pkce.code_challenge_length, idTokenClaims.sub/email/acr
- auth_lifecycle/session-snapshot fires at login (with sessionId_hash) and logout (without hash)
- No raw session ID value in any logEvent metadata
- cibaService.js ciba/initiate has bindingMessage in metadata
- Both modules load without errors
</success_criteria>

<output>
After completion, create `.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-06-SUMMARY.md`
</output>
