---
phase: "233"
plan: "05"
type: execute
wave: 2
depends_on: ["233-02"]
files_modified:
  - banking_api_server/services/bankingAgentLangGraphService.js
autonomous: true
requirements:
  - D-04

must_haves:
  truths:
    - "agent_prompt/llm_invoke events include full prompt text (not truncated) in metadata.prompt"
    - "agent_prompt/llm_invoke events include systemPrompt text in metadata.systemPrompt if available"
    - "agent_prompt/llm_invoke events include model name in metadata.model"
    - "agent_prompt/llm_invoke events include toolsAvailable array in metadata.toolsAvailable"
    - "agent_prompt/llm_complete events include full response content in metadata.response"
  artifacts:
    - path: "banking_api_server/services/bankingAgentLangGraphService.js"
      provides: "LLM invoke and complete events with full prompt text metadata"
      contains: "metadata.prompt"
  key_links:
    - from: "bankingAgentLangGraphService.js processAgentMessage"
      to: "appEventService.logEvent agent_prompt/llm_invoke"
      via: "appEventService.logEvent call with metadata.prompt"
      pattern: "metadata.*prompt"
---

<objective>
Enrich the agent_prompt/llm_invoke and agent_prompt/llm_complete logEvent calls in bankingAgentLangGraphService.js with full prompt text, system prompt, model name, and available tools list in metadata.

Purpose: Demo educational value — Activity Logs shows exactly what was sent to the LLM and what came back. Plan 01 already removed .slice() truncation from the message string. This plan adds the structured metadata fields (D-04) to the metadata object.

Output: Two agent_prompt logEvent calls enriched with { prompt, systemPrompt, model, toolsAvailable } and { response, model } metadata respectively.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-CONTEXT.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-RESEARCH.md
@.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-01-SUMMARY.md

<interfaces>
Current llm_invoke logEvent (after Plan 01 truncation removal):

  appEventService.logEvent('agent_prompt', 'info', `LLM prompt: ${String(message)}`,
    { tag: 'agent_prompt/llm_invoke',
      metadata: { userId, messageLength: message?.length || 0, sessionId } });

Current llm_complete logEvent (after Plan 01 truncation removal):

  appEventService.logEvent('agent_prompt', 'info', `LLM response: ${String(responseContent)}`,
    { tag: 'agent_prompt/llm_complete',
      metadata: { userId, messageCount: finalState?.messages?.length || 0 } });

Target metadata shape (D-04):

  // llm_invoke
  metadata: {
    userId,
    sessionId,
    messageLength: message?.length || 0,
    prompt: String(message),
    systemPrompt: systemPromptText || undefined,   // from langchainConfig or agent init
    model: modelName || undefined,                 // from langchainConfig
    toolsAvailable: toolNames || undefined,        // array of tool name strings
  }

  // llm_complete
  metadata: {
    userId,
    messageCount: finalState?.messages?.length || 0,
    response: String(responseContent),
    model: modelName || undefined,
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich llm_invoke and llm_complete metadata in bankingAgentLangGraphService.js</name>

  <read_first>
    - banking_api_server/services/bankingAgentLangGraphService.js (read fully — understand: what variables are in scope at the llm_invoke logEvent call site; what langchainConfig contains; where the agent graph is created; what tools are registered; what model is configured)
    - REGRESSION_PLAN.md §1 (agent processing — do not alter createBankingAgent call, graph.invoke call, or error handling paths)
  </read_first>

  <files>banking_api_server/services/bankingAgentLangGraphService.js</files>

  <action>
Read the file carefully to understand local variable scope at the logEvent call sites (around lines 273-288).

**llm_invoke enrichment:**

Find what is available in scope at the llm_invoke logEvent call:
- `message` — the user's prompt text (already in scope)
- `userId`, `sessionId` — already in metadata
- `langchainConfig` — passed into processAgentMessage; inspect its shape for model name and system prompt fields
- The `graph` and `initialState` from createBankingAgent — `initialState` may contain tools list

Extract the model name and system prompt from langchainConfig (or wherever they are configured). Read the actual field names before adding — common patterns: langchainConfig.model, langchainConfig.systemPrompt, langchainConfig.modelName.

Extract tool names: inspect initialState or the graph's tool nodes for an array of tool names. If tools are registered in createBankingAgent, check if they are accessible in scope. If tools list cannot be determined at the logEvent call site, set toolsAvailable to undefined (use || undefined pattern).

Update the llm_invoke logAppEvent:

  appEventService.logEvent('agent_prompt', 'info', `LLM prompt: ${String(message)}`,
    { tag: 'agent_prompt/llm_invoke',
      metadata: {
        userId,
        sessionId,
        messageLength: message?.length || 0,
        prompt: String(message),
        systemPrompt: langchainConfig?.systemPrompt || langchainConfig?.system_prompt || undefined,
        model: langchainConfig?.model || langchainConfig?.modelName || undefined,
        toolsAvailable: initialState?.tools?.map(t => t.name || t) || undefined,
      } });

(Use the actual field names from langchainConfig and initialState after reading the file.)

**llm_complete enrichment:**

Extract response content from finalState — the variable `responseContent` holds `lastMessage?.content`. Update the llm_complete logAppEvent:

  appEventService.logEvent('agent_prompt', 'info', `LLM response: ${String(responseContent)}`,
    { tag: 'agent_prompt/llm_complete',
      metadata: {
        userId,
        messageCount: finalState?.messages?.length || 0,
        response: String(responseContent),
        model: langchainConfig?.model || langchainConfig?.modelName || undefined,
      } });

Do NOT change the heuristic logEvent calls (lines 250-251 for agent/heuristic and agent_prompt/heuristic_dispatch) — those are not LLM invocations and should keep their existing minimal metadata.

Do NOT change the agent/error logEvent (line 308) — that has the correct shape already.
  </action>

  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_server && node -e "require('./services/bankingAgentLangGraphService')" && echo "LOAD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "metadata.*prompt:" banking_api_server/services/bankingAgentLangGraphService.js returns at least 1
    - grep -c "metadata.*response:" banking_api_server/services/bankingAgentLangGraphService.js returns at least 1
    - grep "slice(0, 120)" banking_api_server/services/bankingAgentLangGraphService.js returns 0 results (Plan 01 already removed these; verify they remain absent)
    - node -e "require('./services/bankingAgentLangGraphService')" exits 0
    - grep -c "llm_invoke" banking_api_server/services/bankingAgentLangGraphService.js returns 1 (not added a second time)
    - grep -c "llm_complete" banking_api_server/services/bankingAgentLangGraphService.js returns 1
  </acceptance_criteria>

  <done>llm_invoke event metadata includes prompt, systemPrompt, model, toolsAvailable. llm_complete event metadata includes response and model. No truncation. Module loads cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LangGraph processAgentMessage → appEventService | Full LLM prompt written to NDJSON; prompt may contain user PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-233-05-A | Information Disclosure | Full LLM prompt in NDJSON log | accept | Demo app; D-04 explicitly chose full text; logs are server-side; Activity Logs UI is admin-gated (requireAdmin + requireScopes) |
| T-233-05-B | Information Disclosure | systemPrompt may contain internal instructions | accept | Demo app; educational goal is to show the full system prompt; no production secrets in system prompt |
| T-233-05-C | Denial of Service | Large prompts in ring buffer metadata | mitigate | MAX_EVENTS reduced to 200 in Plan 01; oldest events evicted; NDJSON file growth deferred per CONTEXT.md |
</threat_model>

<verification>
1. grep "metadata.*prompt:" banking_api_server/services/bankingAgentLangGraphService.js — at least 1 match
2. grep "metadata.*response:" banking_api_server/services/bankingAgentLangGraphService.js — at least 1 match
3. grep "slice(0, 120)" banking_api_server/services/bankingAgentLangGraphService.js — 0 matches
4. node -e "require('./services/bankingAgentLangGraphService')" exits 0
</verification>

<success_criteria>
- agent_prompt/llm_invoke metadata has prompt, systemPrompt (if available), model, toolsAvailable
- agent_prompt/llm_complete metadata has response and model
- No .slice() truncation anywhere in LLM logEvent calls
- Module loads without errors
</success_criteria>

<output>
After completion, create `.planning/phases/233-enrich-activity-log-with-decoded-token-payloads-log-full-jwt/233-05-SUMMARY.md`
</output>
