---
phase: 153-postman-collections-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - postman/Super-Banking-PingOne-Test.postman_collection.json
  - postman/PingOne Authorization Code — pi.flow.postman_collection.json
  - postman/Super-Banking-Local.postman_environment.json
autonomous: true
requirements: [POST-153-01, POST-153-02, POST-153-03]

must_haves:
  truths:
    - "PingOne Test collection uses sessionCookie variable for auth instead of broken GET login"
    - "pi.flow collection redirect_uri points to local (api.pingdemo.com:4000) not Vercel"
    - "pi.flow callback path is /oauth/user/ not /oauthuser/"
    - "BX Finance renamed to Super Banking in pi.flow collection"
  artifacts:
    - path: "postman/Super-Banking-PingOne-Test.postman_collection.json"
      provides: "Fixed auth flow with session cookie support"
    - path: "postman/PingOne Authorization Code — pi.flow.postman_collection.json"
      provides: "Fixed redirect_uri and callback paths"
  key_links:
    - from: "Postman collections"
      to: "banking_api_server routes"
      via: "API endpoint URLs and auth headers"
---

<objective>
Fix both Postman collections to work with the current server architecture.

Purpose: Collections are broken — login requests use GET instead of browser-based auth, redirect URIs point to production instead of local, callback paths are wrong.

Output: Updated Postman collection JSON files with correct auth flow, URIs, and naming.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@postman/Super-Banking-PingOne-Test.postman_collection.json
@postman/PingOne Authorization Code — pi.flow.postman_collection.json
@postman/Super-Banking-Local.postman_environment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Super Banking PingOne Test collection auth flow</name>
  <files>postman/Super-Banking-PingOne-Test.postman_collection.json, postman/Super-Banking-Local.postman_environment.json</files>
  <action>
1. Read postman/Super-Banking-PingOne-Test.postman_collection.json
2. Identify all requests that use GET for login/auth (these are broken)
3. For session-dependent requests, add Cookie header: `{{sessionCookie}}`
4. Add `sessionCookie` variable to description/docs explaining:
   - User must authenticate via browser first
   - Copy the session cookie from browser DevTools → Network → Cookie header
   - Paste into Postman environment variable `sessionCookie`
5. Remove or mark broken login GET requests as "Browser Auth Required" with documentation
6. Ensure the environment file has `sessionCookie` variable (empty default)
7. Update collection description to explain the browser-first auth workflow
  </action>
  <verify>
    <automated>grep -c "sessionCookie" postman/Super-Banking-PingOne-Test.postman_collection.json postman/Super-Banking-Local.postman_environment.json</automated>
  </verify>
  <done>PingOne Test collection uses sessionCookie for auth, environment has sessionCookie variable, broken login GETs documented</done>
</task>

<task type="auto">
  <name>Task 2: Fix pi.flow collection redirect_uri, callback path, and rename</name>
  <files>postman/PingOne Authorization Code — pi.flow.postman_collection.json</files>
  <action>
1. Read postman/PingOne Authorization Code — pi.flow.postman_collection.json
2. Find all redirect_uri values pointing to Vercel production URLs
3. Replace with local: `https://api.pingdemo.com:4000` (or `{{baseUrl}}` variable)
4. Find all callback paths using `/oauthuser/` and fix to `/oauth/user/`
5. Find all "BX Finance" references and rename to "Super Banking"
6. Verify Steps 1-5 endpoint paths match current banking_api_server routes:
   - Step 1: /api/auth/oauth/user/authorize (or /api/oauth/user/authorize)
   - Step 2: credentials submission
   - Step 3: resume
   - Step 4: token exchange
   - Step 5: RFC 8693 token exchange
7. Save updated collection
  </action>
  <verify>
    <automated>grep -c "oauthuser" "postman/PingOne Authorization Code — pi.flow.postman_collection.json" | grep "^0$" && grep -c "Super Banking" "postman/PingOne Authorization Code — pi.flow.postman_collection.json"</automated>
  </verify>
  <done>pi.flow collection has correct redirect_uri (local), correct callback path (/oauth/user/), BX Finance renamed to Super Banking</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postman → API server | Collection requests cross network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153-01 | Information Disclosure | Session cookie in Postman | mitigate | Document that sessionCookie is sensitive; don't commit real values to env file |
| T-153-02 | Spoofing | redirect_uri | mitigate | Fix redirect_uri to match registered OAuth client config exactly |
</threat_model>

<verification>
1. No `oauthuser` (old path) in pi.flow collection
2. No `BX Finance` in pi.flow collection
3. `sessionCookie` appears in test collection and environment
4. redirect_uri values point to local, not Vercel
</verification>

<success_criteria>
- Both collections updated and valid JSON
- Auth flow documented for browser-first workflow
- All paths match current server routes
</success_criteria>

<output>
After completion, create `.planning/phases/153-postman-collections-fix-auth-flow-and-add-session-cookie-support/153-01-SUMMARY.md`
</output>
