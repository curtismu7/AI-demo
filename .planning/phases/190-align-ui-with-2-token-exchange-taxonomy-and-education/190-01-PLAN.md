---
phase: 190-align-ui-with-2-token-exchange-taxonomy-and-education
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/TokenExchangeFlowDiagram.jsx
  - banking_api_ui/src/components/education/TokenChainEducationPanel.js
  - banking_api_ui/src/components/PingOneTestPage.jsx
  - banking_api_ui/src/components/education/RFC8707Content.js
  - banking_api_ui/src/components/education/TokenExchangePanel.js
autonomous: true
requirements: [TAX-190-01, TAX-190-02, TAX-190-03]
must_haves:
  truths:
    - "All user-facing exchange labels use '1-exchange' or '2-exchange' — no 'Exchange 1/2/3' or 'Phase 184' visible in the UI"
    - "Phase 186 ID-token flow is named distinctly — not collapsed into '2-exchange'"
    - "React production build exits 0"
    - "No server-side token behavior changed"
  artifacts:
    - path: "banking_api_ui/src/components/PingOneTestPage.jsx"
      provides: "Updated test-page labels aligned to canonical taxonomy"
      contains: "1-exchange"
    - path: "banking_api_ui/src/components/education/TokenChainEducationPanel.js"
      provides: "Education panel with aligned exchange copy"
      contains: "2-exchange"
  key_links:
    - from: "PingOneTestPage.jsx user-visible labels"
      to: "docs/TOKEN_TERMINOLOGY_GLOSSARY.md canonical vocabulary"
      via: "Human-visible text must match canonical vocabulary"
      pattern: "1-exchange|2-exchange"
    - from: "fixIssue dispatch keys (single-exchange/double-exchange)"
      to: "inline // legacy dispatch key comments"
      via: "Future developers must know these are intentional legacy keys"
      pattern: "legacy dispatch key"
---

# 190-01 Plan

## Objective

Align user-facing token-exchange terminology and education UI with the Phase 188 RFC 8693 taxonomy.
Replace legacy exchange labels and ambiguous mode names with consistent 1-exchange / 2-exchange
vocabulary while preserving the distinct Phase 186 ID-token flow.

## Execution Context

@.planning/phases/190-align-ui-with-2-token-exchange-taxonomy-and-education/190-CONTEXT.md
@.planning/ROADMAP.md
@docs/TOKEN_TERMINOLOGY_GLOSSARY.md

## Scope

**UI copy and visuals only.** Do NOT change server-side token behavior.
Internal component prop constants ('single'/'double' in TokenExchangeFlowDiagram) are component
API — rename only displayed text, not internal prop values.

## Tasks

<task type="auto">
  <name>Task 1: Audit UI terminology drift — broad src/ scan with before/after map</name>
  <files>banking_api_ui/src/components/TokenExchangeFlowDiagram.jsx, banking_api_ui/src/components/education/TokenChainEducationPanel.js, banking_api_ui/src/components/PingOneTestPage.jsx, banking_api_ui/src/components/education/RFC8707Content.js, banking_api_ui/src/components/education/TokenExchangePanel.js</files>
  <action>
Run a broad scan across the FULL src/ directory (not just the 5 listed files) for legacy terminology:

  grep -rn "Exchange [123]|Phase 184" banking_api_ui/src/ | grep -v ".css" | grep -v "legacy|intentional"

For each match, record a before/after table in the SUMMARY draft before editing.
Confirm replacement wording against docs/TOKEN_TERMINOLOGY_GLOSSARY.md.

Preservation rules:
  - fixIssue('single-exchange') and fixIssue('double-exchange') are internal dispatch keys, not user-visible.
    Mark with "// legacy dispatch key — not user-facing; canonical: 1-exchange" instead of renaming.
  - 'single'/'double' prop constants in TokenExchangeFlowDiagram.jsx are component API — leave unchanged.
  - "Phase 186 ID-token" variant MUST remain distinctly named — do not rename into "2-exchange".
  </action>
  <verify>
    <automated>grep -rn "Exchange [123]" banking_api_ui/src/ | grep -v ".css" | grep -v "legacy|intentional" | wc -l</automated>
  </verify>
  <done>Zero legacy label hits (excluding intentional comments). Before/after table captured for SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Update flow diagrams and education copy</name>
  <files>banking_api_ui/src/components/TokenExchangeFlowDiagram.jsx, banking_api_ui/src/components/education/TokenChainEducationPanel.js, banking_api_ui/src/components/education/TokenExchangePanel.js</files>
  <action>
Update labels, helper text, examples, and explanatory copy to use canonical vocabulary:
  - 1-exchange: subject token only (no actor_token)
  - 2-exchange: subject token + actor token (dual-token, RFC 8693 delegation)
  - MCP-scoped access token: result with sub + act claims

After edits, explicitly verify Phase 186 ID-token variant is still distinctly named:
  grep -n "Phase 186" banking_api_ui/src/components/education/TokenChainEducationPanel.js
  </action>
  <verify>
    <automated>grep -n "1-exchange" banking_api_ui/src/components/education/TokenChainEducationPanel.js | head -3 && grep -rn "Phase 186" banking_api_ui/src/ | grep -v ".css" | head -3</automated>
  </verify>
  <done>Education/diagram copy uses RFC-aligned labels. Phase 186 flow distinctly named (non-zero grep hits on "Phase 186").</done>
</task>

<task type="auto">
  <name>Task 3: Update test-page copy and add fixIssue legacy dispatch comments</name>
  <files>banking_api_ui/src/components/PingOneTestPage.jsx, banking_api_ui/src/components/education/RFC8707Content.js</files>
  <action>
1. Replace all user-visible legacy exchange labels in PingOneTestPage.jsx.

2. REQUIRED — add inline explanatory comments to the two legacy fixIssue dispatch keys:
   The keys 'single-exchange' and 'double-exchange' are internal routing keys (not shown to users).
   Their message strings already use canonical vocabulary. Add trailing comments:

   'single-exchange': { ... },  // legacy dispatch key — not user-facing; canonical: 1-exchange
   'double-exchange': { ... },  // legacy dispatch key — not user-facing; canonical: 2-exchange

3. Update RFC8707Content.js to match canonical taxonomy.
  </action>
  <verify>
    <automated>grep -n "legacy dispatch key" banking_api_ui/src/components/PingOneTestPage.jsx</automated>
  </verify>
  <done>No user-visible legacy labels remain. Both fixIssue keys have // legacy dispatch key comments. RFC8707Content aligned.</done>
</task>

<task type="auto">
  <name>Task 4: Final completeness sweep and build verification</name>
  <files></files>
  <action>
Run completeness checks across FULL src/ directory:

  # Must return 0 lines — all legacy labels removed
  grep -rn "Exchange [123]" banking_api_ui/src/ | grep -v ".css" | grep -v "legacy|intentional"

  # Must return non-zero — Phase 186 still distinctly named
  grep -rn "Phase 186" banking_api_ui/src/ | grep -v ".css" | head -5

  # Build must exit 0
  cd banking_api_ui && npm run build
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Zero legacy label hits across full src/. Phase 186 variant still named. npm run build exits 0.</done>
</task>

## Verification

  # Must be 0
  grep -rn "Exchange [123]" banking_api_ui/src/ | grep -v ".css" | grep -v "legacy|intentional"

  # Must be non-zero
  grep -rn "Phase 186" banking_api_ui/src/ | grep -v ".css" | head -5

  # Build
  cd banking_api_ui && npm run build

- [ ] Zero legacy label hits across full src/
- [ ] Phase 186 ID-token flow still distinctly named (non-zero hits)
- [ ] fixIssue('single-exchange') and fixIssue('double-exchange') have // legacy dispatch key comments
- [ ] No server-side token behavior changed
- [ ] npm run build exits 0

## Output

After completion, update `.planning/phases/190-align-ui-with-2-token-exchange-taxonomy-and-education/190-01-SUMMARY.md`
