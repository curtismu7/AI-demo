---
phase: 267
plan: "267-01"
title: "Fix failing gateway test files — scope name updates and GatewayConfig stub completeness"
wave: 1
depends_on: []
files_modified:
  - demo_mcp_gateway/tests/mortgageDispatch.test.ts
  - demo_mcp_gateway/tests/scopeTopology.test.ts
  - demo_mcp_gateway/tests/gateway-auth.test.ts
  - demo_mcp_gateway/tests/gateway-passthrough.test.ts
  - demo_mcp_gateway/tests/gateway-server.test.ts
  - demo_mcp_gateway/tests/gateway-get-delete-middleware.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "cd demo_mcp_gateway && npm test exits 0 (all 83 tests pass, 0 failures)"
    - "scope-topology.json is the SSOT for scope names; tests must match it, not the other way around"
    - "mortgageDispatch.test.ts scope assertions use 'mortgage:read' not 'banking:mortgage:read'"
    - "scopeTopology.test.ts assertions use 'read', 'write', 'transfer' not 'banking:read', 'banking:write', 'banking:transfer'"
    - "All 4 GatewayConfig-stub test files compile without TS2739 errors"
---

<objective>
Fix all 8 currently failing tests in `demo_mcp_gateway/tests/` so that `cd demo_mcp_gateway && npm test` exits 0 with all tests passing.

Two distinct failure classes:

1. **Scope name mismatch** (`mortgageDispatch.test.ts`, `scopeTopology.test.ts`): Tests assert old `banking:`-prefixed scope names (e.g. `banking:mortgage:read`, `banking:read`) but `scope-topology.json` (the SSOT) defines the current names (`mortgage:read`, `read`). The SSOT wins — tests must be updated to match.

2. **Missing GatewayConfig fields** (`gateway-auth.test.ts`, `gateway-passthrough.test.ts`, `gateway-server.test.ts`, `gateway-get-delete-middleware.test.ts`): `GatewayConfig` interface was extended with `introspectionClientId` and `introspectionClientSecret` after these tests were written. The test stub objects are missing these fields → TypeScript compile error TS2739.
</objective>

<tasks>

<task id="T01" type="execute">
<title>Fix scope name assertions in mortgageDispatch.test.ts</title>
<read_first>
- demo_mcp_gateway/tests/mortgageDispatch.test.ts (current assertions to replace)
- scope-topology.json (SSOT: show_mortgage requires ["mortgage:read"], get_my_accounts requires ["read"])
- demo_mcp_gateway/src/auth/toolScopes.ts (getScopesForGatewayTool fallback returns ['read'])
</read_first>
<action>
In `demo_mcp_gateway/tests/mortgageDispatch.test.ts`, replace every occurrence of `banking:mortgage:read` with `mortgage:read` and every occurrence of `banking:read` with `read`. This affects:

1. The doc comment at the top (line 8: `banking:mortgage:read` → `mortgage:read`)
2. Test "show_mortgage requires banking:mortgage:read" — update title and assertion: `toEqual(['banking:mortgage:read'])` → `toEqual(['mortgage:read'])`
3. Test "missing scope → DENY" — update `missingScopes` assertion: `['banking:mortgage:read']` → `['mortgage:read']`; update the scope string passed to `evaluateScopeDecisionLocally`: `'openid profile banking:read banking:write'` → `'openid profile read write'`
4. Test "scope present → PERMIT" — update scope string: `'openid banking:read banking:mortgage:read'` → `'openid read mortgage:read'`
5. Test "irregular whitespace in the claim is tolerated" — update: `'  banking:read   banking:mortgage:read  '` → `'  read   mortgage:read  '`
6. Test "both DENY when banking:mortgage:read is absent" — token scope string: `'openid banking:read'` → `'openid read'`
7. Test "both PERMIT when banking:mortgage:read is present" — token scope string: `'openid banking:read banking:mortgage:read'` → `'openid read mortgage:read'`
8. Test "a banking:read-only tool still PERMITs" — token scope string: `'openid banking:read'` → `'openid read'`

Do NOT change the test for scope fallback for unknown tools — check what `getScopesForGatewayTool('no_such_tool')` actually returns from scope-topology.json and match it.
</action>
<acceptance_criteria>
- `cd demo_mcp_gateway && npm test -- --testPathPattern=mortgageDispatch` exits 0
- All 15 tests in mortgageDispatch.test.ts pass (no failures)
- No test is removed or skipped — all 15 must run
</acceptance_criteria>
</task>

<task id="T02" type="execute">
<title>Fix scope name assertions in scopeTopology.test.ts</title>
<read_first>
- demo_mcp_gateway/tests/scopeTopology.test.ts (current assertions)
- scope-topology.json (SSOT: create_transfer requires ["write", "transfer"]; fallback is ["read"])
</read_first>
<action>
In `demo_mcp_gateway/tests/scopeTopology.test.ts`:

1. Test "create_transfer requires banking:write + banking:transfer":
   - Change title to "create_transfer requires write + transfer"
   - Change assertion: `toEqual(['banking:write', 'banking:transfer'])` → `toEqual(['write', 'transfer'])`

2. Test "unknown tool falls back to [banking:read]":
   - Change title to "unknown tool falls back to [read]"
   - Change assertion: `toEqual(['banking:read'])` → `toEqual(['read'])`

Do NOT modify the other 3 tests in this file.
</action>
<acceptance_criteria>
- `cd demo_mcp_gateway && npm test -- --testPathPattern=scopeTopology` exits 0
- All 5 tests in scopeTopology.test.ts pass
</acceptance_criteria>
</task>

<task id="T03" type="execute">
<title>Add missing introspectionClientId/introspectionClientSecret to GatewayConfig stubs in 4 test files</title>
<read_first>
- demo_mcp_gateway/src/config.ts (GatewayConfig interface: fields introspectionClientId: string, introspectionClientSecret: string at lines 39-40)
- demo_mcp_gateway/tests/gateway-auth.test.ts (stubConfig and stubConfigNoAuthz)
- demo_mcp_gateway/tests/gateway-passthrough.test.ts (makeConfig function return object)
- demo_mcp_gateway/tests/gateway-server.test.ts (stubConfig object)
- demo_mcp_gateway/tests/gateway-get-delete-middleware.test.ts (stubConfig object)
</read_first>
<action>
Each of these 4 test files has a `GatewayConfig` stub object missing `introspectionClientId` and `introspectionClientSecret`. Add both fields with empty-string values immediately after the existing `introspectionEndpoint: ''` line in each stub.

For `gateway-auth.test.ts`: there are TWO stubs — `stubConfig` (line 48) and `stubConfigNoAuthz` (line 78, defined as spread of stubConfig). The spread-based one inherits from `stubConfig`, so adding both fields to `stubConfig` covers both. Verify whether `stubConfigNoAuthz` is a spread or a separate full object — if full object, add to both independently.

For `gateway-passthrough.test.ts`: the `makeConfig()` function returns an object literal. Add both fields after `introspectionEndpoint: ''`.

For `gateway-server.test.ts`: `stubConfig` object literal. Add both fields after `introspectionEndpoint: ''`.

For `gateway-get-delete-middleware.test.ts`: `stubConfig` object literal. Add both fields after `introspectionEndpoint: ''`.

The values to add in each location:
```
introspectionClientId: '',
introspectionClientSecret: '',
```
</action>
<acceptance_criteria>
- `cd demo_mcp_gateway && npm test -- --testPathPattern="gateway-auth|gateway-passthrough|gateway-server|gateway-get-delete"` exits 0
- Zero TS2739 compile errors in these 4 files
- All tests that were previously passing in these files continue to pass (no regressions)
</acceptance_criteria>
</task>

<task id="T04" type="execute">
<title>Run full gateway test suite and confirm all 83 tests pass</title>
<read_first>
- demo_mcp_gateway/package.json (test script: `jest --forceExit`)
</read_first>
<action>
After T01, T02, T03 are complete, run the full test suite:
```bash
cd demo_mcp_gateway && npm test
```
Confirm the summary line shows: `Test Suites: 19 passed, 19 total` and `Tests: 83 passed, 83 total` (or higher if new tests were added). Zero failures.

If any tests are still failing, investigate and fix before marking this task done. Do NOT use `--testNamePattern` to skip failing tests.
</action>
<acceptance_criteria>
- `cd demo_mcp_gateway && npm test` exits 0
- Summary: 0 failed test suites
- Summary: 0 failed tests
- `Tests: X passed, X total` where X >= 83
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd demo_mcp_gateway && npm test` — all tests pass, exit code 0
2. `cd demo_api_ui && npm run build` — UI build exits 0 (no regressions from any changes)
3. Verify no scope-topology.json was modified (the SSOT must not change — only the tests must align to it)
</verification>

<success_criteria>
- [ ] `cd demo_mcp_gateway && npm test` exits 0 (was: 6 failed suites, 8 failed tests)
- [ ] mortgageDispatch.test.ts: 15/15 tests passing
- [ ] scopeTopology.test.ts: 5/5 tests passing
- [ ] gateway-auth.test.ts: compiles and all tests pass
- [ ] gateway-passthrough.test.ts: compiles and all tests pass
- [ ] gateway-server.test.ts: compiles and all tests pass
- [ ] gateway-get-delete-middleware.test.ts: compiles and all tests pass
- [ ] scope-topology.json unchanged (tests align to SSOT, not vice versa)
- [ ] `cd demo_api_ui && npm run build` exits 0
</success_criteria>
