---
phase: 187-1-token-exchange-401-flow-mcp-401-triggers-user-authz-then-token-exchange-for-mcp-token
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_server/services/agentMcpTokenService.js
  - banking_api_server/routes/bankingAgentRoutes.js
autonomous: true
requirements: [P187-D01, P187-D06]

must_haves:
  truths:
    - "When user token is absent from session, agentMcpTokenService returns need_auth: true"
    - "Both the /init and /message routes in bankingAgentRoutes.js include need_auth: true alongside agentInitRequired: true in 401 responses"
    - "The need_auth field propagates to API response so BankingAgent.js can intercept it"
  artifacts:
    - path: "banking_api_server/services/agentMcpTokenService.js"
      provides: "need_auth: true in null-token return object"
      contains: "need_auth: true"
    - path: "banking_api_server/routes/bankingAgentRoutes.js"
      provides: "need_auth: true in 401 responses"
      contains: "need_auth: true"
  key_links:
    - from: "banking_api_server/services/agentMcpTokenService.js"
      to: "banking_api_server/routes/bankingAgentRoutes.js"
      via: "agentMcpTokenService result bubbled up when need_auth present"
      pattern: "need_auth.*true"
---

<objective>
Add structured `need_auth: true` signals to BFF service and route layers so BankingAgent.js can distinguish "user must authenticate" from other errors.

Purpose: Phase 187 D-01 + D-06 — wire real 401 intercept in the agent path. Currently the missing-token branch returns `{ token: null }` with no indication of why; BankingAgent.js cannot redirect the user to PingOne login.

Output: Two small edits — one to agentMcpTokenService.js (two return sites), one to bankingAgentRoutes.js (two 401 response sites).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/187-1-token-exchange-401-flow-mcp-401-triggers-user-authz-then-token-exchange-for-mcp-token/187-CONTEXT.md

<interfaces>
<!-- From banking_api_server/services/agentMcpTokenService.js -->
<!-- Target: the two null-token return paths -->

Return site 1 (~line 350): user token missing from session
```js
if (!userToken) {
  errorLog('ERROR: No user token in session - returning null');
  return { token: null, tokenEvents, userSub: null };
}
```
→ Change to: `return { token: null, tokenEvents, userSub: null, need_auth: true, exchange_mode: '1-token' };`

Return site 2 (~line 703): mcpResourceUri not configured — this path does NOT need need_auth
  (it's a config issue, not auth). Only the missing-userToken path gets need_auth.

Check: grep for all `return { token: null` to find exact occurrences before editing.

<!-- From banking_api_server/routes/bankingAgentRoutes.js -->
<!-- Lines 25 and 67 (confirmed via grep): -->

```js
return res.status(401).json({ error: 'Session expired', agentInitRequired: true });
```
→ Change both to: `return res.status(401).json({ error: 'Session expired', agentInitRequired: true, need_auth: true });`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add need_auth signal to agentMcpTokenService.js</name>
  <files>banking_api_server/services/agentMcpTokenService.js</files>
  <action>
    Read the file and find ALL occurrences of `return { token: null` in agentMcpTokenService.js.
    
    Only modify the return where `!userToken` is the cause (missing user token from session — D-06):
    - Find: `return { token: null, tokenEvents, userSub: null };` immediately after the `if (!userToken)` block
    - Change to: `return { token: null, tokenEvents, userSub: null, need_auth: true, exchange_mode: '1-token' };`
    
    Do NOT add need_auth to the `mcpResourceUri` not-configured path (line ~703) — that's a config issue, not an auth issue. Leave that return unchanged.
    
    Do NOT add need_auth to the admin token path or any other return site. Only the `!userToken` null-return gets the signal.
    
    Per D-06: also check if there is a token exchange failure path (exchange returns 401-like error from PingOne). If one exists, add need_auth: true there too. If no such explicit catch exists, skip it — the missing-token path is the primary requirement.
  </action>
  <verify>grep -n "need_auth" banking_api_server/services/agentMcpTokenService.js</verify>
  <done>At least 1 `need_auth: true` present in agentMcpTokenService.js, on the !userToken return path</done>
</task>

<task type="auto">
  <name>Task 2: Add need_auth to bankingAgentRoutes.js 401 responses</name>
  <files>banking_api_server/routes/bankingAgentRoutes.js</files>
  <action>
    Read bankingAgentRoutes.js. There are exactly 2 occurrences of the pattern:
    ```js
    return res.status(401).json({ error: 'Session expired', agentInitRequired: true });
    ```
    — one in the POST /init handler (~line 25) and one in the POST /message handler (~line 67).
    
    Change both to:
    ```js
    return res.status(401).json({ error: 'Session expired', agentInitRequired: true, need_auth: true });
    ```
    
    Per D-01: also check if bankingAgentRoutes.js calls agentMcpTokenService and checks its result. If it does, and the service returns `need_auth: true`, bubble it up in the message response. If no such call exists in this file (tokens come from req.agentContext/middleware), that's fine — the middleware-level check and service-level check are separate.
    
    Do NOT modify any other routes or error handlers.
  </action>
  <verify>grep -n "need_auth" banking_api_server/routes/bankingAgentRoutes.js</verify>
  <done>Both 401 responses in bankingAgentRoutes.js include `need_auth: true`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BFF internal | agentMcpTokenService → bankingAgentRoutes; both server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-187-01-01 | Information Disclosure | agentMcpTokenService | accept | need_auth flag reveals only that auth is missing, not token contents; acceptable for BFF internal use |
| T-187-01-02 | Spoofing | bankingAgentRoutes 401 | accept | Response is 401 HTTP status; client cannot escalate privileges by receiving need_auth field |
</threat_model>

<verification>
1. `grep -n "need_auth" banking_api_server/services/agentMcpTokenService.js` → shows need_auth on !userToken return
2. `grep -n "need_auth" banking_api_server/routes/bankingAgentRoutes.js` → shows 2 results (lines ~25 and ~67)
3. No other files modified
</verification>

<success_criteria>
- agentMcpTokenService.js: the `!userToken` null-return object includes `need_auth: true, exchange_mode: '1-token'`
- bankingAgentRoutes.js: both 401 JSON responses include `need_auth: true`
- The existing 401 HTTP status is unchanged (not altered to a different code)
</success_criteria>

<output>
After completion, create `.planning/phases/187-1-token-exchange-401-flow-mcp-401-triggers-user-authz-then-token-exchange-for-mcp-token/187-01-SUMMARY.md`
</output>
