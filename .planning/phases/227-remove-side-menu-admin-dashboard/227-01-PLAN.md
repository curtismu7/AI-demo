---
phase: 227-remove-side-menu-admin-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/App.js
  - banking_api_ui/src/components/AdminLayout.jsx
  - banking_api_ui/src/components/LandingPage.js
  - banking_api_ui/src/components/BankingAgent.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "No <AdminSideNav> element appears in any rendered route"
    - "Main content spans full width — no 280px left margin offset"
    - "Landing page shows no 'Admin Dashboard' button for unauthenticated visitors"
    - "BankingAgent panel shows no 'Admin Dashboard' chip for admin users"
    - "demo-config-fab shows on all authenticated routes (no isOnSidebarRoute suppression)"
    - "Build exits 0 with no unused-import warnings"
  artifacts:
    - path: "banking_api_ui/src/App.js"
      provides: "No AdminSideNav import, no render sites, no isOnSidebarRoute variable"
      contains: "AdminSideNav removed"
    - path: "banking_api_ui/src/components/AdminLayout.jsx"
      provides: "Renders children directly without AdminSideNav"
      exports: ["AdminLayout"]
    - path: "banking_api_ui/src/components/LandingPage.js"
      provides: "No Admin Dashboard buttons in logged-out header or hero section"
    - path: "banking_api_ui/src/components/BankingAgent.js"
      provides: "No admin/dashboard nav chip in agent panel"
  key_links:
    - from: "App.js"
      to: "CSS :has(.admin-side-nav)"
      via: "DOM presence"
      pattern: "admin-side-nav class absent from DOM"
    - from: "App.js demo-config-fab"
      to: "isOnSidebarRoute guard"
      via: "conditional render"
      pattern: "isOnSidebarRoute removed — FAB visible on all routes"
---

<objective>
Remove AdminSideNav from all render sites in App.js and AdminLayout.jsx, remove the
isOnSidebarRoute variable and its FAB guard, remove Admin Dashboard buttons from
LandingPage.js, and remove the Admin Dashboard nav chip from BankingAgent.js.

Purpose: Simplify the UI for demo users by eliminating the sidebar and admin entry
points that add visual noise. Admin routes remain URL-accessible.

Output: Clean build, no AdminSideNav in DOM, full-width layout via CSS :has() natural
fallback, no Admin Dashboard buttons anywhere in the rendered UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From banking_api_ui/src/App.js (relevant lines):
- Line 25: `import AdminSideNav from "./components/AdminSideNav";`  — REMOVE this line
- Lines 582–609: `sidebarRoutePatterns` array + `isOnSidebarRoute` boolean — REMOVE entire block (lines 581–609)
- Lines 695, 708, 722, 736, 749, 762, 787, 796, 826: `<AdminSideNav user={user} />` or `<AdminSideNav user={null} />` — REMOVE each JSX element
- Lines 1216–1228: `!isOnSidebarRoute && (` FAB guard — REMOVE the `!isOnSidebarRoute &&` condition only (keep the button, just drop the guard)

From banking_api_ui/src/components/AdminLayout.jsx:
- Line 2: `import AdminSideNav from "./AdminSideNav";` — REMOVE
- Line 14: `<AdminSideNav />` — REMOVE; render `{children}` directly inside `<div className="admin-layout">`

From banking_api_ui/src/components/LandingPage.js:
- Lines 8–15: `handleAdminDashboard` function — REMOVE entirely (both call sites are being removed)
- Lines 78–83: `<button onClick={handleAdminDashboard} className="btn btn-primary">Admin Dashboard</button>` in landing-header — REMOVE
- Lines 104–109: `<button onClick={handleAdminDashboard} className="hero-cta hero-cta-primary">Admin Dashboard</button>` in hero section — REMOVE
- Keep: `handleCustomerDashboard` and both Customer Dashboard buttons

From banking_api_ui/src/components/BankingAgent.js:
- Lines 5935–5955: The entire nav button block — the button navigates to `/admin` for admins and `/dashboard` for customers; REMOVE the entire `<button>` element (lines 5935–5955 inclusive)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove AdminSideNav from App.js and AdminLayout.jsx</name>
  <files>banking_api_ui/src/App.js, banking_api_ui/src/components/AdminLayout.jsx</files>
  <action>
Read App.js and AdminLayout.jsx in full to confirm exact line numbers match the research, then make the following changes:

**App.js — 4 changes:**

1. Remove line 25: `import AdminSideNav from "./components/AdminSideNav";`

2. Remove lines 581–609 in their entirety — the comment `// Check if we're on a sidebar-visible route` through the end of the `isOnSidebarRoute` assignment. This removes the `sidebarRoutePatterns` array and `isOnSidebarRoute` const.

3. Remove all 9 `<AdminSideNav ... />` JSX elements at lines 695, 708, 722, 736, 749, 762, 787, 796, 826. Remove only the single JSX line each time; leave the surrounding Fragment/Route structure intact.

4. At lines ~1216–1219: remove the `!isOnSidebarRoute &&` condition from the FAB guard. The result must keep the button rendering — only drop `!isOnSidebarRoute &&`. After edit the condition becomes:
   ```jsx
   {user &&
     demoMode !== true &&
     !isApiTrafficOnlyPage && (
       <button ...>Demo config</button>
     )}
   ```

**AdminLayout.jsx — 2 changes:**

1. Remove line 2: `import AdminSideNav from "./AdminSideNav";`
2. Remove `<AdminSideNav />` from the JSX body (line 14). The component must render children directly:
   ```jsx
   export default function AdminLayout({ children }) {
     return (
       <div className="admin-layout">
         <div className="admin-layout__main">{children}</div>
       </div>
     );
   }
   ```

Do NOT touch AdminSideNav.jsx itself. Do NOT touch App.css. Do NOT refactor anything else in App.js.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -n "AdminSideNav" banking_api_ui/src/App.js` returns no results
    - `grep -n "AdminSideNav" banking_api_ui/src/components/AdminLayout.jsx` returns no results
    - `grep -n "isOnSidebarRoute" banking_api_ui/src/App.js` returns no results
    - `npm run build` exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Admin Dashboard buttons from LandingPage.js and BankingAgent.js</name>
  <files>banking_api_ui/src/components/LandingPage.js, banking_api_ui/src/components/BankingAgent.js</files>
  <action>
**LandingPage.js — 3 changes:**

1. Remove the `handleAdminDashboard` function (lines 8–15 per RESEARCH.md). Verify that no other call site exists before deleting — `grep "handleAdminDashboard" src/components/LandingPage.js` should confirm only the two button onClick references and the function definition itself. After removal, only `handleCustomerDashboard` remains.

2. Remove the `<button onClick={handleAdminDashboard} className="btn btn-primary">Admin Dashboard</button>` element from the `landing-header-actions` div (lines ~78–83). Keep the adjacent Customer Dashboard button.

3. Remove the `<button onClick={handleAdminDashboard} className="hero-cta hero-cta-primary">Admin Dashboard</button>` element from the hero section `landing-hero-actions` div (lines ~104–109). Keep the adjacent Customer Dashboard button.

**BankingAgent.js — 1 change:**

Remove the entire nav button block spanning lines 5935–5955. This is the `<button type="button" className="ba-left-auth-btn primary" ...>` element that renders either "👑 Admin Dashboard" or "📊 My Dashboard" text. The button closes at line 5955 (the `</button>` before `{/* Connected services chips */}`). Remove the entire block including any wrapping conditional. Do NOT remove the `{/* Connected services chips */}` comment or the `ba-chips-footer` div that follows.

After removal, verify the JSX still closes correctly around the removed element — check that no orphaned `)}` or `{` remains.

Do NOT touch any other BankingAgent.js logic, auth state, or chip/footer structure.
  </action>
  <verify>
    <automated>cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `grep -n "Admin Dashboard\|handleAdminDashboard" banking_api_ui/src/components/LandingPage.js` returns no results
    - `grep -n "Admin Dashboard" banking_api_ui/src/components/BankingAgent.js` returns no results
    - `npm run build` exits 0
    - `cd banking_api_ui && npm run test:unit -- --watchAll=false --testPathPattern=SideNav` passes (snapshot untouched — SideNav.js was not modified)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated visitor → /admin route | Route remains URL-accessible; navigation buttons removed but no auth bypass introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-01 | Elevation of Privilege | /admin route URL-accessibility | accept | Route was always URL-accessible; auth guard on /admin is handled by App.js route-level protection (unchanged). Removing nav buttons does not change authorization. |
| T-227-02 | Information Disclosure | AdminSideNav reveals admin routes to all users | mitigate | Removing AdminSideNav from DOM means the full admin route list is no longer exposed in rendered HTML to non-admin users. |
</threat_model>

<verification>
After both tasks:

```bash
# No AdminSideNav references in production render paths
grep -rn "AdminSideNav" banking_api_ui/src/App.js banking_api_ui/src/components/AdminLayout.jsx
# Expected: no output

# No Admin Dashboard button text anywhere
grep -rn "Admin Dashboard" banking_api_ui/src/components/LandingPage.js banking_api_ui/src/components/BankingAgent.js
# Expected: no output

# No isOnSidebarRoute variable
grep -n "isOnSidebarRoute" banking_api_ui/src/App.js
# Expected: no output

# Build clean
cd banking_api_ui && npm run build
# Expected: exit 0, no warnings about unused imports

# Snapshot tests unchanged (SideNav.js not modified)
cd banking_api_ui && npm run test:unit -- --watchAll=false --testPathPattern=SideNav
# Expected: passes using existing snapshot
```
</verification>

<success_criteria>
- `<AdminSideNav>` removed from all 9 App.js render sites and from AdminLayout.jsx
- `import AdminSideNav` removed from both App.js and AdminLayout.jsx
- `isOnSidebarRoute` variable and its `sidebarRoutePatterns` block removed from App.js
- `demo-config-fab` FAB guard no longer references `isOnSidebarRoute` — FAB shows on all authenticated routes
- "Admin Dashboard" buttons absent from LandingPage.js (both header and hero locations)
- "Admin Dashboard" / "My Dashboard" nav chip absent from BankingAgent.js agent panel
- `handleAdminDashboard` function removed from LandingPage.js
- `npm run build` exits 0
- SideNav snapshot test passes (no regressions — SideNav.js was not touched)
- Admin routes remain URL-accessible (no routes deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/227-remove-side-menu-admin-dashboard/227-01-SUMMARY.md`
</output>
