---
phase: 263-mcp-server-spec-compliance-fixes-and-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_ui/src/components/ThresholdControls.js
  - banking_api_ui/src/components/ThresholdControls.css
  - banking_api_ui/src/components/UserDashboard.js
  - banking_api_ui/src/services/agentFlowDiagramService.js
  - banking_api_ui/src/services/spinnerService.js
  - banking_api_ui/src/components/AgentUiModeToggle.js
  - banking_api_ui/src/components/AgentUiModeToggle.css
  - banking_api_ui/src/components/BankingAgent.css
  - banking_api_ui/src/components/BankingAgent.js
autonomous: true
requirements:
  - PHASE-263-UI

must_haves:
  truths:
    - "ThresholdControls widget loads/saves thresholds from /api/config/thresholds"
    - "UserDashboard renders ThresholdControls in the dashboard layout"
    - "All compliance checklist steps start as pending (none hardcoded done)"
    - "Spinner is suppressed for SSE and polling routes (SILENT_URL_PREFIXES)"
    - "9 temporary fix-css/fix-toggle/test.js files are deleted from components/"
    - "npm run build in banking_api_ui exits 0"
  artifacts:
    - path: "banking_api_ui/src/components/ThresholdControls.js"
      provides: "Demo controls popover widget"
      contains: "api/config/thresholds"
    - path: "banking_api_ui/src/components/ThresholdControls.css"
      provides: "CSS for ThresholdControls widget"
    - path: "banking_api_ui/src/services/spinnerService.js"
      provides: "Silent URL filtering for SSE/polling routes"
      contains: "SILENT_URL_PREFIXES"
    - path: "banking_api_ui/src/services/agentFlowDiagramService.js"
      provides: "Compliance checklist with all steps as pending"
      contains: "status: 'pending'"
  key_links:
    - from: "banking_api_ui/src/components/UserDashboard.js"
      to: "banking_api_ui/src/components/ThresholdControls.js"
      via: "import ThresholdControls"
      pattern: "ThresholdControls"
    - from: "banking_api_ui/src/services/spinnerService.js"
      to: "SILENT_URL_PREFIXES array"
      via: "isSilentUrl(url) check in increment()"
      pattern: "isSilentUrl"
---

<objective>
Delete 9 temporary fix scripts, verify all UI gap closure changes are present, run the production build, and commit everything.

Purpose: All UI changes are already implemented — the executor confirms key patterns exist, deletes the temp files, builds successfully, then commits.
Output: 9 temp files deleted, 9 UI files committed, npm build exits 0.
</objective>

<execution_context>
@/Users/cmuir/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmuir/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/cmuir/P1Import-apps/Banking/.planning/ROADMAP.md
@/Users/cmuir/P1Import-apps/Banking/REGRESSION_PLAN.md
@/Users/cmuir/P1Import-apps/Banking/CLAUDE.md

NOTE: ALL WORK IS ALREADY DONE — uncommitted. Your job is:
1. Delete 9 temporary script files
2. Confirm each UI change is present (grep checks provided in tasks)
3. Run npm run build in banking_api_ui/ — must exit 0
4. Commit everything

Do NOT rewrite or modify production source files unless the build fails due to a syntax error.

REGRESSION NOTES (from REGRESSION_PLAN.md):
- BankingAgent FAB must still be visible after changes to BankingAgent.js/css
- Agent form account IDs must still work (liveAccounts state in BankingAgent.js)
- HITL OTP email flow must still work (transactionConsentChallenge.js — separate server plan)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete temporary fix/test scripts</name>
  <files>
    banking_api_ui/src/components/fix-css-2.js
    banking_api_ui/src/components/fix-css.js
    banking_api_ui/src/components/fix-css4.js
    banking_api_ui/src/components/fix-css5.js
    banking_api_ui/src/components/fix-css6.js
    banking_api_ui/src/components/fix-toggle.js
    banking_api_ui/src/components/fix-toggle2.js
    banking_api_ui/src/components/fix-toggle3.js
    banking_api_ui/src/components/test.js
  </files>
  <action>
These 9 files are temporary scripts that were never meant to be committed. Delete them all:

```bash
cd /Users/cmuir/P1Import-apps/Banking
rm banking_api_ui/src/components/fix-css-2.js \
   banking_api_ui/src/components/fix-css.js \
   banking_api_ui/src/components/fix-css4.js \
   banking_api_ui/src/components/fix-css5.js \
   banking_api_ui/src/components/fix-css6.js \
   banking_api_ui/src/components/fix-toggle.js \
   banking_api_ui/src/components/fix-toggle2.js \
   banking_api_ui/src/components/fix-toggle3.js \
   banking_api_ui/src/components/test.js
```

Confirm none remain:
```bash
ls banking_api_ui/src/components/fix-* banking_api_ui/src/components/test.js 2>&1
```

The ls command should return "No such file or directory" for all of them.
  </action>
  <verify>
    <automated>ls /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/fix-css.js 2>&1 | grep "No such file"</automated>
  </verify>
  <done>All 9 fix/test files are deleted; ls returns "No such file or directory".</done>
</task>

<task type="auto">
  <name>Task 2: Verify UI file changes are present</name>
  <files>
    banking_api_ui/src/components/ThresholdControls.js
    banking_api_ui/src/components/ThresholdControls.css
    banking_api_ui/src/components/UserDashboard.js
    banking_api_ui/src/services/agentFlowDiagramService.js
    banking_api_ui/src/services/spinnerService.js
    banking_api_ui/src/components/AgentUiModeToggle.js
    banking_api_ui/src/components/AgentUiModeToggle.css
    banking_api_ui/src/components/BankingAgent.css
    banking_api_ui/src/components/BankingAgent.js
  </files>
  <action>
Run each grep check. Every command must produce at least one match.

```bash
cd /Users/cmuir/P1Import-apps/Banking

# 1. ThresholdControls.js — fetches/posts to /api/config/thresholds
grep -n "api/config/thresholds\|confirm_threshold_usd\|mfa_threshold_usd" banking_api_ui/src/components/ThresholdControls.js

# 2. UserDashboard.js — imports and renders ThresholdControls
grep -n "ThresholdControls" banking_api_ui/src/components/UserDashboard.js

# 3. agentFlowDiagramService.js — all COMPLIANCE_STEPS are status: 'pending'
grep -n "status: 'pending'\|status: 'done'" banking_api_ui/src/services/agentFlowDiagramService.js | head -20
# Must NOT show any hardcoded 'done' in the COMPLIANCE_STEPS definition (lines 52-70 approx)

# 4. spinnerService.js — SILENT_URL_PREFIXES + isSilentUrl + DEBOUNCE_MS=2500
grep -n "SILENT_URL_PREFIXES\|isSilentUrl\|DEBOUNCE_MS" banking_api_ui/src/services/spinnerService.js

# 5. AgentUiModeToggle.js — "Always float" label (not "+ FAB")
grep -n "Always float\|placement.*none" banking_api_ui/src/components/AgentUiModeToggle.js

# 6. BankingAgent.css — new modal and avatar CSS classes
grep -n "ba-tx-error-overlay\|ba-tx-error-modal\|banking-agent-msg-avatar--helix" banking_api_ui/src/components/BankingAgent.css

# 7. BankingAgent.js — MarkdownContent import
grep -n "MarkdownContent\|MarkdownText" banking_api_ui/src/components/BankingAgent.js
```

Report the output. If any check finds missing patterns, investigate and report without auto-fixing.

For agentFlowDiagramService.js check: the COMPLIANCE_STEPS array definition (const COMPLIANCE_STEPS = [...]) must have ALL entries as `status: 'pending'`. The `buildCompletedSteps()` function further down may set steps to 'done' dynamically — that is expected and correct.
  </action>
  <verify>
    <automated>grep -c "api/config/thresholds" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/ThresholdControls.js && grep -c "ThresholdControls" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/UserDashboard.js && grep -c "SILENT_URL_PREFIXES" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/services/spinnerService.js && grep -c "ba-tx-error-overlay" /Users/cmuir/P1Import-apps/Banking/banking_api_ui/src/components/BankingAgent.css</automated>
  </verify>
  <done>All grep checks return at least 1 match each. agentFlowDiagramService.js COMPLIANCE_STEPS definition contains no hardcoded 'done' status.</done>
</task>

<task type="auto">
  <name>Task 3: Build UI and commit all UI changes</name>
  <files>
    banking_api_ui/src/components/ThresholdControls.js
    banking_api_ui/src/components/ThresholdControls.css
    banking_api_ui/src/components/UserDashboard.js
    banking_api_ui/src/services/agentFlowDiagramService.js
    banking_api_ui/src/services/spinnerService.js
    banking_api_ui/src/components/AgentUiModeToggle.js
    banking_api_ui/src/components/AgentUiModeToggle.css
    banking_api_ui/src/components/BankingAgent.css
    banking_api_ui/src/components/BankingAgent.js
  </files>
  <action>
Step 1 — Run the production build (required by CLAUDE.md for any UI change):

```bash
cd /Users/cmuir/P1Import-apps/Banking/banking_api_ui && npm run build
```

Build MUST exit 0. If it fails:
- Read the error output carefully
- Identify the specific file and line causing the failure
- Fix only the minimal issue causing the build failure
- Re-run the build
- Do NOT change working functionality while fixing build errors

Step 2 — Once build succeeds, stage and commit from repo root:

```bash
cd /Users/cmuir/P1Import-apps/Banking

git add banking_api_ui/src/components/ThresholdControls.js \
        banking_api_ui/src/components/ThresholdControls.css \
        banking_api_ui/src/components/UserDashboard.js \
        banking_api_ui/src/services/agentFlowDiagramService.js \
        banking_api_ui/src/services/spinnerService.js \
        banking_api_ui/src/components/AgentUiModeToggle.js \
        banking_api_ui/src/components/AgentUiModeToggle.css \
        banking_api_ui/src/components/BankingAgent.css \
        banking_api_ui/src/components/BankingAgent.js

# Stage the deleted temp files (git needs to track removals)
git rm --cached --ignore-unmatch \
  banking_api_ui/src/components/fix-css-2.js \
  banking_api_ui/src/components/fix-css.js \
  banking_api_ui/src/components/fix-css4.js \
  banking_api_ui/src/components/fix-css5.js \
  banking_api_ui/src/components/fix-css6.js \
  banking_api_ui/src/components/fix-toggle.js \
  banking_api_ui/src/components/fix-toggle2.js \
  banking_api_ui/src/components/fix-toggle3.js \
  banking_api_ui/src/components/test.js 2>/dev/null || true

git commit -m "$(cat <<'EOF'
feat(263): UI gap closure — threshold controls widget, compliance step reset, spinner tuning

- ThresholdControls.js/css: new Demo Controls popover — loads/saves confirm and MFA thresholds
- UserDashboard.js: render ThresholdControls widget; add tokenExpiresAt/tokenSecondsLeft state
- agentFlowDiagramService.js: reset ALL compliance steps to status:pending; add agent-token-init
  and agent-llm-reasoning steps; add mcp-gateway + pingauthorize in buildCompletedSteps()
- spinnerService.js: add SILENT_URL_PREFIXES (SSE/polling routes); DEBOUNCE_MS 200→2500;
  MIN_DISPLAY_MS 1500→300; isSilentUrl() guard in increment()
- AgentUiModeToggle.js/css: flex row layout; "Always float" label; flex:1 for segmented
- BankingAgent.css: add tx-error-overlay/modal classes and helix avatar CSS
- BankingAgent.js: import MarkdownContent from shared/MarkdownText
- Delete 9 temporary fix-css/fix-toggle/test.js scripts

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

Confirm the commit with `git log --oneline -1`.
  </action>
  <verify>
    <automated>git -C /Users/cmuir/P1Import-apps/Banking log --oneline -1 | grep -i "263\|threshold\|spinner\|compliance"</automated>
  </verify>
  <done>npm run build exits 0. git log shows the new commit containing UI changes. No fix-css/fix-toggle/test.js files remain in the working tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→/api/config/thresholds | ThresholdControls widget POSTs numeric values — validated server-side (plan 263-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-263-04 | Tampering | ThresholdControls POST | accept | Validation is server-side in thresholds.js (plan 263-01); UI is a demo-only widget, not a security boundary |
| T-263-05 | Information Disclosure | SILENT_URL_PREFIXES | accept | Suppressing spinner for polling/SSE routes is UX-only; no auth data exposed via spinner state |
</threat_model>

<verification>
- npm run build exits 0 (CLAUDE.md requirement for all UI changes)
- ThresholdControls.js exists and contains GET/POST to /api/config/thresholds
- UserDashboard.js imports and renders ThresholdControls
- agentFlowDiagramService.js COMPLIANCE_STEPS array has all steps as status:'pending'
- spinnerService.js has SILENT_URL_PREFIXES array and isSilentUrl() function with DEBOUNCE_MS=2500
- All 9 fix-css*/fix-toggle*/test.js files are deleted
- git log shows the commit
</verification>

<success_criteria>
npm run build in banking_api_ui/ exits 0. All 9 temp files deleted. All 9 production UI files committed. Grep confirms ThresholdControls widget, compliance step reset, spinner silent URL filtering, and AgentUiModeToggle layout change.
</success_criteria>

<output>
After completion, create `.planning/phases/263-mcp-server-spec-compliance-fixes-and-gap-closure/263-02-SUMMARY.md`
</output>
