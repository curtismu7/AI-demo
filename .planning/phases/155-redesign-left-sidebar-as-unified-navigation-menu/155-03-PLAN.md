---
phase: 155-redesign-left-sidebar-as-unified-navigation-menu
plan: 03
type: execute
wave: 2
depends_on: [155-02]
files_modified: [
  "banking_api_ui/src/components/TopNav.js",
  "banking_api_ui/src/components/TopNav.css",
  "banking_api_ui/src/components/UserDashboard.js",
  "banking_api_ui/src/components/Header.js",
  "banking_api_ui/src/App.css"
]
autonomous: true
requirements: [SIDE-155-04]
user_setup: []

must_haves:
  truths:
    - "TopNav displays only brand + hamburger (no theme toggle, token modal, role switch, auto-refresh)"
    - "Toolbar items consolidated: Theme, Token Modal, Role Switch, Auto-Refresh all moved to sidebar"
    - "Remove redundant buttons: No 'Sign in' button in header (use sidebar Logout item instead)"
    - "TopNav is simplified and focused on brand + mobile menu"
    - "No duplicate menu items between TopNav and Sidebar"
  artifacts:
    - path: "banking_api_ui/src/components/TopNav.js"
      provides: "Simplified header with brand + hamburger only"
      modification: "Remove toolbar buttons; keep navigation groups if admin"
    - path: "banking_api_ui/src/components/UserDashboard.js"
      provides: "Updated to remove auto-refresh control from toolbar (moved to sidebar)"
      modification: "Remove auto-refresh toggle; keep main dashboard content"
    - path: "banking_api_ui/src/App.css"
      provides: "Cleanup: remove left-margin/padding that was for old layout"
      modification: "Remove old navigation rail spacing"
  key_links:
    - from: "TopNav"
      to: "Sidebar"
      via: "Both excluded from layout during consolidation"
      pattern: "TopNav.*brand|hamburger"
    - from: "UserDashboard"
      to: "Sidebar"
      via: "Auto-refresh moved to Sidebar handler"
      pattern: "autoRefreshEnabled.*prop"
    - from: "Header.js"
      to: "Sidebar"
      via: "User menu consolidation"
      pattern: "UserMenu|Account.*button"
---

<objective>
Clean up and consolidate toolbar items from TopNav, Header, and UserDashboard into the sidebar menu (Plan 02). Simplify the top navigation bar to focus on brand and mobile hamburger.

Purpose: Remove duplicate navigation, reduce header clutter, establish sidebar as the single authoritative navigation menu
Output: Simplified TopNav + cleaned-up UserDashboard + removed redundant buttons
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/155-redesign-left-sidebar-as-unified-navigation-menu/155-CONTEXT.md
@.planning/phases/155-redesign-left-sidebar-as-unified-navigation-menu/155-01-PLAN.md
@.planning/phases/155-redesign-left-sidebar-as-unified-navigation-menu/155-02-PLAN.md

# Items to Consolidate (From CONTEXT.md)

**Previously in TopNav/Header/Toolbar:**
- ⚙️ Light/Dark theme toggle (moved to Sidebar.js menu, wired via onThemeToggle)
- 🔄 Auto-refresh checkbox (moved to Sidebar.js menu, wired via onAutoRefreshToggle)
- 🔑 Token inspector modal button (moved to Sidebar.js menu, wired via onTokenInspector)
- 🔐 Switch role button (admin only) (moved to Sidebar.js menu, wired via onSwitchRole)
- 👤 Account / Profile menu (moved to Sidebar.js menu account section)
- 🚪 Sign In / Sign Out button (Sign Out moved to Sidebar.js, Sign In handled by auth flow)

**TopNav current structure:**
- Brand + logo (keep)
- Hamburger menu for mobile (keep, now controls SidebarWrapper toggle)
- Admin navigation groups (keep for admin-only pages)
- Toolbar buttons (REMOVE and move to sidebar)

**UserDashboard current structure:**
- Auto-refresh checkbox in toolbar area
- Should be removed (now in sidebar)

**Header.js:**
- UserMenu component for account operations
- Should be simplified or removed (operations moved to sidebar)

# Design Decision
- Sidebar is the PRIMARY navigation for all users (admin + non-admin)
- TopNav remains for brand identity and emergency mobile menu (if sidebar toggle fails)
- Admin-specific navigation stays in TopNav for admin dashboard
- Toolbar is NOW empty (all items moved to sidebar) — can eventually remove it entirely
</context>

<read_first>
- banking_api_ui/src/components/TopNav.js (current toolbar structure, identify buttons to remove)
- banking_api_ui/src/components/UserDashboard.js (find auto-refresh checkbox)
- banking_api_ui/src/components/Header.js (find UserMenu, account operations)
- banking_api_ui/src/App.css (current spacing/layout that assumes old toolbar)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Simplify TopNav by removing toolbar buttons</name>
  <files>banking_api_ui/src/components/TopNav.js</files>
  <action>
Modify banking_api_ui/src/components/TopNav.js to remove toolbar items:

1. **Identify toolbar buttons to REMOVE** (search TopNav.js for these):
   - Theme toggle button (🌙 / ☀️ or similar)
   - Token inspector button (🔑 or "Token" label)
   - Auto-refresh checkbox or button
   - Switch role dropdown or button
   - Any "Settings" or "Tools" button in the right-side toolbar area

2. **Keep in TopNav**:
   - Brand + logo (`<MdAccountBalance />` icon + "Super Bank" text)
   - Hamburger menu toggle (for mobile SidebarWrapper)
   - Admin navigation groups (Overview, Management, Configuration) — these are FOR admin pages only, not toolbar clutter
   - Navigation structure remains (dropdowns for admin users)

3. **Actual changes**:
   - Delete the entire "toolbar" or "right-side" section that contains theme, token, auto-refresh, role switch buttons
   - Search for these patterns in TopNav.js and remove:
     - `className="topnav-toolbar"` or similar
     - `className="topnav-right"` or similar
     - Any buttons with `onClick={handleThemeToggle}`, `onClick={handleTokenInspector}`, `onClick={handleAutoRefresh}`, `onClick={handleRoleSwitch}`
   - Keep the `.topnav-left` (brand + hamburger)
   - Keep the `.topnav-center` (navigation groups if they exist for admin)

4. **Don't remove but simplify**:
   - If there's a UserMenu component embedded in TopNav, you can keep it but it should just show user name/avatar (no dropdown for now)
   - Or remove UserMenu entirely since account operations moved to sidebar

5. **Result**:
   - TopNav is now a simple header: Brand + Hamburger + Admin nav groups
   - No clutter, no duplicate items with Sidebar
   - Mobile hamburger still controls TopNav dropdown (if it has one) — that's OK since SidebarWrapper has its own toggle
   - Two hamburgers is redundant but not broken; Sidebar hamburger is primary, TopNav hamburger is fallback for admin nav

  </action>
  <verify>
    <automated>grep -c "handleThemeToggle\|handleTokenInspector\|handleAutoRefresh\|handleRoleSwitch" banking_api_ui/src/components/TopNav.js 2>/dev/null || echo "0" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - TopNav.js exists and still imports React, useNavigate
    - Brand + logo renders (MdAccountBalance icon + "Super Bank" text)
    - Hamburger button still renders for mobile menu
    - Admin navigation groups still render (for admin-specific pages)
    - Zero instances of: handleThemeToggle, handleTokenInspector, handleAutoRefresh, handleRoleSwitch
    - No theme toggle button visible in TopNav
    - No token inspector button visible
    - No auto-refresh checkbox visible
    - No role switch button visible
    - No build errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove auto-refresh control from UserDashboard toolbar</name>
  <files>banking_api_ui/src/components/UserDashboard.js</files>
  <action>
Modify banking_api_ui/src/components/UserDashboard.js to remove auto-refresh UI:

1. **Find auto-refresh implementation**:
   - Search for: "auto-refresh", "autoRefresh", "auto-refresh checkbox", "refresh checkbox"
   - Identify the checkbox or button that toggles auto-refresh
   - Note: `autoRefreshEnabled` state might be in UserDashboard or passed as prop
   - Check if there's a handler like `handleAutoRefreshChange`, `toggleAutoRefresh`, `onAutoRefreshChange`

2. **Remove the rendered component**:
   - Delete the JSX that renders the auto-refresh checkbox/button
   - This is likely in a toolbar area or control panel within UserDashboard
   - Typically looks like: `<label><input type="checkbox" checked={autoRefreshEnabled} onChange={handleAutoRefreshChange} /> Auto-refresh</label>`

3. **Keep the functionality** (don't break it):
   - The `autoRefreshEnabled` state can remain in UserDashboard or App.js (Plan 02 should have wired it)
   - The auto-refresh polling logic for dashboard updates can stay — it just won't be user-togglable from the dashboard anymore
   - User toggles it from Sidebar instead

4. **Search and identify all references**:
   - Look for comments or section headers like "// Auto-refresh controls", "// Toolbar controls"
   - Delete only the UI (the rendered checkbox/button/label)
   - Keep the state variable, keep the polling logic

5. **Result**:
   - UserDashboard no longer shows auto-refresh checkbox
   - Dashboard still auto-refreshes if user enables it from Sidebar
   - No functionality loss, just UI cleanup

  </action>
  <verify>
    <automated>grep -c "checked={autoRefreshEnabled}\|onChange.*autoRefresh\|auto.refresh.*checkbox\|refresh.*toggle.*button" banking_api_ui/src/components/UserDashboard.js 2>/dev/null || echo "0" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - UserDashboard.js still exists and renders without errors
    - Auto-refresh checkbox/button NOT visible in UI (0 instances of rendered checkbox control)
    - Auto-refresh state variable can still exist in component (for internal polling logic)
    - Dashboard still functions, displays user data, renders panels
    - No build errors
    - On-page elements visible: Transactions, Accounts, Agent panel, etc.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Clean up Header.js UserMenu consolidation and App.css layout</name>
  <files>banking_api_ui/src/components/Header.js, banking_api_ui/src/App.css</files>
  <action>
Modify banking_api_ui/src/components/Header.js and banking_api_ui/src/App.css for layout cleanup:

1. **Header.js changes**:
   - If Header.js has a UserMenu component, it can stay for displaying user name/avatar
   - BUT remove any dropdown items that are now in sidebar (theme, token, role switch, logout)
   - If UserMenu is only used for those dropdown items, you can remove it entirely or simplify it
   - Search for: `<UserMenu`, `UserMenu.*theme`, `UserMenu.*logout`, `UserMenu.*role`, `UserMenu.*token`
   - If Header.js has "Account" or "Profile" buttons, they can be removed (account operations moved to sidebar)
   - Keep: User name display, user avatar (if used for identification only, not interaction)
   - Remove: Dropdown for account operations, logout from header (logout now in sidebar)

2. **How to check if Header.js is used**:
   - Search App.js for: `import Header from`
   - If Header is used in multiple places (Dashboard + UserDashboard), keep it but simplified
   - If Header is only used in old layouts, it might be deprecated already

3. **App.css cleanup**:
   - Search for CSS rules related to old layout assumptions:
     - `.App--has-sidebar-left` (if any) — already exists? Update to work with new grid
     - `.main-content { margin-left: X;` — remove left margin (sidebar handles it via grid)
     - `.main-content { padding-left: X;` — remove left padding
     - `.topnav { margin-left: X;` — remove, topnav should be full-width
     - Old navigation rail spacing (if any) — remove
   - Search for: "sidebar", "nav-rail", "left-nav", "side-panel"
   - Clean up any old layout assumptions

4. **More specific searches in App.css**:
   - Find `.App-content`, `.main`, `.main-wrapper` etc. and ensure they have no fixed left margin
   - Sidebar layout is handled by SidebarWrapper.css (grid), not App.css
   - App.css should focus on top-level App structure (body background, global fonts, etc.)

5. **Result**:
   - Header is simplified or removed (depending on where it's used)
   - UserMenu is simplified (just display, no interaction)
   - App.css has no old sidebar/nav-rail spacing
   - Layout is clean and handles grid via SidebarWrapper.css

  </action>
  <verify>
    <automated>grep -c "margin-left.*[0-9]\|padding-left.*[0-9]\|nav-rail\|side-panel\|old.*sidebar" banking_api_ui/src/App.css 2>/dev/null || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - Header.js (if used) no longer shows logout, theme, token, role switch in dropdowns
    - UserMenu (if exists) simplified to display user name/avatar only
    - App.css has no `.main-content { margin-left: }` (sidebar handles layout via grid)
    - App.css has no `.topnav { margin-left: }` (topnav full-width)
    - No old navigation rail or side-panel CSS rules
    - App still builds, no CSS errors
    - Layout is clean: TopNav spans full width, SidebarWrapper handles grid
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Comprehensive responsive testing (all 4 requirements satisfied)</name>
  <files>banking_api_ui/src</files>
  <action>
Perform end-to-end responsive testing to verify all Phase 155 requirements are satisfied:

1. **Test SIDE-155-01: Sidebar menu component**:
   - [ ] Visit any authenticated page (/dashboard, /admin)
   - [ ] Sidebar renders on left with menu items visible
   - [ ] Menu items: Dashboard, Admin (if admin), Agent, Auto-Refresh toggle, Theme toggle, Token Inspector, Switch Role, etc.
   - [ ] Each menu item has icon (16-18px emoji) + label (14px text)
   - [ ] No console errors

2. **Test SIDE-155-02: Icon + label styling**:
   - [ ] Sidebar menu items display icon + text horizontally
   - [ ] Icon sizing: 16-18px (verify in CSS)
   - [ ] Label sizing: 14px, font-weight 600, dark color
   - [ ] Spacing: 8px between icon and label, 12px item padding
   - [ ] Hover state: Light background highlight
   - [ ] Active state: Bold text or accent left border
   - [ ] Matches screenshot design reference

3. **Test SIDE-155-03: Responsive layout**:
   - Desktop (>768px):
     - [ ] Sidebar full-width (~240px) on left
     - [ ] Always visible
     - [ ] Main-content adjusted beside it (not under)
     - [ ] No hamburger button
   - Mobile (<768px):
     - [ ] Sidebar hidden by default
     - [ ] Hamburger button visible top-left
     - [ ] Click hamburger → sidebar slides in
     - [ ] Click sidebar item → sidebar stays (user navigates)
     - [ ] Can click hamburger again → sidebar slides out
     - [ ] Persistent: Reload page → sidebar in same state
   - Resize test:
     - [ ] Resize window 1200px → 768px → 375px
     - [ ] No errors or layout shifts
     - [ ] Smooth responsive transition

4. **Test SIDE-155-04: Toolbar consolidation**:
   - TopNav cleanup:
     - [ ] TopNav shows brand + hamburger only
     - [ ] No theme toggle in TopNav
     - [ ] No token inspector button
     - [ ] No auto-refresh checkbox
     - [ ] No role switch button
     - [ ] Admin nav groups still visible on admin pages (optional, part of admin TopNav)
   - UserDashboard cleanup:
     - [ ] No auto-refresh checkbox in dashboard toolbar
     - [ ] Dashboard still works (displays data, refreshes via API polling)
   - Header cleanup:
     - [ ] Header UserMenu simplified (no logout, theme, token dropdown)
     - [ ] Or Header removed entirely if not used elsewhere
   - Sidebar now has all consolidated items:
     - [ ] Theme toggle → calls onThemeToggle
     - [ ] Token inspector → calls onTokenInspector, opens modal
     - [ ] Auto-refresh toggle → calls onAutoRefreshToggle, persists in localStorage/sidebar state
     - [ ] Switch role → calls onSwitchRole (admin only)
     - [ ] Logout → calls onLogout, redirects

5. **Cross-browser / cross-device testing**:
   - [ ] Desktop 1920x1080: Sidebar visible, layout consistent
   - [ ] Tablet 768x1024: Sidebar visible, main-content adjusted
   - [ ] Mobile 375x667: Hamburger visible, sidebar hidden, toggle works
   - [ ] Safari, Chrome, Firefox: No layout shifts, animations smooth
   - [ ] Mobile Safari (iOS): Touch interactions smooth, no unintended scrolls

6. **Functional verification**:
   - [ ] Navigate sidebar items → pages load correctly
   - [ ] Click theme toggle in sidebar → theme changes app-wide
   - [ ] Click token inspector → modal opens with token data
   - [ ] Toggle auto-refresh → UserDashboard polling updates
   - [ ] Switch role (admin only) → role changes, sidebar updates
   - [ ] Click logout → redirects to /api/auth/logout
   - [ ] All handlers wired correctly, no JavaScript errors

  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "compiled successfully|error" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - All 4 Phase 155 requirements satisfied:
      * SIDE-155-01 ✓ — Sidebar component exists with menu structure
      * SIDE-155-02 ✓ — Icon + label styling matches screenshot (16-18px icons, 14px bold labels, 8px gap, 12px padding)
      * SIDE-155-03 ✓ — Responsive layout (desktop full sidebar, mobile hamburger toggle, persistent state)
      * SIDE-155-04 ✓ — Toolbar consolidated (TopNav simplified, UserDashboard cleaned, Sidebar has all items)
    - Build succeeds (`npm run build` exit 0)
    - No console errors on authenticated pages
    - Responsive desktop/mobile/tablet tested
    - All menu items functional (navigate, toggle, logout)
    - Sidebar state persists across reloads (localStorage)
    - No layout shifts when resizing viewport
    - Smooth animations (sidebar slide-in on mobile)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Removed UI → Still functional | Auto-refresh and theme still work (UI just moved to sidebar) |
| Simplified Header → User identity | Header still identifies user (avatar/name) even with reduced buttons |
| Consolidated nav → Single source of truth | Sidebar is now primary nav; TopNav is backup (reduces confusion) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-155-03-01 | Information Disclosure | Removed UserMenu | Mitigate | User info still displayed in sidebar; account operations now secured in sidebar handlers |
| T-155-03-02 | Denial of Service | Simplified UI | Accept | Fewer buttons = fewer potential failure points; cleaner code |
| T-155-03-03 | Unintended Navigation | Removed TopNav buttons | Mitigate | Sidebar is primary nav now; TopNav is spacious, less accidental clicks |

</threat_model>

<verification>
## Phase 155 Plan 03: Toolbar Consolidation

After execution, verify:

1. **TopNav simplified**:
   - [ ] Brand + logo renders
   - [ ] Hamburger menu visible on mobile
   - [ ] No theme toggle button
   - [ ] No token inspector button
   - [ ] No auto-refresh checkbox
   - [ ] No role switch button
   - [ ] Admin nav groups optional (if admin-only pages still use them)

2. **UserDashboard cleaned**:
   - [ ] No auto-refresh checkbox in dashboard
   - [ ] Dashboard still displays and refreshes
   - [ ] All dashboard panels render correctly

3. **Header/UserMenu simplified**:
   - [ ] Header shows user name/avatar (if used)
   - [ ] No dropdown for account operations
   - [ ] Or Header removed entirely if unused

4. **App.css cleanup**:
   - [ ] No `.main-content { margin-left: }` rules
   - [ ] No `.topnav { margin-left: }` rules
   - [ ] No old navigation rail CSS
   - [ ] Layout is clean and consistent

5. **Sidebar integration**:
   - [ ] All removed items now in sidebar
   - [ ] Theme toggle works from sidebar
   - [ ] Token inspector works from sidebar
   - [ ] Auto-refresh toggle works from sidebar
   - [ ] Role switch works from sidebar (admin)
   - [ ] Logout works from sidebar

6. **Build & deployment**:
   - [ ] `npm run build` succeeds
   - [ ] No console errors
   - [ ] All pages accessible
   - [ ] Responsive layout works

</verification>

<success_criteria>
Plan 03 is complete when:
- [ ] TopNav.js simplified (removed toolbar buttons)
- [ ] UserDashboard.js cleaned (removed auto-refresh UI)
- [ ] Header.js simplified (UserMenu reduced or removed)
- [ ] App.css cleaned (no old layout spacing)
- [ ] All 4 Phase 155 requirements fully satisfied:
  * SIDE-155-01: Sidebar component ✓
  * SIDE-155-02: Icon + label styling ✓
  * SIDE-155-03: Responsive layout ✓
  * SIDE-155-04: Toolbar consolidation ✓
- [ ] Sidebar is primary navigation for all users
- [ ] TopNav is simplified and focused on brand + admin nav
- [ ] All menu items functional (navigate, toggle, logout)
- [ ] Build succeeds, no errors
- [ ] Responsive testing passed (desktop/tablet/mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/155-redesign-left-sidebar-as-unified-navigation-menu/155-03-SUMMARY.md`
</output>
