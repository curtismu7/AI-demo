---
phase: 164-performance-evaluation-and-optimization
plan: 02
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - banking_api_server/middleware/timing.js
  - banking_api_server/server.js
  - banking_api_ui/src/services/cachedStatusService.js
autonomous: false
requirements: [PERF-164-03]

must_haves:
  truths:
    - "Server responses include X-Response-Time header with millisecond duration"
    - "UI spinner activity feed shows request timing"
    - "Idle dashboard fires < 20 auth status requests per minute"
    - "Login flow completes and reaches /dashboard"
  artifacts:
    - path: "banking_api_server/middleware/timing.js"
      provides: "Express middleware adding X-Response-Time header"
  key_links:
    - from: "banking_api_server/middleware/timing.js"
      to: "server.js"
      via: "app.use(timingMiddleware) early in middleware chain"
---

<objective>
Add timing instrumentation to server and UI, then verify the polling fix from Plan 01 worked.

Purpose: Per D-02, every request path should be measurable (target less than 5s). Per D-04, instrumentation validates the polling fix.

Output: Timing middleware, UI timing integration, verified performance improvement.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/164-performance-evaluation-and-optimization-diagnose-slow-spinners-long-api-waits-and-overall-responsiveness/164-CONTEXT.md
@.planning/phases/164-performance-evaluation-and-optimization-diagnose-slow-spinners-long-api-waits-and-overall-responsiveness/164-01-SUMMARY.md

@banking_api_server/server.js
@banking_api_ui/src/services/cachedStatusService.js
@banking_api_ui/src/services/spinnerActivityService.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server timing middleware and UI timing integration</name>
  <files>banking_api_server/middleware/timing.js, banking_api_server/server.js, banking_api_ui/src/services/cachedStatusService.js</files>
  <action>Part A: Create banking_api_server/middleware/timing.js. Export an Express middleware that records Date.now() on request start, overrides res.end to calculate duration, sets X-Response-Time header if headers not yet sent, and logs any request over 2000ms as SLOW. Part B: In banking_api_server/server.js, add app.use(require('./middleware/timing')) early in middleware chain before routes. Part C: In cachedStatusService.js getCachedStatus(), measure fetch duration with Date.now() before and after the fetch call. For non-cached requests only, call addClientEvent from spinnerActivityService with type perf and message showing the URL path and duration in ms. Guard the import so failure does not break.</action>
  <verify>
    <automated>cd banking_api_ui and npm run build and node -c ../banking_api_server/middleware/timing.js and echo PASS</automated>
  </verify>
  <done>Server adds X-Response-Time header to all responses. UI logs request durations to spinner activity feed for non-cached requests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify polling fix and timing instrumentation end-to-end</name>
  <files>none</files>
  <action>Human verification of combined Plan 01 and Plan 02 results.</action>
  <verify>Manual verification per how-to-verify steps</verify>
  <done>User confirms idle polling reduced, timing visible, login works</done>
  <what-built>Plan 01 plus Plan 02 combined: SQLite fixed, auth status callers cached, AgentFlowDiagramPanel at 10s interval, X-Response-Time header added, spinner shows timing.</what-built>
  <how-to-verify>1. Start server with run-bank.sh — no SyntaxError or DBMOVED. 2. Open browser, log in as bankuser — reaches dashboard. 3. Network tab idle 30s — auth endpoints fire much less frequently. 4. Check X-Response-Time header on API responses. 5. Check spinner activity feed for timing entries. 6. Log out and back in — clean transition.</how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server to Client | X-Response-Time header exposes server processing duration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-03 | Information Disclosure | X-Response-Time header | accept | Only wall-clock duration, no internals. Standard practice. |
</threat_model>

<verification>
1. Server starts without errors
2. Login flow completes end-to-end
3. Idle dashboard request rate visually reduced in Network tab
4. X-Response-Time header visible on API responses
</verification>

<success_criteria>
- X-Response-Time header present on all API responses
- Spinner activity feed displays request timing
- Idle dashboard auth polling reduced from 120 req/min to under 20 req/min
- Login/logout flow works cleanly with cache invalidation
</success_criteria>

<output>
After completion, create .planning/phases/164-performance-evaluation-and-optimization-diagnose-slow-spinners-long-api-waits-and-overall-responsiveness/164-02-SUMMARY.md
</output>
