---
phase: 164-performance-evaluation-and-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - banking_api_server/services/sqliteSessionStore.js
  - banking_api_ui/src/services/cachedStatusService.js
  - banking_api_ui/src/services/sessionResolver.js
  - banking_api_ui/src/App.js
  - banking_api_ui/src/components/UserDashboard.js
  - banking_api_ui/src/components/Dashboard.js
  - banking_api_ui/src/components/BankingAgent.js
  - banking_api_ui/src/components/education/TokenExchangePanel.js
  - banking_api_ui/src/components/AgentFlowDiagramPanel.js
autonomous: true
requirements: [PERF-164-01, PERF-164-02, PERF-164-04]

must_haves:
  truths:
    - "Server starts without admin.js or SQLite errors"
    - "User can log in and session persists (no session_persist_failed)"
    - "Idle dashboard fires < 20 auth status requests per minute (down from ~120)"
    - "Login/logout immediately clears stale cached auth state"
    - "AgentFlowDiagramPanel polls token chain at 10s intervals, not 3s"
  artifacts:
    - path: "banking_api_ui/src/services/cachedStatusService.js"
      provides: "Auth status caching with event-driven invalidation"
    - path: "banking_api_ui/src/services/sessionResolver.js"
      provides: "Auth resolution routed through cache"
  key_links:
    - from: "banking_api_ui/src/App.js"
      to: "cachedStatusService.js"
      via: "getCachedStatus() instead of direct axios.get for auth endpoints"
    - from: "window userAuthenticated event"
      to: "clearStatusCache()"
      via: "event listener in cachedStatusService or App.js"
---

<objective>
Fix server blockers (SQLite DBMOVED) and eliminate the auth status polling storm by routing all auth endpoint callers through the existing cachedStatusService with event-driven invalidation.

Purpose: The idle dashboard hits auth endpoints ~120 times/minute from 7+ independent callers. This plan deduplicates via the existing 3s TTL cache and wires cache invalidation to login/logout events. Also fixes SQLite so the server actually works.

Output: Working server + dramatically reduced idle request rate.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164-performance-evaluation-and-optimization-diagnose-slow-spinners-long-api-waits-and-overall-responsiveness/164-CONTEXT.md

@banking_api_ui/src/services/cachedStatusService.js
@banking_api_ui/src/services/sessionResolver.js
@banking_api_ui/src/App.js
@banking_api_ui/src/components/UserDashboard.js
@banking_api_ui/src/components/Dashboard.js
@banking_api_ui/src/components/BankingAgent.js
@banking_api_ui/src/components/education/TokenExchangePanel.js
@banking_api_ui/src/components/AgentFlowDiagramPanel.js
@banking_api_server/services/sqliteSessionStore.js
</context>

<interfaces>
<!-- Existing cachedStatusService API — use these, don't reinvent -->
From banking_api_ui/src/services/cachedStatusService.js:
```javascript
export async function getCachedStatus(url, config = {})  // 3s TTL, in-flight dedup
export function clearStatusCache()                        // clear all cached responses
export function clearStatusCacheFor(url)                  // clear specific URL cache
```

From banking_api_ui/src/services/sessionResolver.js:
```javascript
export async function resolveSessionUser()  // returns user object or null
// Currently makes 3 direct bffAxios.get calls — needs to use cachedStatusService
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix SQLite DBMOVED + verify server starts</name>
  <files>banking_api_server/data/sessions.db</files>
  <action>
  1. Delete the stale sessions.db file: `rm banking_api_server/data/sessions.db`
  2. Run `node -c banking_api_server/routes/admin.js` to confirm no syntax error (admin.js:769 was already fixed in a prior session)
  3. Start the server with `cd banking_api_server && npm start` and verify:
     - No SyntaxError on startup
     - No SQLITE_READONLY_DBMOVED errors
     - "[session-store] Using SQLite store" message appears
  4. Stop the server after verification

  The DBMOVED error happens when the DB file inode changes (e.g., git operations replacing the file). Deleting forces a fresh DB creation. The sqliteSessionStore.js already has `_reconnect()` logic, so this is a one-time fix.
  </action>
  <verify>
    <automated>rm -f banking_api_server/data/sessions.db && cd banking_api_server && timeout 5 node -e "require('./services/sqliteSessionStore'); console.log('OK')" 2>&1 | grep -q "OK" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Server starts without SQLite errors, sessions.db recreated fresh</done>
</task>

<task type="auto">
  <name>Task 2: Route all auth status callers through cachedStatusService + wire invalidation</name>
  <files>
    banking_api_ui/src/services/cachedStatusService.js,
    banking_api_ui/src/services/sessionResolver.js,
    banking_api_ui/src/App.js,
    banking_api_ui/src/components/UserDashboard.js,
    banking_api_ui/src/components/Dashboard.js,
    banking_api_ui/src/components/BankingAgent.js,
    banking_api_ui/src/components/education/TokenExchangePanel.js
  </files>
  <action>
  **Part A — Enhance cachedStatusService.js:**
  1. Add auto-invalidation: listen for `userAuthenticated` and `userLoggedOut` CustomEvents on `window`, call `clearStatusCache()` when fired. Register listeners at module load time (top-level side effect).
  2. Add a `getCachedJson(url)` convenience wrapper that uses same-origin fetch with `credentials: 'include'` and returns parsed JSON — this is what most callers need (they currently use `axios.get(url)` and read `.data`). The wrapper should return the shape `{ data: parsedJson }` to minimize caller changes.

  **Part B — Update sessionResolver.js (per D-01):**
  Change from direct `bffAxios.get` to `getCachedJson` for all 3 auth endpoints. Remove the "NOT cached intentionally" comment — update to explain: "Cached with 3s TTL + in-flight dedup. Cache cleared on login/logout events. BFF cookie auth means cached status is safe."

  **Part C — Update App.js `checkOAuthSession`:**
  Replace the 3 `axios.get('/api/auth/oauth/status')`, `axios.get('/api/auth/oauth/user/status')`, `axios.get('/api/auth/session')` calls with `getCachedJson()` from cachedStatusService. Keep the same sequential logic (admin → user → session fallback). The `applyUser` + `dispatchEvent('userAuthenticated')` logic stays — it triggers cache clear for subsequent fresh reads.

  **Part D — Update UserDashboard.js `fetchUserData`:**
  Replace `axios.get('/api/auth/oauth/user/status')` and `axios.get('/api/auth/oauth/status')` with `getCachedJson()`.

  **Part E — Update Dashboard.js:**
  Replace `axios.get('/api/auth/oauth/status')` and `axios.get('/api/auth/oauth/user/status')` with `getCachedJson()`.

  **Part F — Update BankingAgent.js:**
  Replace the two `fetch('/api/auth/oauth/status', ...)` and `fetch('/api/auth/oauth/user/status', ...)` calls (~lines 1097-1098) with `getCachedJson()`.

  **Part G — Update TokenExchangePanel.js:**
  Replace `axios.get('/api/auth/oauth/status')` and `axios.get('/api/auth/oauth/user/status')` with `getCachedJson()`.

  **Do NOT touch:** `apiClient.js` `getTokenFromSession()` — it's only called from tests, not production. Leave it alone.
  </action>
  <verify>
    <automated>cd banking_api_ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All auth status callers use cachedStatusService. Build passes. Cache auto-clears on userAuthenticated/userLoggedOut events.</done>
</task>

<task type="auto">
  <name>Task 3: Slow AgentFlowDiagramPanel polling interval</name>
  <files>banking_api_ui/src/components/AgentFlowDiagramPanel.js</files>
  <action>
  Change the `setInterval(loadTokenChain, 3000)` (~line 139) to `setInterval(loadTokenChain, 10000)` (10 seconds). The token chain only changes during active agent operations, so 10s is plenty. Add a brief comment: `// 10s poll — chain only changes during active agent ops`.
  </action>
  <verify>
    <automated>grep -n "setInterval.*loadTokenChain.*10000" banking_api_ui/src/components/AgentFlowDiagramPanel.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>AgentFlowDiagramPanel polls at 10s instead of 3s</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→BFF | Auth status responses cached client-side — must invalidate on auth transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-164-01 | Information Disclosure | cachedStatusService cache | accept | Cache only holds same data as uncached requests (session cookie auth, same-origin). TTL is 3s. No amplification risk. |
| T-164-02 | Spoofing | Stale auth state after logout | mitigate | clearStatusCache() wired to userLoggedOut event — ensures no stale "authenticated" response served after logout |
</threat_model>

<verification>
1. `cd banking_api_ui && npm run build` → exit code 0
2. Server starts without SQLite errors after deleting sessions.db
3. `grep -r "cachedStatusService" banking_api_ui/src/ --include="*.js" | grep -v __tests__ | grep -v node_modules` shows imports in all updated files
4. `grep "setInterval.*10000" banking_api_ui/src/components/AgentFlowDiagramPanel.js` shows slowed interval
</verification>

<success_criteria>
- Server starts clean (no SQLite, no admin.js errors)
- All auth status callers route through cachedStatusService
- Cache auto-invalidates on login/logout events
- AgentFlowDiagramPanel polls at 10s
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/164-performance-evaluation-and-optimization-diagnose-slow-spinners-long-api-waits-and-overall-responsiveness/164-01-SUMMARY.md`
</output>
