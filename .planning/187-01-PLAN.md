# 187-01-PLAN.md — Remove all duplicate/corrupted code after the scenario array in AuthzTestPage.jsx and restore a valid React component

## Goal
Remove all duplicate import statements, stray hook calls, and corrupted code after the scenario array in AuthzTestPage.jsx. Restore the file to a valid, buildable React component so the UI can run and the step-up demo is unblocked.

## Steps
1. Read AuthzTestPage.jsx and identify the correct end of the scenario array.
2. Remove all code after the scenario array that consists of duplicate imports, stray hook calls, or corrupted code.
3. Ensure only the correct imports, scenario array, and main component code remain.
4. Validate the file is a valid React component (default export, no duplicate imports, no syntax errors).
5. Run `npm run build` to confirm the UI builds successfully.
6. Verify the step-up demo and scenario rendering are functional.

## Success Criteria
- All duplicate import statements and stray hook calls after the scenario array are removed from AuthzTestPage.jsx
- The file is a valid, buildable React component
- UI build completes without errors
- Step-up demo is unblocked and functional
- No regression in scenario rendering or step-up logic
