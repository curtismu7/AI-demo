# Core dependencies
# WR-13: Ollama is the only supported provider (llm_factory.get_llm rejects
# anything else). langchain-{openai,groq,anthropic,google-genai} and the
# openai SDK were never imported (verified by grep) — ~80MB of dead deps.
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-ollama>=0.2.0,<0.3.0
pydantic>=2.0.0,<3.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=11.0.0
httpx>=0.24.0
aiohttp>=3.8.0
cryptography>=41.0.0
PyJWT[crypto]>=2.8.0  # Path A: PingOne JWKS access-token validation (token_validator.py)
python-dotenv>=1.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
aioresponses>=0.7.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Optional dependencies for specific features
redis>=4.5.0  # For token caching
sqlalchemy>=2.0.0  # For persistent storage
alembic>=1.10.0  # For database migrations
